# B-14 / R7. The painted 864 x 576 map is 197,826,489 bytes (188.66 MiB) of ForceText YAML — that size IS the R7
# finding (Unity/Docs/evidence/phase-b/b14-r7-paint.json), so the scene is measured and then kept out of the
# repository rather than committed. Regenerate it in about 3.5 s with the editor menu Relight/Prototypes/Paint R7;
# the R7 and R2 play-mode tests Assert.Ignore themselves with that instruction when it is absent.
R7-Tilemap.unity
R7-Tilemap.unity.meta
