# Tasks — Unity migration

**Latest scene authoring, 2026-09-15:** World.unity now exposes editable buildings, props, resources, roads, paved areas and terrain overrides, compiled into the simulation at session start. [Editing guide and verification](SCENE_EDITING_2026-09-15.md). C-ACC remains human.

**Latest ore opening, 2026-09-15:** approved ore-to-plate recipes, power-independent Home smelting and eastern-yard deposits are implemented for New Game. Older saves retain their original layout/balance. [Behavior and verification](ORE_OPENING_2026-09-15.md). C-ACC remains human.

**Latest inventory correction, 2026-09-15:** visible empty fuel/input/output/storage slots, pointer-following drag preview, validated drops, hotbar structure drops and inventories-first small-screen layout. [Behaviour and actual gesture checks](INVENTORY_FIXES_2026-09-15.md). C-ACC remains human.

**Latest activity feedback, 2026-09-15:** live cable highlights, machine working/full/no-power cues, generator running/standby/off and conveyor movement/jam animation are implemented. Conveyors remain independent of electricity by owner choice. [Behaviour and evidence](ACTIVITY_ANIMATIONS_2026-09-15.md).

**Latest debug tools, 2026-09-15:** F8 / Admin opens resource and weapon grants, enemy and raid controls, player/base recovery, pause and temporary testing overrides. [Controls and verified behaviour](ADMIN_MENU_2026-09-15.md). C-ACC remains human.

**Latest gameplay correction, 2026-09-15:** power state in hover/inspection, visible extractor output, smooth spaced conveyor items, single direction arrows, persistent power cables and pole previews are implemented. [Changes and actual Unity verification](FLOW_POWER_FIXES_2026-09-15.md). C-ACC remains human.

**Current owner-authorised illustrated UI pass, 2026-09-15:** the approved reference style is implemented with reusable painted frames, fourteen painted item icons, compact HUD/Backpack, live recipe equations and target-adjacent feedback. [Implementation, assets and actual Unity captures](UI_INDUSTRIAL_PASS_2026-09-15.md). Earlier C-UX controls and gameplay remain intact. **C-ACC remains human; world art and standalone/campaign acceptance remain open.**

**This file is the only owner of migration task status.** The reference tracker `docs/PROGRESS.md` is paused and points here. One log entry per meaningful status change goes in §8.

Status values: `todo` · `in progress` · `done` · `blocked (reason)` · `human` (needs an owner verdict, never marked done by an agent). Code status, automated checks, design approval and human play are recorded separately; a passing test never closes a `human` row.

Phase rule: a phase is complete only when every non-human task in it is `done` and its playable acceptance has been run by a person and recorded. Later phases may start engineering work early only where a task's dependencies are all `done`.

Sources for every task are in [MIGRATION_MAP.md](MIGRATION_MAP.md) (system entries `S-nn`, `S-Ann`) and the specialist documents; a task cites the entries it implements rather than repeating source paths. Decisions and open questions (`U-D`, `U-M`, `U-P`, `U-Q`) are in [DECISIONS.md](DECISIONS.md).

---

## Phase A — Documentation and inventory

| ID | Task | Depends on | Deliverables | Acceptance | Status |
|---|---|---|---|---|---|
| A-01 | Establish reference state: branch, uncommitted changes, build commands, implemented systems, open gates, conflicting docs | — | [SOURCE_INVENTORY.md](SOURCE_INVENTORY.md) | Checkout state and command list verified against `git status`, root `package.json` | done 2026-09-11 |
| A-02 | Game design and content catalogue extracted from code and confirmed decisions | A-01 | [GAME_DESIGN.md](GAME_DESIGN.md), [CONTENT_CATALOGUE.md](CONTENT_CATALOGUE.md) | Every value has a source and a current/approved/provisional tag; 17 owner decisions verified against code | done 2026-09-11 |
| A-03 | World data, coordinates, canonical sources, export/import mapping, asset inventory and provenance | A-01 | [WORLD_AND_ASSETS.md](WORLD_AND_ASSETS.md) | Canonical-source statement; Tiled ruled reference-only with evidence; every asset group listed with licence status | done 2026-09-11 |
| A-04 | UI, controls, HUD, transfers, exact opening sequence, known defects | A-01 | [UI_AND_ONBOARDING.md](UI_AND_ONBOARDING.md) | Guidance strings quoted from source; defects table distinguishes fixed/open and states required behaviour | done 2026-09-11 |
| A-05 | Reference and target technical architecture; per-file system inventory | A-01 | [TECHNICAL_ARCHITECTURE.md](TECHNICAL_ARCHITECTURE.md); the inventory is absorbed into [MIGRATION_MAP.md](MIGRATION_MAP.md) §2–§7 | Every sim/game/harness/tools source file appears in a migration-map row (178/178); Unity proposal marked subject to verification | done 2026-09-11 |
| A-06 | Migration map, decisions, tasks, README; cross-document consistency and coverage checks | A-02–A-05 | [MIGRATION_MAP.md](MIGRATION_MAP.md), [DECISIONS.md](DECISIONS.md), this file, [../README.md](../README.md) | Path/link/§/ID check script passes; each system has a disposition and a task; values agree across documents | done 2026-09-11 |
| A-07 | Independent omission/contradiction review (one Opus worker) and resolution | A-06 | Review findings folded into the documents; residue listed in §7 | Findings resolved or explicitly deferred; no repeated review cycles | done 2026-09-11 |
| A-08 | Reference project handoff: `docs/PROGRESS.md` paused pointer, `CLAUDE.md`/`AGENTS.md` pointer | A-07 | Edited reference files | Pointers present; unrelated rules untouched; reference game still runs | done 2026-09-11 |

---

## Phase B — Unity foundation, data and simulation core

Goal (revised 2026-09-11, U-M-22): a working Unity project that compiles cleanly with its editor tooling verified; an engine-independent simulation foundation (state, tick, commands and events, PRNG, inventory and ledger, map and movement core, save/load, timing host) with a few small checks (inventory conservation, save/load round trip, sim timing, replay consistency); the data assets the foundation and the opening need; and one visible scene with movement, one machine and an editable UI Toolkit panel. No authored world, no production chain and no complete gameplay yet: crafting and machines (B-07), power (B-09) and belts (B-10) moved into Phase C, where the opening needs them, with their IDs kept. Verification is proportionate: each subsystem's checks live on the task that ports it; broader harnesses, bots, nightly runs and soaks are deferred with explicit triggers (DEF-01–DEF-04 below, U-M-23). Exit: B-ACC passes.

Scope rule for Phase B (2026-09-11): checks are written for behaviour that exists; nothing is tested before it is built. A ported reference test that fails in Unity is investigated when it is ported, not triaged in bulk. The 146 reference test failures (318/464 on 2026-09-11) are **reported failures whose individual provenance is not established**; they are neither labelled pre-existing wholesale nor made a migration prerequisite (MIGRATION_MAP.md §1).

| ID | Task | Depends on | Deliverables | Sources | Acceptance | Status |
|---|---|---|---|---|---|---|
| B-01 | Install and record the Unity target: Unity 6.6 (6000.6.x, an Update release — U-M-01), UI Toolkit runtime, Input System, 2D Tilemap and the Vector Graphics package (**`com.unity.vectorgraphics`, the SVG importer, is verified absent from the shell's `Packages/manifest.json`; the `com.unity.modules.vectorgraphics: 1.0.0` entry that *is* listed is the built-in module, a different thing that does not import `.svg`** — B-01 therefore either installs the package or records that the world pipeline rasterises SVG at export: TECHNICAL_ARCHITECTURE.md §4.0.2.1); record the exact installed editor and package versions in DECISIONS.md U-M-01; no silent version change (if 6.6 or a package cannot be installed, report and stop for the owner). Observed 2026-09-11: the owner has installed `6000.6.0f1` and created a project shell at `Unity/Relight/` via Unity Hub (TECHNICAL_ARCHITECTURE.md §4.0.2, observed note), so the task now starts by recording that install and its package manifest, and by logging two deviations for B-02: the project lives under `Unity/Relight/` rather than `Unity/`, and it carries its own nested git repository | A-08; owner go-ahead | Updated DECISIONS.md U-M-01; TECHNICAL_ARCHITECTURE.md §12 checklist ticked; SOURCE_INVENTORY.md baseline row points at `reference-snapshot.sha256` (U-M-19) | S-A3; TECHNICAL_ARCHITECTURE.md §4.0, §12; U-M-01, U-M-19 | Installed version recorded with evidence: the exact patch string is read from `ProjectSettings/ProjectVersion.txt` (not from memory or the Hub list) and written into U-M-01; every deviation from the proposal decided and logged; `grep -rn "6.6 LTS" Unity/Docs Unity/README.md` finds only the explicit withdrawal notes (DECISIONS.md U-M-01, TECHNICAL_ARCHITECTURE.md §4.0.1 and §12) | done 2026-09-12 — DECISIONS.md U-M-27 records `6000.6.0f1 (f7f8ed4d1e24)` from `ProjectVersion.txt`, every package version, the template-package trim with justification, SVG rasterise-at-export, the shell/nested-git/LFS deviations and the upgrade-review trigger; TA §12 ticked; SOURCE_INVENTORY re-verified the snapshot (254 OK). Sources: the owner's shell `ProjectSettings/`, `Packages/manifest.json`. Checks: `grep -rn "6.6 LTS"` finds only withdrawal notes |
| B-02 | Create the Unity project in `Unity/` with the assembly layout of TECHNICAL_ARCHITECTURE.md §4.1 (`Relight.Sim` engine-independent, holding the plain sim input/data record types; `Relight.Data` ScriptableObjects and converters, which references `Relight.Sim`; `Relight.World`, `Relight.Presentation`, `Relight.UI`, `Relight.Editor`, `Relight.Sim.Tests` edit-mode, `Relight.Tests.Play` play-mode), `.gitignore`, and the Boot / MainMenu / World scene shells with a `BootController` (no monolithic constructor) | B-01 | Project folders, asmdefs, scene shells | S-38, S-A3; TECHNICAL_ARCHITECTURE.md §4.1, §7.1; U-M-02, U-M-07, U-M-08, U-M-25 | Project opens and compiles without errors; `Relight.Sim` has no UnityEngine reference and no reference to `Relight.Data` (asmdef references checked, not assumed); `Relight.Sim.Tests` references `Relight.Sim` only; removing `Relight.Data` and `Relight.World` from the solution leaves `Relight.Sim` compiling | done 2026-09-12 — project at `Unity/` (shell integrated, no nesting, README/Docs preserved); eight asmdefs per TA §4.1 (`Relight.Sim` `references: []` + `noEngineReferences`; `Relight.Sim.Tests` → `Relight.Sim` + Unity Test Runner only; Data/World/Presentation/UI/Editor/Tests.Play as specified); `.gitignore`; scenes Boot/MainMenu/World/GameUI written by the one-shot editor method `Relight.Editor.ProjectBootstrap.CreateScenes` (Boot holds `BootController` only; World has `World/Generated`, `World/Manual`, `Sim` roots; URP 2D template camera + global light). Checks: batchmode import + compile exit 0 with no `error CS` (`Logs/bootstrap.log`); `AssemblyBoundaryTests` asserts the Sim assembly's references; scratch `dotnet build` of `Assets/Relight/Sim/**` alone succeeds (the "remove Data/World, Sim still compiles" criterion). Editor-run of the EditMode test is recorded under B-12 **Amended 2026-09-12 (U-M-32):** the owner corrected the location; the project root is now `Unity/Relight/` (the shell), with `Assets/`, trimmed `Packages/`, `ProjectSettings/` and `Tools/` moved in after wave 2 and `README.md`/`Docs/` left at `Unity/`; batchmode uses `-projectPath "E:\Factorio2\Unity\Relight"` and the owner's editor must be closed while it runs. The nested `Unity/Relight/.git` remains the owner's action. |
| B-02a | Configure and verify the Unity MCP integration (`coplay-mcp`, U-M-20) against the open project: attach to the correct project/editor instance; asset refresh with compilation result and console read-back; scene and asset operations; Play Mode enter/exit; screenshot capture; interaction/input driving. Record what is actually exposed and the manual fallback for each capability that is not. The server is an interface to a Unity-side Coplay package: the task includes installing that package through the Package Manager git URL and authenticating it with the Editor open on this project, which is an owner decision point (third-party editor plugin plus an account). The two vendor pages give different plugin git URLs (`CoplayDev/coplay-unity-plugin.git#beta` and `CoplayDev/unity-plugin.git#beta`, both resolving on 2026-09-11); record which one actually installs. Configuration only: no packages or tools are installed beyond what the integration itself requires and the owner has agreed. Explicit exit: if the server does not connect after a reasonable attempt, recording "MCP unavailable, all capabilities manual" completes this task, since every capability has a manual or CLI fallback (TECHNICAL_ARCHITECTURE.md §14.5) and no other task's acceptance depends on an MCP tool | B-02 | Capability table in DECISIONS.md U-M-20 (exposed / not exposed / manual fallback) including the verbatim tool list the server registers (the surface depends on the plugin version and cannot be enumerated from documentation); README "Working agreement" updated if a rule changes | U-M-20, U-M-21; TECHNICAL_ARCHITECTURE.md §14; Unity/README.md "Working agreement" | Each of the six capability groups has a recorded verdict from an actual call, not from the server's description; a compile error deliberately introduced and reverted is visible through the integration's console read-back or the fallback is documented | done 2026-09-12 on the explicit exit — **MCP unavailable, all capabilities manual/CLI** (DECISIONS.md U-M-28 + §2.1 table with the 98 registered tools). Claude Code's connection timed out at every session start; the server starts by hand (`coplay-mcp-server 4.0.3`); the Unity-side plugin was **not** installed (owner decision point per this row) so nothing was exercised against the editor; TA §14.5 observed column filled; owner action to enable it named in U-M-28 |
| B-03 | Port core state, block tick, command dispatcher, event queue, explicit public façade, ruleset identity and PRNG | B-02 | `Relight.Sim` state types, tick pipeline, command/event records, seeded PRNG, `ITileLayer` in place of the `tileHooks` global | S-01, S-02, S-03, S-04, S-06; TECHNICAL_ARCHITECTURE.md §2, §10.2 | Edit-mode checks: PRNG reproduces the reference sequence (first 10,000 values from `seedRng(3)`; seeds 1–10 vectors); tick order matches TECHNICAL_ARCHITECTURE.md §2.4; no module-global mutable state | **done 2026-09-12** (Opus worker, coordinator-integrated). Delivered: `Assets/Relight/Sim/Core/{Simulation, Prng, StateHash, CommandDispatcher, SecondPhase, SimComposition.Core, SimState.Core}.cs`, `Core/Serialization/CanonicalJsonWriter.cs` (format spec in its class header), coordinator contracts `Core/Contracts/*`; tests `Assets/Relight/Tests/Sim/Core/*` + `Fixtures/prng-vectors.txt` (generated from the reference `seedRng` by `mulberry32`, 11,700 lines). Sources: `packages/sim/src/prng.ts`, `flow.ts` (`advanceFlow` 1104–1120, `stepFlow`), `sim.ts` (`step` 793–1085, `applyCommand`), `session.ts:226/258` (accumulator reset), `types.ts`. Intentional differences: (1) `maxTicks` is counted in tile ticks (`MaxTicksPerCall` = 5,120 = 256 s) not seconds; (2) `T` advances 1/20 per tick continuously (the reference only bumped it per second); (3) the accumulator lives on `Simulation`, not in saved state, and there is no `speed`; (4) `hourRow`/block economy of `step` omitted (retired, §17); (5) per-second `shots`/`iframes` resets deferred to Phase C combat; (6) `ITileLayer.AfterCommand` fires only for accepted commands (documented defect correction, the reference also fired on refusals); (7) `SecondPhase` runs at the top of tick n+1 rather than after tick n, same observable order; (8) doubles serialise shortest-round-trip (U-M-13); (9) `Prng.PickIndex` is a new helper; (10) danger counters live on `Engineer` (reference `player.danger`), moved by the coordinator from `SimState`. Checks (scratch .NET 8 build of the real files, not Unity's runner yet — see B-12): 37 tests green at handoff, 81 green after B-06/B-08 partials joined; PRNG vectors bit-exact for seed 3 × 10,000 and seeds 1–10 × 1,000, `int`/`uniform`/`hash01`/`pyRound` vectors exact; two-run equality and hash checks green. Limitations: H4 (long headless soak) not attempted; not yet executed under Unity's Test Runner (recorded when B-12 runs it). |
| B-04 | Simulation host: fixed 20 Hz step (50 ms tick interval) with the `floor(acc + 1e-6)` accumulator rule and `maxTicks` clamp, pause, no speed API, presentation interpolation hook | B-03 | `SimHost` MonoBehaviour, pause API | S-21 (`advanceFlow`), S-37; TECHNICAL_ARCHITECTURE.md §4.2; U-D-04 | Sim-timing check (play-mode): 1,000 ticks elapse in 50 s ± tolerance of wall time; pause halts ticks; no speed API exists; measured per-tick CPU cost logged separately from the interval (TECHNICAL_ARCHITECTURE.md §10.1 terminology) | **done 2026-09-12** (coordinator, Fable). Delivered: `Assets/Relight/Presentation/Host/SimHost.cs` (MonoBehaviour; the only sim clock: fixed 20 Hz via `Simulation.Advance`, `floor(acc+1e-6)` and `MaxTicksPerCall` clamp in the façade, `Paused` bool as the sole clock control which zeroes the accumulator, `Submit` for commands, `Alpha` interpolation hook, `TickBoundary`/`LastFrameEvents` for presentation, per-tick CPU cost measured with a Stopwatch and logged as avg/max ms separately from the 50 ms interval; `Time.timeScale` never touched); play-mode tests `Assets/Relight/Tests/Play/SimHostTests.cs`. Sources: `packages/sim/src/flow.ts` `advanceFlow` (accumulator rule, maxTicks), `game/src/session.ts:226` (clockPolicy zeroes accumulation on pause/resume) and `:258` (frame gap > 0.25 s discarded, never fast-forwarded). Intentional differences: (1) no speed field, speed buttons or fast-forward hooks exist at all (U-D-04; reference still carries `speed` state for headless runners — headless speed lives only in `Simulation.Advance`'s tick-count argument); (2) pause is a host bool, not `timeScale`, so UI/camera animation continues; (3) commands submitted while paused still apply at the next frame (reference speed-0 behaviour kept). Checks: Unity PlayMode Test Runner (batchmode, `-runTests -testPlatform PlayMode`) 4 passed / 0 failed / 1 skipped by default (`TestResults` copied to `Docs/evidence/phase-b/b04-playmode-short-2026-09-12.xml`): `TicksFollowWallClock` 200 ticks in 10.000 s wall (10.001 s accepted), `PauseHaltsTicks_AndUnpauseReleasesNoBacklog`, `CommandsApplyWhilePaused`, `NoSpeedApiExists` (reflection: no member named speed/timeScale/fastForward on the host); long form `TicksFollowWallClock_Long` run separately with `RELIGHT_LONG_TIMING=1`: **1,000 ticks in 50.000 s wall (50.009 s accepted), passed** (`b04-playmode-long-2026-09-12.xml`); logged tick cost avg 0.007 ms, max 2.132 ms over the run (cost, not interval). Limitations: batchmode `-nographics` play mode, not a rendered frame loop — the interactive scene check is B-13/B-ACC; tick cost measured on the synthetic 64×48 map with an idle engineer. |
| B-05 | Content data assets for the foundation and the opening: generate ScriptableObjects in `Relight.Data` from CONTENT_CATALOGUE values for the **retained** content the foundation and the Home opening use (items, recipes, machines, weapons, ammunition, turrets, the opening enemies, power, time, raid director, opening encounter, starting stake); each asset converts to the plain `Relight.Sim` record type; item icons and display strings as asset fields; editor menu `Relight/Export Data Tables` writing Markdown for doc diffing (docsync direction reversed). Tables for later subsystems (factories, tram, strongholds, further enemies, approved Phase E additions) are generated by the task that ports that subsystem with the same generator, so a catalogue row without an asset early on is expected, not a gap | B-02 | `Relight.Data` definitions and converters, generator script, generated assets in a named folder, export menu | S-A1, S-20, S-48, S-61; CONTENT_CATALOGUE.md tables (rows labelled Implemented and retained) and §17 (runtime content scope: which rows generate assets, the explicit exclusion list, the scoped approved additions, and the `Kind`/`Source` fields every asset carries); TECHNICAL_ARCHITECTURE.md §5; U-M-04, U-M-12 | Every catalogue row the foundation and the opening use has an asset; no asset exists for a Retired row or a row CONTENT_CATALOGUE.md §17.2 excludes; each asset carries its catalogue `Kind` and `Source`; the record types produced are declared in `Relight.Sim`, not in `Relight.Data` (U-M-25), and no sim code takes a `ScriptableObject` parameter; provisional values carry a `Provisional` flag; edit-mode validator passes; exported tables match CONTENT_CATALOGUE.md for the generated rows | done 2026-09-12 — **Delivered** (Opus worker B-05 under the Unity lock, coordinator-integrated): the export pipeline reference TypeScript (read-only) → `Unity/Relight/Tools/export/exportCatalogue.ts` (`cd /mnt/e/Factorio2 && npx tsx Unity/Relight/Tools/export/exportCatalogue.ts`) → `Tools/export/out/catalogue.json` + `Assets/Relight/Sim/Data/Generated/CatalogueData.g.cs` (deterministic, md5-identical on re-run) → 79 `.asset` files via `Relight/Setup/Generate Data Assets` (`Assets/Relight/Data/GameDataRegistry.asset` + `Assets/Relight/Data/Generated/{Items 20, Machines 26, Recipes 15, Weapons 4, Enemies 2, Ammunition 2, Turrets 2, Tuning 7}/`) → `Relight/Validate Game Data` → `Relight/Export Data Tables` writing 14 tables to `Unity/Docs/generated/*.md` (reversed docsync, S-61). `Relight.Data` assembly: `DataDefinition` base with `Key`/`Provisional`/`Source` provenance, Item/Recipe/Machine/Weapon/Enemy/Ammo/Turret definitions, seven tuning assets, `GameDataRegistry.Build()` converting once to plain `Relight.Sim` records (U-M-25). `Relight.Sim/Data/GameData.cs` extended with the weapon, enemy, ammunition, turret and tuning records; `ReferenceData.Create()` is now a thin wrapper over `CatalogueData.Build()`. Editor: `Editor/Data/{DataAssetGenerator,DataValidator,DataTables}.cs`. Tests: `Tests/Sim/Data/CatalogueDataTests.cs` (10). Twelve named extension points and the §17.2 exclusions are recorded in `catalogue.json`; 13 provisional rows carry the flag. **Sources:** `packages/sim/src/{flow.ts 61–953, constants.ts 17–65, recipes.ts 9–43, tiles.ts 17–114, campaignThreat.ts 23–198, campaignDefence.ts 13–75, campaignPower.ts 10–20, engineer.ts 16–36, progression.ts 17–50, openingEncounter.ts 27–55, rules.ts, equipment.ts, fabrication.ts, gameplayCombat.ts, turretTracking.ts, walk.ts, weaponProfiles.ts, itemNames.ts}`; every generated value carries a `file:line` resolved at export time (a stale needle fails the export). **Intentional differences:** generator `PowerKw = −300` (signed supply, catalogue :136) although `MACHINE_KW.generator = 0`; the chest is a machine with a 200-slot inventory (B-06 transfer semantics); `InventorySlots` derived (processors = max distinct inputs, fuel/ammo holders 1, else 0); live minor-raid band 6–10 over `CAMPAIGN_THREAT` 8/12 (§16 row 7); turret damage 10 (campaign column) not the excluded legacy 4; sprint/stamina expressed as per-second rates; display names from `itemNames.ts`/`KIND_LABEL` except belt/inserter/pole; generated numbers are `double` only. Delegated choices in U-M-33. **Checks:** exporter exit 0 (items 20, machines 26, recipes 15, weapons 4, ammunition 2, turrets 2, enemies 2, tuning 7, extension points 12); batchmode Generate ×2 (second run 0 created/0 removed, all 79 `.meta` GUIDs identical), Validate (78 assets checked, 0 problems), Export Tables (14 files), all with 0 `error CS`; ten hand spot-checks catalogue line → generated value (steel stack 50, bullets 200, chest 10 S / 200 items, generator −300 / fuel 50, turret 20 kW provisional, hopper 50 ×1.25, rifle row, skitter row, assault roster 60 = 40 + 20 / 4 sectors, coal 4 MJ / 20 Hz / day 1200 s / night 900 s) all agree; scratch `dotnet test` and Unity EditMode after the move: 172 total, 171 passed, 0 failed, 1 explicit long form skipped (`evidence/phase-b/b12-editmode-wave2-2026-09-12.xml`); `GameDataHash` extended by the coordinator to cover all nine tables. **Limitations:** no test loads the `.asset` files and compares them field-by-field against `CatalogueData.g.cs` (they agree by construction from the same records); item icons are null (C-06); `OnValidate` warnings and `[CreateAssetMenu]` not exercised interactively (no GUI session, B-ACC); no docsync check yet enforces `Docs/generated/*.md` against a fresh export; nothing in B-05 is PlayMode-dependent; an Inspector edit of a generated asset is a B-ACC observation. **Handoff closure 2026-09-12 (U-M-36):** the generated assets are the editable source of truth once seeded — `DataDefinition` carries a hidden `generatedFingerprint`, `Generate Data Assets` stamps what it writes and keeps any asset whose values no longer match the stamp (console lists them), and `Regenerate Data Assets (discard hand edits)` is the only overwrite path. Verified in batchmode on a project copy: first Generate stamped all 78 assets (`no hand-edited assets found`); editing `walkTilesPerS` 6→7 in `Tuning - Engineer.asset` then Generate reported `kept 1 hand-edited asset(s)` and left 7; Regenerate restored 6 with the identical fingerprint. The stamped assets were copied into the real project (only the fingerprint line differs). **Repair pass 2026-09-12 (audit F7, U-M-37):** the generator prints its mode line (`Generate Data Assets` keeps hand edits; `Regenerate Data Assets (discard hand edits)` is the only reset), reports `kept=N conflicts=N`, keeps an asset whose definition no longer exists (listed as orphaned, never deleted) and reports `CONFLICT` when both the asset and the catalogue value changed since the last stamp; batchmode aliases `GenerateKeepingHandEdits`/`RegenerateDiscardingHandEdits`. A new game reads its starting stake from `GameData.Stake` (`InventoryInitializer`), and `InventoryInitializer.StakeProblem` reports a missing or invalid stake asset. Verified in batchmode on the project copy (`Docs/evidence/phase-b/repair-2026-09-12/generator-gen-*.log`): Generate (kept=0), edit stake steel 20→33, Generate (kept=1, all `.meta` GUIDs unchanged), a copy-only PlayMode check saw steel 33 in a new game with no ledger problem, Regenerate restored 20, then a catalogue edit plus a hand edit produced the `CONFLICT` line. `StakeTests` 8/8 in Unity EditMode. |
| B-06 | Port inventory, engineer pockets/Backpack allocation, stacks, transfers, conservation ledger, read-only selectors, item/bullet semantics | B-03, B-05 | Inventory model, ledger, transfer commands, query layer | S-07, S-08, S-18; U-D-05, U-D-08, U-D-09 | **Inventory conservation check** (edit-mode): ledger balances across every transfer, refusal and cancel path on a scripted sequence; presentation has no path to `SimState` other than the query layer | **done 2026-09-12** (Opus worker, coordinator-integrated; note the row's B-05 dependency is satisfied by the temporary `ReferenceData.Create()` stub, which B-05 replaces with exported data without changing the `GameData` shape). Delivered: `Assets/Relight/Sim/Actor/{Engineer.Inventory, Pockets, InventoryCommands, HandCraft, Stats, Ledger, SimComposition.Inventory}.cs`, `Sim/Building/{MachineInventory, Placement}.cs`, `Sim/Queries/{InventoryQueries, LedgerQueries}.cs` (immutable view records); tests `Assets/Relight/Tests/Sim/Actor/{InventoryFixture, PocketsTests (4), LedgerTests (6), HandCraftTests (5), TransferTests (6)}.cs`. Sources: `packages/sim/src/engineer.ts` 1–175; `machineInventory.ts` 1–65; `ledger.ts` 1–114; `flow.ts` 387–390, 768–782, 1002–1042, 1281–1318, 1367–1421, 1467–1490, 2202, 2218–2320, constants 64/237/244/1766; `ground.ts` 590–598; `rules.ts:10`; `test/ledger.test.ts`. Intentional differences: (1) chest transfers use the single machine-transfer path with capacity from `MachineSpec` (the reference `handPool`/`chestTake`/`chestPut` half lives in the retired abstract stock, U-D-32); (2) a mid-batch hand-craft cancel keeps unreturnable plates as `Consumed` instead of dumping them in retired Depot stock, so the ledger still balances (U-D-05); (3) 'near the Depot' = within reach of a placed `depot` machine, pending the authored HQ lot from Phase C world data; (4) one magazine = one bullet (U-D-08), `ROUNDS_PER_MAG` branches removed; (5) machine `Out` counts as magazines until recipes arrive (Phase C); (6) placement charges no build cost yet (Phase C); (7) truck pockets, kits, hand mining and the retired stats (`railCoal`, `ringDraw`, `ringFired`, `handMined`) omitted (§17); (8) display names come from `GameData` — coordinator set `steel` → 'Steel plates' and `turret` → 'Gun turret' to match `itemNames.ts`/`KIND_LABEL`, so refusal strings are verbatim; (9) turret hopper 50 and generator fuel cap 50 are `MachineSpec.AmmoCap`/`FuelCap` data (coordinator change on the worker's proposal); (10) the Backpack is visited as `hasPack`+`packItems`+`packCounts` because the visitor list cannot carry nulls. Checks: scratch build 0 errors; 21 tests green (whole tree 102); ledger `Conservation` balances across take/drop/chest transfer, refused transfer, hand-craft cancel and generator overfill (tolerance 0.01, reference problem text); reference `ledger.test.ts` 3/3, `playtestFix2.test.ts` 5/5, `playerTestFixes.test.ts` 3/3 pass on the reference; two pre-existing reference failures observed and not ported (`fabrication.test.ts` #1 power, `uiRedesign.test.ts` #4 quickbar). Limitations: belts/inserter hands/committed deliveries are declared ledger places but always 0 (Phase C); no production, power, build cost, hand mining, equipment or weapon logic; power/production/belt/affordability/threat selectors from `queries.ts` are Phase C; the 'presentation has no path to SimState' half of the acceptance is checked when B-13 builds the presentation. **Repair pass 2026-09-12 (audit F1–F2, U-M-37):** hand crafting locks the engineer for the whole batch (`HandCraft.HandLocked`; move, walk and sprint commands are refused while locked; the reference deviations — velocity retained, cooldowns decay — are recorded in U-M-37); cancelling a batch with a full Backpack keeps the overflow as a saved, ledger-counted pending refund (`HandState.RefundSteel/RefundCopper`) that supersedes the U-M-31 (2) discard compromise; save schema bumped to v2. `HandCraftTests` 9/9 and `HandLockTests` 7/7 in Unity EditMode (`repair-2026-09-12/editmode-handcraft-handlock.xml`); the `ShortRun` replay fixture now hand-crafts at tick 250 after the walk so the lock is exercised by the replay checks (`ReplayConsistencyTests` 8/8). |
| B-08 | Port map/tiles/ground/footprints/graph and the movement/navigation core with a small synthetic test map; geometry injected, never compiled in (the sim-side light model moves to C-11, where the opening first needs it) | B-03 | Tile grid, footprint checks, path queries, `ICityGeometry` injection seam | S-09, S-11; WORLD_AND_ASSETS.md §2; U-M-14 (Y-down sim space) | Path/walk/body checks on the synthetic map; blocked/walkable semantics match WORLD_AND_ASSETS.md §2.6 | **done 2026-09-12** (Opus worker, coordinator-integrated). Delivered: `Assets/Relight/Sim/World/{Tiles, ArrayGeometry, SyntheticMap, Ground, PathFinder, WorldQueries, SimComposition.World}.cs`, `Sim/Geometry/Segments.cs`, `Sim/Actor/Movement/{MovementCommands, EngineerMovement}.cs` (`MoveCommand`, `WalkCommand`, `SprintCommand`, `DodgeCommand`, `AimCommand`; body tick; spawn initialiser); tests `Assets/Relight/Tests/Sim/World/{WorldTestSupport, GroundTests (12), PathFinderTests (10), PathVectorTests (4), EngineerBodyTests (18)}.cs`. `SyntheticMap` is new Unity test content (64×48: street grid, river with one crossing, one solid building, rubble/patch/deposit/inert, spawn). Sources: `packages/sim/src/ground.ts` 30–75, 440, 479–540, 589–600, 620–646; `walk.ts` 22–32, 50–57, 59–142, 146–165, 167–261; `engineer.ts` 141, 215–225, 321–338 + movement constants; `tiles.ts` 20–35; `test/walk.test.ts`, `test/body.test.ts`. Intentional differences: (1) `Inert` tiles are walkable, as in the reference (`ground.ts:440` excludes only river and solid building walls) — the brief's contrary parenthetical was the coordinator's error, not a decision; (2) building collision is a second mask (`urban.solid`), exposed through the geometry contract rather than a tile class (WA §2.6); (3) `DODGE_COST` 0.25 is now `EngineerTuning.DashCost` data, not a constant; (4) no `WalkToCommand` — the reference `walkTo` takes a block index (retired lattice), click-to-move is `MoveCommand`; (5) `Engineer.TakeDamage` replaces `hurt` (name taken by the `Hurt` field); (6) dug state is keyed by tile, not block; (7) HQ/patch layout constants (`HQ_PATCHES`, `HQ_SUBSTATION`, `hqLot`, `markPatches`) dropped — authored content the Phase C importer supplies through `ICityGeometry` (U-M-14); a real Phase C gap, recorded; (8) no truck speed multiplier (Phase C); (9) `walkedHour` not recorded (retired telemetry); (10) the engineer's route is not saved (reference `walk.ts:8`), load re-plans next tick. Checks: scratch build 0 errors; 44 tests green (whole tree 102 after integration); A* vector check — `walk.ts:80–142` copied verbatim into a node script over the same synthetic map, four routes (incl. clearance 1, 53 steps on the longest) `diff` identical to the C# output, baked into `PathVectorTests`; grep for `float`/`Mathf`/LINQ/`Random`/`Dictionary`/`UnityEngine` in the delivered files → none. Limitations: body tick has no vector comparison against `tickEngineerTiles` (needs a full reference `flow`), checked against documented rates only; `Sight` omits wall/barricade machines and the freight gate (Phase C); `SolidMap` omits the truck, unrecovered sites and gates, and treats every machine as intact (no `Machine.Hp` yet); truck seat, hand-craft lock, `fireRound` and the per-second `iframes` reset are Phase C; per-patch authored unit counts are Phase C content. |
| B-11 | Save/load: versioned JSON schema v1 with `dataVersion`, transient exclusion, canonical writer, file slots under `persistentDataPath`; **manual slots plus a rotating autosave ring (U-D-35): autosaves are written to their own namespace, never overwrite a manual slot, use a configurable interval and rotation count with recorded defaults (**5 minutes of unpaused sim time, 3 ring slots** — TECHNICAL_ARCHITECTURE.md §9.4, which also fixes the ring layout, the tick-boundary capture point and the disk-full / file-locked behaviour), and every write is atomic (write to a temporary file, fsync, replace) so a failed or interrupted write leaves the previous save intact and loadable**; no old-save import, no migration path and no general reference-save reader (U-D-27, U-M-06, U-M-13); a narrow one-off reader for one named save is permitted only on a stated concrete trigger (TECHNICAL_ARCHITECTURE.md §9.3: a subsystem disagreement no other evidence settles); inert `profile` slot field kept (U-D-32); **a `playSeconds` accumulator is added to the schema — the reference save format has no playtime field (`save.ts:74-88`, `makeSave` `save.ts:128-134`) and `Session.realElapsed` (`session.ts:137`, `:259`) is never serialised, so U-D-41’s playtime metadata is new content this row originates** | B-06 | Serializer, slot manager, play-time accumulator | S-05, S-37, S-A4; TECHNICAL_ARCHITECTURE.md §9; U-D-27, U-D-35, U-M-06, U-M-13 | **Save/load round-trip check** (contract H1, TECHNICAL_ARCHITECTURE.md §10.4: `UnityHash(Load(Save(state))) == UnityHash(state)`, never a comparison against the reference): Unity-side state hash equal after save → load for a 5-minute headless run; a corrupted or old-format file is refused with a message, not migrated; **autosave checks:** an autosave never writes into a manual slot, the ring keeps exactly the configured number of entries and drops the oldest, and a write failure injected mid-save leaves the previous autosave loadable | done 2026-09-12 — **Delivered** (Opus worker B-11, coordinator-integrated): `Relight.Sim/Core/Serialization/{JsonReader,JsonStateReader}.cs` (strict parser, reading `IStateVisitor`); `Relight.Sim/Persistence/{PlayClock, SaveSchema, GameDataHash, IFileSystem, SystemFileSystem, AtomicWrite, SaveResult, SaveSerializer, SaveStore, AutosaveSettings, AutosaveStore, AutosaveScheduler}.cs`; `Relight.Presentation/Persistence/AutosaveController.cs` (the only engine-facing file: `persistentDataPath`, `SystemFileSystem`, `SimHost.TickBoundary`, save-on-quit, `Debug.LogWarning` on failure); tests `Tests/Sim/Persistence/{PersistenceTestSupport (MemoryFileSystem with injectable faults), SaveRoundTripTests 7, SaveRefusalTests 17, AutosaveTests 20}.cs`. Schema v1, kind `relight-save-unity`, one canonical JSON document, header `{schema, kind, dataVersion, savedAt, playSeconds, hash}` with the checksum covering only `state`; `playSeconds` is an unhashed header field on a `partial SimState` (U-D-41); transients excluded by not being visited; every write is `.tmp` → durable write → atomic replace with a retained `.bak`; layout `{persistentDataPath}/saves/exploration-v2/slot-<name>.json`, `auto/auto-<i>.json`, `auto-quit.json`, `auto-index.json` (TA §9.4.1); recorded defaults (interval, ring size, event and quit saves) in `AutosaveSettings`; 3-strike failure policy with resume on manual save. **Sources:** `packages/sim/src/save.ts:1–120` (envelope, `SAVE_TRANSIENT` :48, hash over state), `saveLoad.ts` (refusal order version → kind → shape), `packages/game/src/session.ts:~190–260` (save/load wiring, save on quit; confirms no reference autosave), TA §9.1–9.4.6/§10.4–10.6, U-D-04/27/32/35/41, S-05/S-37/U-M-06/U-M-13. **Intentional differences and corrected defects:** different marker (`relight-save-unity`, not interchangeable with reference saves); no migrations and no converter (exactly v1, everything else refused by number); checksum mandatory (reference advisory); `dataVersion` mismatch warns and loads (never refuses); transient exclusion by construction rather than by name; atomic writes (reference wrote in place); a corrupt own file is set aside as `.corrupt` and the loader falls back to `.bak` then the next-newest ring slot (reference deleted/overwrote); foreign files are refused and left untouched, never renamed; the whole autosave ring, index and `playSeconds` are new (no reference). **Checks:** scratch `dotnet test` 44 persistence tests green; Unity EditMode on `Unity/Relight`: 172 total, 171 passed, 0 failed (`evidence/phase-b/b12-editmode-wave2-2026-09-12.xml`) — the first Unity run had 20 persistence failures caused by `MemoryFileSystem` keying directories with `/` while `Path.GetDirectoryName` returns `\` on Windows; fixed by normalising in the test support (`Dir()`), production untouched; H1 wired in `SharedChecksTests.SaveThenLoadGivesTheSameHash` (hash, canonical text and conservation equal after write → read of the shared short run); `AutosaveController.cs` compiled by the Unity EditMode and PlayMode runs (0 `error CS`). **Limitations:** the real `SystemFileSystem` is not exercised by a test (all tests use the in-memory file system; the `File.Replace` → `File.Move` fallback and `PlatformNotSupportedException` branch are reasoned, not observed — the first real save in the editor is a B-ACC observation); no directory fsync (accepted, TA §9.4.4); the TA §9.4.3 background writer is not implemented (write is synchronous behind `IFileSystem`; Phase F measures the cost); event triggers exist (`AutosaveScheduler.Trigger`) but nothing calls them yet (Phase C rows own the events); settings persistence and the HUD notice surface are not built (notice string exists on the scheduler); save-file size 1,747 bytes for the round-trip state (one chest, 57 ticks); **handoff closure 2026-09-12:** one real filesystem round trip performed by the `[Explicit]` `Relight.Tests.Play.DiskRoundTrip` pair (`Tests/Play/Persistence/DiskRoundTripTests.cs`) in two Unity processes — phase 1 moves the engineer (6.5, 6.5)→(9.5, 8.5), places a chest at (10, 8), transfers 7 steel into it and saves slot `roundtrip-check` through `SystemFileSystem`; phase 2 in a fresh process loads it and matches hash `8d00c38e`, tick 57, position, 13 steel / 5 copper carried, chest contents, then ticks on with the views following (evidence `b11-disk-roundtrip-phase{1,2}.json`; README “Disk round trip”); `AutosaveController` must be added to the `SimHost` object by B-13 (null-guarded otherwise). **Repair pass 2026-09-12 (audit F3–F4, U-M-37):** a missing or unreadable primary loads from its `.bak` and the primary is restored from it (`SaveStore.RestorePrimary`), repeatably across load cycles; `auto-index.json` is a cache reconciled against the ring files on every read, oldest chosen by saved-at → tick → previous sequence → file name, so a stale or lost index can no longer restart the ring at slot 1 or overwrite the newest autosave. Unity EditMode (`repair-2026-09-12/editmode-focused.xml`): `RecoveryTests` 9 (MemoryFileSystem, simulated failures), `RealDiskRecoveryTests` 1 (real NTFS files under %TEMP%), `AutosaveTests` 20, `SaveRefusalTests` 17, `SaveRoundTripTests` 7, all passed. The two-process disk round trip was repeated on the real save directory after the schema change: phase 1 hash `3148eb4f`, tick 53, 1783 bytes; phase 2 loaded it in a fresh process; the previous `slot-roundtrip-check.json` was kept as `.bak` and the primary replaced (`b11-disk-roundtrip-phase{1,2}.json` rewritten). **Loose-ends pass 2026-09-13 (U-M-38):** (a) a **version-1 file is read** through `Sim/Persistence/SaveUpgrade.cs` — its checksum verified over the document as written (`JsonValue.ToCanonicalJson`, byte-identical to the writer), `state.hand.refundSteel/refundCopper` defaulted to zero on the parsed document, the built state required to hash as the upgraded document, `state.Version` set to 2 in memory; the file is never written by a load and the next save keeps it as the `.bak`; `SaveSchema.OldestReadable = 1`, `ReadHeader` lists v1 slots, the ring treats a v1 autosave as a working save, a file claiming version 1 with version-2 members is refused and left alone, every other number is refused as before (`LoadResult.Upgraded` carries the player note); (b) `AutosaveController` **resets its scheduler on every `SimHost.SessionChanged`** and on enable / at a tick boundary when `SimHost.Session` moved while it was disabled (elapsed interval, failure count, stopped ring, notice, `LastSave`, `LastLoad`), so a new game from `WorldBootstrap` no longer inherits the previous session's clock or stopped ring; `Adopt` records `LastLoad` after the attach; (c) refund delivery confirmed unchanged (see B-06/U-M-38). Checks (`evidence/phase-b/loose-ends-2026-09-13/`): scratch `dotnet test` over the real `Sim` + `Tests/Sim` sources in place — `SaveUpgradeTests` 11 (new), `SaveRefusalTests` 17, `SaveRoundTripTests` 7, `AutosaveTests` 20, `RecoveryTests` 9, `RealDiskRecoveryTests` 1, `SharedChecksTests` 7, `HandCraftTests` 10 (one new) = **82/82**; `AutosaveController.cs`, `SimHost.cs`, `SceneFixture.cs` and the new PlayMode `Tests/Play/Persistence/AutosaveSessionTests.cs` (3) compiled against `UnityEngine.CoreModule` 6000.5.8f1 with 0 errors — **the PlayMode tests were not run and no editor was opened** (6000.6.0f1 not installed here; MCP reported no instance). Not observed: the console note on loading a v1 file, the real-disk v1 load (`slot-roundtrip-check.json.bak` on the owner's machine is the one known v1 file), and the session reset in Play Mode — B-ACC items 12–13. |
| B-12 | Test foundation: `Relight.Sim.Tests` edit-mode assembly that steps `Relight.Sim` without a scene, a small scenario builder, and the **replay-consistency check** (same seed and command list run twice → equal Unity-side hash). Hosts the checks named on B-03, B-04, B-06, B-08 and B-11; later subsystem tasks add their own checks here. No separate .NET runner, no hour bot, no bulk port of reference tests, no triage list of the 146 reference failures | B-03 | Test assembly, scenario builder, replay check | S-36, S-59, S-A2; TECHNICAL_ARCHITECTURE.md §10.6; U-M-13 | The four shared deterministic-fixture checks (PRNG vectors and two-run equality from B-03, save round-trip from B-11, conservation from B-06) plus the B-04 and B-08 checks run green from the Test Runner and from the command line (`-runTests -testPlatform EditMode`); replay check green | done 2026-09-12 — **Delivered** (Opus worker B-12, coordinator-integrated): `Relight.Sim/Core/Replay.cs` (production `LoggedCommand` + `Replay.Run`, the ported `hour.ts replay`); `Tests/Sim/Support/{ScenarioBuilder, Scenarios, SimTestUtil}.cs` (fluent seed + map + commands-at-ticks fixture; the one shared short run `Scenarios.ShortRun`: seed 7, 21 commands, 6,000 ticks = 5 sim minutes, source → chest → hand-craft → Backpack; `Step`, `Canonical`, `ConservationProblems`, `FirstDifference`, `SwapCommands`); `Tests/Sim/Replay/ReplayConsistencyTests.cs` (8: two runs of the same log give equal hashes and canonical text, a different seed differs, swapping two logged commands changes the outcome, an empty log is just ticking, `dropAim` filter) and `SharedChecksTests.cs` (8: conservation at the end and sampled through the run at tolerance 0.01, save round trip H1, timing `N ticks == N × 0.05 s` ±1 tick with `+1e-6`, every visited field reaches the canonical text; long form `[Explicit]` gated on `RELIGHT_LONG_TIMING=1`). The `Relight.Sim.Tests` asmdef steps the sim with no scene. **Sources:** `packages/sim/src/hour.ts:1481–1497` (replay loop, command-before-tick ordering), `flow.ts:1107–1113`, `packages/sim/test/hour.test.ts:103–107` (two-run comparison), `ledger.test.ts:28,:91` and `ledger.ts:103` (conservation tolerance 0.01), TA §10.4–10.6, DEF-01/DEF-02, U-M-13. **Intentional differences:** whole-state comparison via `StateHash` instead of the reference's selected fields (stronger); the hour bot (1,548 lines) and `replayVerdict` are not ported (S-36/S-60, DEF-01) — the hand-written deterministic log plays the bot's part; the `setSpeed` filter has nothing to match (U-D-04) and is dropped, `dropAim` kept; no cross-language hash comparison (U-M-13, DEF-02); conservation sampled through the run; hand mining absent (needs Phase C ground deposits); three anti-vacuity guards have no reference counterpart. **Checks:** scratch `dotnet test` (NUnit 3.14 = com.unity.test-framework 1.8.0's) 171 green; Unity EditMode on `Unity/Relight`: 172 total, 171 passed, 0 failed, 1 skipped (the explicit long form), 0 `error CS` (`evidence/phase-b/b12-editmode-wave2-2026-09-12.xml`); long timing form previously run under Unity for B-04 (1,000 ticks in 50.000 s); all new `.cs` files have `.meta` files after the import. No production defect found (one test's own expectation corrected: swapped commands change the hash and distance walked, not the end position). **Limitations:** H3 (cross-language agreement on selected outcomes) is not covered (deferred with reference parity); `EveryVisitedFieldReachesTheCanonicalText` matches leaf names, not paths; the hashes quoted in the worker report are self-consistency figures, never golden values; the PRNG vectors and two-run checks are B-03's and are cross-referenced, not duplicated. |
| B-13 | Minimal visible scene: engineer sprite moving on the synthetic map, camera, one machine prefab bound to its data asset, renderer-only view state, one UI Toolkit panel bound through the view-model layer, USS token sheet | B-04, B-05, B-08 | World scene stub, prefab pattern, view-state object, UXML/USS example, `tokens.uss` | S-08, S-39, S-41, S-45, S-49; TECHNICAL_ARCHITECTURE.md §7, §8; UI_AND_ONBOARDING.md §10; U-M-21 | Inspected in the Editor and in Play Mode (screenshot or owner look): prefab and data asset editable; panel opens in UI Builder and a layout change made there survives without code changes; view state never serialised | done 2026-09-12 — **Delivered** (Opus worker B-13 under the Unity lock, coordinator-verified): `World.unity` (World/Generated `Grid` with `Terrain` + `Solid` tilemaps painted from `SyntheticMap` by `Relight/Setup/Paint Synthetic Map`; `Sim` object with `SimHost`, `WorldBootstrap` (seed field, `GameDataRegistry.Build()` + `SyntheticMap.Create()`, additive `GameUI` load), `AutosaveController`, `WorldInput`, `MachinePresenter`; `Actors/Engineer` prefab instance; `Main Camera` orthographic with `CameraRig`; `Global Light 2D`) and `GameUI.unity` (`UIDocument` + `PanelSettings.asset`, `InputRouter`, `EscapeChain`, `UiShell`, `StatusPanelController`), both authored by the editor menu `Relight/Setup/Build World Scene` (`Editor/Scene/SceneSetup.cs`). `World/WorldSpace.cs` is the one sim→Unity mapping (1 tile = 1 unit, 32 px/unit, Y-flip in one place, cell `(x, −y−1)`); `World/TilePalette` + eight placeholder tile assets; `Presentation/{ViewState (literal port of view.ts), EngineerView (interpolates by SimHost.Alpha), MachineSnapshot, MachineView, MachinePresenter (id-keyed, rebuilt on SimState.Rev), PrefabRegistry, CameraRig, WorldBootstrap, WorldInput}.cs`; prefabs `Prefabs/Engineer.prefab`, `Prefabs/Machines/Chest.prefab` (holds its `MachineDefinition`), `Prefabs/PrefabRegistry.asset`; `UI/{IEscapeHandler, EscapeChain (eight ordered links, six named placeholders), InputRouter (World/UI/Global maps, World disabled while a panel is open), UiShell (one panel at a time), StatusPanelViewModel (150 ms throttle, no engine types), StatusPanelController (queries by name only)}.cs`, `UI/Styles/tokens.uss` (UI_AND_ONBOARDING §10 tokens), `UI/Panels/StatusPanel.{uxml,uss}`, `UI/RelightRuntimeTheme.tss`; `Input/RelightControls.inputactions`; play-mode tests `Tests/Play/Scene/{WorldSceneTests, CameraInsetTests, ViewStateSerialisationTests, HeldKeyTests, EvidenceCapture ([Explicit]), SceneFixture}.cs`; `Relight.Tests.Play.asmdef` gained the `Unity.InputSystem` reference. **Sources:** `packages/game/src/view.ts` (whole, literal), `worldScene.ts:89,:909,:921,:926,:1330` (follow speed, dt clamp, menus never move the follow target, draw at tile × TILE_PX), `uiShell.ts:45–49,:85–92,:114–120` (inset for layout only, held keys, escape order), `hud.ts:18,:47,:56`, `tiles.ts:17`, `style.css:120–132`. **Intentional differences:** engineer view interpolates between tick boundaries (the host ticks independently of the frame, B-04; one tick of display lag, toggle on the component); fixed framing `viewTilesHigh = 20` instead of `fitZoom` (which sized against the retired block lattice, CC §17); the status panel opens on start so the World map starts disabled (TA §8.3 on display; `UiShell.openOnStart`); Y-flip confined to `WorldSpace` (U-M-14); `EngineerView` ignores the zero-tick `TickBoundary` frames `SimHost` emits. Delegated choices in U-M-34. **Checks (automated):** batchmode compile 0 `error CS`; scene build menu exit 0; EditMode 172 (171 pass, 1 explicit skip) unchanged; PlayMode 16 (14 pass, 0 fail, 2 explicit skips: the long timing form and the evidence capture) — `evidence/phase-b/b13-playmode-2026-09-12.xml`: sim ticks and views follow the sim, a `PlaceMachineCommand` spawns the chest prefab bound to `Machine - Supply chest.asset` at the right footprint and `RemoveMachineCommand` removes it, the panel reads through the view-model and its button submits a real command, camera transform/size/rect bit-identical across panel open/close and `CameraRig` has no UI-typed field, `ViewState` is not `IVisitable`/`UnityEngine.Object`/`[Serializable]` and no serialised field in Presentation/UI holds a view type, a key held across a panel open/close does not re-fire (no consume-until-release guard needed; virtual devices via `InputSystem.QueueStateEvent`, `backgroundBehavior = IgnoreFocus` during the test). Three defects found by these tests and fixed in B-13's own code: `UIDocument.panelSettings` assigned in batchmode did not persist (Single-mode scene open unloaded the unreferenced asset, so the panel never laid out), zero-tick boundary frames, `walk-here` target equal to the spawn. **Checks (editor observations, headless):** `evidence/phase-b/b13-playmode-world.png` (RenderTexture read-back of the scene camera in Play Mode: painted map, engineer, chest at (8,8)) and `b13-playmode-panel.png` (the status panel with live values) — genuine Play Mode renders, coordinator-inspected, but not window captures, so world and panel are not composited. **UI Builder survivability proxy:** the worker moved `#row-ticks` and relabelled it in the UXML only, rebuilt and re-ran PlayMode with 0 failures and no C# change; the panel image shows the new order. **Inspector editables:** `MachineView.definition`/`z` on the chest prefab, `PrefabRegistry.asset` rows, the generated `MachineDefinition` assets, `CameraRig.viewTilesHigh/target`, `EngineerView.interpolate/z`, `WorldBootstrap.seed/uiSceneName`, `WorldInput.placeKind`, `UiShell.openOnStart`, `StatusPanelController.walkTarget`, `TilePalette` slots. **Limitations:** all sprites/tiles are flat-colour placeholders (no rule tiles); six escape links are no-op placeholders tagged C-02/C-06/C-10; no `@font-face` (C-07); one panel only; keyboard+mouse only, no rebinding; machine views not pooled; `MachineSnapshot` reads `SimState.Machines` directly (a selector is proposed, see U-M-34); `MachineDefinition` has no prefab field yet (`PrefabRegistry` is the interim); the owner's own Editor/Play look, an Inspector value edit and a UI Builder edit remain the B-ACC human observations. **Repair pass 2026-09-12 (audit F5–F6, U-M-37):** `SimHost` raises `SessionChanged` and counts `Session`; `MachinePresenter` clears and rebuilds its views on a session change even when the loaded state carries the same revision, and `EngineerView` reseeds instead of interpolating across the change; `WorldInput` submits one walk-stop and one sprint-release when a panel takes the map, so sprint → open panel → release Shift → close panel no longer leaves the engineer sprinting. PlayMode on the project copy (`repair-2026-09-12/playmode-focused-first-run.xml`): `SessionChangeTests` 3/3, `HeldKeyTests` 3/3 (new sprint test), `WorldSceneTests` passed. Not observed in the owner's editor (it held the real project open and the Unity MCP server did not connect); the human checks are in the B-ACC checklist. Residue: ~~`AutosaveController` does not reset its scheduler on `SessionChanged`~~ (closed 2026-09-13, U-M-38: it resets on every session change); `StakeProblem` has no player-facing surface. |
| B-14 | Proportionate prototypes, each inspected and recorded before scene structure is fixed: (a) inventory interaction — UI Toolkit runtime drag-and-drop over a 40-slot grid with a UGUI fallback decision (R6); (b) world rendering — `Tilemap` at 864×576 with the planned layer count (R7); (c) lighting approach at city scale (R2). Prototype code that works is kept and grown into C-06 / D-09 / C-11 rather than thrown away | B-13 | Prototype scenes and one recorded note per prototype in DECISIONS.md §2 (frame-time figure or fallback decision) | S-09, S-44, S-45, S-47; TECHNICAL_ARCHITECTURE.md §13 R2/R6/R7 | Each prototype has a recorded frame-time figure or a recorded fallback decision; kept code is marked as the basis of its later task | done 2026-09-12 — **Delivered** (Opus worker B-14 under the Unity lock, coordinator-verified): `Assets/Relight/Prototypes/` with its own `Relight.Prototypes` asmdef (+ nested editor asmdef; nothing in Sim/World/UI references it) and `README.md` naming which later task grows each prototype: `R6-SlotGrid/{SlotGridModel, SlotDragController, SlotGridPrototype}.cs` + `SlotGrid.uxml/.uss` + icons + `PrototypePanelSettings.asset`; `R2-Lighting/LightingRig.cs`; `Shared/{CameraSweep, FrameSampler, EvidenceWriter}.cs`; `Editor/PrototypeSetup.cs` (menus `Relight/Prototypes/{Build All, Build R6, Paint R7, Build R2}`); scenes `Scenes/{R6-SlotGrid, R2-Lighting}.unity` (not in Build Settings). Tests `Tests/Play/Prototypes/{PrototypeFixture, R6SlotGridTests (13), R7TilemapTests (2), R2LightingTests (2)}.cs` in `Relight.Tests.Play.Prototypes`. **Sources:** `packages/game/src/uiDrag.ts` (whole: left button + pointer capture, 7 px threshold, ghost, `elementFromPoint` → `IPanel.Pick`, drop-target class, drop only inside the page, 250 ms click suppression, cancel on pointercancel/lost capture/blur), `inventoryPanel.ts` (slot/stack structure, swap on occupied), `uiShell.ts:114–120` (Escape cancels drag first), TA §3.3 (reference worst frames 116.8 / 150.1 / 266.8 / 300.2 ms, light-mask repaints ~24 ms) and §7.1, WORLD_AND_ASSETS.md §7.1 (four tilemap layers), CONTENT_CATALOGUE.md lamp radii (4 / 6 / 12 tiles). **Results (all from named runs, GPU-fenced `Camera.Render` + 1-px `ReadPixels` probe at 1920×1080, D3D12, editor closed, no `-nographics`):** *R6* — every required behaviour works on one runtime UI Toolkit `UIDocument` (40 + 20 slots, pointer capture, ghost, pick hit-testing, swap, cancel outside, Escape-cancel ran first at order 100, cancel on focus loss, 250 ms click swallow; 13 tests pass); 60-slot full rebuild 0.474 ms, pointer move 0.409 ms (240 samples), 200 drag frames mean 1.92 ms / p95 1.99 ms (`evidence/phase-b/b14-r6-drag.json`) → **UI Toolkit kept, no UGUI fallback**. *R7* — four layers, Terrain 497,664 / 497,664 cells + RoadSurface 131,436 + TerrainVariant 91,248 + ResourcePatch 21,358 painted by the editor menu (array build 28 ms, four `SetTilesBlock` 1,023 ms, scene save 2,516 ms), scene load 1,883 ms, 10 s corner-to-corner sweep at the B-13 zoom mean 2.12 ms / p95 3.00 ms / max 12.8 ms; `TilemapRenderer` already chunks (Chunk mode, 32×32×32, max 32) (`b14-r7-{paint,load,frames}.json`, `b14-r7-map.png`) → **one tilemap per layer kept, no region chunking**; the painted scene serialises to 188.66 MiB of ForceText YAML, which is the finding itself (runtime painting is recorded in U-M-35 as a *proposed* D-09 approach, not a validated one: the figures are empty-scene figures). *R2* — over the full map at night (global 0.12) with 200 `Light2D` (120 × 4 t, 60 × 6 t, 20 × 12 t), 20 flickering and 10 moving per frame, then 50 `ShadowCaster2D`: tilemap only mean 1.92 ms, + lights 2.21 ms (+0.29), + shadows 2.49 ms (+0.28); p95 2.65 / 2.97 / 3.38 ms (`b14-r2-frames.json`, `b14-r2-night.png`) → **URP 2D lights as-is, no culling or baked split yet**. Decisions with kept code and fallback triggers in U-M-35. **Checks:** EditMode 172 (171 pass, 1 explicit skip) unchanged; PlayMode 31 total, 29 passed, 0 failed, 2 explicit skips with the R7 map painted (`b14-playmode-2026-09-12.xml`) and 26 passed / 5 skipped without it (`b14-playmode-clean-2026-09-12.xml`); 0 `error CS` in every log; screenshots coordinator-inspected as genuine renders. **Intentional differences:** drop policy is move/swap only (transfer/equip/quickbar policies are C-06 sim commands replacing three callbacks); lamp radii follow the catalogue (4 / 6 / 12), not the brief's ~4/8/12; Escape is hooked from the prototype through the existing `EscapeChain.Link` API (no UI change); **the painted `R7-Tilemap.unity` is measured and then kept out of the tree** (`Scenes/.gitignore`, rebuilt in ~3.5 s by `Relight/Prototypes/Paint R7`; the three map tests `Assert.Ignore` with that instruction when it is absent) — a deliberate deviation from "prototype scenes are kept", justified by the 188.66 MiB finding. **Limitations:** placeholder terrain (`R7Map`), 32 px placeholder sprites, no atlas, no entities/UI/sim in the sweeps — floor figures for the rendering risk, not a frame budget; `drag_max_ms` 113 ms is the first sampled frame (covers the synthetic setup) and the R7/R2 max frames are single outliers; memory figures are whole-editor-process numbers (575–590 MB allocated, 1.6–1.87 GB reserved) that bound but do not isolate the tilemap; events are synthesised (no real input device in the loop); `Light2D`/`ShadowCaster2D` live in `Unity.RenderPipelines.Universal.2D.Runtime` in 6000.6 and any future asmdef needs that reference. |
| B-ACC | **Phase B acceptance** | B-01–B-06, B-08, B-11–B-14 | Report in §8 | — | Project compiles with no errors; the B-03, B-04, B-06, B-08, B-11 and B-12 checks are green; a short scripted headless run (5 sim-minutes of move, mine, transfer) repeated twice gives equal hashes; the owner opens the project, edits one balance value in the inspector, moves the engineer in the visible scene and opens the panel in UI Builder | human — **pending owner inspection, not passed; on 2026-09-14 the owner explicitly deferred it as a prerequisite for starting Phase C** (§8) |

Deferred work (trigger-gated, U-M-23). Each row starts only when its trigger is recorded in §8; none is an open obligation and none blocks any acceptance row.

| ID | Deferred work | Trigger | Notes | Status |
|---|---|---|---|---|
| DEF-01 | Broad reference test port, a separate Unity-free `dotnet` test solution, the scripted hour bot, general replay infrastructure (logged command streams from the reference, long soaks, nightly runs), and triage of the 146 reported reference failures | A concrete requirement appears: a determinism drift that B-12's check cannot localise, a subsystem port that needs the hour bot's scripted run, the E-16 workload measurement, or the owner asking for a cross-implementation comparison | Per-subsystem test selection happens inside each subsystem's own task instead (§7). Nothing about the 146 reported failures is a migration prerequisite. Sources S-36, S-52, S-59, S-60; TECHNICAL_ARCHITECTURE.md §10.6–§10.8 | deferred |
| DEF-02 | Cross-language state-hash identity with the TypeScript `stateHash` | The owner asks for byte-identical cross-implementation hashes | Decided against in U-M-13; recorded here so the decision stays visible | deferred |
| DEF-03 | Telemetry for Unity (S-50) | Someone names a report they will actually read | The owner retired the old telemetry and reporting system (U-D-34): small local diagnostic tools only on a concrete need, no analytics service, no scheduled reporting programme | deferred |
| DEF-04 | Gamepad support | The owner asks for it | Out of initial scope by owner decision (U-D-42): desktop keyboard and mouse only. The Input System action maps stay extensible; no unused controller interface is implemented | deferred |

---

## Phase C — Complete Home opening, including saves

Goal: the opening is fully playable and correct in Unity on the real Founders Court area, giving the intended experience "I built a turret, saw it protect me, automated its supply, and could then leave to explore": New Game → starting stake → mining → Generator and power → first fully loaded operational turret → short directional warning → small introductory attack from one approach → visible ammunition consumption → automatic replenishment through real delivery → three turrets covering different approaches → a useful nearby excursion while production continues, with save/continue at any point. Focused, documented tuning of provisional values is permitted in this phase (U-M-12). Exit: C-ACC passes.

| ID | Task | Depends on | Deliverables | Sources | Acceptance | Status |
|---|---|---|---|---|---|---|
| C-01 | Import the Home region only (Founders Court and its approaches) through the minimum world pipeline (D-01a exporter, D-02a importer) so the opening runs on real geometry, bound to the import-manifest id. The full-city pipeline (D-01b/D-02b) is not a prerequisite. **Source confirmed 2026-09-14:** the live `packages/sim/src/city/riverfront.ts` (`RIVERFRONT_ID = riverfront-arc-v4-editor-ac16d9188c05`, 479 buildings, `homeOrigin [58,345]`) plus `city/gameplaySites.ts` and `maps/phaser/manifest.json`; the Tiled export under `maps/tiled/` is stale (287 buildings) and is never an input (WORLD_AND_ASSETS.md §1.3). The exporter is `packages/tools/src/exportUnity.ts` with the npm script `map:export-unity`, written under this row on 2026-09-14 (the earlier note that no exporter existed was true when the row was drafted and is superseded) | B-08; D-01a, D-02a | Generated Home-region tilemap and sites asset **Delivered 2026-09-14:** reference exporter `packages/tools/src/exportUnity.ts` with the npm script `map:export-unity`; `Unity/Import/home/city.json` (86,433 B) plus four side-cars of 28,626 B each (`terrain.kind/variant/patch/solid.bin`); `Editor/World/{CityFile,HomeRegionImporter,ImportedRegionPainter}.cs`; `World/Generated/Home/HomeGeometry.asset` and `HomeSites.asset`; `World/Manual/HomeSitesOverrides.asset`; `Sim/World/ImportedGeometry.cs` (`RegionGeometry : ICityGeometry`) behind `Presentation/WorldBootstrap.cs` (`geometry` optional, `SyntheticMap.Create()` fallback) (wave1-WA report) | S-15; WORLD_AND_ASSETS.md §2.7, §3–§4, §7; `packages/tools/src/exportUnity.ts`; `Assets/Relight/Editor/World/CityFile.cs`, `HomeRegionImporter.cs`, `ImportedRegionPainter.cs`; `Assets/Relight/Sim/World/ImportedGeometry.cs` | Home core, substation tile, deposits and approaches at the documented coordinates **Run 2026-09-14** through `Relight/World/Import Home Region`: region `78x367 at 43,91`, 49 buildings, 33 props, 24,360 accessible of 28,626 land tiles, map `riverfront-arc-v4-editor-ac16d9188c05`, source `b19f5a64d9e6`; the Founders Court substation (78, 347) is inside the window; the solid mask was re-derived on import and compared (0 mismatches of 28,626). **Deliberate difference:** the imported window carries **7** street-light sites, not the reference city's 18, because only emitters inside the Home rect are imported (wave3-WB report §6.3; WORLD_AND_ASSETS.md §2.8 correction owed). The 78x367 rect is the coordinator's ruling, kept as imported | done 2026-09-14 (limitations: the Home import window carries 7 street lights, not the city's 18; WORLD_AND_ASSETS.md §2.4, §2.8, §4.5 and §6.2 corrected 2026-09-14, U-P-10) |
| C-02 | Engineer: movement, reach, mining with feedback, interaction, ghost placement subset, equipment/action bar with unique slots, weapon ownership | B-06, B-08 | Engineer controller over sim commands **Delivered:** `Sim/Actor/Engineer.Equipment.cs`, `Sim/Actor/Mining/{Mining,MiningCommands}.cs`, `Sim/Building/Placement.cs`, `Presentation/WorldInput.cs`, `EngineerView.cs`, `Presentation/Engineer/MiningFeedbackPresenter.cs`, `Input/RelightControls.inputactions` (wave1-WC report) | S-18, S-19, S-26 (placement subset), S-41; U-D-07; UI_AND_ONBOARDING.md §5; `Assets/Relight/Sim/Actor/Mining/**`, `Assets/Relight/Sim/Building/Placement.cs`, `Assets/Relight/Presentation/WorldInput.cs` | Action-bar duplicate-slot test (one tool, one slot, swap); weapon-as-item transfer test; mining feedback visible **Deliberate differences (wave1-WC report §7):** right-click empties the hand, so the reference's B-13 chest binding is retired; a weapon can never be put in a chest (`ItemCounts` is enum-only); mining progress is engineer state (`Engineer.VisitEquipment`), not a map field; removal refunds the machine, not its price (coordinator ruling); there is no starting rifle — the run opens on a `StartingStake` of 20 Steel and 5 Copper. Ghost rotation was presentation-only here; rotating a **placed** machine is C-13 | done 2026-09-14 (limitations: in-world placement feedback — ghost, pick-up, rotate-on-ground and visible refusals — was carried to C-13, itself done 2026-09-14; a weapon still cannot be put in a chest, so the weapon-as-item transfer criterion is deferred beyond Phase C) |
| B-07 | Moved from Phase B (2026-09-11), ID kept. Port recipes, handcrafting, fabrication stations, machine inventories, and the `stepFlow` subsystem skeleton in reference order. **Owner brief 2026-09-14:** the stationary-handcraft lock and every incompatible action (moving, mining, placing, transfers that would break the batch) are refused through the real command paths, never only in the UI; Cancel, Escape and closing the interface follow the U-D-11 interruption rules, keep ingredients (pending refund, U-M-37 F2) and release input. **Superseded in part by U-D-44 (owner brief 2026-09-15, GP-W1):** the stationary-handcraft lock is withdrawn — the Home workshop processes its queue unattended at the same speeds, and moving, mining, placing and transfers are never refused for it. The cancel/refund accounting rules stand and are extended: every unprocessed ingredient comes back, Backpack first. | B-06 | Crafting and machine processing; `Flow.cs` split per subsystem, same call order **Delivered:** `Sim/Production/Machines/{ProductionState,ProductionRules,MachinePhase,ProductionQueries,SetRecipeCommand,SimComposition.Machines}.cs`; `MachineSpec` gained `RecipeStation`, `DefaultRecipe`, `SpeedMul`, `InputBufferMul`, `OutputBufferCap` and `Data/MachineDefinition.cs` five matching `SerializeField`s; `Tools/export/exportCatalogue.ts` extended and the catalogue regenerated by running the exporter (wave1-WB report) | S-20, S-21; U-D-10 (superseded by **U-D-44**); `Assets/Relight/Sim/Production/Machines/**`; `Assets/Relight/Tools/export/exportCatalogue.ts` | ~~Stationary-handcraft rule enforced in sim~~ (withdrawn by U-D-44; the unattended rule is checked by GP-W1); cancel accounting check; subsystem-order check **Deliberate differences:** per-machine Phase C state lives in subsystem side tables keyed by machine id, never as new `Machine` fields (Wave 0 save-schema rule); a processor's output goes to `Machine.Inv[outputItem]` and `Machine.Out` is retired — the field stays, unread and always 0 (coordinator ruling; wave2-WA report §5 item 10); the Mixer gets a station and a default recipe; `Yield` is the full count (U-D-08); the Arsenal gate is declared in data but not applied in Phase C; `HandCraft.HandLocked` was **not** extended to the rifle craft (coordinator ruling) | done 2026-09-14 |
| B-09 | Moved from Phase B (2026-09-11), ID kept. Port power network: nodes, footprint-measured links, circuits, throttle, outages, fuel consumption, turbine sites; struct cache key. **Owner brief 2026-09-14:** Generators hold finite fuel taken from real inventories, refuelled by hand or by inserter/belt delivery; the network exposes demand, available supply and each machine's operating state (running / throttled / unpowered / out of fuel / output full) as sim queries for C-07 | B-07, B-08 | Power model **Delivered:** `Sim/Power/{PowerState,PowerNetwork,PowerPhase,PowerEvents,PowerQueries,SimComposition.Power}.cs` and `Sim/Power/PowerLinks.cs`; the operating-state enum C-07 reads is `MachineOperatingState { Idle, Running, Throttled, Unpowered, OutOfFuel, OutputFull, NoInput, Disabled }` (`Disabled = 7` added in Wave 2 for a knocked-out turret) (wave1-WB report §3; wave2-WB report) | S-25; U-D-12, U-P-06; `Assets/Relight/Sim/Power/**` | Link-rule check (Generator 2×2 reach), no-outage-at-start check, turret-holds-fire-unsupplied check; a reference outage test that fails is investigated here, not copied as an expectation **Deliberate differences:** no outage at start (U-D-12) — the reference's brownout path is not ported, and exactly one power-alert producer exists (`Sim/UI/Hud/PowerAlertSource.cs`, defect U-3); `PowerPhase` runs build → report → burn; circuit identity is a `NetworkKey` struct (S-25) with no block/district layer; node kinds are data-driven (`ReachTiles > 0` is a reach node, `PowerKw < 0` a source); street-light demand is added unconditionally before load and throttle, matching `campaignPower.ts:51` (wave3-WB report §2.1). Generator burn ported literally from `flow.ts:1045-1057` with `EPS = 1e-9` | done 2026-09-14 |
| B-10 | Moved from Phase B (2026-09-11), ID kept. Port production flow: conveyors, fast belts, inserters, undergrounds, splitters, direct conveyor loading, routing cache (not serialised), storage | B-09 | Flow model **Delivered:** `Sim/Production/Flow/{FlowState,FlowRules,FlowPhase,FlowQueries,FlowCommands,SimComposition.Flow}.cs` (TileTps 20, BeltSpacing 0.25, UndergroundHidden 4, InserterSwing 0.5, SplitterCapacity 8) and `Presentation/Flow/BeltItemPresenter.cs` (wave2-WA report) | S-21, S-22; U-D-13; `Assets/Relight/Sim/Production/Flow/**` | Conveyors draw no power; belt/inserter delivery check; `routeCache` absent from the save **Deliberate differences (wave2-WA report §5):** flow runs **after** machines, so an item a processor finishes on tick N can be taken on tick N; `running()` is always true because the Held/Dark block economy is retired (U-D-32); conveyors register no demand at all (U-D-13) and the inserter's 10 kW comes from the catalogue, not a special case; `beltDeliver`, the Depot's abstract delivery and tram cargo are not ported (U-D-32); belt-over-belt replacement is not ported; `PlaceUndergroundPairCommand` is new; turret feeding is kind-agnostic through `TurretHopper`. **Port bug found and fixed:** an unpowered inserter still grabbed one item — `if (throttle <= 0) continue;` | done 2026-09-14 |
| C-03 | Rifle and ammunition: owned Rifle, magazine, reload (also with Backpack open), visible player shots, distinct ranges from the weapon asset. One inventory unit is one bullet and a stack holds many (UI_AND_ONBOARDING.md §5.5, `goal.ts:126`); owned weapons, the equipped selection, magazine and reload agree between UI and sim (owner brief 2026-09-14) | C-02 | Weapon runtime and projectile presentation **Delivered:** `Sim/Combat/Weapons/{WeaponState,Ballistics,Equipment,WeaponPhase,WeaponQueries,SimComposition.Weapons}.cs`; `Presentation/Combat/TracerPresenter.cs`; `Input/RelightControls.inputactions` gains `World/Equip` (E), `Global/Reload` (R) and `Global/Slot` (1-9, 0) (wave1-WC report) | S-19, S-41; U-D-15, U-P-03; `Assets/Relight/Sim/Combat/Weapons/**`, `Assets/Relight/Presentation/Combat/TracerPresenter.cs` | Reload works with any panel open; tracers rendered from the real muzzle; range values from data asset **Verified:** damage falloff, the 0.2-tile bolt sweep and the 24-tracer cap are ported from `weaponProfiles.ts:13` and `playerBallistics.ts:9-14`; reload while the Backpack is open is routed through `UI/InputRouter.cs` (defect U-4, `OpeningDefectsTests`). **Deliberate differences:** one inventory unit is one bullet (U-D-08) and no player-facing string uses the word "magazine" (asserted by test); a weapon cannot be stored in a chest; `Ballistics.Fire` originates at the engineer centre, not a muzzle transform in the sim | done 2026-09-14 |
| C-04 | Gun turret: hopper, power draw, target tracking and rotating cannon, visible shots, cadence from data asset; shots are visible tracers consistent with the damage model, and range, damage, reload/cadence and turn speed are read from the turret asset at runtime (owner brief 2026-09-14) | B-09, C-03 | Turret prefab (fixed base + rotating cannon child) and sim **Delivered:** `Sim/Combat/Turrets/**` (5 files) and `Presentation/Combat/TurretPresenter.cs`; the feeding API other subsystems use is `TurretHopper` (`Accepts/Room/Give/Capacity/Ammo/IsTurret`, `FullProblem`) (wave2-WB report) | S-31; U-D-12, U-D-15, U-P-05, U-P-06; `Assets/Relight/Sim/Combat/Turrets/**`, `Assets/Relight/Presentation/Combat/TurretPresenter.cs` | Unsupplied turret holds fire; cannon rotates to target at the asset's turn speed; cadence is an editable asset value **Deliberate differences:** `MachineOperatingState.Disabled` is new so a knocked-out turret can say why it is silent; aim leads the target by 0.1 tile; combat tick order is Weapons → Turrets → Enemies → Director (coordinator ruling). **Prefab requirement:** the turret prefab needs a child named exactly `Cannon` whose local +X runs down the barrel. Cadence, range, damage and turn speed are read from the turret asset at runtime | done 2026-09-14 |
| C-05 | Home core: HP, repair from the workshop, disabled/recovery kit, one action-bar repair tool, core card first in the Home drawer | C-02 | Core and workshop UI **Delivered:** `Sim/Campaign/HomeCore/{HomeState,HomeCore,HomeCorePhase,HomeCommands,HomeQueries,SimComposition.HomeCore,EnemyCoreHook.HomeCore}.cs` and `Data/Tuning/DefenceTuningAsset.cs` (`Tuning - Defence` generated by `Relight/Setup/Generate Data Assets` and referenced from the registry, 2026-09-14) (wave2-WA report) | S-30 (`DEFENCE` table); U-D-19; UI_AND_ONBOARDING.md §9 U-6; `Assets/Relight/Sim/Campaign/HomeCore/**`, `Assets/Relight/Data/Generated/Tuning/Tuning - Defence.asset` | Repair priced by sim; kit path when disabled; repair reachable in one interaction from the damaged core **Deliberate differences (wave2-WA report §5):** one core, not `bases[]` (U-D-19, U-D-32); no block commissioning notice and no DISCOVERY repair multiplier; `CancelRepairCommand` is new because the port hand-locks the engineer for the repair, refunding exactly what was charged (U-D-05); the abort notice travels as `CoreRepairAbortedEvent`; with no imported core site the core falls back to a 3x3 at the spawn tile (`HomeQueries.CoreIsFallback`); a core knocked out mid-patch voids the work and refunds; three refusal texts are the brief's, not the reference's. Covered by `OpeningDefectsTests.U6_*` (wave3-WC report) | done 2026-09-14 |
| C-06 | Home workshop and Backpack UI: recipe cards with icon/name/quantities/progress, equipment strip, stationary handcraft with cancel, storage↔Backpack transfers across all slots with repeated merges, Escape chain. **Owner brief 2026-09-14:** workshop cards show recipe icon, ingredient quantities available/required, output quantity, duration, progress and a clear Cancel; **the redundant equipment block is removed from the workshop layout** — the equipment strip stays in its intended place under the Backpack grid (UI_AND_ONBOARDING.md §5.5); transfers work on every slot including the rightmost and scrolled ones, same-item stacks merge repeatedly for every supported resource, partial transfers respect stack limits with no loss or duplication, and the approved split-stack (§5 item detail) and quickbar interactions (§5.5) are included. **Superseded in part by U-D-44 (owner brief 2026-09-15, GP-W1):** the stationary-handcraft lock is withdrawn — the Home workshop processes its queue unattended at the same speeds, and moving, mining, placing and transfers are never refused for it. The cancel/refund accounting rules stand and are extended: every unprocessed ingredient comes back, Backpack first. The panel gains the queue, the output tray and Collect under GP-W1. | B-07, C-02, B-14 | UI Toolkit panels (or the recorded fallback): Backpack, storage, workshop **Delivered:** thirteen engine-free `Sim/UI/**` files (TransferText, WorkshopText, Bindings, BindingProblem, Confirmation, FrontEndText, SaveRow, SaveRowFormatter, SaveCatalogue, ContinuePicker, AutosaveChoices, SessionDirty, NewGameRequest) plus `UI/Inventory/**` and `UI/Workshop/**`; `UI/InputRouter.cs` gained `Reload` and `Slot` from the Global map (wave2-WC report) | S-45, S-47; UI_AND_ONBOARDING.md §3.5, §5–§6; U-D-09–U-D-11; `Assets/Relight/Sim/UI/**`, `Assets/Relight/UI/Inventory/**`, `Assets/Relight/UI/Workshop/**` | Rightmost/scrolled slot drop test; merge-until-full test; cancel accounting test; Escape cancels drag first **Deliberate differences (wave2-WC report §7.2):** the redundant equipment block is gone from the workshop layout, the equipment strip staying under the Backpack grid (U-D-11); the Escape ladder is `EscapeOrder` 100 CancelDrag / 200 CloseSlotMenu / 300 CloseNavMenu / 400 CloseModalChild / 500 Unpause; a drag carries a pick-up snapshot so a stack that changed underneath the player is refused, never re-targeted. Covered by `OpeningDefectsTests` rows U-1, U-2, U-7, U-8 (wave3-WC report) | done 2026-09-14 |
| C-07 | HUD: content-width status strip with day/time, network demand/supply, core HP, ammo, alerts/toasts, objective strip; exactly one power-alert producer; alert pip honours reduced motion; actionable messages for missing ingredients, no fuel, disconnected power and full output storage, driven by the B-09/B-07 operating-state queries (owner brief 2026-09-14) | B-09, C-05 | HUD document **Delivered:** `Sim/UI/Hud/{HudViewModel,HudNotices,PowerAlertSource}.cs` (engine-free so `Relight.Sim.Tests` can reach them — an ownership deviation the coordinator accepted) and `UI/Hud/{Hud.uxml,Hud.uss,HudController.cs}` (wave3-WB report §1.2, §6.1) | S-35, S-46; UI_AND_ONBOARDING.md §4, §8; U-D-14, U-M-16; `Assets/Relight/Sim/UI/Hud/**`, `Assets/Relight/UI/Hud/**` | HUD strip matches the documented layout; no outage toast at start; no animated pip under reduced motion **Verified 2026-09-14:** `HudViewModelTests` (17) green; the content-width strip was **fixed during coordinator verification** — `UI/Hud/Hud.uss` `#status-strip` was stretching and now sizes to content, and `UI/Guide/GoalCard.uss` was given a 96 px top offset because the goal card overlapped the status panel. **Deliberate differences:** exactly one power-alert producer (`PowerAlertSource`), and "has ever placed a generator" is derived from `PowerGrid.IsSource` / `Stats.GenFed` / `Stats.CoalBurned` rather than stored; street lamps never appear in HUD problem rows; only `Announced` and `MinorRaid` map to the warning cue. Defect U-5 (content-width strip) has **no sim-side check at all** and is evidenced only by the Play Mode run: 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run) | done 2026-09-14 (limitations: U-5 has no sim-side check; it is evidenced by the Play Mode run only — 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run)) |
| C-08 | Minimal raid behaviour for the playable Home milestone: Skitter/Spitter actors, one-approach group spawning, the single global director with saved timestamps and its first ordinary attack at the documented cadence, `IRaidDirector` in place of the hooks global (the brief said `IThreatLayer`; that name was already the danger-seconds layer in `Sim/Core/Contracts/Layers.cs`, so the raid seam shipped as `IRaidDirector` — see Acceptance and TECHNICAL_ARCHITECTURE.md §4.3). Coordination rules: an active ordinary raid **defers** the introductory attack until the area is safe (never skips it); a deferred introduction is scheduled once, never duplicated, and never postponed indefinitely (bounded by the `OPENING_ENCOUNTER` guard); enemies attack at the turret rate chosen in the data asset. After the opening, the regular defence Phase C needs: ordinary raids choose a fair, reachable approach and vary it between raids so the three-turret guidance matters (single-approach groups; multi-sector majors stay in E-09–E-16) — owner brief 2026-09-14. Includes an editor-only debug command that stages one ordinary raid on demand, excluded from player builds and labelled as a debug tool (new Unity work: the reference has no raid-forcing parameter, `packages/game/src/session.ts:21-45`) | B-08, C-04 | Director subset, two enemy types, labelled debug raid trigger **Delivered:** `Sim/Combat/Enemies/**` (8 files), `Sim/Combat/Director/**` (8 files), `Presentation/Combat/EnemyPresenter.cs` and the editor-only `Editor/Debug/DebugRaidMenu.cs` (wave2-WB report) | S-29, S-30 (subset); U-D-16, U-D-17, U-D-26; `Assets/Relight/Sim/Combat/Enemies/**`, `Assets/Relight/Sim/Combat/Director/**`, `Assets/Relight/Editor/Debug/DebugRaidMenu.cs` | Checks: director and introduction never overlap; a raid active at the trigger moment defers the introduction and it runs exactly once afterwards; enemies path to the Home core; one ordinary attack after the opening can be produced on demand with a labelled debug trigger (the trigger proves behaviour, not natural pacing) **Name change:** the brief's `IThreatLayer` is shipped as `IRaidDirector` with a static `Director` facade, because `IThreatLayer` was already the danger-seconds layer in `Sim/Core/Contracts/Layers.cs` (wave2-WB report). **Deliberate differences:** a BFS distance field replaces the reference's per-body A*, stored as a 141x141 box (79 KB rather than 2 MB) with identical answers; multi-base machinery, radio intelligence and target nomination are retired (one Home core); **defer vs skip** — ordinary majors keep the reference `future()` skip because U-D-38 forbids accumulated attack debt, and `Director.Defer` exists for the C-09 opening encounter only (U-D-26, U-Q-21); danger seconds are charged by `SecondPhase`, not `EnemyPhase`, so they are never double-counted | done 2026-09-14 |
| C-09 | Opening tutorial state machine and guide, in this order: schedule on the first fully loaded, powered, running turret; short directional warning (25 s provisional); small introductory attack from one approach, readable and forgiving (the player can watch the turret work and is not expected to fight alone); feedback on the ammunition actually consumed; automatic replenishment taught through a real delivery (inserter/conveyor from real stock); guidance to three turrets covering different approaches (guidance, not a guarantee of safety); hand-off to a useful nearby excursion while production continues; repelled/lost/skipped/deferred outcomes, where `deferred` (a live raid or an imminent one at the trigger moment) resumes once the area is safe under the U-Q-21 default bound and is never a permanent skip (U-D-26); a progressed save (turrets already placed, delivery already running) skips completed steps; no duplicate encounter or reward; objectives advance from actual sim state and recognise already-satisfied conditions, including an already-equipped weapon; recovery paths for interrupted crafting, destroyed or emptied turrets and partially completed steps (owner brief 2026-09-14) | C-06–C-08 | Opening state machine in sim, guide panel and strings, including a new player-facing deferred notice (the reference has none) **Delivered:** `Sim/Campaign/Opening/{OpeningState,OpeningEvents,OpeningRules,OpeningPhase,OpeningQueries,SimComposition.Opening}.cs` and `UI/Guide/{GoalCard.uxml,GoalCard.uss,GoalCardViewModel.cs,GoalCardController.cs,OpeningHint.uxml,OpeningHint.uss,OpeningHintController.cs}` (wave3-WA report) | S-32, S-35; GAME_DESIGN.md §4, §12.2; UI_AND_ONBOARDING.md §7; U-D-17, U-D-26, U-P-04, U-Q-21; `Assets/Relight/Sim/Campaign/Opening/**`, `Assets/Relight/UI/Guide/**` | State-machine tests for all transitions; deferred → scheduled once the obstruction clears, exactly once, ordinary raids continuing on their cadence meanwhile, and dropped without reward at the U-Q-21 cap; a save made in every state resumes correctly; a save with three turrets skips completed steps **Implemented deferral (wave3-WA report §3):** `Director.Blocked` puts the encounter in `Deferred`, `DeferCount` rises once per entry and `DeferredUntil` is refreshed from `OpeningRules.SafeAt`; the U-Q-21 cap drops the encounter **without reward** once `TurretQueries.Loaded >= GameData.Opening.TurretObjective` (three loaded turrets) or `OpeningRules.LeftForFirstCamp`. **Deliberate differences:** repelled/lost is decided by the prepared turret, not the core; resupply watches `Stats.TurretFed`; `Director.Reserve` expresses `openingBlocksRaids`; material rows append "· N at Home"; no "(K)" suffix on "Return to engineer"; key names come from live bindings, else documented defaults; the camera peek toggles `CameraRig.enabled` so the camera still never reads a UI inset (D-UI-10). **Every new player-facing string (`DeferReason`, `CapNotice`, `ReserveReason`, the `opening-attack` row title "Enemy group delayed") is provisional under U-P-04** | done 2026-09-14 (limitations: every new guide string is provisional, U-P-04) |
| C-10 | Front end and settings (U-D-41): title screen with **New Game, Continue, Load and Settings**; Continue selects an appropriate valid recent save (manual or autosave, whichever is newer and loads); the Load list shows each save's metadata — day, playtime, save time, map id — and marks autosaves as such (day from `t`, save time from `savedAt`, map id from `params.map`; **playtime comes from the B-11 `playSeconds` accumulator, which the reference save format does not have** — until it exists a row must not label sim elapsed as playtime, UI_AND_ONBOARDING.md §2.6.2); clear feedback and an explicit confirmation before anything overwrites a slot or discards unsaved progress. Settings: rebindable controls stored as preferences outside the save, context-aware binding collision check, no `slower`/`faster` bindings, **autosave interval and rotation count (U-D-35)** and **audio volume controls — master plus the group sliders D-11 defines (U-D-33)**. The reference has no front end (UI_AND_ONBOARDING.md §2.1), so this originates it | B-11 | Menu documents, input actions asset, preferences store **Delivered:** `UI/FrontEnd/**`, `UI/Settings/**`, `UI/PauseMenu/**` over the engine-free `Sim/UI/**` models; `AutosaveController.LoadAutosave(string fileName)` added; MainMenu wired 2026-09-14 (TitleScreen.uxml, `SettingsController`, `TitleScreenController.worldSceneName = World`) (wave2-WC report) | S-37, S-40; UI_AND_ONBOARDING.md §2–§3; U-D-04, U-D-33, U-D-35, U-D-41; `Assets/Relight/UI/FrontEnd/**`, `Assets/Relight/UI/Settings/**`, `Assets/Relight/UI/PauseMenu/**` | Save/continue at every opening step; Continue resumes the expected save after a mix of manual saves and autosaves; no overwrite or discard happens without a confirmation; preferences separate from inventory; `bindingProblem` cases ported as tests **Deliberate differences (wave2-WC report §7.2):** autosave defaults are 10 minutes / 5 kept in the UI against the engine's 5/3; only nine actions are rebindable and `reload` is deliberately absent; rebinding uses key capture rather than `PerformInteractiveRebinding`; preferences are `PlayerPrefs`, not `localStorage`; audio buses are Master/UI/World/Alerts at 80/100/100/100 %; "follow system" motion means the port holds no opinion; `AutosaveEntry` has no `T`; Delete stays enabled on an unloadable row; autosaves count as save points; `ConfirmDialog` sits at `EscapeOrder.CloseNavMenu` (300); the title screen reads Escape from `Keyboard.current`; Continue is never hidden. **Limitation:** no AudioMixer asset exists, so the volume sliders have nothing to drive — the exposed parameters must be exactly `MasterVol, UiVol, WorldVol, AlertsVol` when one is created (D-11) | done 2026-09-14 (limitations: no AudioMixer asset exists, so the volume sliders drive nothing yet — D-11) |
| C-11 | Presentation for the opening and the sim-side light model (moved here from B-08): light sources, lit-area queries and the day/night state in `Relight.Sim`; day/night lighting per the B-14 outcome, Home fully lit (**U-D-58, 2026-09-19:** the day and night cycle is retired and "Home fully lit" means the Home core lot; see ALWAYS_DARK_SPEC.md), mouse flashlight (visibility only), machine/power link previews | C-01, C-07, B-14 | Sim light model; lighting and preview visuals; one `AudioCue` presentation hook (signature and event routing only, no clips — D-11 supplies the cue map and assets) **Delivered:** `Sim/Campaign/Light/{LightRules,LightState,LightSources,LightPhase,LightQueries,StreetLights,SimComposition.Light}.cs`; `Presentation/Light/{LightingPresenter,MouseFlashlightPresenter}.cs`; `Presentation/Building/PlacementPreviewPresenter.cs`; `Presentation/Audio/{AudioCue,AudioCueRouter}.cs` with the full `SimEvent` → cue-key → mixer-group table (20 rows, wave3-WB report §4.5) (wave3-WB report) | S-10, S-44; WORLD_AND_ASSETS.md §2.8; D-UI-11; `Assets/Relight/Sim/Campaign/Light/**`, `Assets/Relight/Presentation/Light/**`, `Assets/Relight/Presentation/Audio/**`, `Assets/Relight/Data/Generated/Tuning/Tuning - Power.asset` | Lit/unlit queries answered by the sim, not the renderer; power link preview reflects the sim rule only; opening scene frame time recorded **Queries:** `LightQueries.LitAt(SimState, tx, ty)` is pure and sits in the Combat slot; `LitAt(SimContext, SimState, tx, ty)` builds first and is for presentation; `Mask` / `MaskSize` expose the cached mask. **Deliberate differences:** no per-block loop; the Home lot is always lit, derived from `HomeQueries.CoreRect` plus `GameData.World.MarginTiles` (4); the mask is cached and re-stamped only on a `Rev` or fold change; `LightState` saves nothing; the darkness overlay is a code-built texture on `Sprites/Default`, not a URP 2D light. **Limitations:** the pure `LitAt` is one tick stale; the overlay sorting order (500) is a guess; the audio hook has no mixer and no clips (D-11); the imported region has 7 street lights, and `Tuning - Power.asset` now carries `streetLightKw` 2 and `streetLightRadiusTiles` 7 (U-M-12 record) | done 2026-09-14 (limitations: the pure `LitAt` is one tick stale; the darkness overlay's sorting order 500 is unverified; no audio clips and no mixer; the street-light change was not retested at runtime in Phase C (the integrated run never reached night); owed at C-ACC) |
| C-12 | Opening defect regression set: every row in UI_AND_ONBOARDING.md §9 marked fixed becomes an automated or scripted check, plus the camera-does-not-shift-on-panel-toggle check (D-UI-10). U-2/U-5/U-6 are carried in as intended behaviour and verified in Unity (U-D-43); the browser retests are not repeated and are recorded as superseded, never as passed | C-06–C-10 | Test list **Delivered:** `Tests/Sim/Regression/{RegressionFixture,OpeningDefectsTests,OpeningSaveStatesTests}.cs` (18 + 13 checks) and `Tests/Play/OpeningUiPlayTests.cs` (4 `[UnityTest]`); the superseded browser retests are recorded in [`evidence/phase-c/wave3-WC-superseded.md`](evidence/phase-c/wave3-WC-superseded.md), copied into `Unity/Docs` at handoff 2026-09-14 (wave3-WC report; wave3-WC-superseded) | S-45; UI_AND_ONBOARDING.md §2.3, §9; U-D-43; `Assets/Relight/Tests/Sim/Regression/**`, `Assets/Relight/Tests/Play/OpeningUiPlayTests.cs` | All checks green, including one tool occupying one action-bar slot, core repair reachable in one interaction from the damaged core, and a content-width HUD strip **Coverage as built (wave3-WC report §2):** UI_AND_ONBOARDING.md §9 rows U-1, U-2 (x3), U-3 (x2), U-4 (x2), U-6 (x3), U-7 (x2), U-8 (x2), U-9 and U-10, plus a 3,000-tick scripted-opening conservation sweep and the D-UI-10 camera check. **U-5 has no sim half at all** — the strip's width is a fact about the rendered panel, so it is evidenced only by `OpeningUiPlayTests.U5_TheHudStatusStripIsContentWidthAtEveryLayoutInTheMatrix`: 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run). An `Assert.Ignore("needs seam: …")` is not a pass. U-11 and U-12 are unresolved design questions, not defects, and are out of this set. **Finding F-1:** `CommandResult.Problem` is **not** empty on success — it doubles as the confirmation note ("Stack moved.", "Reloading · 2 seconds…"), so acceptance is read from `Accepted` only (the earlier shared note was retracted by the coordinator) | done 2026-09-14 (limitations: U-5 is evidenced only by the Play Mode run — 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run)) |
| C-13 | In-world building feedback: a ghost preview driven from the live `WorldInput` hand, right-click pick-up of a placed machine, `R` to rotate a machine already on the ground, and every command refusal surfaced to the player as a HUD notice. Opened 2026-09-14 as a gap found during Wave 4 verification: the placement subset delivered under C-02 rotated only the ghost and printed refusals nowhere the player could see them | C-02, C-06, C-07 | `Presentation/WorldInput.cs` ghost preview; `RemoveMachineCommand`; `RotatePlacementCommand`; a new `CommandResultEvent` routed to the HUD notice strip **Delivered 2026-09-14:** `Presentation/WorldInput.cs` (ghost preview through `PlacementPreviewPresenter`, right-click pick-up, `R` rotate under the cursor), `Sim/Core/Contracts/CommandResultEvent`, `Simulation.FlushPending` (an event is raised only when the result text is non-empty), `HudViewModel.Intake` and `HudController.OnTickBoundary`. `RemoveMachineCommand` and `RotatePlacementCommand` already existed with their sim-side refusals; presentation submits unconditionally and the sim words every refusal, so no rule is restated in presentation. `HudViewModel.Intake` posts an accepted result with a note as **Info** and a refusal as **Warning**, so a confirmation never renders as a refusal (wave4-WD report; coordinator review) | S-26 (placement subset), S-46; UI_AND_ONBOARDING.md §4, §5; U-D-14 | A ghost follows the hand and shows validity; right-click picks a placed machine back up and refunds the machine (not its price); `R` rotates a placed machine and re-validates only a non-square footprint; a refused command prints its `CommandResult.Problem` text as a HUD notice exactly once. Counts and evidence: accepted 2026-09-14 (coordinator review): `CommandResultEventTests` 4/4 and `HudViewModelTests` 22/22 in EditMode; `WorldInputFeedbackTests` 3/3 in PlayMode (ghost preview, right-click pick-up, `R` rotate) **Deliberate differences:** there is **no success toast on pick-up** in Phase C (coordinator ruling — the machine vanishing and the Backpack line are the feedback; the reference sentence needs stack arithmetic that presentation must not re-derive); an accepted command carrying a note is posted as Info, because `CommandResult.Problem` doubles as the confirmation text | done 2026-09-14 (limitations: no success toast on pick-up; a burst of refusal notices can push the power alert out of the three notice rows for a few seconds) |
| C-ACC | **Playable-opening check (Phase C acceptance)** | C-01–C-12, B-07, B-09, B-10 | Report in §8 | — | Owner plays New Game through: first turret → warning → introductory attack → ammunition feedback → automatic replenishment → three turrets → a short excursion (leave Home carrying at least eight bullets, the objective's own threshold in `goal.ts`, reach the nearest first-region camp, return, and find production still running); then one representative ordinary attack at the chosen turret rate (waited for, or produced with the labelled debug trigger, which is recorded as not proving natural pacing); no blocking defect; saves and resumes twice; records feel notes and any tuning made under U-M-12 / U-D-28 for U-P-01/02/05. No full campaign run is part of this check **Not run.** Automated evidence standing at the end of 2026-09-14: **EditMode 543/543 passed** (`Relight.Sim.Tests`, Unity 6000.6.0f1 through the Unity CLI 1.0.0-beta.8, 6.09 s — the final run, including the new `OpeningSupplyTests` (2), `HomeDepotTests` (4) and `PlacementSolidTests` (1)); the Unity-free `dotnet test` sim loop last agreed at 527/527 on the pre-fix sources. The PlayMode suite went 41 total / 28 passed / 9 failed / 4 skipped on the first run, 41 total / 32 passed / 5 failed after the coordinator's first fixes and 44 run / 36 passed / 3 failed after the integrated run's production fixes (three Phase B scene tests that assumed the synthetic map), with the final result 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run). The integrated Play Mode run and screenshots are a fresh game on the imported region: the chain built, the Rifle crafted and equipped, a turret placed and loaded, the opening encounter scheduled, `slot-phasec-int` saved and loaded back over the active attack with no duplicate wave, the MainMenu -> New Game flow verified, and the three-turret step and automatic resupply verified after the `Machine.Out` fix, with two debug raids. Captures `Unity/Docs/evidence/phase-c/integrated-run/raid2-1600x900.png` and `raid2-960x540.png`, plus six `fix-*.png` interface captures at 1600x900 and 960x540; the saves `slot-phasec-int.json` and `slot-phasec-int-end.json` are copied to `evidence/phase-c/integrated-run/` (originals in `C:/Users/Admin/AppData/LocalLow/DefaultCompany/Relight/saves/exploration-v2/`). Owner play, the feel notes and the U-M-12 / U-D-28 tuning record are still owed; B-ACC also remains `human`, deferred by the owner on 2026-09-14 rather than passed The owner handoff is [PHASE_C_HANDOFF.md](PHASE_C_HANDOFF.md); its §9 checklist mirrors this row and adds the turret-sight defect and the night-time street-light look. | human — pending owner play, not run; the automated evidence is recorded in Acceptance. **2026-09-14 correction pass:** the owner's first playtest produced the six defects the CP rows answer; the opening now runs on the full city with the corrected controls, and the retest of those six items is the next owner action before this check |

---

## Correction pass — owner playtest of the Home opening (2026-09-14)

Owner brief after playing the Phase C build (screenshot `5.png`): poor UI presentation, no tooltips, only a crop of the level, a click on the ground walked the engineer, Tab did not open the Backpack and B did nothing. Authorised scope: a focused correction pass — controls/UI fixes plus the full riverfront map import and presentation brought forward from Phase D; later campaign content stays in its phases. Executed by the coordinator (Fable) with three Opus workers under exclusive file ownership (W-A controls/shell, W-B UI/build/tooltips, W-C full map/saves/presentation); the coordinator alone drove Unity. Rulings R-CP-1–12 and the findings are DECISIONS.md U-M-39 and U-M-40. Evidence: [`evidence/correction-pass/`](evidence/correction-pass/) — `repro/repro-hud.png`, `repro/repro-tab.png` (the reproduced defects), `full-map-import.md` (export/import counts and hashes) and the seven final captures listed under CP-07. Suites after the pass: **EditMode 563/563 passed**, **PlayMode 60 run / 55 passed / 0 failed / 5 skipped** (the 50 s timing form, three R7-Tilemap prototypes by design, and one drag test whose fixture save carries no split stack). No git commit was made (owner did not ask).

| ID | Brief section | Delivered | Verified | Status |
|---|---|---|---|---|
| CP-01 | §1 Reproduce from Boot → MainMenu → New Game | Reproduced through the real `menu-new` / `new-start` buttons in Play Mode: 78×367 crop, `TogglePanel` bound to Tab but `UiShell.tabPanel = status-panel`, no action bound to B anywhere, `WorldInput` submitting `MoveCommand` on an empty-hand click, the objective printed twice, no build panel, no tooltip system | `repro/repro-hud.png` matches the owner's screenshot; the Input System state events queued by `eval_file` never reach the actions while the Game view is unfocused, so keyboard reproduction rests on the asset/scene facts plus the owner's report | done 2026-09-14 |
| CP-02 | §2 Controls | `Global/ToggleBuild` (B) added to `Input/RelightControls.inputactions`; `UiShell.tabPanel` = `inventory-panel`, `buildPanel` = `build-panel` (scene value edited); the `MoveCommand` path and the empty-hand fallback deleted from `WorldInput`, the status drawer's *Walk east* button removed; left click = the hand's action only; presses that begin over UI never reach the world (`UiShell.PointerOverUi` published as `WorldInput.UiPointerProbe`, the held rifle does not fire over UI); R is one binding resolved in one place — rotate the ghost, else rotate the machine under the cursor, else reload; Escape chain 600 close drawer → 700 empty the hand → 800 pause; `BindingMap.Controls` lists the ten live controls in Settings (`controls-list`) and the HUD buttons read the stored rebinding (`KeyTextFor`); stored rebindings that predate `build`/`rotate` survive (`Preferences` filter) | PlayMode `Tests/Play/Input/ControlsCorrectionTests.cs` (5: held machine placed by a real mouse press, empty-hand click moves nothing, Tab/B toggle the right panels, Escape order, `PointerOverUi` over the strip and not over bare world); `WorldSceneTests` repointed to `#sort-pockets` | done 2026-09-14 |
| CP-03 | §3 Usable inventory and build menu | `UI/Build/{BuildPanel.uxml,BuildPanel.uss,BuildCard.uxml,BuildPanelController.cs}` over the engine-free `Sim/UI/Build/BuildCatalogue.cs`: four category tabs, cards with icon, name, cost line, availability and the sim's own refusal reason, a detail pane, *Add to action bar* (ten destinations through `AssignBarCommand`), card click = `WorldInput.HoldTool` and the drawer closes so the ghost is visible; the HUD `placement-toolbar` shows the held kind, the validity problem and Rotate/Cancel; the HUD `action-bar` (*Inventory [Tab]*, *Build [B]*) survives `UiShell.Rebuild()` and stays uncovered while a drawer is open | PlayMode `Tests/Play/Ui/PanelPlayTests.cs` (4) and `InventoryDragPlayTests.cs` (5, one skipped by fixture); captures `build-1920.png`, `build-1280.png` | done 2026-09-14 — item and machine icons are placeholder two-letter squares (`UI/Icons/ItemIconLibrary.cs`) until `ItemIcons.asset` and art exist |
| CP-04 | §4 UI redesign in editable UXML/USS | One token sheet `UI/Styles/tokens.uss` (fonts, colours, `ui-button` hierarchy, panel chrome, drawer band `--ui-drawer-top/-bottom`, `--ui-toolbar-bottom`); every `ui:Instance` host in `GameUI.uxml` carries `doc-host` (absolute, four insets 0 — a `TemplateContainer` is otherwise a zero-height box, which collapsed every `bottom:`-anchored document); the HUD strip is three aligned, contained blocks with the core meter inside its block and **no objective row** — the goal card (`UI/Guide/GoalCard.*`) is the only statement of the objective, compact, with *Why this next?* and material rows whose source text comes from `OpeningQueries.Source` (nearest real patch, direction and distance) — no resource grant anywhere; Backpack drawer 820 px wide in the shared band, its body a `ScrollView` (workshop + Backpack/Storage pair, grids capped at two rows and scrolling) with item detail, notice and hint in a pinned footer; the opening-hint bar hides while a drawer is open | `Tests/Sim/Campaign/Opening/ObjectiveSourceTests.cs` (9); `PanelPlayTests` counts the objective once; captures at 1920×1080 and 1280×720 (CP-07) | done 2026-09-14 — limitations: the alerts block (top-right) is under an open drawer; the Storage side of the pair shows only with a machine open; at 1280×720 the Backpack body scrolls (expected) |
| CP-05 | §5 Shared runtime tooltips | `UI/Tooltips/{Tooltips.cs,TooltipController.cs}`: one tooltip element authored last in `GameUI.uxml`, 300 ms delay on the panel scheduler, `picking-mode: Ignore` at every level so it never swallows a click, clamped to the screen, hidden when the anchor disappears; attached to Backpack/equipment/quickbar slots, build cards, workshop recipe cards (live have/need) and the three HUD blocks | PlayMode `Tests/Play/Ui/TooltipPlayTests.cs` (2: nothing on the first frame, a titled tooltip after 0.4 s, hidden on leave; HUD blocks likewise); captures `inventory-tooltip-1920.png`, `inventory-tooltip-1280.png` | done 2026-09-14 — `Tooltips.OnDetach` leaves the provider entry in the static registry (bounded leak, `Detach` is the documented route) |
| CP-06 | §6 Full canonical map import and presentation | Exporter `packages/tools/src/exportUnity.ts --region full` (region 864×576 at 0,0; the Home export is byte-identical to Phase C's, hashes in `full-map-import.md`); `Editor/World/RegionImporter.cs` (menu *Relight/World/Import Full City*) wrote `World/Generated/Full/{FullGeometry,FullSites}.asset` (4,475,315 B / 11,582 B) and re-derived the solid mask (0 mismatches of 497,664); **imported counts equal the source:** 479 buildings (22 enterable), 370 props, 412,634 land / 345,482 accessible tiles, roads 210 nodes / 371 edges, 479 paths, 4 drives, tram 4,909 samples / 4 stops / 1,243 rail tiles, 72 site records (9 substations, 18 lights, 11 resources, 4 yards, 1 service area, 3 plants, 3 cores, 3 artifacts, 9 projects, 7 recruits, 4 stops) + 113 squares + 9 labels, 3 camps / 12 groups, 2 freight gates, the arena; `mapId riverfront-arc-v4-editor-ac16d9188c05`. **No giant serialised Tilemap:** `World/WorldPainter.cs` paints Terrain at Awake from the asset (9 `SetTilesBlock` bands, 555–593 ms measured), *Relight/World/Clear painted cells* emptied `World.unity` from 8,772,520 B (32,892 painted entries) to 38,433 B. `WorldBootstrap` runs the sim on `Full*`; `CameraRig` clamps to the map. **Coordinate change and saves:** save schema **v4** adds `region {id, originX, originY, width, height}`; v1–v3 riverfront saves are stamped as the Home crop `(43,91) 78×367` and `SaveRelocate` shifts every positional field by +43/+91 on load (indices re-formed against the new width), refusing with a named reason only when the engineer, a machine or the core would land off-map or inside a building; `AutosaveController` threads the context so autosaves carry the region. **Presentation:** `Presentation/City/{CityPresenter,CityPalette,CitySprites}.cs` draw the whole city procedurally (the `riverfrontDraw.ts` palette, one 80×64 generated texture, 3,901 renderers built in 101–107 ms, fading roofs on the 22 enterable buildings, `TextMesh` region labels); `World/CityArtSet.cs` is the drop-in hook with the 40 roof and 13 floor keys listed in `full-map-import.md` | EditMode `Tests/Sim/Persistence/SaveRelocateTests.cs` (7, incl. the Phase C evidence save relocating to engineer (70.5, 360.5)) and `Tests/Sim/World/RegionOffsetTests.cs` (4); the console lines quoted in DECISIONS U-M-40; capture `outside-crop-1920.png` (camera at tile 430,200, well outside the old crop) | done 2026-09-14 — **art unavailable:** the 68 riverfront SVGs cannot be imported (no `com.unity.vectorgraphics`, no rasteriser on this machine), every surface is a flat tint; the freight arena and gates are imported but nothing reads them; `Editor/Scene/SceneSetup.cs` still paints the synthetic map into the scene if its menu is run (run *Clear painted cells* afterwards); `HomeSitesOverrides.asset` had no entries to convert |
| CP-07 | §7 Verification from a normal New Game | Boot → MainMenu → New Game in Play Mode on the full map at both window sizes; captures `hud-1920.png`, `hud-1280.png`, `inventory-tooltip-1920.png`, `inventory-tooltip-1280.png`, `build-1920.png`, `build-1280.png`, `outside-crop-1920.png` in `evidence/correction-pass/`; visible problems found in the first captures and fixed before the final ones: collapsed template hosts, overlapping drawer sections (UI Toolkit `flex-shrink: 1` default), the Rifle recipe card wrapping, drawers covering the status strip, the hint bar under the drawers and the placement toolbar, the tooltip hovering a scrolled-out slot at 720p | EditMode 563/563, PlayMode 55/0/5 after the final layout; the Game view was switched between 1920×1080 and 1280×720 by reflection (no CLI command sets it) | done 2026-09-14 — C-ACC stays `human`; the owner's retest of the six reported items is the acceptance |

## Proposed Home opening repairs — 2026-09-14

Scope and acceptance details: [HOME_OPENING_REPAIR_PLAN.md](HOME_OPENING_REPAIR_PLAN.md). Dependencies below are technical prerequisites; execution also needs the owner's implementation direction. The existing C-ACC human verdict remains the acceptance owner.

| ID | Repair package | Depends on | Reconciles existing work | Status |
|---|---|---|---|---|
| C-R01 | Record baseline, preserve current work and verify every opening player-action route | Owner implementation direction | C-02, C-06, C-07, C-12, C-13; audit | todo — the correction pass recorded a reproduction baseline (`evidence/correction-pass/repro/`, CP-01) but did not verify every player-action route |
| C-R02 | Repair viewport hosts, responsive HUD/drawers and UI input routing | C-R01 | C-06, C-07, C-10, C-12 | UI scope delivered through CP and Codex UI passes: viewport hosts, compact layouts, input isolation, live bindings and tooltips, 1080p/720p/125% evidence. Native OS mouse and the older exhaustive resolution/scale matrix remain unverified; see UI_UX_PASS_2026-09-14.md. |
| C-R03 | Connect Build, machine/weapon action bar, Backpack, contextual E and craft interruption | C-R02 | C-02, C-06, C-13, B-07 | Player controls delivered: contextual E, persistent machine/owned-weapon dock, real Tab/B/R/number input, Backpack pairing reset and craft cancel. Focused pointer/submit and command checks passed; full ordinary-control campaign acceptance and exhaustive subscriber audit remain open. |
| C-R04 | Connect production configuration, transfers and conveyor construction controls | C-R03 | B-07, B-09, B-10, C-06, C-07 | Partly delivered: production recipe/filter/priority controls, transfers/splits, underground pairing and save-state restoration in the Codex passes. Repeated conservation and recipe save-roundtrip checks passed; complete live automation/capacity journey remains open. |
| C-R05 | Restore Home reference visuals, distinct opening-machine views, icons and readable lighting | C-R01, C-R02; integrate with C-R03/C-R04 | C-01, C-04, C-07, C-11; bounded opening subset of later art work | Partly delivered: full canonical procedural city presentation plus 60 original reference UI icons and recipe/dock icons. Distinct world machine/actor artwork, SVG presentation and comprehensive lighting/art validation remain open. |
| C-R06 | Verify tutorial, defence, recovery and promised excursion; proposed essential audio subset | C-R03, C-R04, C-R05 | C-04, C-05, C-08, C-09, C-11; bounded D-11 subset proposed | todo — proposed, implementation not started |
| C-R07 | Ordinary-control acceptance journey, final regressions and Windows player save/continue handoff | C-R02–C-R06 | C-10, C-12, C-ACC (human verdict stays open) | todo — proposed, implementation not started |

---
## Phase D — Authored world, navigation and exploration

Goal: the whole `riverfront-arc-v4` city is in Unity through the import pipeline, navigable on foot and by tram, with the map, wayfinding, districts and the first-region sites. Exit: D-ACC passes.

| ID | Task | Depends on | Deliverables | Sources | Acceptance | Status |
|---|---|---|---|---|---|---|
| D-01a | Minimum reference-side exporter for the Home region: ground/roads/pavements/parcels, building instances, the Home core, substation tile, deposits, approaches and gameplay sites inside Founders Court and its approaches, from `city/riverfront.ts`, `city/gameplaySites.ts` and the Phaser density manifest, in the WORLD_AND_ASSETS.md §7 schema restricted to a region filter; added as a `packages/tools` script (allowed reference change: tooling only, no gameplay change, no commit without authorisation) | A-08 | `npm run map:export-unity -- --region home`, JSON under `Unity/Import/` | S-12, S-16, S-51, S-64; WORLD_AND_ASSETS.md §2.7, §7; TECHNICAL_ARCHITECTURE.md §6.2; U-M-05, U-M-15 | Home-region export lists the core, substation, deposits and approaches at the documented coordinates; manifest id and hash recorded | todo |
| D-01b | Full-city exporter: extend D-01a to the whole `riverfront-arc-v4` city including `city/riverfrontRail.ts` and `firstRegion.ts`; this is the final export the Phaser Editor workflow produces (U-M-15) | D-01a | Full export | as D-01a | Export validates against `validateRiverfront` counts (buildings 479, props 370, road edges 371, sites list); manifest hash recorded | **done 2026-09-14** (brought forward by the correction pass, CP-06): `exportUnity.ts --region full` writes `Unity/Import/full/` (city.json 1,030,827 B + four 497,664 B side-cars + `export-report.json` with a `counts` block); console `479 buildings, 370 props, 345482/412634 accessible land tiles; roads 210 nodes / 371 edges`; `manifest.sourceHash c4a874ce…` and `sourceSha256 b19f5a64…` recorded in `evidence/correction-pass/full-map-import.md`. The counts equal the reference's `validateRiverfront` figures; the rule set itself is not run by the exporter (that is D-02b's `CityValidator`) |
| D-02a | Minimum Unity editor importer: tilemaps for ground/roads/pavements/parcels, building prefab instances and the gameplay-sites ScriptableObject for one region; generated content in a `Generated/` folder, hand-edited overrides outside it, re-import idempotent, Y flipped at import | B-02, B-08, D-01a | Editor importer, generated Home-region assets | S-12, S-14, S-15, S-64; TECHNICAL_ARCHITECTURE.md §6.3–§6.4; WORLD_AND_ASSETS.md §7; U-M-14 | Re-import twice produces identical assets; a manual override survives re-import | todo |
| D-02b | Full importer: rail geometry, city validation at import time (`CityValidator`: the ported `validateRiverfront` / `validateFirstRegion` rule set, WORLD_AND_ASSETS.md §4.7.1, which is also the authoring gate for content made in Unity after handover, D-08 and E-15), the whole city at 864×576 with the B-14(b) layer decision | D-02a, D-01b | Full import, `CityValidator` | S-56; TECHNICAL_ARCHITECTURE.md §6.3–§6.4 | Full city imports; an invalid city fails the import naming the offending id | **partial 2026-09-14** (correction pass, CP-06): `Editor/World/RegionImporter.cs` imports the whole 864×576 city (rail geometry included: 4,909 baked samples, 1,243 rail tiles, 4 stops) into `World/Generated/Full/` and fails the import on a solid-mask or count mismatch against `validation` in `city.json` (0 mismatches of 497,664); the B-14(b) layer decision is honoured by painting the Tilemap at runtime (U-M-35). **Not done:** `CityValidator` (the ported `validateRiverfront` / `validateFirstRegion` rule set, §4.7.1) — an invalid city is not yet named by id |
| D-03 | Full-map navigation: city graph, walk/move rules, light-aware routing (D-GP-10), path caches | B-08, D-02b | Navigation over the full city | S-11 (full) | Ported navigation tests; path from Home to each district within budget | todo — note (correction pass 2026-09-14): the sim, `PathFinder` and `SolidMap` already run on the full 864×576 map (scratch buffers reused, no per-query full-map allocation, verified by reading); the city graph, light-aware routing and path caches of this row are not ported and no Home-to-district budget was measured |
| D-04 | Districts and regions: Home → Riverside → Ironworks → Civic order, district records and validation, discovery, survey and site restoration state | D-02b | District data, discovery and survey | S-17, S-33; WORLD_AND_ASSETS.md §3 | Discovery order and unlock rules match GAME_DESIGN.md §5 | todo |
| D-05 | Tram and freight: fixed four-stop tram loop, stops, freight gates and arena logistics, powered stop service; ordered `tramTransfer` | B-09, D-03 | Tram runtime and stops | S-23; WORLD_AND_ASSETS.md §4.8, §5.1, §5.4; D-UI-13 | Tram completes the loop; stops require power as documented; transfer order test | todo |
| D-06 | Truck: recovery, truck work orders, queued blueprint delivery from a chosen supply chest | D-05, B-10 | Truck runtime | S-24 | Ported truck tests | todo |
| D-07 | City map, navigation panel, named pins, wayfinding, map inspection (lattice map retired) | D-04 | Map UI documents | S-42, S-47; UI_AND_ONBOARDING.md §2 | Pins saved; inspection matches sim state | todo |
| D-08 | First-region gameplay sites: camps, gates, arenas, rewards, Freight guardian arena, plant sites, resource patches, recruit points — imported from the canonical sources where they exist there; anything not in the export is authored in Unity outside `Generated/` (U-M-15) | D-02b, D-04 | Site records and prefabs | S-16; WORLD_AND_ASSETS.md §4; GAME_DESIGN.md §5 | Every site ID from the export is present with the documented coordinates | **partial 2026-09-14** (correction pass, CP-06): every site record of the export is imported as data in `FullSites.asset` (72 site records + 113 squares + 9 labels; 3 first camps / 12 groups, 2 freight gates and the freight arena) at the documented absolute coordinates, with `RegionOffsetTests` pinning the +43/+91 relation to the Home crop. **Not done:** prefabs, rewards, Quarry/Wharf, anything authored in Unity outside `Generated/`; nothing beyond the Home opening reads the freight gates or the arena |
| D-09 | World presentation: building forms, roofs, lighting rules, rail art, machine connection indicators, placeholder art from the SVG set; `worldScene` responsibilities split into camera/selection/ghost/machine/actor views | D-02b, B-14 | Presentation prefabs and materials | S-41, S-43; WORLD_AND_ASSETS.md §6, §8; U-M-10 | Whole city renders; frame time profiled and recorded (no 60 fps claim without a measurement) | **partial 2026-09-14** (correction pass, CP-06): the whole city renders through `Presentation/City/CityPresenter.cs` with the procedural `riverfrontDraw.ts` palette (3,901 renderers, one generated 80×64 texture, built in 101–107 ms; the tile paint takes 555–593 ms for 497,664 cells at session start — both editor measurements on the owner's machine); roof fade on the 22 enterable buildings; `worldScene` is already split (B-13, C-02, C-11, C-13). **Not done:** placeholder art from the SVG set (unimportable here — `CityArtSet` is the drop-in hook), rail vehicle art, machine views beyond the chest fallback, and **frame time is not profiled** |
| D-10 | Exploration objective after the opening: the nearby objective, guide hand-off, exploration-v2 discovery flow | C-09, D-04 | Guide steps | S-34, S-35; GAME_DESIGN.md §4–§5 | Objective reachable and completable from a C-ACC save | todo |
| D-11 | Audio foundation (U-D-33): an `AudioCue` catalogue mapping presentation events to clips, an `AudioMixer` with master, effects, **alerts**, ambience and UI groups wired to the C-10 volume settings (alerts are split out of effects so a raid warning is never ducked or voice-stolen — TECHNICAL_ARCHITECTURE.md §15.3), a pooled one-shot player with distance attenuation and per-cue throttling (32-voice pool, max 4 simultaneous instances per cue, 40 ms same-cue debounce, voice stealing by priority then age — §15.4), and the first clip set — player and turret weapons, impacts and enemy cues, machine operation, crafting and construction feedback, raid warnings, UI interactions. Every clip is properly sourced with its licence recorded. `Relight.Sim` gains no audio reference: cues are raised from presentation events only | C-11, D-09 | Cue catalogue asset, mixer, one-shot player, licensed clip set, licence table | U-D-33; U-M-24; WORLD_AND_ASSETS.md §9; UI_AND_ONBOARDING.md §11 Q9 | Every cue in the catalogue fires from a presentation event and never from the sim; mute and each volume group work; a missing clip degrades to silence without an exception; no clip without a recorded licence | todo |
| D-ACC | **Phase D acceptance** | D-01a–D-11 | Report in §8 | — | Owner walks and trams from Home to Civic, reaches the first-region camps and the Freight arena, saves and resumes; hears weapon, machine and warning audio at sensible levels; frame-time measurement recorded | human |

---

## Phase E — Remaining progression, factories, transport, weapons, enemies, strongholds, raids

Goal: everything Implemented-and-retained in the reference is ported, and the Approved-but-not-implemented GP-15–25 content is built where its decision gate is passed. Exit: E-ACC passes.

| ID | Task | Depends on | Deliverables | Sources | Acceptance | Status |
|---|---|---|---|---|---|---|
| E-01 | Progression state: Keys, Schematics, Artifacts, Alien Keys/quest pouch, milestones M0–M4, claimed source IDs, plant commissioning, station/radio/tram-reward sites (the live half of `expansion.ts`) | D-08 | Progression model | S-28 (live half), S-33, S-34; GAME_DESIGN.md §5 | Ported progression validators pass; unlocks persist across save/load | todo |
| E-02 | Plants and cores: core install, one Home + one factory per plant, defended sites lifecycle, recovery | E-01, D-06 | Plant lifecycle | S-34; GAME_DESIGN.md §2, §5 | Commission first plant (M2) and second (M3) in a scripted run | todo |
| E-03 | Full production set: Excavator, Foundry, Mixer, Assembler, workbench, Overclock, Big pole, Substation, turbine, lamps, radio; factory scenario harness | B-10, E-02 | Machine prefabs and data; headless factory scenarios | S-21, S-55; CONTENT_CATALOGUE.md machines | Each machine's rate and kW from its asset; ported factory tests | todo |
| E-04 | Construction: blueprints, clipboard, library, ghost orders, Foreman, bots, conserved area removal; refusals as `CommandResult` data | D-06, E-03 | Construction system and panels | S-26, S-27, S-47, S-57 | Ported construction and blueprint round-trip tests; conservation across order cancel | todo |
| E-05 | Full raid director: harassment and major tiers, four-sector approaches, reinforcement, caps (`activeRaidBudget`, `livingBudget`), suppression and recovery grace, truthful warnings, Radio intelligence; defence scenario harness | C-08, D-03 | Director; headless raid scenarios | S-29, S-30 (full), S-54; GAME_DESIGN.md §9; CONTENT_CATALOGUE.md §8; U-D-16, U-P-07, U-Q-15 | Scripted 90-minute run: first major 25–30 min, subsequent 18–22 min start-to-start, no overdue burst | todo |
| E-06 | Freight guardian and first stronghold: signalled charge, cover, defenders, gate → fight → recovery; guardian HP 600 provisional (U-D-40, U-P-09) with its own encounter role, tuned after implementation | E-05, D-08 | Guardian actor and arena | S-16, S-29; GAME_DESIGN.md §5, §9; U-D-40, U-P-09 | Ported guardian tests; arena completion rewards once | todo |
| E-07 | Camp reoccupation and Artifact route (GP-14) | E-06 | Reoccupation scheduling | S-33; D-GP-12 | Announced delay; never spawns in occupied space | todo |
| E-08 | Survivors and facilities: Electricians, Concrete crew, Gunsmith, Rail crew, Foreman, Lamplighters, Surveyors as currently implemented | E-03 | Recruit system | S-33; GAME_DESIGN.md §10 | Ported recruit tests | todo |
| E-09 | **GP-15 Shotgun and cartridges, Gunsmith acquisition** (values provisional, U-M-12) | E-08 | Weapon, ammo, recipes | S-19; CONTENT_CATALOGUE.md weapons (Approved but not implemented) | Spread/range from asset; save/transfer/cancel integrity | todo |
| E-10 | **GP-16 Arsenal two-barrel paired shot** | E-09 | Weapon behaviour | S-19; D-GP-07 | Two-round paired shot UI | todo |
| E-11 | **GP-17 Arc schematic, reusable energy cells, workbench charging** (also gives Arc its award route). Cells are Approved but not implemented (U-D-25); cell recipes, charge counts and charging rates are provisional numbers under U-M-12, not a separate approval | E-03 | Cell lifecycle, weapon | S-19; D-GP-08; U-D-25 | Charge conserved across save/transfer as a saved integer (D-GP-08); no artifact per shot; cells never replace bullets for Rifle/Shotgun; no recipe, charge count or charging rate is presented as approved | todo |
| E-12 | **GP-18 Plasma and M3 fabrication** (also gives Plasma its award route) | E-11 | Weapon | S-19; D-GP-03/04 | Partial charge saved | todo |
| E-13 | **GP-19 Stalker and Breaker** (five-type roster per U-D-37; **Breaker HP 220 provisional**, replacing the reference's 100, U-P-08) | E-05 | Enemy actors | S-29; D-GP-09; U-D-37, U-P-08 | Structure attacks and breach; Breaker HP read from the data asset at 220, not hard-coded | todo |
| E-14 | **GP-20 Howler and full roster composition** | E-13 | Enemy actor | S-29; D-GP-09 | Bounded alert, no summons | todo |
| E-15 | **GP-21 Quarry stronghold** and **GP-22 Wharf stronghold**: the Quarry and Wharf places and core sites already in `city/riverfront.ts` arrive through the import; the stronghold gameplay content (arenas, guardians, rewards) is authored and validated in Unity outside `Generated/` (U-M-15); guardian HP 1,000 / 1,500 provisional (U-D-40, U-P-09) | E-06, D-08 | Sites, guardians, rewards | S-16; WORLD_AND_ASSETS.md §4.7; D-GP-14; U-M-15 | Imported places match the canonical coordinates; authored content passes the Unity-side validator (the ported `validateRiverfront` / `validateFirstRegion` rule set, WORLD_AND_ASSETS.md §4.7.1, built in D-02b); rewards granted once | todo |
| E-16 | **GP-23 four-factory pressure and workloads** and the later raid tiers from the approved design (U-D-37; values provisional), machine/belt-item ceiling measured (R13), performance budget (D-GP-15) | E-05, E-15 | Tuning and profiling report; later-tier raid data assets | S-21, S-55; D-GP-15; U-D-37, U-D-38 | Measured CPU/path/actor/machine budget recorded; later tiers keep the U-D-38 cadence and reinforcement limits | blocked (E-15) |
| E-ACC | **Phase E acceptance** | E-01–E-16 | Report in §8 | — | Owner plays from a C-ACC save to a commissioned second plant and one major raid repelled; records verdicts separately for each GP-15–23 feature actually built | human |

---

## Phase F — Remaining content, balancing, presentation, completion

| ID | Task | Depends on | Deliverables | Sources | Acceptance | Status |
|---|---|---|---|---|---|---|
| F-01 | **GP-24 gate/cost/recruit audit and engineering rewards.** Frames and Boards keep their existing Alien workbench and Overclock consumers (U-D-39); no new production chain is invented for them and the "vestigial" label is withdrawn | E-ACC | Adjusted data assets; audit record | S-34; D-GP-13; U-D-39 | Every gate, cost and recruit route reachable with the shipped recipes; reward products named and recorded | todo |
| F-02 | Balancing record: every provisional value (U-P-01–U-P-09 and any value tuned under U-M-12 / U-D-28 during Phases C–E) is presented with its chronology and its final tuned figure. Tuning itself is delegated (U-D-28), so this row is a record for the owner, not an approval gate: it does not block F-ACC | C-ACC, E-ACC | Updated data assets, DECISIONS.md §3 | S-A1; DECISIONS.md §3; U-D-28 | Every value has a recorded final figure with its reason and retest outcome; the owner may overturn any of them, which becomes a new tuning row | todo |
| F-03 | Final art integration: the owner's art package, the tileset licence outcome, replacing the placeholder rasters, bundled UI font. Assets with unverified permissions stay out of the build until source and licence arrive (U-D-29) | D-09; U-Q-04 (external information) | Art assets, font, licence table | S-43; WORLD_AND_ASSETS.md §8–§9; U-D-29, U-M-10, U-M-16 | No asset without a recorded licence in the build | blocked (U-Q-04 — awaiting the owner's art package and the licence outcome; no other row waits on it) |
| F-04 | Audio completion (U-D-33): environmental ambience with an interior/exterior variation (the day/night variation is dropped under U-D-58; the two relief cues of ALWAYS_DARK_SPEC.md §5.4 are added), machine operation loops with distance falloff and voice limits, the raid-warning and raid-outcome cue set, and a mix pass across the D-11 groups so warnings are audible over combat and machinery. Proportionate by decision: no voice acting, no bespoke soundtrack, no elaborate audio framework | D-11, D-09 | Ambience beds, machine loops, warning cues, mixer snapshot settings, updated licence table | U-D-33; WORLD_AND_ASSETS.md §9, §11 Q6; UI_AND_ONBOARDING.md §11 Q9 | Ambience changes with day/night and with entering an interior; machine loops respect the voice limit and do not mask the raid warning; every clip has a recorded licence; mute and each volume group still work | todo |
| F-05 | Ending / final objective. **Decision needed (U-Q-02, Q07):** what completes the campaign. The only recorded direction is D-GP-13: "a final physical restoration project supplied by the four-factory network, followed by one declared defence and visible restoration result", and "third plant alone never sets victory" (U-D-23). **Bounded recommendation for the owner to accept, amend or replace:** a single named restoration project at a fixed city site, with (1) a bill of materials drawn from all four factory outputs, (2) one announced final defence using the existing director, (3) a visible city-wide result, and (4) a completion screen; no new systems beyond a project site and a completion state. **Proceeds independently, now:** plateau handling — when a save reaches the last implemented objective, the guide ends with a truthful terminal message, no objective dangles, and the plateau state saves and reloads; all other Phase F rows; the port never invents an ending or marks the game complete at three plants. **Follow-on deliverable once decided:** a GAME_DESIGN.md §11 addendum, then implementation rows F-05a (project site and bill of materials), F-05b (final defence), F-05c (completion state and screen) added to this table | E-ACC; U-Q-02 | Decision record and design addendum; then F-05a–c | S-34; D-GP-13; U-D-23 | Plateau handling: a save at the last objective shows the terminal message and reloads cleanly. Decision: owner-selected project, requirements and rewards recorded before any of F-05a–c is opened | todo (plateau handling); F-05a–c blocked (U-Q-02) |
| F-06 | Optional one-off converter for one named reference save. **Closed by owner decision (U-D-27):** Unity starts fresh, existing saves stay with the reference game, and no converter is required. Reopens only if the owner later names a specific save | B-11; owner naming a save | §8 entry recording the closure | S-05, S-A4; TECHNICAL_ARCHITECTURE.md §9; U-D-27, U-M-06 | Nothing to build; F-ACC does not depend on this row | done (not required) 2026-09-11 |
| F-07 | Performance certification on the reference machine: 60 fps gate actually measured (D-GP-15) with a new profiling harness. Measured against the art actually in the build: the pending art package (F-03) is **not** a prerequisite, because U-D-29 forbids it blocking playable milestones. If the final package lands later and changes the asset budget materially, the measurement is re-run and both results are recorded | E-16 | Measurement report, profiling harness | S-44, S-58; D-GP-15; U-M-17; U-D-29 | Median and max frame time recorded; pass/fail stated honestly; the art state the measurement was taken under is named | blocked (E-16) |
| F-08 | Owner play checks on the Unity build, each recorded separately: (1) base-defence session — warning comprehension, return and preparation time, an unattended raid, recovery and maintenance (provenance: P6-H); (2) discovery session — self-directed exploration, useful rewards, restoration and recipe comprehension, optional route choice (P7-H); (3) construction session — repeated station/factory work with Foreman, blueprints and truck delivery (P8-H); (4) wayfinding session — place recognition and route choices across the city map (P9-H); (5) representative loop — one continuous play session through the ordinary loop with interventions and gaps noted (EX-08H); (6) build a two-assembler ammunition line unaided in ten minutes (T18) | F-02 | Verdict records | U-D-24; reference `docs/PROGRESS.md` rows named in provenance | Each check has its own recorded verdict; none is inferred from another | human |
| F-09 | Reference retirement decision: when the Unity build is accepted, decide what of the TypeScript project is archived (procedural generator, authoring scripts, evidence corpus stay as provenance) | F-08 | DECISIONS entry | S-13, S-65, S-A4; U-M-18 | Owner decision | human |
| F-ACC | **Phase F acceptance / completion** | F-01–F-05, F-07–F-09 (F-05a–c only once U-Q-02 is answered; **F-03 only once the owner's art package and the tileset licence arrive** — until then the phase is accepted on placeholder art with the art state named, so external information never holds completion, U-D-29; F-06 is closed as not required and is not a dependency) | Report in §8 | — | Owner release verdict | human |

---

## 6. Dependency summary

- Phase B is a foundation, not a feature phase: A → B-01 → B-02 → B-02a (editor tooling) and {B-03 → B-04, B-08, B-12}; B-05 after B-02; B-06 after B-03 and B-05; B-11 after B-06; B-13 after B-04/B-05/B-08; B-14 after B-13 and before C-06/C-11/D-09 fix scene structure. DEF-01–DEF-04 are trigger-gated with no dependants.
- B-07, B-09 and B-10 (crafting and machines, power, belts) sit in Phase C with their IDs kept: B-07 after B-06, B-09 after B-07 and B-08, B-10 after B-09; C-04 needs B-09, C-06 needs B-07, C-07 needs B-09, D-05 needs B-09, D-06 needs B-10. The sim-side light model is in C-11.
- C needs B-ACC plus the minimum pipeline D-01a → D-02a for C-01; no other Phase D task is a Phase C prerequisite. (The B-ACC half of that sentence no longer holds in practice: on 2026-09-14 the owner deferred the manual B-ACC checks as a prerequisite for starting Phase C and authorised the phase, as §8 records. B-ACC itself is still `human` and still owed.) D-01a/D-02a are implemented inside Phase C limited to the Home region (the only later-phase dependency the opening needs). C-08 needs B-08 and C-04; C-09 is the phase's integrating task; C-10 originates the front end the reference never had.
- D-01a/D-01b are reference-side tooling and can start as soon as A-08 is done; they are the only planned change to the reference project. D-02b and everything that needs the whole city (D-03, D-04, D-07, D-08, D-09) follow D-01b.
- E-09–E-15 and F-01 no longer wait on any approval gate: the owner approved the full gameplay scope (U-D-31), so each depends only on its engineering prerequisites, and their values are provisional and tunable (U-M-12, U-D-28). E-16 follows E-15 and carries the later raid tiers. F-07 follows E-16 alone: performance is certified against the art actually in the build, because the pending art package must not block a playable milestone (U-D-29).
- Only two rows in the whole plan wait on the owner. F-03 waits on the art package and the tileset licence (U-Q-04, external information), and F-05's implementation rows F-05a–c wait on the ending (U-Q-02) while F-05's plateau handling proceeds now. Nothing in Phases B–E depends on either. F-ACC treats both as conditional in the same way: it is accepted on placeholder art with the art state named if the package has not arrived, and F-05a–c join it only once the ending is answered. F-06 is closed as `done (not required)` (U-D-27) and F-ACC does not list it.
- Audio is in scope (U-D-33): C-11 defines the `AudioCue` hook, D-11 builds the cue catalogue, mixer and first clip set, C-10 exposes the volume controls, and F-04 completes ambience and the mix. D-ACC and F-ACC include them; no audio row is optional any more.
- Nothing gates Phase B except the owner's go-ahead: the baseline is the preserved snapshot (U-M-19, confirmed by U-D-36), the Unity target is fixed (U-M-01, confirmed by U-D-30), and B-11 proceeds on settled owner decisions — fresh start with no converter (U-D-27), an inert `profile` field (U-D-32), manual slots plus a rotating autosave ring (U-D-35).

## 7. Known residue after Phase A

- The provisional values (U-P-01–U-P-09) are ported as starting points with their chronology in DECISIONS.md §3. Focused, documented tuning during Unity playtesting is delegated to the workers (U-D-28, U-M-12); the owner is not asked to approve individual numbers, and F-02 is the record rather than a gate. Two values were set by the owner rather than taken from code: Breaker 220 (U-P-08) and the guardian trio 600 / 1,000 / 1,500 (U-P-09).
- Unity 6.6 is the fixed target (U-D-30) and `6000.6.0f1` was observed installed on 2026-09-11; B-01 records the exact version, the package manifest and the two shell deviations; the MCP integration's real capabilities are unverified until B-02a; UI Toolkit drag-and-drop, `Tilemap` scale and the lighting approach are unverified until B-14.
- Old-save import is not required and not built (U-D-27): the Unity build starts fresh and the reference saves stay with the reference game. F-06 is closed.
- The reference checkpoint is uncommitted; the baseline is the preserved snapshot `reference-snapshot.sha256` (U-M-19). No commit is made without authorisation.
- The 146 reference sim test failures are reported failures with unestablished individual provenance: a sample was inspected and looked pre-existing (`docs/Implementation/GP_CHECKPOINT.md:178`), and no more than that is claimed. They are neither triaged in bulk (that is DEF-01, trigger-gated) nor inherited as expectations: a ported test is written against the intended behaviour in GAME_DESIGN.md and DECISIONS.md, never ported as a passing expectation because it currently fails, and never assumed correct because it currently passes (Phase B scope rule).
- The reference's human gates were never passed; F-08 replaces them with plain-language owner play checks (old IDs kept as provenance). The two browser retests on port 5178 are not repeated (U-D-43): the intended fixes are carried into Unity and verified there as C-12 checks, and the browser evidence is recorded as superseded, never as passed.
- The `Assets/urban-decay/48px+MVMZ` tileset licence is unknown and the owner's art package is pending (U-Q-04, U-D-29); Unity must not reference either until source and licence arrive, and neither may block foundation work or a playable milestone. F-03 is the only affected row.
- Roster naming: the Unity roster is the five regular types Skitter, Spitter, Stalker, Breaker and Howler, with guardians separate (U-D-37). The legacy third enemy type was **Hulk**, not Conductor; Conductor is a campaign role.
- Telemetry is retired (U-D-34, DEF-03) and gamepad support is out of initial scope (U-D-42, DEF-04); both are owner decisions and neither blocks anything.

## UI/UX pass — current owner brief

| ID | Scope | Evidence | Status |
|---|---|---|---|
| C-UX | Research-informed dock, world hover/mining, achievable material guidance, Backpack/Workshop, compact layouts, shared tooltips and menu readability | [UI_UX_PASS_2026-09-14.md](UI_UX_PASS_2026-09-14.md); compile and 36 focused tests; bounded normal-opening and prepared-state UI checks, actual 1080p/720p/125% captures | done — implementation and bounded verification, 2026-09-14. Native mouse and campaign owner playtest pending; C-ACC remains human. |
| C-ARTUI | Implement approved illustrated industrial UI reference | [UI_INDUSTRIAL_PASS_2026-09-15.md](UI_INDUSTRIAL_PASS_2026-09-15.md); 19 generated assets, shared styles/components, live 1080p/720p/125% captures, 12 focused objective tests and bounded UI checks | done — implementation and bounded verification, 2026-09-15. C-ACC remains human; existing late-game icons and world art retained. |
| C-FLOWPOWER | Owner power/conveyor/extractor corrections | [FLOW_POWER_FIXES_2026-09-15.md](FLOW_POWER_FIXES_2026-09-15.md); 87 focused tests, normal-speed Play Mode extraction/delivery/collection/conservation, cable and queue captures, inter-tick sprite motion check | done — implementation and bounded verification, 2026-09-15; C-ACC remains human. |
| C-ADMIN | Owner-requested in-game debug admin menu | [ADMIN_MENU_2026-09-15.md](ADMIN_MENU_2026-09-15.md); 22 focused tests, live keyboard/button/paused-save checks and 1080p/720p captures | done — implementation and bounded verification, 2026-09-15; C-ACC remains human. |
| C-ACTIVITY | Owner-requested machine, power-line and conveyor activity animations | [ACTIVITY_ANIMATIONS_2026-09-15.md](ACTIVITY_ANIMATIONS_2026-09-15.md); live motion/outage/jam/pause checks and 1080p/720p captures | done — presentation implementation and bounded verification, 2026-09-15; C-ACC remains human. |
| C-INVENTORYFIX | Owner empty machine slots and inventory dragging correction | [INVENTORY_FIXES_2026-09-15.md](INVENTORY_FIXES_2026-09-15.md); full UI pointer sweeps, 6 transfer regressions, 1080p/720p inspection | done — implementation and bounded verification, 2026-09-15; native mouse/C-ACC remain human. |
| C-OREOPEN | Approved ore recipes and build-friendly starting deposits | [ORE_OPENING_2026-09-15.md](ORE_OPENING_2026-09-15.md); 596 sim tests, paid starter line, live workshop pointers and legacy adoption | done — implementation and bounded verification, 2026-09-15; C-ACC remains human. |
| C-SCENEEDIT | Editable Scene world and simulation footprints | [SCENE_EDITING_2026-09-15.md](SCENE_EDITING_2026-09-15.md); map parity, 9 authoring tests, 596 sim tests, live movement/save checks | done — implementation and bounded verification, 2026-09-15; C-ACC remains human. |
| GP-UX-1 | **A movement key closes the open drawer and the engineer walks on the same press (U-D-49a).** Owner: *"If the player presses any movement buttons while interacting with the anything, close the UI its interacting with and allow movement eg with the homeworkshop."* Read through the player’s own rebindable `World/Move` bindings, not a WASD mirror; exempt while paused, while dragging and while a text field has focus. | `UI/MovementKeys.cs` (new), `UI/InputRouter.cs`, `UI/UiShell.cs`; 2 new Play-mode tests in `Tests/Play/Input/ControlsCorrectionTests.cs` (the close-and-walk, and the text-field guard), compiled offline against the editor’s managed assemblies | done — implementation and offline compile only, 2026-09-16; **Play Mode not run on this machine**; owner acceptance pending |
| GP-UX-2 | **Every producer presents its output as an inventory (U-D-49b).** Owner: *"Everything that can create an item needs to have a inventory. Eg the Homeworkshop just has a button to collect created items instead of an inventory."* Processors and miners already opened as stores; the Home workshop’s hand-craft tray becomes a third 20-slot grid chunked by `HandCraft.TrayUsed`’s own rule, drag/click one stack out through `CollectWorkshopCommand(item, count)`, drops in refused, `Collect` → "Collect all". | `Sim/Actor/HandCraft.cs`, `Sim/UI/WorkshopText.cs`, `UI/Workshop/*`, `UI/Inventory/{InventoryViewModel,InventoryPanelController,StackDrag}.cs`; 617/618 sim tests pass (2 new tray tests in `Tests/Sim/Actor/HandCraftTests.cs`), 2 new Play-mode tests in `Tests/Play/Ui/InventoryDragPlayTests.cs`, offline compile clean | done — implementation, sim tests and offline compile, 2026-09-16; **Play Mode not run on this machine**; owner acceptance pending |
| GP-UX-3 | **The dragged stack is carried by its icon, under the pointer (U-D-49c).** Owner: *"When dragging from inventory to inventory, have the icon drag with the mouse pointer."* The ghost’s bounds CLAMP is replaced by icon-centre anchoring plus a `row-reverse` flip near the right-hand edge, which is where the storage column and the workshop tray sit. | `UI/Inventory/StackDrag.cs` (`Place`/`GhostPlace`), `UI/Inventory/InventoryPanel.uss` (`.drag-flip`); 5 new pure placement tests in `Tests/Play/Ui/DragGhostPlacementTests.cs` (new), offline compile clean | done — implementation and offline compile only, 2026-09-16; **Play Mode not run on this machine**; owner acceptance pending |
| GP-UX-4 | **Every producer’s slots are on screen the moment its drawer opens, and the drawer takes the width to do it (U-D-50).** Owner: *“The inventory for every producer, can you improve the UI? I have to scroll down to see the inventory slots. The UI can take up more of the screen if needed”* — the second sentence is the authorisation for the width. `.inventory-pair` becomes a SIBLING of `#backpack-body` rather than a child, so the machine controls and the Home workshop can never push a slot below the fold; `.inventory-scroll` loses `industrial.uss`’s fixed 144 px window (two rows at any size) for `flex-grow: 1` with a one-row floor; the drawer goes 520 px → 900 px alone and 800 px → 96%/1560 px with a producer open; and the secondary blocks become wrapping rows so they cost height only when used. **No C# changed.** | `UI/Inventory/InventoryPanel.uxml`, `UI/Inventory/InventoryPanel.uss`, `UI/Styles/industrial.uss` (the decisive sheet — it imports last), `UI/Workshop/WorkshopPanel.uss`; 4 new Play-mode tests in `Tests/Play/Ui/InventoryLayoutTests.cs` (new), compiled offline against the editor’s managed assemblies; 617/618 sim tests unchanged | done, then **superseded in shape by GP-UX-5** — the owner rejected this layout on 2026-09-16 (*“That’s not what I wanted…”*); the grid growth and the drawer widths survive into U-D-51, the scrolling body under both columns does not. Implementation and offline compile only, 2026-09-16; **Play Mode not run on this machine, no screenshot taken at any resolution**; owner acceptance pending |
| GP-UX-5 | **The drawer is two columns and nothing else: the player on the left, and on the right the open producer with its slots at the top and its own controls underneath (U-D-51).** Owner, rejecting what GP-UX-4 shipped: *“That’s not what I wanted. I want the players inventory to be on the left and the producer to be on the right. The Producers UI needs to be redone a little to have the slots at the top. Do you know what I mean?”* Checked first: the Backpack was already left and the producer already right, so the real fault was the shared scrolling strip GP-UX-4 left UNDER both columns, holding the machine controls and the Home workshop. `#backpack-body`, `#workshop-foldout` and the `workshop-view` mode are deleted; `#store-inventory` (slots) and `#store-body` (status, progress, repair, recipe / filter / priority, description) are nested inside `#side-store`; the Home workshop becomes an ordinary producer in that column — 20 tray slots first, then the recipe cards — reached from the heading's **Home workshop** button. With a machine open the controls are capped at 46% so the slots win; at Home the depot's storage and the workshop divide the column. Drawer widths unchanged from GP-UX-4. | `UI/Inventory/InventoryPanel.uxml` and `UI/Workshop/WorkshopPanel.uxml` (both rewritten), `UI/Inventory/InventoryPanelController.cs` (`SetWorkshopView` retired, the column's contents decided in `Paint`), `UI/Inventory/InventoryPanel.uss`, `UI/Styles/industrial.uss` (the decisive sheet — it imports last), `UI/Workshop/WorkshopPanel.uss`; `Tests/Play/Ui/InventoryLayoutTests.cs` — one test rewritten, one added — compiled offline against the editor's managed assemblies; 617/618 sim tests unchanged | done, then **amended in part by GP-UX-6** — U-D-52 retired the 46%/52% column cap described here and fixed the producer’s grid at the top of the column instead, and gave each producer its own scroller; the two-column shape and everything else here stand. Implementation and offline compile only, 2026-09-16; **Play Mode not run on this machine, no screenshot taken at any resolution**; owner acceptance pending |
| GP-UX-6 | **The producer’s slots are fixed at the top of its column and only what sits below them scrolls; the recipe cards run two to a row (U-D-52).** Owner: *“Can you make it so the inventory on the producer is fixed at the top and only the recipes are scrollable? Make the recipes fit 2 horizontally per row instead of 1.”* The third report in the series, and the first that accepts the shape it is correcting: U-D-51’s two columns and slots-at-the-top stand, but the slots could still grow into the column, and `#store-body` was one scroller for everything under them — so at Home the workshop’s 20-slot tray scrolled together with its cards. Now `#store-inventory` takes exactly the height its slots need (`flex-grow: 0`, the grid between a 72 px floor and a 300 px ceiling) and `#store-body` takes the rest; the body is a plain column and each producer brings its own scroller — `#machine-scroll` for a machine’s controls, `#workshop-cards` for the workshop’s cards, with the tray fixed above them. The 46%/52% `store-machine` cap is retired: nothing competes for the height any more. Cards are `flex-basis: 50%` with `flex-grow: 0` and an 8 px PADDING gutter, so two come to exactly 100% (Yoga measures `flex-basis` border-box); `min-width: 180px` drops back to one per row in a column too narrow for two. **A fourth report the same day** — *“The recipe cards need to have the same height each. At the moment they’re all different”* — is rule (d): the card fills its host (`flex-grow: 1`, replacing an `industrial.uss` `height: auto`), which squares the two cards in a row off against each other, and `.recipe-host` gets a `min-height: 152px`, which squares one row off against the next — flex cannot do that on its own, because wrapped rows are measured independently. | `UI/Inventory/InventoryPanel.uxml` (`#store-body` is a `VisualElement`, `#machine-scroll` added), `UI/Workshop/WorkshopPanel.uxml` (`#workshop-cards` is a `ScrollView`), `UI/Inventory/InventoryPanel.uss` (id rules, so the geometry beats `industrial.uss` by specificity rather than import order), `UI/Styles/industrial.uss` (one-per-row rule and both caps deleted), `UI/Workshop/WorkshopPanel.uss`, `UI/Inventory/InventoryPanelController.cs` (three sites); `Tests/Play/Ui/InventoryLayoutTests.cs` — three tests added, one rewritten, one assertion removed — compiled offline against the editor’s managed assemblies; 617/618 sim tests unchanged | done — implementation and offline compile only, 2026-09-16; **Play Mode not run on this machine, no screenshot taken at any resolution**; owner acceptance pending |
| GP-OPEN-1 | **The opening’s own machines arrive with no recipe, its ammunition step becomes two, and a hand-fed turret no longer stalls the chain (U-D-53).** Owner: *“Give the foundry no default. Do the rest”*, answering a ranked list of four fixes drawn from a row-by-row audit of the opening ladder. (a) Foundry, Assembler and Assembler Mk2 lose their `DefaultRecipe` in `OpeningBalance.Apply` — the one seam both data paths run through, so the generated catalogue and the editor `.asset` files are covered without either being edited. Ladder rows 10 and 28 used to complete themselves the instant the machine was placed, because the guards test the recipe and the data already supplied it. (b) `opening-ammo` splits: a machine that supports `bullet-batch` but has no recipe now gets its own “Set up the Assembler / Select the Bullets recipe” row, so the step does not keep saying “Build 1 Assembler” to a player who has built one. (c) `OpeningPhase.NoteSupply` latches “Automatic resupply working” off a FULL turret whose supplier still resolves, not only off a delivery — both write sites of `Stats.TurretFed` need hopper room, so a player who kept the turret topped up by hand had a working belt and a dead chain. The walk is gated on `ProducedAt >= 0` and on `TurretSupplier`, so a hand load cannot fake it. (d) `Words[0] = “no”` deleted — unreachable, since the row only renders while `more >= 1`. **Refinery, Mixer and Alien workbench keep their defaults** (out of scope; open consistency question for the owner). | `Sim/Data/OpeningBalance.cs` (the override, plus an aliasing fix in the loop), `Sim/Campaign/Opening/OpeningPhase.cs` (`NoteSupply`), `Sim/Campaign/Opening/OpeningQueries.cs` (row split, `UnsetBulletProcessor`, `KindName`, `Words`); tests `Tests/Sim/Campaign/Opening/OpeningObjectiveTests.cs` (+2, one `Sweep` step added for the split), `Tests/Sim/Campaign/Opening/OpeningSupplyTests.cs` (+2), `Tests/Sim/Production/ProductionFixture.cs` (new `Recipe` helper), `MachineProcessingTests.cs`, `ProductionPersistenceTests.cs`, `Tests/Sim/Power/PowerNetworkTests.cs` (explicit recipes where the catalogue default had been doing the work); **621 passed / 0 failed / 1 skipped of 622** (was 617/618), engine-side rebuild clean apart from the 3 pre-existing CS0252 warnings | done — implementation and offline tests only, 2026-09-16; **Play Mode not run on this machine**; an existing v9 save whose Foundry or Assembler relied on the data default loads that machine idle until a recipe is chosen, and no save upgrade repairs it (a v9→v9 fill-in needs a v10 bump, not authorised); owner acceptance pending |
| GP-PLAYTEST-1 | **Seven fixes from a 30 sim-minute objective playthrough of a fresh save, and two of the report’s recommendations corrected on their premises (U-D-54).** Owner: *“do a playtest of the first 30 minutes… tell me any errors, worrying spots, anything missing and improvements that can be made”*, then *“OK lets fix these”*. (a) `OpeningPhase.Active` now needs the turret alive AND `HomeQueries.CoreOperational` — the run ended “Attack repelled” over a core at 0/300, because attackers had walked past an out-of-range turret. (b) **The report’s second recommendation had a wrong premise and the fix was reshaped:** `DirectorQueries.OpeningOrigin` requires a placed turret and picks the approach passing NEAREST it, so the direction is chosen FROM the turret and cannot be named before it exists — instead a new `Uncovered` helper reuses `DirectorQueries.Clearance` and warns, on the Scheduled and Active rows, when that clearance still exceeds the turret’s range. (c) A new chain row for the shared-chest deadlock: the step-4 ore belt fills the same 200-item chest the Foundry’s plates need, the Foundry stalls at “output full” for good and `ReachesKind` calls both routes connected — raised only once the chest is half full of ore or the Foundry already reports `OutputFull`. (d) New `BeltSight.Advise`: a conveyor PULLS from the tile behind its arrow and `FlowRules.Accepts` never tests direction, so a belt laid beside its source is accepted in silence and carries nothing; this is placement-preview advice, never a refusal. (e) Honest teaching text — the fuel row computes one Coal ≈ 13 s at 300 kW from `PowerTuning` and names the Excavator-on-Coal exit, the Rifle and Equip rows say the finished Rifle waits in the workshop tray (U-D-44), the turret-power row quotes the machine status instead of splicing it (“hold fire without power. no power. Poles…”), `Source` names the tile rather than calling everything salvage, and `home-recovery` carries its Steel/Copper as material rows. (f) A chest “holds 200 items”, not “200 slots” — `MachineInventory.ChestCap` reads `InventorySlots` as whole items. **(g) was written and backed out:** the report read “Shift does nothing on a walk-here” off the harness, which drives `MoveCommand`, but `WorldInput` submits no `MoveCommand` — C-02/R-CP-3 deleted click-to-move deliberately — so no player can reach that path and `EngineerMovement` is unchanged. | `Sim/Campaign/Opening/OpeningPhase.cs`, `Sim/Campaign/Opening/OpeningQueries.cs` (eleven patches + `Uncovered`), new `Sim/Production/Flow/BeltSight.cs`, `Presentation/Building/PlacementPreviewPresenter.cs`, `Sim/UI/Build/BuildCatalogue.cs`, `Sim/Actor/Movement/EngineerMovement.cs` (written, then reverted — see (g)); tests — new `Tests/Sim/Regression/PlaytestFixTests.cs` (+8) and three wording assertions updated in `Tests/Sim/Campaign/Opening/ObjectiveSourceTests.cs`; **629 passed / 0 failed / 1 skipped of 630** (was 621/622), engine-side `-t:Rebuild` clean apart from the 3 pre-existing CS0252 warnings | done — implementation and offline tests only, 2026-09-16; the playthrough is a **headless command-driven run, not a human playtest**, so it evidences these defects and nothing about balance or normal progression; **Play Mode not run on this machine, no screenshot taken**; `BeltSight.cs` and `PlaytestFixTests.cs` have no `.meta` siblings until the editor regains focus; owner acceptance pending |
| GP-UX-7 | **A buffered line counts as connected, an outage is said once in a legible strip, Tab opens the Backpack alone, and every build card holds one height (U-D-55).** The owner’s fourth report of this series, four unrelated bullets. (a) *Connect your plate production* never completed over `Excavator → conveyor → storage → conveyor → foundry → conveyor → storage` — the owner’s own build — because `ReachesKind` was a single belt hop and stopped at the buffer chest; it is now a bounded walk THROUGH chests, and U-D-54’s shared-chest deadlock row was re-scoped to a DIRECT shared sink in the same patch so a legitimate buffer is never accused. (b) The “no power” toast re-posted itself every 150 ms refresh, climbing its own repeat counter about seven times a second, beside an urgent strip already saying the words — the post is gone, and the strip, which `industrial.uss` had left as #151d1e text on its own #202920 ground (≈1.2:1, invisible, which is why only the toast registered), is now a deep-red bar with light bold text. (c) Tab and “E at the depot” both pair the drawer with machine `-1` and the paint read that as “show the Home workshop” — the caller now states its intent (`OpenStore` vs the new `OpenWorkshop`), so Tab is the Backpack alone and only an interaction opens the workshop. (d) Build cards were each their own content height because the cost line wrapped freely in a fixed-width card in a `flex-start` wrap grid — the card is restacked into a column and dropped into a `.build-host` by U-D-52 rule (d)’s pattern, with `PaintCard` hiding the HOST on a tab switch so an off-tab card leaves no blank square. | Owner report 2026-09-17, four verbatim bullets (quoted in full in the U-D-55 provenance column). Offline checks only: `dotnet test` over the sim gives **633 passed / 0 failed / 1 skipped** of 634 — up four by the new `Tests/Sim/Regression/BufferedLineTests.cs` (a chest mid-line completes the row; a direct Foundry belt still completes it; two belts into one sink is still called out, but only when that sink is full; a full buffer on a working line is never called the deadlock) — and `Tests/Sim/UI/HudViewModelTests.cs` now holds 40 refreshes of an unfuelled generator and asserts the notice inbox never carries `PowerAlertSource.AlertKey`. The engine-side scratch csproj rebuilds with 0 errors and the 3 pre-existing CS0252 warnings. Two Play tests added (`PanelPlayTests.TheBuildCardsAreAllTheSameHeight`, `InventoryLayoutTests.TabOpensTheBackpackAloneAndNotTheHomeWorkshop`) and three amended for the Tab/workshop split. The card’s two geometry numbers were measured from `Inter-Regular.ttf` offline rather than observed. | **Done — engineering only; offline checks only; Unity Play Mode is not available on this machine, so the new and amended Play tests compile and have NOT been run, no screenshot exists, and rules (b) and (d) were diagnosed by code inspection and font measurement alone. Owner acceptance pending.** |
| GP-UX-8 | **E interacts and nothing else, and a tracer is drawn from where the sim recorded the shot (U-D-56).** Owner: *“Pressing 'E' when having the rifle equipped does something weird. I can't inspect things”* and *“Also when the turret fires when in range of the player, it looks like the player shoots and not the turret”*. Both premises were checked first. E’s equip role was a port invention — the reference binds E to interaction alone — and its branch sat above BOTH the Home-workshop branch and the “point at a machine” notice, so with a weapon in hand E opened a machine only when the cursor already rested on one, could never open the Home workshop, explained nothing, and holstered the rifle into an in-hand-but-unequipped state that made `WeaponPhase` refuse every shot. The turret’s sim origin was correct; `TracerPresenter` was re-anchoring the newest tracer to the engineer, and `st.Weapons.Shots` is shared with every turret. | `Presentation/WorldInput.cs` (weapon branch and `Equip()` deleted, `_interact` lookup, `Interact` re-ordered and documented; the input ACTION keeps its asset name `Equip` so saved rebindings survive), `UI/Settings/BindingMap.cs` (the printed label is now “Interact”), `Presentation/Combat/TracerPresenter.cs` (every tracer starts at its recorded origin; `muzzleOffset` pushes along each shot’s own line). One Play test added, `WorldInputFeedbackTests.PressingEWithAWeaponInHandOpensTheMachineAndNeverUnequipsIt`. No `Sim/**` file changed, so the sim suite was not re-run (633 passed / 0 failed / 1 skipped of 634 as of GP-UX-7); the engine-side scratch csproj rebuilds (`-t:Rebuild`) with 0 errors and the same 3 pre-existing CS0252 warnings. | **Done — engineering only; offline checks only; Unity Play Mode is not available on this machine, so the new Play test compiles and has NOT been run, nothing was seen running and no screenshot exists. Owner acceptance pending.** |
| GP-UX-9 | **A dropped stack lands in the slot the pointer is over, Sort moves onto the Backpack header, and Shift-drag splits into the hovered slot (U-D-57).** Owner: *“Also when you drag an item in your inventory it automatically sorts it into the first available slot instead of the slot hovered over(which is correct). Can you also add a sort button and stack splitting functionality”*. The premise was checked first and two thirds of it did not hold: a Sort button already existed in three places and splitting already existed as a button, a menu entry and `InventorySplitCommand`. A probe run against the real handlers proved the sim honours the hovered destination even in the never-arranged `Engineer.Pack == null` state a new game is in — the state the existing tests never reached, because both of them sort first. The real defect was in the drawer: `Drop` resolved only what `panel.Pick` returned, and `#pack-grid` is a wrapping grid inside a ScrollView with 4 px gutters, so a release a pixel off a slot resolved to nothing, moved nothing and said nothing — which, against an auto-compacted Backpack, reads exactly as “it sorted my stack to the front”. | `UI/Inventory/StackDrag.cs` (`DragPoint`: live pointer position and live Shift state, passed to `preview` and `drop`), `UI/Inventory/InventoryPanelController.cs` (`NearestSlot`/`Nearest`/`Clip` snap a near miss to the nearest slot in the grid the pointer is inside, viewport-clipped and never across columns; `Drop` says `DropOffSlot` for a genuine miss; `pack-sort` wired with no enable/disable; the Shift branch sends `InventorySplitCommand` to the hovered slot via `SplitRefusal`/`SplitAmount`; `DragTo(…, split)` and `DragToPoint` test seams), `UI/Inventory/InventoryPanel.uxml` (`pack-sort` on `#pack-head`, new hint), `UI/Inventory/InventoryPanel.uss` (`#pack-sort` and its `.compact-ui` override — selected by ID because industrial.uss imports LAST and its `.strip-button { min-height: 32px; margin: 2px }` beats any class rule here), `Sim/UI/TransferText.cs` (`DropOffSlot`, `SplitNeedsEmptySlot`, `SplitTooFew`, `DragSplit`, new `Hint`). Tests: two sim regressions in `Tests/Sim/UI/BackpackDropTests.cs` and three Play tests in `Tests/Play/Ui/InventoryDragPlayTests.cs`. The sim suite was RUN — **635 passed / 0 failed / 1 skipped of 636** (was 633/0/1 of 634) — and the engine-side scratch csproj rebuilds (`-t:Rebuild`) with 0 errors and the same 3 pre-existing CS0252 warnings. | **Done — engineering only; offline checks only; Unity Play Mode is not available on this machine, so the three new Play tests compile and have NOT been run, nothing was seen running and no screenshot exists. Owner acceptance pending.** |



## Gameplay improvement pass — owner brief 2026-09-15

The owner authorised one implementation pass over the playable opening and defence loop: *"Relight should be demanding, understandable and rewarding to master. Poor preparation should cause shortages, damaged defences, failed expeditions and meaningful setbacks. Good preparation, factory design and combat skill should noticeably improve the outcome. Regular raids consume supplies and expose weaknesses. Major assaults test sustained production and defence. Exploration unlocks useful capabilities and advances city restoration."* The existing plan for the complete game is preserved; approved dependencies are brought forward only where an item needs them.

**Owner acceptance is pending for every row below. Nothing here has been playtested or accepted by the owner.**

| ID | Scope | Depends on | Acceptance | Status |
|---|---|---|---|---|
| GP-W1 | **Unattended Home workshop (U-D-44).** One persistent workshop queue with stable job ids; ingredients reserved out of the Backpack at queue time so they cannot be spent twice; a physical output tray collected in reach, never delivered remotely; a full tray pauses the job at full progress without consuming or repeating; cancellation refunds every unprocessed ingredient; jobs, reservations, tray and progress survive save/load; existing saves and legacy craft queues migrate without duplication or loss; the UI Toolkit workshop panel shows ingredients, quantities, duration, queue, progress, tray and clear Cancel/Collect controls and says that production continues while away. Recipe costs, durations and power requirements are unchanged. | B-07, C-05, C-06 | Conservation, cancellation, full-tray, migration and save/resume covered by focused sim tests (592 pass); the movement lock narrowed to a core repair and pinned by test | done — implementation and focused checks, 2026-09-15; owner acceptance pending |
| GP-W2 | **Decision record and guidance amendments.** U-D-44 recorded; U-D-10 marked superseded with what survives and what is withdrawn; UI_AND_ONBOARDING.md §4/§6/§9, CONTENT_CATALOGUE.md and GAME_DESIGN.md amended; B-07 and C-06 annotated. | GP-W1 | No document still asserts the immobilising craft as current behaviour | done 2026-09-15 |
| GP-W3 | **Raid scheduling, warnings and placement feedback.** Major-assault staging computed from the actual map, reachable approaches and valid spawn locations, with no invalid fallback accepted; a configurable minor-raid preparation warning (initially 30–45 s) with an accurate approach indication; stale notices cleared; announced information matches the encounter that arrives; approaches chosen before they are announced and varied where the map supports it; encounter state preserved across save/load so warnings and waves cannot duplicate or disappear; the first-turret teaching encounter kept separate from later sieges and fired once from a loaded, operational turret; turret range and obstructed sight shown during placement with a clear blind-position warning, and the tutorial's suggested position made viable. **Delivered (U-D-45, U-D-46):** minor raids split into a warning and a wave with `SiegeTuning` 30 + 0..15 s of preparation; one shared reachability/clearance definition behind `Origin`, `Approaches` and `Staging`; authored camps joining rather than replacing the map's approaches; every notice given an expiry; `SaveSchema` 6 → 7 with a v6 wave upgraded rather than re-announced; `Sightline.Clear` made the single line-of-fire rule for turrets, enemy target selection and the engineer's rifle; `TurretSight.Coverage` drawn as the placement outline with a blind/restricted advisory. | GP-W1 | Focused encounter-scheduling, notice-expiry, save/resume and v6→v7 migration tests (`DirectorTests`, `DirectorSaveTests`); staged origins and headings verified against the shipped Home export through a scratch console harness; turret coverage and the wall rule pinned by `TurretSightTests` | done — implementation and focused checks, 2026-09-15; owner acceptance pending |
| GP-W4 | **Major-siege director redesign.** Several waves, varied composition, pressure on multiple defensive positions, complexity introduced with progression and available tools; the encounter calculated against actual production, stored ammunition, turret cadence, damage, delivery capacity and fuel use; useful player interventions that a well-prepared base needs less; reserves that help significantly; escalation through approved milestones and destinations rather than machine-count scaling. Three to five waves over six to eight minutes is an initial tuning proposal, not a required result. Parameters stay editable through the existing data pipeline and the chosen values and reasoning are recorded. | GP-W3 | Wave plan, composition and approach growth pinned by `SiegePlanTests`; whole-roster arrival, deferral and v7→v8 save resume pinned by `SiegePlanTests`/`DirectorSaveTests`; 606/607 sim tests pass (1 long-run skip); the plan checked end to end against the shipped Home export through the scratch console harness. Values and reasoning recorded in **U-D-47**; tunables on `Data/Generated/Tuning/Tuning - Siege.asset` (`SiegeTuningAsset`), with `SiegeTuning.Fallback` until the owner runs the Unity data generator | done — implementation and focused checks, 2026-09-15; owner acceptance pending |
| GP-W5 | **Combat and losses.** Enemy movement, perception and attack driven by the configured data through the real command paths; readable attacks with viable responses; the approved Breaker role brought forward for real pressure on structures and production connections, from the existing roster; invulnerable barriers made of ordinary machinery removed; sensible pathing and obstruction attacks with consistent damage rules; recoverable backpack drops and repairable machine wrecks with explicit rules for equipment ownership, dropped contents, repeated deaths and save persistence; progression protected from permanent loss; retreat from camps through readable pursuit rather than an invisible boundary; range assumptions checked against the current assets. **Delivered (U-D-48):** `Sim/Data/CombatBalance.cs` is a new port-owned combat layer applied after `OpeningBalance` in both data paths — it brings the approved Breaker forward from CONTENT_CATALOGUE §7.4 and gives every BUILDABLE machine an integrity row (`min(120, 20 + 2 × costItems)`), which removes the invulnerable chest wall at its source rather than patching `HostileOpen`; walk-through kinds and the costless Depot keep none. `EnemyPhase` was re-pointed at the tables for perception, contact damage, chase and march speed, structure chew and contact reach, and `TickCombatActor` gained the readable leash (commit to `GuardPursuitTiles`, walk home STILL remembering, re-engage only inside `GuardReengageTiles`). `SiegePlan` gained `WaveBreakers`, taking `MajorBreakerShare` out of the skitters from `MajorBreakerAssault`/`MajorBreakerWave` and leading each carrying wave. A wreck stays on the map at 0 hp and now stops working everywhere at once (`PowerQueries.Throttle`, `PowerNetwork.Build`, `PowerPhase.ReportChanges`, `FlowPhase` routing, `ProductionQueries.OperatingState`, `Ground.SolidMap`, `Sightline.Ensure`), with `HomeQueries.MachineRepairCard`/`MachineRepairLine` and `HomeCore.RepairProblem` giving it a player-facing price, instalment count and the sim's own refusal. `Sim/Actor/DeathCache.cs` drops the Backpack contents into a never-despawning pile at the death tile, retaining weapon keys, merging repeated deaths on one tile and conserving exactly once each way. `SaveSchema` 8 → 9 adds `director.major.waveBreakers` and `drops`. | GP-W1 | Nine focused tests added across `EnemyTests`, `TurretSightTests`, `TurretRepairTests`, `MachineProcessingTests`, `PowerNetworkTests`, `SiegePlanTests`, `DirectorSaveTests` and `LedgerTests` — perception from data, the pursuit leash, wreck sight/movement/routing agreement, a wreck stopping production and severing a circuit, Breaker composition and its v8→v9 save round-trip, the repair card, and drop conservation through death, a second death, refusals and collection. 615/616 sim tests pass (1 long-run skip), no duplicate items or rewards. Values and reasoning recorded in **U-D-48**; tunables on `Data/Generated/Tuning/Tuning - Siege.asset` (`SiegeTuningAsset`), with `SiegeTuning.Fallback` until the owner runs the Unity data generator | done — implementation and focused checks, 2026-09-15; owner acceptance pending |
| GP-W6 | **Fuel, power and failure readability.** Estimated fuel time at current load, labelled an estimate, with a useful low-fuel warning that teaches a practical reserve; generator delivery connected where needed with finite fuel and proper item accounting preserved; power shortages visibly affecting the machines they constrain; concise persistent indicators for dry turrets, disconnected machines and insufficient power; objective and HUD figures derived from the same authoritative state; internal identifiers, notification spam and developer diagnostics replaced with player messages, diagnostics kept in logs; a compact post-attack account reporting only causes supported by actual events. | GP-W3 | One authoritative source per figure, pinned by test; no identifier leaks in player text | todo |
| GP-W7 | **Opening and first expedition.** Opening reworked around the unattended workshop and earlier dedicated automation, with less compulsory starter processing and fewer repetitive objectives, preserving the mining feedback that works; objectives that recognise queued jobs, materials in relevant storage and conditions already satisfied, and useful actions while batches process; a named reachable destination with a reason to go and preparation advice grounded in the encounter; the approved site kinds omitted by the importer restored from the canonical world source with their identities, locations and references, and verified to connect to gameplay; the first camp's encounter, reward and usable follow-through completed before the player is sent there. Progression meanings preserved: Alien Keys unlock strongholds, Schematics unlock approved technology and equipment, Artifacts support the approved crafted-upgrade system. City restoration stays a lasting objective through the approved core/plant sequence. | GP-W3, GP-W5 | Imported site kinds present with canonical ids and coordinates; the reward has an acquisition-to-use path | todo |
| GP-W8 | **Production balance against working gameplay.** Output given competing uses through defence, repairs, expansion, equipment and approved restoration projects; ammunition production reassessed after the siege and first expedition function, measuring both total consumption and shortages at individual positions (**no blanket reduction from the audit's "25x surplus" figure**); copper automation added or connected where it relieves repetitive manual work, as a supply lane rather than an ammunition consumer; opportunities to stockpile and prepare preserved, with intense periods testing those reserves and recovery periods after them. | GP-W4, GP-W6, GP-W7 | Measured consumption and shortage figures recorded with the chosen values | todo |


## Always dark — owner decision 2026-09-19 (U-D-58)

The owner decided the world is always dark, with no day and night cycle, and approved [ALWAYS_DARK_SPEC.md](ALWAYS_DARK_SPEC.md), which owns the rules. **Owner acceptance is pending for every row below.**

| ID | Scope | Depends on | Acceptance | Status |
|---|---|---|---|---|
| L-01 | **Make it dark** (spec §3, §4): `DaylightSeconds` 0 means no sun; twilight overlay (provisional 0.55) with a brightness setting; alien silhouettes; feedback drawn above the darkness; brownouts shrink lights; in-light HUD cue; elapsed-time HUD clock; flashlight always on; substation step text; the document corrections of spec §9 | Court D55 | Spec §7 stage 1 checks: sim tests, and a Play Mode run of the opening in the dark | done 2026-09-20 (engineering; owner acceptance pending: overlay strength to be tuned by eye, opening to be played in the dark) |
| L-02 | **Make light matter** (spec §5): hesitation at lit edges, raids prefer dark approaches and never strand, what stops a bullet stops light, light-coverage placement preview, relief events, perception in light, teaching the rule | L-01 | Spec §7 stage 2 checks, Founders Court Verify C20 still passing | todo |
| L-03 | **Populate the dark** (spec §6): fixed roaming population per dark district | L-02, E-13 | Spec §7 stage 3 checks | todo |
| L-ACC | Owner check: the opening played in the dark; a raid against a partly lit base | L-02 | Owner verdict | human |

## 8. Log

- 2026-09-11 — A-01–A-05 delivered (worker documents). A-06 in progress: MIGRATION_MAP.md written from the per-file inventory (178/178 files), TASKS.md source references rewritten to the real system IDs, B-14 added, DECISIONS.md extended with worker questions. No implementation started; reference game untouched.
- 2026-09-11 — A-06 done: automated coverage checker (paths, links, S-ID/task/decision cross-references) reports 0 problems across the ten package files; value/term agreement greps clean; `_drafts/SYSTEM_INVENTORY.md` absorbed into MIGRATION_MAP.md and removed. A-07 review worker dispatched.
- 2026-09-11 — A-08 done (run alongside A-07): paused-for-migration pointers added at the top of `docs/PROGRESS.md` (plus one log line), `docs/PROGRAMME_STATE.md`, `CLAUDE.md` and `AGENTS.md`; no other reference text, saves or evidence changed; CRLF endings preserved.
- 2026-09-11 — A-07 done: one Opus review worker (read-only) returned 20 findings (9 high, 9 medium, 2 low; 19 confirmed, 1 suspected). All 20 resolved in a single pass: Quarry/Wharf renumbered GP-21/GP-22 and later raid tiers to GP-23 (verified against `docs/PROGRESS.md:85-89` and `docs/EXPLORATION_DEFENCE_PLAN.md:101`); C-01 now depends on D-02b; F-07 blocked (E-16, F-03); B-02 lists the eight §4.1 assemblies; E-14 and B-11 dependencies aligned with U-Q-13/U-Q-08; U-Q-04/U-Q-05/U-Q-08 Blocks corrected; light-mask over-claim in U-M-03/TECHNICAL_ARCHITECTURE R2 restated to what `GP_CHECKPOINT.md:47` and `P5_05_INTEGRATION_REPORT.md:76` actually say; W-8 withdrawn as a defect (two distinct cell sizes, not a wrong comment); W-1 citation, art counts (65 SVG + 3 generators), turret-cadence labels and nine line-number citations corrected; TECHNICAL_ARCHITECTURE §13 items 1 and 5 now point at U-M-13/U-M-06. Coverage checker re-run: 0 problems. Phase A complete.
- 2026-09-11 — Correction pass (owner brief, 11 items) applied by the coordinator with three Opus workers (tasks/verification; architecture/Unity version/MCP; gameplay/chronology/opening) plus one Opus checker: Phase B rescoped to the engine-independent foundation with checks alongside implementing tasks (U-M-22), B-07/B-09/B-10 moved to Phase C, the former "later checks" row removed, DEF-01–DEF-04 deferred with triggers (U-M-23), B-02a MCP verification added with an explicit unavailable exit, the exporter and importer split into Home-region (D-01a, D-02a) and full (D-01b, D-02b) rows, F-05 split into plateau handling now and F-05a–c after U-Q-02; Unity 6.6 recorded as an Update release, not LTS (U-M-01), no 6.6 install present; assembly boundary (U-M-25) and tick-cost terminology (U-M-26) recorded; playtest chronology for mining, hand ammo and turret rate rewritten as provisional starting points with retests pending (DECISIONS §3, GAME_DESIGN §12.2); energy cells split (U-D-25); opening deferral defect recorded (U-D-26, U-Q-21) with C-08/C-09 amended; hopper endurance corrected to about 50 s; "Shot magazines" player wording marked for replacement; CONTENT_CATALOGUE.md §17 (runtime content scope) added and its §17.3 gate rows aligned with U-M-15 and U-Q-13; reference-snapshot.sha256 (254 files) is the baseline (U-M-19). The 146 reported reference failures stay reported, not proven pre-existing, and are not a prerequisite (§7). Checker: 0 problems. Later the same day: Unity `6000.6.0f1` observed installed and a project shell observed at `Unity/Relight/` (owner action via Unity Hub, not a migration task); B-01 set to in progress; the shell was not touched.
- 2026-09-11 — Owner decision brief (Q01–Q20) recorded. Seventeen owner decisions added (DECISIONS.md U-D-27–U-D-43) and U-D-22/U-D-23 amended; nineteen of the twenty-one owner questions are now resolved, leaving U-Q-02 (ending) and U-Q-04 (art package and tileset licence, awaiting external information). Consequences in this file: B-11 gains a rotating autosave ring separate from manual slots with atomic writes (U-D-35); C-10 becomes the full front end — New Game, Continue, Load, Settings with save metadata, overwrite confirmations, autosave settings and audio volumes (U-D-41); audio is in scope, so C-11 defines the `AudioCue` hook, **D-11 (new)** builds the cue catalogue, mixer and first clip set, and F-04 becomes ambience and the mix pass (U-D-33); the per-feature go-ahead is withdrawn, so E-09–E-15 and F-01 move from `blocked` to `todo` on their engineering prerequisites alone (U-D-31); Breaker HP is 220 provisional (U-P-08) and the guardians 600 / 1,000 / 1,500 provisional (U-P-09); tuning during playtesting is delegated, so F-02 is a record rather than a gate (U-D-28); F-06 closes as `done (not required)` and leaves the F-ACC dependency list (U-D-27); DEF-03 and DEF-04 now rest on owner decisions (U-D-34, U-D-42); C-12 verifies the browser fixes in Unity instead of waiting for retests (U-D-43). No gameplay code was written: U-D-31 approves planning only, and implementation begins under a separate owner instruction.

- 2026-09-11 — Decision brief applied across the whole package by the coordinator with three explicitly model-selected Opus workers under exclusive file ownership (Worker A: GAME_DESIGN.md, CONTENT_CATALOGUE.md; Worker B: UI_AND_ONBOARDING.md, WORLD_AND_ASSETS.md; Worker C: TECHNICAL_ARCHITECTURE.md, MIGRATION_MAP.md, SOURCE_INVENTORY.md). New specialist sections: GAME_DESIGN.md §1.4, §9.1.1, §9.3.1, §15.1 (the A-1…A-9 answers); UI_AND_ONBOARDING.md §2.6, §2.7, §3.8, §9.1, §12 (the cue inventory); WORLD_AND_ASSETS.md §9.1, §9.2; TECHNICAL_ARCHITECTURE.md §4.0.2.1, §8.4.1, §9.4 (saving), §15 (audio architecture) and risk R17; MIGRATION_MAP.md row S-A5 (audio). Cross-file corrections merged back into this file and DECISIONS.md: B-01 now records that `com.unity.vectorgraphics` is **absent** from the shell manifest and that the built-in `com.unity.modules.vectorgraphics` is a different thing that does not import `.svg`; B-11 gains a `playSeconds` accumulator, because the reference save format has no playtime field (`save.ts:74-88`, `session.ts:137`), and carries the delegated autosave defaults (5 minutes of unpaused sim time, 3 ring slots); C-10's metadata list now says which fields come from the reference format and which are new; D-11 gains an **Alerts** mixer group and a voice budget. Two dependencies corrected so external information cannot hold completion (U-D-29): F-07 no longer waits on the art package and is measured against the art actually in the build, and F-ACC treats F-03 and F-05a–c as conditional. Stale gates removed from specialist text: WORLD_AND_ASSETS.md §11 item 4 and §4.7 (Quarry/Wharf were still "open as a scope question"), MIGRATION_MAP.md S-34 (Frame/Board consumers were still listed as outstanding work, contrary to U-D-39) and TECHNICAL_ARCHITECTURE.md §13 (U-Q-06 and U-Q-20 were still listed as open). Two pre-existing unescaped-pipe table defects in MIGRATION_MAP.md rows S-64 and S-A1 were escaped. The referenced-path and link checker reports 10 files checked, 0 problems. No gameplay code was written and no Unity project file was touched.
- 2026-09-12 — Phase B implementation authorised by the owner. B-01 done (U-M-27), B-02 done (project integrated into `Unity/` from the owner's shell, asmdefs, scene shells via batchmode `-executeMethod`, compile clean), B-02a done on the explicit exit (U-M-28, 98 tools registered by the server, none exercised: Unity-side plugin not installed). Shared `Relight.Sim` contracts written by the coordinator (`Sim/Core/Contracts/`, `Sim/Data/`) before parallel work; three explicitly model-selected Opus workers dispatched with exclusive folders: B-03 (`Sim/Core`), B-06 (`Sim/Actor`, `Sim/Building`, `Sim/Queries`), B-08 (`Sim/World`, `Sim/Geometry`, `Sim/Actor/Movement`). Workers compile through a scratch `dotnet` project outside Unity (not a project artefact; DEF-01 stays deferred); only the coordinator runs the editor.
- 2026-09-12 — Phase B wave 1 integrated (coordinator Fable, three Opus workers under exclusive ownership): B-03 core/tick/dispatcher/PRNG, B-08 world/ground/path-finding/engineer body, B-06 inventory/hand-craft/ledger/machine transfer, plus B-04 host written by the coordinator. Coordinator contract changes recorded in U-M-29/30/31 (danger counters on `Engineer`, `ICityGeometry.Solid`, `EngineerTuning.DashCost`, `MachineSpec.FuelCap/AmmoCap`, reference display names). Checks: `Relight.Sim` builds with 0 errors; 102 edit-mode tests pass in the scratch NUnit runner and under Unity's EditMode Test Runner (`Docs/evidence/phase-b/b12-editmode-wave1-2026-09-12.xml`); PlayMode SimHost checks 4/4 plus the 50 s long form 1,000 ticks in 50.000 s (`b04-playmode-*-2026-09-12.xml`). The owner's Hub editor remains open on the untouched `Unity/Relight/` shell; it does not block batchmode on `Unity/`. Wave 2 (B-05 with the Unity lock, B-11, B-12) dispatched.
- 2026-09-12 — Phase B wave 2 integrated (coordinator Fable, three Opus workers under exclusive ownership; B-05 held the Unity lock): B-05 data pipeline and 79 assets, B-11 persistence (schema v1, atomic writes, autosave ring, `AutosaveController`), B-12 replay/conservation/timing checks with the shared short run. Coordinator work: H1 round trip wired into `SharedChecksTests`, `GameDataHash` extended to all nine tables, the Windows-only persistence test failure fixed in the in-memory file system. **Owner correction applied:** the project root is `Unity/Relight/` (U-M-32) — moved with the editor closed, exporter and table-writer paths repointed, README/TA/B-02 amended. Unity on `Unity/Relight`: EditMode 172/171 passed/0 failed/1 explicit skipped, PlayMode 5/4/0/1, 0 `error CS` (`evidence/phase-b/{b12-editmode-wave2,b04-playmode-wave2}-2026-09-12.xml`). Delegated choices in U-M-33. Wave 3 (B-13 with the Unity lock, then B-14) dispatched.
- 2026-09-12 — Phase B wave 3 integrated and **Phase B engineering complete pending owner acceptance** (coordinator Fable; two Opus workers run one after the other under the Unity lock). B-13 `World.unity` scene (bootstrap, 20 Hz host, camera rig, four-layer synthetic map, engineer body, click-to-walk and chest placement, UI Toolkit status panel; PlayMode evidence screenshots `b13-playmode-{world,panel}.png`, U-M-34). B-14 prototypes with measured verdicts (U-M-35): UI Toolkit drag-and-drop kept, one tilemap per layer kept with the 188.66 MiB scene-serialisation finding, URP 2D lights as-is; painted R7 scene regenerated on demand and git-ignored. Final coordinator run on the complete tree with the editor closed: EditMode 172 total / 171 passed / 0 failed / 1 explicit skip; PlayMode 31 / 26 / 0 / 5 (the R7 map absent, three map tests and two long checks skipped by design; 31 / 29 / 0 / 2 in the worker's run with the map painted, `b14-playmode-2026-09-12.xml`); 0 `error CS` (`evidence/phase-b/final-{editmode,playmode}-2026-09-12.xml`). B-ACC stays `human`: owner play/inspection acceptance per the README checklist is the open gate; Phase C is not started.
- 2026-09-12 — **Phase B handoff closure** (coordinator Fable alone, no workers; owner's editor open on `E:\Factorio2\Unity\Relight`, so all batchmode checks ran on a throwaway copy in `%TEMP%\relight-check`, deleted afterwards). (1) One real disk save/load round trip through `World.unity` in two Unity processes (`DiskRoundTrip` explicit pair; state changed, saved, restored and verified in a fresh session; B-11 record updated). (2) Production and power clarified: B-07, B-09 and B-10 were **intentionally moved** to Phase C by U-M-22 with IDs kept and are not started; Phase B has no incomplete row (README bullet “Not in Phase B, by decision”). (3) Runtime map painting reclassified in U-M-35 as a *proposed* D-09 approach pending validation of compact storage, editor inspectability and full-game load/runtime cost. (4) Generated balance assets confirmed as the editable source of truth with fingerprint-based hand-edit preservation, verified through Generate → edit → Generate (kept) → Regenerate (discarded) on the copy; all 78 real-project assets stamped (U-M-36; B-05 record updated). (5) No background workers or wait loops of this task remained. Not re-run: the full EditMode/PlayMode suites (owner instruction); expected effect is PlayMode 33 total with two more explicit skips. B-ACC stays `human`; Phase C pending.

- 2026-09-12 — **Phase B repair pass** (owner-supplied Astra audit F1–F8; coordinator Fable, three explicitly selected Opus workers under exclusive file ownership, coordinator alone on Unity; U-M-37). All eight findings verified against the code and fixed: F1 hand-craft lock, F2 cancellation conservation (pending refund, schema v2, supersedes U-M-31 (2)), F3 repeatable `.bak` recovery with primary restore, F4 autosave index reconciled on every read, F5 `SessionChanged` rebuild/reseed, F6 sprint one-shot clear, F7 stake from `GameData.Stake` plus generator mode/orphan/CONFLICT reporting, F8 repository baseline inspected. Checks (`Docs/evidence/phase-b/repair-2026-09-12/`): local sim runner 200/200; Unity EditMode on the copy 70/70 (persistence, replay, stake) + 16/16 (hand craft/lock); PlayMode `HeldKeyTests`, `WorldSceneTests`, `SessionChangeTests` passed; real two-process disk round trip repeated; generator keep/edit/new-game/reset/conflict sequence on the copy. Simulated failures (MemoryFileSystem) and real disk operations (RealDiskRecoveryTests on NTFS, DiskRoundTrip pair) are recorded separately; no editor observation was possible (owner's editor open, MCP not connected) and the full suites were not rerun (owner instruction). Repository: `Unity/` is untracked in the parent (`?? Unity/`) and `Unity/Relight/.git` is a one-commit Hub shell; a recoverable snapshot (source tar.gz with 814 entries incl. 383 `.meta`, nested-git bundle, sha256) was written to `Unity/Snapshots/`; recommendation and the exact owner decision are in README “Repository”. B-ACC stays `human`; Phase C not started.

- 2026-09-13 — **Phase B loose ends before owner acceptance** (owner brief of four bounded fixes; coordinator Fable alone, no workers; no Unity Editor available on the machine — 6000.6.0f1 not installed, MCP `instance_count: 0`; U-M-38). **Fixed:** (2) `AutosaveController` resets its scheduler on every session change (B-13 residue closed); (3) version-1 Unity saves load through `SaveUpgrade` with the two refund fields defaulted to zero, checksum verified as written, file untouched. **Confirmed, no change:** (1) the pending crafting refund is drained into the Backpack by every hand tick and every cancel, counted by the ledger, saved, and delivered after a save/load — one new test frees the space with real `MachineTransferCommand`s into chests. **Inspected, nothing changed:** (4) the repository — `origin/main` already tracks the whole Unity project in the parent (`aa00d1a8`, PR #6), the nested `.git` is absent from a fresh clone, the snapshot tarball and both bundles verify and match the tracked tree; the accidental part is `Unity/Snapshots/relight-nested-git-dir-2026-09-12/` (839 tracked files, a raw copy of the nested `.git`); `E:\Factorio2` was not visible. The 2026-09-12 line above that reads “all eight findings … fixed” is corrected here: **F8 was inspected, not resolved — repository ownership remains an owner decision.** **Checks (all on the actual project sources, none on a copy):** scratch `dotnet test` 82/82 across the affected classes; scratch compile of the changed Presentation files and the new PlayMode test against `UnityEngine.CoreModule` 6000.5.8f1, 0 errors; PlayMode not run; full suites not rerun (owner instruction); `evidence/phase-b/loose-ends-2026-09-13/`. **Owner actions remaining:** the repository commands below (approval, not executed); B-ACC items 1–13 — with item 11 corrected: nothing in this build loads a save when Play starts, so “rename `auto-quit.json` and press Play” cannot be performed as written; the `.bak` recovery is covered by `RealDiskRecoveryTests` (real NTFS files) and the `DiskRoundTrip` pair, or by calling `AutosaveController.LoadNewestAutosave()` from a test. B-ACC stays `human`; Phase C not started.
  **Repository plan (for owner approval; nothing below was run).** Goal: the Unity project — source, assets, `.meta`, `Packages/`, `ProjectSettings/`, `Tools/`, the three dot-files — tracked in the parent repository only, generated caches excluded, no raw repository snapshot in the tree. State found on `origin/main`: already tracked (731 files under `Unity/Relight`, 382 `.meta`; `Library/`, `Temp/`, `Logs/`, `obj/`, `UserSettings/`, `*.csproj/.sln` and the 189 MB R7 scene ignored by the nested `.gitignore`, which the parent honours). Step 1 (on `E:\Factorio2`, read-only): `git status --short Unity | head` and `Test-Path Unity\Relight\.git` — if the nested `.git` is still there, the checkout predates `aa00d1a8` or was never pulled; `git log --oneline -1 -- Unity/Relight/Packages/manifest.json` says which. Step 2 (only if the nested `.git` exists; the owner's decision): `Rename-Item Unity\Relight\.git .git-nested-archived` (its history is already in `Unity/Snapshots/relight-nested-git-2026-09-12-final.bundle`, verified complete; `git clone Unity/Snapshots/relight-nested-git-2026-09-12-final.bundle <dir>` restores it), then `git pull` so the tracked Unity files land. Step 3 (untrack the accidental raw snapshot; keeps the file on disk): `git rm -r --cached Unity/Snapshots/relight-nested-git-dir-2026-09-12` and add `Unity/Snapshots/relight-nested-git-dir-*/` to the root `.gitignore`; the tarball and the two bundles (3.7 MB, verified) stay tracked as the recoverable backup. Step 4 (verify before committing): `git status --short Unity` shows only the removals from step 3 and this pass's edits; `git ls-files Unity/Relight | wc -l` ≈ 731 and `git ls-files Unity/Relight | grep -c '\.meta$'` = 382; `git check-ignore -v Unity/Relight/Library/x` matches the nested `.gitignore`. Step 5: commit — nothing is pushed, merged or deleted by any step.- 2026-09-14 — **Phase C preparation only; B-ACC still pending** (owner Phase C brief received; coordinator Fable alone, no workers dispatched, nothing implemented). **State confirmed:** B-ACC is `human` with no acceptance recorded anywhere (TASKS.md, README, DECISIONS, evidence, git after `f5ae6930`), so the brief's conditional authorisation does not apply and Phase C stays `todo`. **Environment:** the project targets `6000.6.0f1`, but this machine has only `Unity 6000.4.5f1` and `Unity 6000.5.4f1` under `C:\Program Files` (plus an unversioned `Unity\Editor` folder); the Unity MCP bridge (`coplay-mcp`) timed out again on connection — no batchmode, Play Mode or Test Runner check is possible until 6000.6.0f1 is installed (or U-M-01/U-D-30 is overturned by the owner). **Rows amended (omissions from the owner's brief filled, no new tracking):** C-01 canonical source, B-07 handcraft lock through command paths, B-09 finite fuel and operating-state queries, C-03 one unit = one bullet, C-04 tracers and asset-driven cadence, C-06 workshop card contents and removal of the redundant equipment block (equipment strip stays under the Backpack), C-07 actionable problem messages, C-08 fair varied approaches for post-opening raids, C-09 already-satisfied conditions and recovery paths, §6 note that D-01a/D-02a run inside Phase C for the Home region only. **Prepared sequence (three Opus workers max, exclusive file ownership, coordinator alone on Unity; contracts integrated before dependants):** Wave 0 (coordinator) — shared `Relight.Sim` contracts for power, flow, combat, director and opening state, the command/event list and the save-schema v3 fields. Wave 1 — W-A: D-01a exporter (`packages/tools/src/exportUnity.ts`, `npm run map:export-unity -- --region home`, JSON under `Unity/Import/`) + D-02a importer (`Assets/Relight/Editor/World/`) + C-01 Home-region assets; W-B: B-07 (`Sim/Production/`) then B-09 (`Sim/Power/`); W-C: C-02 + C-03 (`Sim/Actor/`, `Sim/Combat/Weapons/`, `Presentation/Engineer/`, `Presentation/Combat/`). Wave 2 — W-A: B-10 flow + C-05 core (`Sim/Production/Flow*`, `Sim/Building/Core*`); W-B: C-04 turret + C-08 director/enemies/debug trigger (`Sim/Combat/`, `Prefabs/Turret`, editor-only trigger); W-C: C-06 Backpack/storage/workshop panels + C-10 front end and settings (`UI/Panels/`, `UI/Menus/`, `Presentation/Persistence` wiring of `AutosaveController.Save/Load`). Wave 3 — W-A: C-09 opening state machine and guide (`Sim/Campaign/Opening*`, `UI/Panels/Guide*`); W-B: C-07 HUD + C-11 light model, previews and `AudioCue` hook; W-C: C-12 regression set and per-state save/load checks. Wave 4 (coordinator alone on Unity) — scene integration, one integrated Play Mode run through the opening with a save/load, screenshots at two window sizes, any U-M-12 tuning recorded in DECISIONS.md §3, handoff; C-ACC remains the owner's. **Owner actions before Phase C starts:** (1) install `6000.6.0f1` (or rule on the target); (2) perform B-ACC items 1–13 as listed under “Owner acceptance checklist” in `Unity/README.md` (item 11 as corrected 2026-09-13) and record the verdict in the B-ACC row; (3) rule on the repository plan in the 2026-09-13 entry above (F8); (4) re-issue or confirm the Phase C brief once B-ACC reads accepted.
- 2026-09-14 — **Phase C implementation authorised by the owner** (explicit instruction superseding the earlier conditional): B-ACC stays `human`, **pending owner inspection and not passed**; the owner deferred the manual B-ACC checks as a prerequisite for starting Phase C. Implementation proceeds on the prepared wave sequence above with the project target kept at `6000.6.0f1`; repository housekeeping stays untouched and nothing is committed under this instruction.
- 2026-09-14 — **Phase C Wave 0 integrated (coordinator Fable); environment corrected.** *Correction to the preparation entry above:* `Unity 6000.6.0f1` **is** installed, at `D:\Unity\Editor\Unity.exe` (Hub-registered); the earlier survey missed that folder. The owner's editor holds `E:\Factorio2\Unity\Relight`, so any batchmode compile/test in this session runs on a robocopy under `%TEMP%\relight-check` with `Library` removed, never on the live project; the MCP editor bridge (`coplay-mcp`) again timed out on connection this session, so no Play Mode observation has been made yet. **Wave 0 (shared `Relight.Sim` contracts, no behaviour change):** `SimComposition` gains fixed Production → Combat → Campaign slots after Inventory, each fanning out to per-task sub-slots (`Sim/Production/SimComposition.Production.cs`, `Sim/Combat/SimComposition.Combat.cs`, `Sim/Campaign/SimComposition.Campaign.cs`) with matching `SimState` visitor hooks (`SimState.Production/Combat/Campaign.cs`) that each own one top-level save object (`power`, `production`, `flow`, `weapons`, `turrets`, `enemies`, `director`, `home`, `opening`, `light`). **Save schema v3** (`SaveSchema.Version`/`SimVersion.SchemaVersion` = 3, `OldestReadable` still 1): `SaveUpgrade.ToCurrent` now chains v1→v2 (the U-M-38 refund defaults) then v2→v3, which fills members the file lacks with a fresh `new SimState()`'s values — recursively through objects, never into arrays, never over a present value — after the file's own checksum is verified as written; the file is never rewritten. Consequence recorded as a rule for Phase C: per-machine state lives in subsystem side tables keyed by machine id, not as new `Machine` fields, so v1/v2 saves upgrade honestly. `Tests/Sim/Persistence/SaveUpgradeTests.cs` reworked: v2 and v1 fixtures are forged from a current document by pruning to the member names the v2 build wrote (golden lists captured 2026-09-14), the fill rule is pinned on a synthetic document, and the future-version refusal is derived from `SaveSchema.Version`. **Verification:** Unity-free `dotnet` compile of `Assets/Relight/Sim/**` + `Tests/Sim/**` (net8.0, C# 9, NUnit 3.14): 216 pass, 0 fail (baseline before Wave 0: 212). Not Unity compilation; no `.meta` files were generated for the six new sources (the editor will create them on import). Wave 1 dispatched next: W-A D-01a + D-02a + C-01, W-B B-07 then B-09, W-C C-02 + C-03.
- 2026-09-14 — **Phase C Wave 1 integrated (coordinator Fable, three Opus workers under exclusive file ownership).** **W-A (D-01a, D-02a, C-01):** a new reference-side exporter `packages/tools/src/exportUnity.ts` with the npm script `"map:export-unity"` writes `Unity/Import/home/city.json` (86,433 B) and four `terrain.*.bin` side-cars of 28,626 B each from the live `packages/sim/src/city/riverfront.ts`; the Unity side is `Editor/World/{CityFile,HomeRegionImporter,ImportedRegionPainter}.cs`, `Sim/World/ImportedGeometry.cs` (`RegionGeometry : ICityGeometry` wrapping `ArrayGeometry`), `World/{WorldGeometryAsset,HomeSitesAsset,ImportedGeometry}.cs` and an optional `geometry` field on `Presentation/WorldBootstrap.cs` that still falls back to `SyntheticMap.Create()`. The Home rect is `78x367 at (43,91)` — 49 buildings, 33 props, 28,626 land tiles of which 24,360 are accessible and 4,266 solid; the solid mask is exported **and** re-derived on import and compared (0 mismatches of 28,626). `city.json` is shaped for `JsonUtility` (no nulls, no nested arrays, `isFixed` not `fixed`). 9 new `ImportedGeometryTests`; the reference-side export gate is `validateRiverfront` + `validateFirstRegion`, both clean. **W-B (B-07, B-09):** `Sim/Power/{PowerState,PowerNetwork,PowerPhase,PowerEvents,PowerQueries,SimComposition.Power}.cs` and `Sim/Production/Machines/{ProductionState,ProductionRules,MachinePhase,ProductionQueries,SetRecipeCommand,SimComposition.Machines}.cs`, ported from `campaignPower.ts:16-19,29,30-34,41-44,46-47,57,61` and `flow.ts:929-978,1045-1057` with `EPS = 1e-9`; `MachineSpec` gains `RecipeStation`, `DefaultRecipe`, `SpeedMul`, `InputBufferMul`, `OutputBufferCap`, `Data/MachineDefinition.cs` five matching `SerializeField`s, and the catalogue was regenerated by running `Tools/export/exportCatalogue.ts`, not hand-edited. The operating-state enum C-07 reads is `MachineOperatingState { Idle, Running, Throttled, Unpowered, OutOfFuel, OutputFull, NoInput }`. **No outage at start** (U-D-12) and a `NetworkKey` struct replaces the reference's string circuit key (S-25). 27 new tests; the run stood at 288 total / 279 passed, the 9 failures being pre-existing Wave-0 tests drifting against W-C's new placement cost. **W-C (C-02, C-03):** `Sim/Actor/Engineer.Equipment.cs`, `Sim/Actor/Mining/{Mining,MiningCommands}.cs`, `Sim/Building/Placement.cs` (charges `MachineSpec.Cost`, refunds the machine on removal, splits `Validity` from `Buildable`, gates on the hand lock) and `Sim/Combat/Weapons/{WeaponState,Ballistics,Equipment,WeaponPhase,WeaponQueries,SimComposition.Weapons}.cs`, ported from `flow.ts:1019,1333-1340,1367-1375,1439-1457`, `worldScene.ts:215,704,711,728,743,810`, `equipment.ts`, `weaponProfiles.ts:13` and `playerBallistics.ts:9-14`; Unity-side `Presentation/WorldInput.cs`, `EngineerView.cs`, `Presentation/Engineer/MiningFeedbackPresenter.cs`, `Presentation/Combat/TracerPresenter.cs` and `Input/RelightControls.inputactions` (`World/Equip` E, `Global/Reload` R, `Global/Slot` 1-9 and 0) were written but not compiled by the worker. 31 new tests. W-C diagnosed the 9 drifted tests and supplied the exact patches (`Tests/Sim/Actor/InventoryFixture.cs` funds the cost, `Tests/Sim/Support/Scenarios.cs` 3 batches / 30 bullets / 4-in-1-out, `ReplayConsistencyTests`, `SharedChecksTests` `Sinks[Steel]` 10 → 16), which the coordinator applied. **Rulings recorded this wave:** removal refunds the machine, not its price; `HandCraft.HandLocked` is not extended to the rifle craft; weapons cannot be stored in chests (deferred beyond Phase C); a processor's output goes to `Machine.Inv[outputItem]`; the 24 worker-authored `.meta` files are kept for Phase C, superseding `contracts.md`'s "no .meta" rule.
- 2026-09-14 — **Phase C Wave 2 integrated (coordinator Fable, three Opus workers).** **W-A (B-10, C-05):** `Sim/Production/Flow/{FlowState,FlowRules,FlowPhase,FlowQueries,FlowCommands,SimComposition.Flow}.cs` (TileTps 20, BeltSpacing 0.25, UndergroundHidden 4, InserterSwing 0.5, SplitterCapacity 8) ported from `flow.ts:755-767,769-810,845-875,880-925,1063-1100,1275-1460`, all 120 lines of `routing.ts` and all 62 of `directConveyor.ts`; `Sim/Campaign/HomeCore/{HomeState,HomeCore,HomeCorePhase,HomeCommands,HomeQueries,SimComposition.HomeCore,EnemyCoreHook.HomeCore}.cs` from `campaignDefence.ts`; `Data/Tuning/DefenceTuningAsset.cs` carrying `DefenceTuning(BarricadeHp 240, WallHp 120, TurretHp 100, CannonHp 140, CoreHp 300, RepairHp 40, RepairSeconds 4, RepairSteel 2, RepairCopper 1, CoreSteel 10, CoreCopper 5, CoreRepairSeconds 12)`; `Presentation/Flow/BeltItemPresenter.cs`; and 24 hand-authored `.meta` files. Flow deliberately runs **after** machines, so an item a processor finishes on tick N can be taken on tick N; conveyors register no power demand at all (U-D-13); `running()` is always true and `beltDeliver`, the Depot's abstract delivery and tram cargo are not ported (U-D-32); `Machine.Out` is retired, the field kept but unread and always 0; `PlaceUndergroundPairCommand` and `CancelRepairCommand` are new. A real port bug was found and fixed: an unpowered inserter still grabbed one item (`if (throttle <= 0) continue;`). 36 own tests, loop 410/410. **W-B (C-04, C-08):** `Sim/Combat/Turrets/**` (5 files), `Sim/Combat/Enemies/**` (8) and `Sim/Combat/Director/**` (8), with Unity-side `Presentation/Combat/{TurretPresenter,EnemyPresenter}.cs` and the editor-only `Editor/Debug/DebugRaidMenu.cs`; `MachineOperatingState.Disabled = 7` added so a knocked-out turret can say why it is silent. The brief's `IThreatLayer` ships as **`IRaidDirector`** with a static `Director` facade, because `IThreatLayer` was already the danger-seconds layer in `Sim/Core/Contracts/Layers.cs`. A BFS distance field replaces the reference's per-body A*, stored as a 141x141 box (79 KB against 2 MB) with identical answers; danger seconds stay in `SecondPhase`, never charged twice; ordinary majors keep the reference `future()` skip (U-D-38) and `Director.Defer` exists for the C-09 opening encounter only (U-D-26, U-Q-21). Combat tick order is fixed at Weapons → Turrets → Enemies → Director with a 0.1-tile aim lead; the turret prefab needs a child named exactly `Cannon` with local +X down the barrel. 21 tests, loop 408/408. **W-C (C-06, C-10):** thirteen engine-free `Sim/UI/**` models (TransferText, WorkshopText, Bindings, BindingProblem, Confirmation, FrontEndText, SaveRow, SaveRowFormatter, SaveCatalogue, ContinuePicker, AutosaveChoices, SessionDirty, NewGameRequest) with `UI/Inventory/**`, `UI/Workshop/**`, `UI/FrontEnd/**`, `UI/Settings/**` and `UI/PauseMenu/**`, ported from `inventoryPanel.ts`, `uiDrag.ts`, `buildPanel.ts`, `controls.ts` and `settingsPanel.ts`; `UI/InputRouter.cs` gained six lines for `Reload` and `Slot` from the Global map and `AutosaveController.LoadAutosave(string fileName)` was added. Seventeen deliberate differences are recorded, among them: no equipment block in the workshop (U-D-11); no `slower`/`faster` bindings (U-D-04); nine rebindable actions with `reload` deliberately absent; `PlayerPrefs` rather than `localStorage`; autosave UI defaults 10 minutes / 5 kept against the engine's 5 / 3; audio buses Master/UI/World/Alerts at 80/100/100/100 %; `ConfirmDialog` at `EscapeOrder.CloseNavMenu` (300); Continue never hidden. Four UXML files were repaired where a comment contained a `--` run (`TitleScreen.uxml:37,49,66,82,92`). 79 tests, loop 432/432.
- 2026-09-14 — **Phase C Wave 3 integrated (coordinator Fable, three Opus workers); the Home opening is feature-complete in the sim.** **W-A (C-09):** `Sim/Campaign/Opening/{OpeningState,OpeningEvents,OpeningRules,OpeningPhase,OpeningQueries,SimComposition.Opening}.cs` ported from `openingEncounter.ts`, `campaignThreat.ts:204` and `goal.ts:68,80-167,151`, with `UI/Guide/{GoalCard.uxml,GoalCard.uss,GoalCardViewModel.cs,GoalCardController.cs,OpeningHint.uxml,OpeningHint.uss,OpeningHintController.cs}` (150 ms refresh throttle, `hud.ts:47`). Deferral as implemented: `Director.Blocked` moves the encounter to `Deferred`, `DeferCount` rises once per entry, `DeferredUntil` is refreshed from `OpeningRules.SafeAt`, and the U-Q-21 cap drops the encounter **without reward** once `TurretQueries.Loaded >= GameData.Opening.TurretObjective` (three loaded turrets) or `OpeningRules.LeftForFirstCamp`. Repelled or lost is decided by the prepared turret, not the core; resupply watches `Stats.TurretFed`; `Director.Reserve` expresses `openingBlocksRaids`; no player-facing string contains the word "magazine" (asserted by test); the camera peek toggles `CameraRig.enabled`, so the camera still never reads a UI inset (D-UI-10). Every new string (`DeferReason`, `CapNotice`, `ReserveReason`, the `opening-attack` row title "Enemy group delayed") is **provisional under U-P-04**. 31 tests, loop 510/510. **W-B (C-07, C-11):** `Sim/Campaign/Light/{LightRules,LightState,LightSources,LightPhase,LightQueries,StreetLights,SimComposition.Light}.cs` and `Sim/Power/PowerLinks.cs`; `Sim/UI/Hud/{HudViewModel,HudNotices,PowerAlertSource}.cs` (placed in `Relight.Sim` so `Relight.Sim.Tests` can reach them — an ownership deviation the coordinator accepted); `UI/Hud/{Hud.uxml,Hud.uss,HudController.cs}`; `Presentation/Light/{LightingPresenter,MouseFlashlightPresenter}.cs`; `Presentation/Building/PlacementPreviewPresenter.cs`; `Presentation/Audio/{AudioCue,AudioCueRouter}.cs` with a 20-row `SimEvent` → cue-key → mixer-group table. `Sim/Power/PowerNetwork.cs` took eight numbered edits, adding `AttachSite`/`OfSite` and placing the street-light demand block before load and throttle per `campaignPower.ts:51`. `LightQueries.LitAt(SimState, …)` is pure and sits in the Combat slot; the `SimContext` overload builds first and is for presentation only; the mask is cached and re-stamped only on a `SimState.Rev` or fold change, and `LightState` saves nothing. The darkness overlay is a code-built texture on `Sprites/Default`, not a URP 2D light. Exactly one power-alert producer exists (defect U-3), and "has ever placed a generator" is derived through `PowerAlertSource.EverHadPower` rather than stored. 33 tests (16 `LightTests` + 17 `HudViewModelTests`), loop 527/527. **W-C (C-12):** `Tests/Sim/Regression/{RegressionFixture,OpeningDefectsTests,OpeningSaveStatesTests}.cs` (18 + 13 checks) and `Tests/Play/OpeningUiPlayTests.cs` (4 `[UnityTest]`, not compiled by the worker), covering UI_AND_ONBOARDING.md §9 rows U-1, U-2 (x3), U-3 (x2), U-4 (x2), U-6 (x3), U-7 (x2), U-8 (x2), U-9 and U-10 plus a 3,000-tick scripted-opening conservation sweep; isolated run 494/494. **U-5 has no sim half at all** and stays unevidenced until the Play Mode checks run. The superseded browser retests for U-2, U-5 and U-6 are recorded in `wave3-WC-superseded.md` as "browser retest not performed", never as passed, for copying into `Unity/Docs` at handoff (UI_AND_ONBOARDING.md §9.1, Q20). **Shared note retracted:** `CommandResult.Problem` is **not** `""` on success — `Sim/Core/Contracts/Command.cs` documents it as "why refused, or a short confirmation text when accepted" (for example "Stack moved.", "Reloading · 2 seconds…"), so acceptance is read from `Accepted` only.
- 2026-09-14 — **Phase C Unity integration by the coordinator: assets, scenes and the cross-worker collisions.** `Input/RelightControls.inputactions` force-reimported; `Relight/Setup/Generate Data Assets` created `Tuning - Defence` and the registry now references it; `Relight/World/Import Home Region` wrote `World/Generated/Home/HomeGeometry.asset`, `World/Generated/Home/HomeSites.asset` and `World/Manual/HomeSitesOverrides.asset`, logging `78x367 at 43,91, 49 buildings, 33 props, 24360/28626 land, map riverfront-arc-v4-editor-ac16d9188c05, source b19f5a64d9e6`; `Relight/World/Paint Imported Region` and `Relight/Validate Game Data` both ran. All 26 machine assets reported as "hand-edited AND changed in the reference export" because `MachineDefinition` gained five serialized fields in Wave 1 — a **false conflict**, resolved with `Relight/Setup/Regenerate Data Assets (discard hand edits)`, after which `git diff` showed only six `source:` strings and the fingerprints changed. `HomeSitesOverrides.asset` was first written with `m_Script: {fileID: 0}` because its class lived in `World/HomeSitesAsset.cs` (file name ≠ class name); the class was moved to `World/HomeSitesOverrides.cs` and the YAML hand-repaired. A cross-worker type collision (`ObjectiveView` ambiguous between `Relight.Presentation` and `Relight.Sim`, CS0104) was resolved by renaming `Relight.Presentation.ObjectiveView` to `ObjectiveTrackingView`. The composite shell root `UI/GameUI.uxml` was created (templating `StatusPanel.uxml` and `InventoryPanel.uxml`, then the HUD, goal card and opening hint); all 25 element names resolve. Scene wiring: **GameUI** gains `InventoryPanelController`, `WorkshopPanelController`, `FrontEndBootstrap`, `SettingsController`, a `PauseMenu` object at sortingOrder 10, `GoalCardController`, `OpeningHintController` and `HudController`, with `currentMapId = riverfront-arc-v4-editor-ac16d9188c05` and `daySeconds = 1200`; **MainMenu** gains `TitleScreen.uxml`, `SettingsController` and `TitleScreenController` with `worldSceneName = World`; **World** gains a `Lighting` object carrying `LightingPresenter` and `MouseFlashlightPresenter`, with `PlacementPreviewPresenter` and `AudioCueRouter` on `Sim`, and `WorldBootstrap.siteOverrides` bound to `HomeSitesOverrides.asset`. **No AudioMixer asset exists in the project**, so `AudioCueRouter.mixer` is left null and the volume sliders drive nothing; when one is authored its exposed parameters must be exactly `MasterVol, UiVol, WorldVol, AlertsVol` (D-11). Under the coordinator's standing ruling nothing was committed (the full repository state is recorded in the final entry below and in [PHASE_C_HANDOFF.md](PHASE_C_HANDOFF.md) §7).
- 2026-09-14 — **Street lights: the imported Home region carries 7 emitters, not the reference city's 18 (U-M-12 tuning record).** `Unity/Import/home/city.json` `sites.lights` has 7 entries and `scalars.lightKw = 2`, because the exporter emits only the emitters inside the Home import window while `riverfront.ts:341` authors 18 across the whole city. `Data/Generated/Tuning/Tuning - Power.asset` now records `streetLightKw` **2** (unchanged) and `streetLightRadiusTiles` **7** (the reference's `constants.ts:58 STREETLIGHT_RADIUS`, D-B5-4). No code depends on the count; the lit mask is computed per tile from whatever emitters exist. **Documentation corrected 2026-09-14:** `WORLD_AND_ASSETS.md` §2.8, §4.5 and §6.2 now distinguish the reference's city-wide 18 from the Home import's 7, and the §2.4 content-count row keeps its whole-city 18 with the import scope named beside it. Build: 2026-09-14. Retest: not retested at runtime in Phase C (the integrated run never reached night); owed at C-ACC. Recorded in [DECISIONS.md](DECISIONS.md) §3 as **U-P-10** under the U-M-12 tuning rule; `Tuning - Power.asset` has no change-note field, so the §3 row is the record.
- 2026-09-14 — **C-13 opened and dispatched as Wave 4 W-D: in-world building feedback.** Verification found that the C-02 placement subset rotated only the ghost and surfaced command refusals nowhere the player could see them. C-13 adds a ghost preview driven from the live `WorldInput` hand, right-click pick-up of a placed machine through `RemoveMachineCommand`, `R` to rotate a machine already on the ground through `RotatePlacementCommand`, and a new `CommandResultEvent` that routes every refusal to the HUD notice strip. Status, counts and evidence: accepted 2026-09-14 (coordinator review): `CommandResultEventTests` 4/4 and `HudViewModelTests` 22/22 in EditMode; `WorldInputFeedbackTests` 3/3 in PlayMode (ghost preview, right-click pick-up, `R` rotate).
- 2026-09-14 — **Phase C Wave 4 verification (coordinator Fable, Unity 6000.6.0f1 with the owner's editor open, driven through the Unity CLI 1.0.0-beta.8).** **EditMode: 527 / 527 passed** (`Relight.Sim.Tests`) at the start of the wave, and the Unity-free `dotnet test` sim loop agreed at 527/527; the final EditMode run after the integrated fixes below is **543 / 543** (6.09 s), including the new `OpeningSupplyTests` (2), `HomeDepotTests` (4) and `PlacementSolidTests` (1). **PlayMode full suite:** first run 41 total / 28 passed / 9 failed / 4 skipped — 3 of the skips are the R2/R7 prototype scene absent by design and 1 is a long timing form; after the fixes below a run reported 41 total / 32 passed / 5 failed; the final rerun result is 44 run / 40 passed / 0 failed / 4 skipped (07:00 UTC 2026-09-14; the skips are the 50 s timing form and three R7-Tilemap tests, by design; the `DiskRoundTrip` Phase1/2 and `EvidenceCapture` forms are `[Explicit]` and were not run). The Phase B Play tests were adapted where Phase C changed the surface: `AutosaveSessionTests.NearlyDue`, the `WorldSceneTests` walk-here check, the `SessionChangeTests`/`WorldSceneTests` placements, and the U6 card greying its button. **Four production fixes were made during verification, in the shipped code:** `UI/Hud/Hud.uss` `#status-strip` was stretching and is now content-width (defect U-5); `UI/Guide/GoalCard.uss` takes a 96 px top offset because the goal card overlapped the status panel; `Presentation/CameraRig.cs` re-snaps to the engineer when `SimHost.Session` changes; and `UI/StatusPanelController.cs` walk-here is now a relative six-tile step (`walkStepTiles`), which orphans the old absolute `walkTarget` scene value (harmless, but it should be removed when the scene is next edited). An earlier PlayMode rerun was aborted by the editor ("Playmode tests were aborted because the player was stopped") and restarted.  **C-01–C-12, B-07, B-09 and B-10 are `done` on this evidence, several with recorded limitations** (see the Phase C table); C-ACC stays `human` and unrun, and B-ACC stays `human`, deferred by the owner rather than passed. **Interface and production fixes from the same pass:** `WorkshopPanelController` now force-paints on the first frame the workshop is shown, so the drawer can no longer open on a stale core card (the 150 ms throttle alone let `OpeningUiPlayTests.U6` see a disabled repair button; the reference does `adapter.enter()` before `sync()`, `uiShell.ts:63-70`); the status panel became a right-side drawer at `top: 96px`, closed at start (`UiShell.tabPanel`, scene `GameUI.unity` saved); `StatusPanelController` scopes its queries to `#status-panel` because the HUD's own `clock` label was being found first and froze the panel clock at 00:00; `.notice-row` and `.alert-strip` take `max-width: 100%` so a long director notice wraps; and `#objective-text` gained a translucent plate because accent text over the tan Depot sprite was unreadable. All verified live at 1600x900 and 960x540 (`fix-*.png`). **The integrated Play Mode run** was a fresh game on the imported region: the chain built, the Rifle crafted and equipped, a turret placed and loaded, the opening encounter scheduled, `slot-phasec-int` saved and loaded back over the active attack with no duplicate wave, the MainMenu -> New Game flow verified, and the three-turret step and automatic resupply verified after the `Machine.Out` fix, with two debug raids. Captures and the two saves' provenance are in [`evidence/phase-c/integrated-run/`](evidence/phase-c/integrated-run/) — `raid2-1600x900.png`, `raid2-960x540.png` and six `fix-*.png` interface captures; the saves `slot-phasec-int.json` and `slot-phasec-int-end.json` are copied beside them (originals in the owner's `saves/exploration-v2/` folder). **Root cause found in the run:** automatic resupply never registered because `Machine.Out` is never written and the opening read that retired field; fixed in `OpeningRules.OutputBuffer`/`ProducedThisTick` and `OpeningPhase.NoteProduction` with buffered `OpeningQueries`, **no save-schema change** (the buffer is derivable), covered by `OpeningSupplyTests` (2). Two further rulings from the run: `Placement.GeometryProblem` refuses a solid city tile ("a city structure is there") per the reference (`PlacementSolidTests`, 1); and `HomeCoreInitializer` places one 6x6 Depot inside the authored core at new game (`Sim/Campaign/HomeCore/HomeDepot.cs`), while `HandCraft.NearDepot` is also true within reach of the authored Home rect, so a save made before the Depot existed still crafts at Home (`HomeDepotTests`, 4). The Play scene tests were made map-agnostic for the imported region (`SceneFixture.FreeTile`/`Last`; the chest view is checked by id because the Depot draws through the `PrefabRegistry` fallback, the Chest placeholder). **Owner-facing findings, not fixed:** a turret placed inside the workshop walls cannot see out and never fires, and the introductory attack then disables the core (observed in the run; the fix candidates are an objective hint to place it outside the walls, or sight through Home walls); skitters down a close-range engineer in about 10 s; after a repair the objective chain can re-surface an earlier step's problem text ("Start your extraction line - output full") when the chest output is full or the generator is empty; the debug raid needs `st.Director.DebugAllowed` and refuses an origin within 28 tiles of the engineer; and the run used debug material grants (120 Steel / 60 Copper / 10 Coal, then 100 Steel / 40 Copper, then +30 Copper) which are not player-facing. **Verification limitation:** PlayMode `run_tests --filter` returns 0 tests through the CLI, so only the unfiltered PlayMode run (about 25 s) is usable; EditMode filters need the full fixture name. **No git commits were made during Phase C** (coordinator ruling): the whole tree is uncommitted, including `Unity/Relight/Packages/manifest.json` and `packages-lock.json`, the untracked `.claude/skills/`, the untracked `Unity/Relight/Assets/Relight/Sim/UI/Hud/`, and a git-ignored leftover `Unity/Relight/Assets/InitTestScene….unity` from the test runner (safe to delete). The owner handoff for this phase is **[PHASE_C_HANDOFF.md](PHASE_C_HANDOFF.md)**.

- 2026-09-14 — **Home opening repair plan proposed at the owner's request.** The live audit found offscreen/collapsed UI, absent Build selection, disconnected Home/storage interactions and missing production controls despite historical subsystem completion. Added proposed C-R01–07 with scope in [HOME_OPENING_REPAIR_PLAN.md](HOME_OPENING_REPAIR_PLAN.md). Planning only; no gameplay implementation, phase completion or human acceptance claimed. The Phaser-baseline visual direction remains the recommendation pending the owner's preference.
- 2026-09-14 — **Correction pass after the owner's Phase C playtest (coordinator Fable, three Opus workers under exclusive ownership, coordinator alone on Unity).** CP-01–CP-07 done: click-to-move removed with its empty-hand fallback, Tab → Backpack, B → new Build menu, left click = the hand's action, presses over UI never reach the world, R contextual, Escape 600/700/800, live control list in Settings; Build drawer, action bar and placement toolbar; HUD redesign on one token sheet with the objective stated once (goal card, real material sources, no grants); shared 300 ms tooltips; the full 864×576 riverfront city exported, imported (counts equal the source: 479 buildings, 370 props, 345,482/412,634 accessible land, 72 site records), painted at runtime (no serialised tilemap: `World.unity` 8.77 MB → 38 KB) and drawn procedurally (3,901 renderers; SVG art unavailable); save schema v4 with region relocation so Phase C saves load on the full map. Verified from Boot → MainMenu → New Game at 1920×1080 and 1280×720 (seven captures in `evidence/correction-pass/`); EditMode 563/563, PlayMode 55 passed / 0 failed / 5 skipped. D-01b done, D-02b/D-08/D-09 partial, C-R02/C-R03/C-R05 partly covered — each row says what remains. Rulings U-M-39/U-M-40. Not committed; C-ACC still `human`.

- 2026-09-14 — **Owner-authorised UI/interaction repair by Codex.** The owner accepted the correction-review order, explicitly required Tab for inventory and authorised UI improvements. Fixed live region propagation and unsafe unknown-region loading; unified global R/number handling; connected contextual E, storage, recipe/filter/priority controls and underground pairing; revised Backpack layout and imported 60 original reference UI icons. Evidence and limits: [UI_REPAIR_HANDOFF_2026-09-14.md](UI_REPAIR_HANDOFF_2026-09-14.md). Verification: compile passed, EditMode 564/564, PlayMode 60 passed / 0 failed / 5 skipped; Backpack inspected at 1080p and 720p. C-R02–05 are advanced in part; no whole C-R row, ordinary opening acceptance or human gate is claimed complete.

- 2026-09-14 — **Comprehensive UI/UX pass, Codex, owner authorised.** C-UX implemented; C-R02–05 descriptions reconciled with delivered controls/UI. Persistent dock, reachable material sources, world target/mining feedback, Workshop focus and compact layouts, live tooltips, warning priority and readable menu fields. Focused tests: Mining 8/8, Transfer 6/6, HUD 22/22; normal-stock generator reached through queued input; prepared storage/craft/weapon/save checks and 1080p/720p/125% visual inspection. Full suites, native OS mouse, standalone Windows and complete raid/excursion acceptance were not run. [Evidence and limitations](UI_UX_PASS_2026-09-14.md). No commit or human acceptance claimed.

- 2026-09-15 — **C-ARTUI implemented, Codex, owner-approved reference.** Painted equipment-case UI kit and fourteen item illustrations, compact dock/drawers, live recipe equations and material/power meters, target-adjacent mining feedback. Compile and 12 objective tests passed; actual 1080p/720p/125% inspection and bounded input/transfer/craft/build checks. Documentation scripts blocked by missing `tsx`; native mouse, standalone and C-ACC remain pending. [Report and screenshots](UI_INDUSTRIAL_PASS_2026-09-15.md). No commit or human acceptance claimed.

- 2026-09-15 — **C-FLOWPOWER implemented, Codex, owner gameplay corrections.** Restored one-item extractor output through its conserved/saved inventory; adjacent belts pull it. Connected all item icons, interpolated conveyor sprites, enforced gaps at belt seams, showed single travel chevrons and persistent power cables, extended pole previews, and shared explicit power feedback in hover/inspection. Machine mining no longer claims stock reached Backpack. Compile, 87 focused tests, actual 1080p/720p Play Mode checks and inter-tick motion sampling passed. [Evidence and limitations](FLOW_POWER_FIXES_2026-09-15.md). C-ACC remains human.

- 2026-09-15 — **C-ADMIN implemented, Codex, owner request.** F8 / HUD admin drawer adds validated resources, packed structures and weapons, nearby enemies and Home raids, recovery/refill tools, pause and transient god/freeze/lighting overrides. Persistent edits made while paused mark unsaved progress; grants remain conserved. Compile, 15 admin tests, 7 session-dirty tests, actual keyboard/UI button checks and 1080p/720p visual checks passed. [Controls, evidence and limits](ADMIN_MENU_2026-09-15.md). C-ACC remains human.

- 2026-09-15 — **C-ACTIVITY implemented, Codex, owner request.** Added travelling voltage highlights, state-driven mechanical motion and shaped working/full/no-power/fuel badges, generator exhaust and conveyor treads/jam cues. The owner explicitly retained electricity-independent conveyors. Shared pooled rendering, no gameplay/save changes. Compile and 19 live motion/state/pause assertions passed; actual 1080p/720p views inspected. [Evidence and limits](ACTIVITY_ANIMATIONS_2026-09-15.md). C-ACC remains human.

- 2026-09-15 — **C-INVENTORYFIX implemented, Codex, owner report.** Added empty typed machine slots and chest cells, capacities and accepted-item hints; corrected drag overlay coordinates, layout clamping, cancellation and output-only refusal; enabled packed-structure hotbar drops and moved inventories above settings for 720p. Existing pooled storage and real transfer rules retained. Compile, 6 TransferTests, full synthetic UI pointer gestures, final 720p visible-slot/drop checks and conservation passed. [Evidence and limitations](INVENTORY_FIXES_2026-09-15.md). C-ACC remains human.

- 2026-09-15 — **C-OREOPEN implemented, Codex, owner-approved recipe review and deposit relocation.** New Game mines ore, smelts at Home or in Foundries, uses cheaper bootstrap buildings and clearly separated eastern-yard deposits. Schema 5 preserves old-save layout/balance and in-flight value. Final sim suite 596/596; paid no-grant line, live UI pointers, legacy adoption and visual inspection verified. See ORE_OPENING_2026-09-15.md; human pacing and standalone acceptance remain open.

- **2026-09-15 — C-SCENEEDIT done (engineering):** owner-authorised editable Scene world, tile footprint compilation, sparse terrain painting, original map parity, move/Undo/save/reopen, original/authored save selection. 9 authoring tests, 596 simulation tests and 13 live Play checks passed. Temporary verification objects removed. [Handoff](SCENE_EDITING_2026-09-15.md). C-ACC remains human.

- 2026-09-15 — **Gameplay fun audit written (Fable, analysis only).** Normal-stake New Game run 0–2336 s with no grants, three source workers and a `/deep-research` run (21 sources, 14 surviving findings) behind [GAMEPLAY_FUN_AUDIT.md](GAMEPLAY_FUN_AUDIT.md) and the reusable [GAMEPLAY_AUDIT_WORKFLOW.md](GAMEPLAY_AUDIT_WORKFLOW.md); evidence in evidence/gameplay-audit/. Confirmed live: the major assault never commits on the shipped map, minor raids arrive unwarned, the opening ends without a destination. No code, decision, save or status changed; proposals labelled. Ran alongside the C-OREOPEN/C-SCENEEDIT sessions. C-ACC remains human.

- **2026-09-15 — C-NODEART done (engineering):** owner-approved painted resource nodes imported: six types, three variations, three depletion stages (54 sprites). Current world shows 105 tiles; actual remaining amounts drive partial/sparse/cleared art and save adoption. SceneSite selection/movement retained. Compile, 18 authoring/art tests, 11 live mining/save assertions and Unity visual inspection passed. [Handoff](RESOURCE_NODE_SPRITES_2026-09-15.md). C-ACC remains human.

- **2026-09-15 — C-BUILDTIPS done (engineering):** owner-requested Build hover descriptions added for all 25 cards, with production outputs derived from supported recipes and extraction/electricity summaries. Costs and power remain visible. Compile, all-card synthetic hover/leave checks and 1080p/720p tooltip layout checks passed. [Handoff](BUILD_TOOLTIPS_2026-09-15.md). C-ACC remains human.

- **2026-09-15 — GP-W5 done (engineering; owner acceptance pending):** combat and losses were connected to the configured data and made recoverable (**U-D-48**). The invulnerable barrier was fixed at its SOURCE: `Sim/Data/CombatBalance.cs`, a new port-owned layer applied after `OpeningBalance` in both data paths, gives every buildable machine an integrity row (`min(120, 20 + 2 × costItems)`) — so `MachineSpec.Hp` is no longer 0 for everything but wall/barricade/turret/cannon, `TurretRules.IsDefence` is no longer false for a Supply chest, and `DirectorRules.HostileOpen` and `RaidField.Open` became correct with **no edit of their own**. Walk-through kinds (belt, fast belt, pole, track, tram) deliberately get NO integrity, so a raid can never stall itself chewing the cheapest thing on the map. The same layer brings the approved Breaker forward from CONTENT_CATALOGUE §7.4 (220 hp, 15 damage, 2 s interval, 2.7 tiles/s) — a roster addition, not an invention. Twelve hard-coded values in `EnemyPhase` became table reads: perception 1.2 → `raids.NoticeTiles` (8), contact and structure damage through one rule `def.Damage × StructureMul` where `StructureMul = StructureDps / ContactDps = 1.6` (which reproduces the exported skitter 8 dps exactly and gives the Breaker §7.2's ×3 per bite without a second mechanic), chase speed `def.SpeedTilesPerS`, march speed scaled off the skitter baseline, and contact reach `def.RangeTiles`. `TickCombatActor` gained a readable leash: a camp resident commits until IT is `GuardPursuitTiles` (36) from its OWN camp, breaks off **still remembering the engineer**, walks home, and turns round only inside `GuardReengageTiles` (22) — replacing a circle drawn round the camp, measured against where the player stood, that wiped the body's memory mid-stride and made retreating free. `SiegePlan` gained `WaveBreakers`: none in the first assault, then 5% of the roster from the second assault's third wave, taken OUT of the skitters so the announced head count still arrives, and placed at the FRONT of each carrying wave because a Breaker marches at 1.0 tiles/s against a skitter's 2.0. For the second assault's 69 bodies that is waves of **10 / 15 / 19 / 25** carrying **0 / 6 / 7 / 10** spitters and **0 / 0 / 1 / 2** Breakers — ~267 turret rounds against the first assault's 207. A wreck now stays on the map at 0 hp and stops working EVERYWHERE at once — power throttle and network, outage reporting, flow routing, operating state, the shared walk grid and line of sight all read the same ruin — and `HomeQueries.MachineRepairCard`/`MachineRepairLine` with `HomeCore.RepairProblem` finally SHOW that it is repairable, at the machine, quoting the price (2 Steel + 1 Copper restores 40 hp in 4 s), how many instalments a full repair takes and the sim's own refusal for a greyed button. `Sim/Actor/DeathCache.cs` drops the Backpack contents into a pile on the tile the engineer fell on which never despawns and is never culled (a backpack can hold an Alien Key, a schematic or an artifact — progression must not be destroyed by a death); weapon keys are retained; two deaths on one tile merge into one pile; `Ledger` counts the pile where it stands, so nothing is a source and nothing is a sink. `SaveSchema.Version` **8 → 9** adds `director.major.waveBreakers` and a top-level `drops`, stamping a version-8 in-flight assault with zero Breakers so an assault begun without heavies finishes without them. Checks: **615 passed / 0 failed / 1 skipped** of 616 sim tests (`dotnet test` over `Sim/**` + `Tests/Sim/**`, out of the editor), including 9 new focused tests; `Relight.Sim` and the Unity-side assemblies compile clean. Known limitation carried forward from GP-W4: until the owner runs **Relight ▸ Data ▸ Generate tuning assets** there is no `Tuning - Siege.asset` on disk, `DataValidator` reports one missing reference and the sim falls back to `SiegeTuning.Fallback` — the same numbers, so gameplay is correct either way. No Unity Play Mode run has been performed and the owner has not playtested or accepted this row.

- **2026-09-15 — GP-W4 done (engineering; owner acceptance pending):** the major assault is now a PLANNED, SAVED sequence of waves (**U-D-47**). `Sim/Combat/Director/SiegePlan.cs` decides the whole encounter once, when it is scheduled — wave sizes by cumulative rounding of a growth weight so the parts always sum to the exported roster, spitters held back and then distributed by the same rounding so the exported split survives, one wave start per gap, and an approach count that grows wave by wave and is clamped to the approaches the map actually stages. `MajorRaid` carries that plan (`Wave`, `WaveSpawned`, `WaveAnnounced`, `WaveCount`, `WaveSpitters`, `WaveStart`, `WaveApproaches`, `WaveOffset`), so `DirectorPhase.Spawn` became a pure reader of it: a wave can neither duplicate itself nor vanish across a save. Each later wave is announced once, as it opens, naming the quarters it is actually using; the pre-assault warning now names the FIRST wave's quarter plus the wave count and length, so the player is told the shape rather than a coordinate. `Director.Defer` slides the whole plan (`SiegePlan.Shift`) instead of restating a window, and `DirectorRules.Birth` takes an explicit kind so the roster comes from the plan rather than the legacy every-third-body rule. `RaidTuning.WindowS` no longer describes an assault's length anywhere (`OpeningRules.SafeAt` included). Shipped defaults — 4 waves, 110 s apart, a 40 s spread inside each wave and a 30 s tail, spitters from wave 2, one more approach per wave, escalation 15% per assault SURVIVED capped at 2× — give waves of **9 / 12 / 18 / 21** carrying **0 / 5 / 7 / 8** spitters over **400 s**. They are derived, not guessed: the roster is 1800 hp, which is exactly 180 gun-turret rounds (no overkill waste), which is 18 `bullet-batch` = 36 Steel + 18 Copper + 108 s of one Assembler — and three loaded turrets hold only 150, so the siege outlasts loaded hoppers and forces one mid-siege delivery. Spitters reach 7.0 against the turret's 9.0, so they do not outrange turrets; four concentrated on one turret kill it in 5 s, which is why the pressure lever is the growing approach count rather than a longer clock. New Unity-owned tuning (`SiegeTuning`, `SiegeTuningAsset`, `GameDataRegistry.siege`, `DataAssetGenerator`) keeps every parameter editable through the existing data pipeline; until the owner runs **Relight ▸ Data ▸ Generate tuning assets** the registry reports one missing reference and the sim falls back to `SiegeTuning.Fallback`, which is the same numbers. `SaveSchema.Version` 7 → 8: a version-7 in-flight assault is stamped as the single wave it was already sending, because a fresh state has `major: null` and `FillMissing` therefore has no template to default a plan from. Checks: `dotnet build` clean on the sim and Unity-side scratch projects; **606 passed / 0 failed / 1 skipped** of 607 sim tests, including 7 new ones; and the real Home export plans 9/12/18/21 with the approach count honestly clamped to the two staging-valid approaches that region has. No Unity Play Mode run has been performed and the owner has not playtested or accepted this work.
- **2026-09-15 — GP-W3 done (engineering; owner acceptance pending):** raid scheduling, warnings and turret placement feedback repaired under **U-D-45** and **U-D-46**. A minor raid is now announced before it exists: `MinorRaid` carries `StartsAt`, `Owed`, `Spawned` and the `Heading` its bodies will actually arrive on, and it is only staged once `DirectorRules.Staging` has found open ground — a raid that cannot be staged is retried for 30 s and dropped rather than announced against an invalid coordinate. `DirectorRules.Origin`, `Approaches` and `Staging` were rewritten onto one reachability and clearance definition (they previously disagreed, which is why majors announced staging that never arrived), the approach scan box was widened from the core's ±30 tiles to the full 60-step walk, and authored `SiteKind.Camp` sites now join the map's candidate approaches instead of replacing them. Every HUD notice is written through `Director.Say(text, untilT)` and cleared by `Director.Expire`, so a stale "assault announced" cannot outlive its encounter. Tuning is the Unity-owned `SiegeTuning` — **30 s + 0..15 s warning, 30 s staging retry, 45 s notice hold** — held outside the exported `RaidTuning` so the next catalogue export cannot drop it. `SaveSchema.Version` 6 → 7: a version-6 save whose raid was already on the map upgrades to a `Spawned` wave instead of being announced a second time (writing that test found and fixed a genuine migration refusal — `FillMissing` cannot supply `director.minor.heading`, because a fresh director has no minor to copy a default from). Line of fire was made consistent: `Sim/Combat/Sightline.cs` holds the one rule (a wall or barricade stops a shot, both endpoints' own tiles exempt) over a `WallMask` keyed on `SimState.Rev`, and `TurretPhase`, `EnemyQueries.Nearest` and `Ballistics` all use it — previously only the engineer's rifle respected player-built walls. `TurretSight.Coverage` sweeps that same rule over 48 bearings, and the placement preview now draws the ground a turret can actually cover inside a faint nominal ring, with an amber blind/restricted advisory in the build toolbar and matching wording in the opening objective. Checks: **599 passed, 0 failed, 1 skipped** (`dotnet test` over `Sim/**` + `Tests/Sim/**`, out of the editor); `Relight.Sim` and `Relight.Game` both compile clean; staging, warning and arrival verified end to end against the shipped Home export in a scratch console harness. No Unity Play Mode run and no owner playtest has been performed for this row.
- **2026-09-15 — GP-W1 and GP-W2 done (engineering; owner acceptance pending):** the owner's gameplay improvement brief opened a new pass (§ Gameplay improvement pass). **U-D-44** records the owner's decision that the Home workshop processes queued jobs while the player is elsewhere, at its current slow speeds, superseding U-D-10. `HandState` gained a persistent job queue with stable ids, a `Reserved` ingredient bag and a physical `Output` tray; `HandCraft.HandLocked` narrowed to a running Home core repair; the rifle craft runs unattended into the same tray; a full tray pauses a finished batch without consuming or repeating it; cancellation refunds every unprocessed ingredient, Backpack first; `Ledger` counts reservations and the tray under `LedgerPlace.Machines`; `SaveSchema.Version` 5 → 6 with a generic fill step plus a lazy idempotent normalise so pre-U-D-44 saves and their craft queues migrate without duplication or loss. The UI Toolkit workshop panel gained queue, progress, output-tray and Collect controls and the "keeps working while you are away" wording. Checks: the sim suite runs headlessly out of Unity (`dotnet test` over `Sim/**` + `Tests/Sim/**`) — **592 passed, 0 failed, 1 skipped**; `Relight.Sim`, `Relight.Game` and the test assembly all compile clean out of the editor. Documentation amended: DECISIONS.md (U-D-44 added, U-D-10 superseded, U-M-37 F1 context), UI_AND_ONBOARDING.md §4/§6/§9, CONTENT_CATALOGUE.md, GAME_DESIGN.md, and the B-07/C-06 rows above. No Unity Play Mode run and no owner playtest has been performed for this row.

- **2026-09-16 — GP-UX-1/2/3 done (engineering; owner acceptance pending):** three owner UI/UX reports fixed under **U-D-49**. (1) A drawer is a place the engineer is standing, not a mode — any bound movement key now closes the open panel and the walk starts on that same press, read through the player’s own rebindable `World/Move` bindings (`UI/MovementKeys.cs`) rather than a WASD mirror that would diverge the moment a key was rebound, and exempt while paused, while dragging and while a text field has focus. (2) The Home workshop’s output tray — the one producer with a button instead of an inventory — is now a 20-slot grid beside the Backpack and the store, chunked by exactly the rule `HandCraft.TrayUsed` already used so it can never disagree with what the workshop calls full; one stack drags or clicks out through `CollectWorkshopCommand(item, count)`, drops back in are refused, and `Collect` becomes "Collect all". It is a third grid rather than the store because `HandState.Output` has no machine id. A real selection bug surfaced and was fixed: `PaintCell` decided "selected" by pack-or-not-pack, so a third grid made tray slot *n* light up with store slot *n*. (3) The drag ghost no longer clamps the 300 px card into the drawer’s bounds — which moved the icon up to a card’s width off the cursor exactly where the storage column and the tray sit — but anchors the icon centre on the pointer and flips the card left when it will not fit right. **Checks:** 617/618 sim tests pass (1 long-run skip) including 2 new tray tests; a scratch csproj compiling `Sim/**`, `Data/**`, `World/**`, `Presentation/**`, `UI/**` and `Tests/Play/**` from source against the installed editor’s managed assemblies builds clean (3 pre-existing CS0252 warnings on untouched lines), so the 9 new Play-mode tests are known to compile. **Unity Play Mode is not available on this machine: none of the Play-mode tests have been run, no screenshot or live pointer check was taken, and the owner has not playtested or accepted this work.** Changes remain uncommitted.

- **2026-09-16 — GP-UX-4 done (engineering; owner acceptance pending):** a fourth owner UI report — *“The inventory for every producer, can you improve the UI? I have to scroll down to see the inventory slots. The UI can take up more of the screen if needed”* — fixed under **U-D-50**. **Root cause, two independent geometry faults, neither in C#.** `UI/Styles/industrial.uss` imports LAST of the drawer’s five sheets, and it pinned `.inventory-scroll` to `height: 144px` — exactly two rows at the theme’s 69 px slot pitch, whatever the drawer’s height, leaving ~400 px of the drawer unused at 1080p — and gave the drawer 520 px, 800 px with a store open, which is ~365 px of grid a side, five columns, so the forty Backpack slots were eight rows shown two at a time. On top of that the grids sat INSIDE the body `ScrollView` underneath the workshop section and the machine controls, so opening a processor with a recipe put its own slots below the fold: the literal report. **The fix is structural, vertical and horizontal.** `.inventory-pair` is now a direct child of `#inventory-panel` and the growing one; `#backpack-body` is capped at 42% and carries only the machine controls and the collapsed workshop foldout; `.inventory-scroll` is `flex-grow: 1; height: auto; max-height: none` with a one-row floor; the drawer is 900 px alone and 96% capped at 1560 px with a producer open (860 px / 1240 px under `compact-ui`, whose slots drop to 56 px so a narrow window gains columns instead of losing them); and the secondary blocks — side headings, machine status and progress, recipe/filter/priority, the item detail, the equipment strip — were re-laid out as wrapping rows. **No C# was touched:** every element is fetched by name in `InventoryPanelController.Bind`, so the re-parenting is invisible to the controller. **By computation from the shipped values:** at 1920×1080 with a producer open the drawer is 1560 px, each side ~759 px, 10–11 columns — the forty Backpack slots are 4 rows and a producer’s own slots 1 row, neither scrolling, against 2 rows of 8 before; at 1280×720 the band is only 520 px tall so the Backpack still scrolls, by about one row (3 of 4 rows at 10 columns, against 2 of 8 at 5 columns). **Checks:** 617/618 sim tests still pass (1 long-run skip; no sim code changed); the scratch csproj compiling `Sim/**`, `Data/**`, `World/**`, `Presentation/**`, `UI/**` and `Tests/Play/**` against the installed editor’s managed assemblies builds clean with the same 3 pre-existing CS0252 warnings, so the 4 new Play-mode tests in `Tests/Play/Ui/InventoryLayoutTests.cs` are known to compile; both UXML files parse as well-formed XML and every element name the tests look up is present. Two stale class doc comments in `Tests/Play/Ui/{UiFixture,InventoryDragPlayTests}.cs` that still claimed the Play tests had never been compiled were corrected. **Unity Play Mode is not available on this machine: the layout tests have NOT been run, no screenshot or live measurement was taken at 1080p, 720p or 125%, and the owner has not playtested or accepted this work.** Changes remain uncommitted.
- **2026-09-16 — GP-UX-5 done (engineering; owner acceptance pending):** the owner rejected what GP-UX-4 shipped — *“That’s not what I wanted. I want the players inventory to be on the left and the producer to be on the right. The Producers UI needs to be redone a little to have the slots at the top. Do you know what I mean?”* — and the drawer was rebuilt under **U-D-51**, which amends U-D-50(a). **Checked before assuming, and the check changed the fix:** the Backpack was ALREADY the left column and the producer the right (`InventoryPanel.uxml` orders `#side-pack` before `#side-store`; no `row-reverse` anywhere), so the first sentence described what the owner expected to see rather than asking for a flip. The actual fault was the third thing on the screen: GP-UX-4 had lifted the slot grids out of the scrolling body but LEFT that body there, under both columns, still carrying every producer's controls and the Home workshop as a collapsed foldout — so a producer's UI was split between the right column and a strip beneath it, and the workshop was not a producer at all. The owner was offered the two candidate shapes as options and chose “Producer column owns its controls” and “Right column, like any producer”. **What changed.** `#backpack-body`, `#workshop-foldout` and the `workshop-view` mode are gone; `#inventory-panel` is a heading row, `.inventory-pair` and the pinned footer. The left column is purely the player (Backpack heading, grid, equipment strip, quickbar) and always wears its own name. The right column IS the open producer: `#store-inventory` — heading, capacity, slot grid, hint — and then `#store-body` with the status, the progress bar, repair, recipe / item filter / output priority and the description, plus the workshop section. The Home workshop is now a producer like any other: its 20 tray slots first, the three recipe cards under them (`WorkshopPanel.uxml` reordered, `.workshop-heading` un-hidden and the section's own frame dropped, since the column is its frame), reached from the drawer heading's **Home workshop** button, which hides while the workshop is what the column is holding. Where the column must be divided the slots win — `store-machine` caps the controls at 46% (52% under `compact-ui`) — except at Home, where the depot's storage AND the workshop are open at once and the cap comes off so the two halves divide the column instead of one being crushed. `storage-open` now means “the right column has a producer in it”, so a machine with no storage at all (a belt: controls only) still gets the wide drawer. **Drawer widths, slot pitch and the `compact-ui` rules are unchanged from GP-UX-4.** **C# changes are small and structural:** `SetWorkshopView` is retired, `Bind` picks up `#store-inventory`, `Paint` decides the column's contents, and `OpenStore` resets `#store-body`'s scroll offset so a producer just opened starts at its slots. **By computation from the shipped values, not observed:** at 1920×1080 with a producer open the drawer is 1560 px, each side about 758 px, about 744 px of grid after the 14 px scroller → 10 columns at the 69 px pitch; the forty Backpack slots are 4 rows and a machine's slots 1 row; the pair is about 720 px tall, so a machine's controls get about 331 px against the roughly 250 px they need, and the workshop case gets the whole 720 px. **Checks:** 617/618 sim tests still pass (1 long-run skip; no sim code changed); the scratch csproj compiling `Sim/**`, `Data/**`, `World/**`, `Presentation/**`, `UI/**` and `Tests/Play/**` against the installed editor's managed assemblies builds clean with the same 3 pre-existing CS0252 warnings, so the Play-mode tests — `TheProducerColumnOwnsItsControls` (replacing GP-UX-4's test of the old shape) and the new `TheWorkshopTrayIsAboveItsRecipeCards`, alongside the three kept from GP-UX-4 — are known to compile; both UXML files parse as well-formed XML, every element name the controllers and tests look up is present, and `backpack-body` / `workshop-foldout` / `workshop-view` survive nowhere but in explanatory comments. **Unity Play Mode is not available on this machine: the layout tests have NOT been run, no screenshot or live measurement was taken at 1080p, 720p or 125%, and the owner has not playtested or accepted this work.** Changes remain uncommitted.

- **2026-09-16 — GP-UX-6 done (engineering; owner acceptance pending):** a third report in the drawer series, and the first that accepts the shape it corrects — *“Can you make it so the inventory on the producer is fixed at the top and only the recipes are scrollable? Make the recipes fit 2 horizontally per row instead of 1.”* — implemented as **U-D-52**, which amends U-D-51(c). **What was actually wrong.** U-D-51 put the producer’s slots at the top and its controls in a `#store-body` scroller beneath them, but the slots themselves still had `flex-grow: 1`, so the column divided by a 46% cap on the controls rather than by the slots taking what they need; and because `#store-body` was ONE scroller, at Home the workshop’s 20-slot output tray scrolled together with its recipe cards — the tray is slots, and slots were supposed to have stopped scrolling. **What changed.** `#store-inventory` is `flex-grow: 0` and its grid (`#store-scroll`) sits between a one-row 72 px floor and a 300 px ceiling, so a forty-slot chest still cannot push the controls off the bottom; `#store-body` takes everything left and is a plain `VisualElement` now, with each producer bringing the scroller its own contents need — a new `#machine-scroll` around a machine’s controls, and `#workshop-cards` itself a `ScrollView` so the workshop’s tray stays fixed with the other slots. The `store-machine` class and both caps (46%, 52% compact) are gone. Cards are two to a row: the row and wrap moved onto the scroller’s content container and a `.recipe-host` is `flex-basis: 50%` / `flex-grow: 0` with its 8 px gutter as PADDING, because Yoga measures `flex-basis` border-box and a margin would have wrapped the second card away; `min-width: 180px` is the one-per-row fallback. The new USS is written as **id** rules in `InventoryPanel.uss` so it wins over `industrial.uss` by specificity instead of by import order. **By computation from the shipped values, not observed:** at 1920×1080 the right column is about 758 px wide, the cards’ scroller about 730 px across, a host about 365 px and a card about 357 px against a 232 px design width; vertically the column is about 720 px, a one-row producer grid takes about 130 px and `#store-body` about 590 px, so a machine’s roughly 250 px of controls never scroll and the workshop’s cards keep roughly 360 px under the tray. **Checks:** 617/618 sim tests unchanged (1 long-run skip; no sim code touched); the scratch csproj compiling `Sim/**`, `Data/**`, `World/**`, `Presentation/**`, `UI/**` and `Tests/Play/**` against the installed editor’s managed assemblies rebuilds clean with the same 3 pre-existing CS0252 warnings, so the two new Play-mode tests — `AProducersSlotsAreFixedAtTheTopOfItsColumn` and `TheRecipeCardsSitTwoToARow` — are known to compile; both UXML files parse as well-formed XML and all three stylesheets balance. **Unity Play Mode is not available on this machine: no layout test has been RUN, no screenshot or live measurement was taken at 1080p, 720p or 125%, and the owner has not playtested or accepted this work.** Changes remain uncommitted.
- **2026-09-16 — GP-UX-6 rule (d): equal-height recipe cards (engineering; owner acceptance pending):** a fourth report, arriving after the rule (c) work was reported — *“The recipe cards need to have the same height each. At the moment they’re all different”* — added to **U-D-52** as rule (d) rather than opened as a decision of its own, because it corrects what rule (c) did: two to a row is what made ragged heights visible, a short card now sitting BESIDE a tall one instead of above it. **What was actually wrong.** `industrial.uss` imports last and held `.recipe-card { height: auto }` — a live-inspection fix from when a card was a full-width row of its own and stretching one meant stretching it down the whole column — which defeated `WorkshopPanel.uss`’s `height: 100%` and left every card sized to its own text. **What changed.** The card now fills its host with `flex-grow: 1` in both sheets (not a percentage height: the host has no height of its own, it takes the row’s, so a percentage would resolve against a parent still being measured), and `.recipe-host` carries `min-height: 152px` in both. The two mechanisms do different jobs: stretch within a wrapped row squares the pair off against each other, and only the floor squares one row off against the next, because `align-content` is `flex-start` and wrapped rows are measured independently. The card’s bottom margin is gone, so the host’s padding is the only gutter. **By arithmetic on the shipped values, not observed:** 16 px padding + 2 px border + ~21 px title + 58 px ingredient row + 9 px bar + ~18 px for a one-line note ≈ 124 px of card, plus ~15 px for a second wrapped note line, plus the host’s 8 px bottom gutter ≈ 147 px, rounded up to 152; `.compact-ui` trades 4 px of padding for a taller title, so it needs no figure of its own. A recipe that outgrows the floor takes its own row taller and is a failure, not a silent return to ragged: the new `TheRecipeCardsAreAllTheSameHeight` asserts that every card fills its host and that every host matches the first, and reports the height the floor would have to clear. **Checks:** 617/618 sim tests unchanged (1 long-run skip; no sim code touched); the scratch csproj rebuilds clean with the same 3 pre-existing CS0252 warnings, so the new test is known to compile; all three stylesheets still balance their braces and comments. **Unity Play Mode is not available on this machine: the test has NOT been run, no screenshot or live measurement was taken at any resolution, the 152 px is arithmetic rather than a measured tallest card, and the owner has not playtested or accepted this work.** Changes remain uncommitted.

- **2026-09-16 — GP-OPEN-1 done (engineering; owner acceptance pending):** the owner asked what the opening objective ladder got wrong (*“OK which ones need fixing?”*), was given four ranked fixes, and authorised them verbatim — *“Give the foundry no default. Do the rest”*. **U-D-53.** (a) The Foundry, Assembler and Assembler Mk2 now ship with an empty `DefaultRecipe`, overridden in `OpeningBalance.Apply` because both data paths run through it, so the generated catalogue and the editor `.asset` files are covered without either being touched; ladder rows 10 and 28 were completing themselves the moment the machine was placed, since their guards test the recipe and the data already supplied one. (b) `opening-ammo` split into a build row and a “Set up the Assembler / Select the Bullets recipe” row, because with no default “built but unset” is a state the player can sit in and the old row would have kept saying “Build 1 Assembler” to someone who had built one. (c) `OpeningPhase.NoteSupply` now also latches “Automatic resupply working” off a turret with no room whose supplier still resolves: both write sites of `Stats.TurretFed` require hopper room, so a player keeping the turret full by hand had a working belt and a permanently stalled chain; the walk stays gated on `ProducedAt >= 0` and on `OpeningRules.TurretSupplier`, so a hand load cannot fake it, and the cheap test runs first. (d) the unreachable `Words[0] = “no”` deleted. **621 passed / 0 failed / 1 skipped of 622** (four new tests; ten existing production, power and persistence tests given an explicit recipe through a new `ProductionFixture.Recipe` helper, since they test the production loop rather than the catalogue), engine-side rebuild clean apart from the 3 pre-existing CS0252 warnings. **Play Mode is not available on this machine, so nothing here has been seen running and no screenshot was taken.** Two things the owner should know: an existing v9 save whose Foundry or Assembler was running on the data default loads that machine **idle** until a recipe is chosen — nothing recorded what it was making, and `SaveUpgrade` cannot repair a v9→v9 load without a schema bump that was not authorised; and the **Refinery, Mixer and Alien workbench keep their defaults**, which the opening never names but which leaves the port inconsistent about whether a placed machine starts with a recipe.
- **2026-09-16 — GP-PLAYTEST-1 done (engineering; owner acceptance pending):** the owner asked for a simulated first thirty minutes (*“do a playtest of the first 30 minutes. simulate a new player experience and tell me any errors, worrying spots, anything missing and improvements that can be made”*), read the ranked report, and authorised the fixes — *“OK lets fix these”*. **U-D-54. Seven of the eight shipped; two of the report’s recommendations were corrected on their premises, one reshaped and one withdrawn.** The run is a headless objective playthrough driven through sim commands; it establishes that these defects exist and nothing about balance or normal progression. **What the run actually showed, with the evidence:** it ended `Home core … hp 0/300 DISABLED` beside `opening encounter: Repelled shots 0` — a turret that never fired, and a “win” printed over a dead core; `chest #4 … inv ironore 200` (exactly `ChestCap`) beside `foundry #10 … state OutputFull`, the shared-chest deadlock, with the chain still calling both belts connected; the literal HUD line “hold fire without power. no power. Poles connect…”; “Fuel your Generator” recurring at 02:08, 13:45 and 22:49 with the coal receding 15 → 33 → 28 tiles; and “Mine Coal salvage 33 tiles south-east”, which Coal is not. **One recommendation in the published report was wrong and is corrected here.** It asked for the attack direction to be named when the turret is built. `DirectorQueries.OpeningOrigin(ctx, st, turretId)` requires a placed turret and scores `DirectorRules.Origin` + `Approaches` by `Clearance`, picking the origin whose breach path passes NEAREST the turret — the direction is derived FROM where the player put the turret, so it cannot be named earlier, and the log shows it already named once the group is announced (“about 5 basic enemies from the south marker”). The real gap is the case the director cannot fix: the nearest of a poor set can still be far out of range, and the only way to discover that was to watch the group walk past a silent turret. The new `Uncovered` helper reuses the same `Clearance` and says so on the Scheduled and Active rows. **A second recommendation was withdrawn on its premise.** The report said Shift did nothing on a walk-here. It does not — but `WorldInput` submits no `MoveCommand` anywhere, because C-02/R-CP-3 deleted the empty-hand walk-here on purpose (its doc comment: “if one reappears, click-to-move is back”). The finding came out of the harness, which drives `MoveCommand` directly; no player can reach that path. The one-line widening was written and then backed out rather than shipped, because it would have changed sim behaviour on a deliberately retired route for tests and headless runs alone. **Both corrections are the same lesson the owner has already taught once — check the premise before fixing it — and both were caught by reading the code the report was describing.** **A judgment call stated plainly:** the fuel treadmill was answered with teaching text and honest arithmetic — one Coal ≈ 13 s at the full 300 kW, the slot holds 50, and an Excavator on a Coal patch belted into the Generator ends hand-feeding for good — rather than by raising what one hand load buys. **No balance change, no save-schema change, and no new blocking row**: the chest-deadlock row fires only once the chest is half full of ore or the Foundry already reports `OutputFull`, so a working base is never nagged. **Checks:** **629 passed / 0 failed / 1 skipped of 630**, up from 621/622 by the eight tests in the new `Tests/Sim/Regression/PlaytestFixTests.cs`; the U9 core-repair test was **not** loosened — it guards a real contract, so the production text was written back to contain its phrase, and only three tile-wording assertions in `ObjectiveSourceTests.cs` were updated; the engine-side scratch csproj rebuilds (`-t:Rebuild`, for an honest count) with the same 3 pre-existing CS0252 warnings. **Unity Play Mode is not available on this machine: none of this has been seen running, no screenshot was taken, and the owner has not playtested or accepted it.** Changes remain uncommitted.

- **2026-09-17 — GP-UX-7 done (engineering; owner acceptance pending):** four unrelated bullets from the owner, and the pass is a good argument for reading before fixing. The plate-production objective was not failing to notice a working base — it was refusing one, because `ReachesKind` hopped a single belt and the owner’s buffer chest answered “chest” where the guard wanted “foundry”; the walk now passes through `BufferKind`, the one kind `FlowRules.Wants` accepts unconditionally and `FlowPhase.SourceCount` drains. Making it transitive immediately endangered U-D-54’s shared-chest deadlock row — every chest downstream of an Excavator is now “reached” from it — so that row was narrowed to a direct shared sink (`DirectEnds`/`SharedSink`) and now weighs the ore in that chest rather than the fullest chest on the map. “No power” was being drawn twice: `HudViewModel` posted an infinite-TTL notice on every 150 ms refresh and `HudNotices.Post` counts a repeat whenever the row it replaces is still live, so the toast ticked its own counter ~6.7×/s next to a strip that already said it; the post is gone and the key is cleared. The second half is the more interesting half — the strip the toast was duplicating had been grouped with `.notice-row` in `industrial.uss`, which repainted its ground to #202920 and left Hud.uss’s `color: var(--ui-bg)` (#151d1e) sitting on it: ≈1.2:1, so the owner had never actually seen it, which is exactly why the toast read as the only message. It now owns a deep-red ground with light bold text. Tab showing the Home workshop was a sentinel collision: `UiShell.ToggleBackpack` and `WorldInput.Interact` both pass machine `-1`, so the id could not say which route the drawer arrived by — the caller says it now (`OpenWorkshop()` beside `OpenStore(id)`), and `GameplayDock`’s Backpack tooltip stopped promising a workshop that Tab no longer opens. The build cards took the tested U-D-52 rule (d) host/card pattern, with the two numbers measured out of `Inter-Regular.ttf` because Play Mode cannot be run here: the worst cost string is 458px at 16px, which wrapped to a fourth line in the 156px column beside the icon and fits three in the ≥174px column the restacked card gives it. One trap came with the host and was caught before finishing — a host holds its 144px floor whether or not its card is on the open tab, so `PaintCard` had to hide both, and the new Play test asserts every shown host still contains a visible card. **Nothing in this row has been seen running.**
- **2026-09-17 — GP-UX-8 done (engineering; owner acceptance pending):** two reports, and both were fixed by removing code rather than adding it. Pressing E with the rifle out *“does something weird. I can't inspect things”*: `Interact` asked what the HAND held before it asked what the player was pointing at, and the weapon branch returned early — so E opened the machine under the cursor only when the pointer was already on it, never opened the Home workshop the HUD advertises, printed no notice at all, and silently toggled the rifle in and out of equipment slot 0. The unequip half is what made it *weird*: the rifle stayed in hand and on the dock while `WeaponPhase` began refusing every shot with “Equip a Rifle”. The reference binds E to interaction and nothing else, so the equip role had no decision behind it and is gone; the settings label now says **Interact**, while the input action keeps its asset name `Equip` so no player loses a saved rebinding. Second: *“when the turret fires… it looks like the player shoots”* — the turret’s own muzzle was recorded correctly by the sim, but `TracerPresenter` re-anchored the NEWEST tracer to the engineer and the shot list is shared with every turret, so a turret’s shot was drawn leaving the player. Every tracer now starts where the sim put it, which also stops the player’s own tracer sliding along with them for its half second. No `Sim/**` file changed. Engine-side rebuild: 0 errors, the 3 pre-existing CS0252 warnings. Play Mode is unavailable here, so the new Play test compiles and has not been run and nothing has been seen running. See DECISIONS.md U-D-56.
- **2026-09-17 — GP-UX-9 done (engineering; owner acceptance pending):** the sixth drawer report, and the clearest case yet for checking the premise before fixing it. Two of the three asks already existed — Sort in three places, splitting as a button, a menu entry and a sim command — so the work was to put them where a player looks and to give splitting a destination the player chooses. The third ask, the one that read as a gameplay bug, was not in the sim at all: a probe run against the real handlers accepted a move from slot 0 to slot 20 in a Backpack with no recorded layout and put the stack in slot 20, which is the state a new game is in. The drawer was losing the drop. `#pack-grid` is a wrapping grid inside a ScrollView, so `panel.Pick` on a release a few pixels off a slot returns the grid, the gutter or the viewport — not a slot — and `Drop` did `Say(""); return;`. A null `Engineer.Pack` is displayed auto-compacted, so every stack already sits at the front and a lost drop is indistinguishable from an unwanted sort. Fixed by resolving the picked element FIRST and falling back to the nearest slot in the grid the pointer is inside (grid as bound, clipped to the scroll viewport, so no cross-column snapping and no snapping to a slot that is scrolled out of sight), and by saying `DropOffSlot` when the release really was off the grids. Sort gained a copy on the Backpack header, always enabled; the footer copy and `menu-sort` stay. Shift-drag splits into the hovered empty slot for the Quantity field's amount or half, previewing the sim's own refusal on the ghost before release; the Split BUTTON keeps first-empty, because a button has no destination to read. Two sim regressions now cover the never-arranged state and a chosen split slot — 635 passed / 0 failed / 1 skipped of 636 — and three Play tests cover a gutter release, a Shift-drag split and a release off the grids; those compile but cannot be run here.

- **2026-09-19 — First-attack origin fixed (engineering; owner acceptance pending):** the owner asked *“Can you look at the first initial spawn and make sure it comes from the right area?”*, read the assessment (the introductory group was born at (62,347) inside Founders Court because the raid field's 70-tile box never reached the row-432 raid line and `Origin` accepted a tile with no tier), and authorised the fix — *“Do it. Use gizmos where you can”*. **U-D-RAID-01, court D54.** `RaidField.Reach` 88 from every edge of the core rect, `Origin` skips non-entry tiles, Verify C20, `RaidOriginTests` (3/3), full `Relight.Sim.Tests` 644/644, Verify 20/20, debug-raid births now at (67,432) on the south ring road, 71 steps, heading S. New `Relight/Gizmos/Raid Director` Scene gizmo. Report and captures: `FOUNDERS-COURT-SPAWN-FIX.md`.
- **2026-09-19 — Founders Court substation assessed; streetlight proposal accepted (design; implementation pending):** the owner asked *“What does the Substation do in the starting area? It's a part of the tutorial but does nothing”*. Finding: in the port the authored `substation:0` site is a solid 3×3 lot and not a power node; streetlights join the nearest pole; the core has no demand; objective 8 passes on a supplied pole within about 9.5 tiles and changes nothing, while its detail text describes the reference power model. The owner proposed *“Maybe it could power some light poles in the cul de sac?”* and accepted the worked proposal (*“Yeah thats perfect”*): substation sites become reach-8 nodes, each streetlight is fed by its nearest substation site, objective 8 tests the real circuit, text rewritten; a reach rule cannot do it because the six court lights are 10–32 tiles from the site. **Court D55.** Record: `FOUNDERS-COURT-SUBSTATION.md`. No code changed.
- **2026-09-19 — Founders Court substation implemented (engineering; owner acceptance pending):** the owner asked *“Implement the substation plan”*. **Court D55** as recorded in `FOUNDERS-COURT-SUBSTATION.md` §7. Authored substation sites are reach-8 power nodes that placed nodes cable to and through. Consumers never join a site. Each streetlight is billed to its nearest substation's circuit and stays dark until that substation is reached. Objective 8 now tests `OfSite(substation).Supply > 0`, and its text drops the core-draw and "no power" claims. The preview and persistent cables draw to the lot. Founders Court Verify gains C21. Checks: scratch `dotnet test` of `Relight.Sim.Tests` 648 passed, 0 failed, 2 skipped (seven D55 tests), and a scratch compile of Sim/World/Data/Presentation/Editor against the 6000.5.8f1 DLLs succeeded. Later the same day, in the owner's 6000.6.0f1 editor through the Unity CLI: compile clean, EditMode `Relight.Sim.Tests` 650 passed and 0 failed, Founders Court Verify 21 of 21 passed (C21 included). Not run: the Play Mode night check. No light was added on the cul-de-sac circle (scene edit pending). Evidence: `evidence/substation-2026-09-19/checks.md`.
- **2026-09-19 — Always dark decided and specified (design; nothing implemented):** the owner reported *“the darkness aspect of this game has disappeared and that makes the lighting irrelevant”*. Findings in the port: the night curve is a V that is at least half dark for 133 s of each 1200 s day (302 s in the reference) and first darkens about 16½ minutes into a session; the Home core lot is always lit; nothing in `Sim/Combat` reads `LightQueries.LitAt`, so light has no effect on aliens although GAME_DESIGN.md §8.3 said light avoidance was implemented. A forced-night Play Mode capture through the Unity CLI confirmed the overlay itself works. The owner chose permanent darkness over a day and night cycle (**U-D-58**) and approved [ALWAYS_DARK_SPEC.md](ALWAYS_DARK_SPEC.md). Rows L-01, L-02, L-03 and L-ACC opened. C-11's note that the integrated run never reached night is closed by L-01's Play Mode check when it runs.
- **2026-09-20 — L-01 stage 1 implemented (engineering; owner acceptance pending):** no sun (`DaylightSeconds` 0), brownouts shrink lights but not the always-lit Home core lot, a twilight overlay (provisional 0.55, brightness setting clamped 0.35–0.70), alien silhouettes drawn above the darkness, an in-light HUD cue, an elapsed-time HUD clock, and the substation objective's text no longer waiting for night (Court D55's line dropped "at night"). Checks: full EditMode `Relight.Sim.Tests` 664 passed, 0 failed; Founders Court Verify 21 passed, 0 failed of twenty-one checks; PlayMode `WorldSceneTests` 3 passed, 0 failed. A Play Mode capture took four screenshots (spawn, spawned-alien silhouettes, darkest brightness setting, and a power cable over dark ground); no HUD clock or light-state text was visible in any of them and the captured view read as a resumed save near the Works Yard rather than a fresh spawn at the core lot — noted as an open concern, not fixed here. One test's added assertion was changed from `Does.EndWith(...)` to `Does.Contain(...)` on the controller's ruling, because `OpeningQueries.Explain` appends a what-to-get-next sentence after the substation line whenever a material row is short, which the fixture's empty starting inventory always does. Not done: the owner has not yet tuned the overlay strength by eye, and the opening has not been played by a person from spawn to the first Excavator. Evidence: `evidence/always-dark-stage-1/checks.md`.
