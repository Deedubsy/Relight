# Phase 9 — city scope review

Reviewed 2026-09-08 under the owner's “Ok lets continue with P9” (D-EX-46). **P9-00 review complete; implementation starts with P9-01.** [PROGRESS](PROGRESS.md) owns task status. This extracts city generation, fairness and wayfinding from wider EX-09/RI-11; full project progression, mystery reveals, heavy equipment and endgame remain dependent on Q07 and their own acceptance.

The current request permits independent city work before completion of the outstanding shared playtest. It does not report that play occurred or waive any human criterion. EX-08D remains a valid frozen post-P8 checkpoint on port 5180; its guide, starts and blank record remain unchanged. Existing human rows stay available, and the original ammo retest remains unresolved.

## Reuse and remaining gaps

| Existing source/evidence | Credit and Phase 9 boundary |
|---|---|
| `packages/sim/src/city/generate.ts`, `city/spec.ts` | Street-first river geometry, irregular lots, facilities, seeded rejection sampling and an explicit invalid-map refusal already exist. `createCampaign` uses the river preset. Other presets remain legacy regression coverage; this review does not expose five new campaign modes. Existing generator validation runs before the campaign's composed sites/reservations. Its legacy 10,000-seed result does not validate the current campaign. |
| `city/urban.ts`, `ground.ts`, [city rebuild report](CITY_REBUILD_REPORT.md) | Retain saved urban infill, one shared solid mask for movement/build/rendering, named local anchors, protected factory pads and rail reservations. Ground/resource identity and old-save layout are valuable compatibility contracts. The historical report's human and performance gaps remain; do not rebuild its art or claim Phase 9 complete from that integration. |
| `city/opening.ts`, `campaign.ts`, `expansion.ts` | A 24-tile starter frame with one four-tile gate, local resources, station/radio placement and the first surveyed route already exist. Test the composed opening after all later systems initialize, including an able on-foot approach to interactions and ordinary build eligibility along the actual survey. Reachability of a tile alone is insufficient for a 2×3 truck or a powered machine footprint. |
| `campaignDistricts.ts`, `districtValidation.ts` | Three distinct persistent steel/copper/coal sources, later station, repair workshop and connected route exist. Structural save checks are useful but do not substitute for a finished-map geometry/fairness report. Validate source access, footprint, service space and legal route/stop construction without moving stock or bypassing restoration. Preserve finite home salvage and current production rules. |
| `campaignDiscovery.ts`, `campaignRecruits.ts`, `campaignTurbine.ts`, `campaignGuide.ts` | Stable saved discoveries, five recruit kinds including Foreman, optional Turbine and knowledge-gated site information already exist. Test the full set together for reservation collisions and reachable interaction positions. The existing 32-seed shelter test specifically checks the first recruit; it is not evidence that every crew/site on 10,000 seeds is valid. Do not turn optional discoveries into mandatory progression gates. |
| `campaignReliability.test.ts`, `truckWork.ts` and P8-05 evidence | Threat staging and truck footprint/routing have dedicated checks, including holdouts and a 16-seed query sweep. Reuse them in representative campaign probes; neither a walking BFS nor a valid base graph proves truck construction access or every attack approach. Static validation must not promise that every arbitrary player layout is serviceable. |
| `cityMapScene.ts`, `worldScene.ts`, current guide | Existing place labels, map/world navigation, Depot beacon, known-site markers and Surveyors district information are implemented. There is no general player rename/pin command in the reviewed campaign interfaces. Retained C.1, 7.2, 7.3/C.5 map to saved naming and wayfinding work; preserve earned Surveyors information and avoid revealing unvisited rewards or unreceived attack targets. |

## Implementation acceptance

These are deliverable definitions; only PROGRESS records execution status. Technical limits are to be documented with implementation evidence, not attributed to the owner as approved balance.

**P9-01 — composed campaign city validator and evidence runner.** Provide a reusable read-only report over the actual initialized campaign. Check home entrance/collision and accessible starter resources/factory space; station/radio/workshop/Turbine/recruit/discovery footprints and interaction approaches; distinct source regions; connected, non-overlapping and ordinarily buildable tram survey/stop pads; retained urban access; relevant base threat approaches. Clearly distinguish structural geometry, engineer reach, truck service queries, supplied scenario evidence and human fairness. Return stable failure categories, seed/profile/config identity and reproducible measurements. Never repair a save, award materials, reroll silently or mutate the queried state. Begin with seeds 3/4/5 and holdouts 8/11/13 plus a declared small discovery sweep; retain failing seeds as evidence. Check deterministic repeat/load results and malformed fixtures. This is the first implementation increment, not the full 10,000-seed run.

**P9-02 — generator/reservation reliability.** Address failures established by P9-01, using the existing city algorithm and shared collision/placement rules. Apply complete footprint reservations and valid approach/service space where needed, with deterministic bounded selection or an explicit generation failure. Preserve the actual saved layout and surveyed route of existing campaigns. If new-generation geometry changes, version/fingerprint it deliberately and mark old logs' replay compatibility honestly. Exercise affected seeds with ordinary paid restoration, track/stop construction, freight and representative truck delivery; no teleport/inventory shortcut may establish opening feasibility. If the validator finds no defect in a proposed area, record that rather than inventing a geometry rewrite.

**P9-03 — saved names and wayfinding.** Complete the retained naming and map orientation subset: deterministic readable place/district labels with bounded player overrides, player pins with selection/removal and an off-screen distance cue, and explicit visited/known presentation. Reuse existing map/world and discovery state. Surveyors retain their earned district-information role; annotation does not reveal hidden sites/resources or future attacks. Store player-authored state through validated ordinary commands with stable IDs, save migration and replay. Show the same chosen label in relevant map/world/inspection/navigation feedback, with a recoverable default and no identity collision when renamed. Define bounded text/pin storage, validate imports/saves and treat text as text. Verify keyboard focus, map/world transitions, UI at 1366/900 widths and no gameplay mutation by camera-only actions. This adds navigation aids within current discovery rules, not a new radar/resource-scanning mechanic or mystery catalogue.

**P9-04 — declared campaign seed-fairness evidence.** Run the composed current-code validator over **10,000 declared campaign seeds**, retaining every attempted seed, failures, generator retries, timing and version/config provenance. Make the runner resumable without mixing sources or dropping failed seeds. Separate cold/warm timing and query-only geometry from played logistics. Use representative ordinary-command probes on seeds 3/4/5 and holdouts 8/11/13, plus all distinct failure classes found; do not tune only to seed 3. Report resource access, route/interaction constraints and distributions under Version 2 rather than legacy compact-front ratios. Keep old E-variance and old presets' evidence untouched. A partial run remains partial; no extrapolation or smaller sweep counts as 10,000.

**P9-05 — engineering readiness and refreshed human preparation.** Review each increment against the source/build actually being handed off; run required full code/profile checks and meaningful focused/browser/save/performance checks. Preserve EX-08D/5180 and prepare a separate post-P9 frozen checkpoint/guide/blank record if the source changes. Include a declared ten-seed human review route and coverage, observations of recognisable places and meaningful route choices, naming/pin usability, construction/freight access, and remaining P5–P8/ammo/performance observations. Link prior owner success without duplicating or inventing it. This is preparation and technical readiness, not a human phase verdict.

**P9-H — human ten-seed city review and Phase 9 verdict.** Retain the ten-seed review obligation, recording actual seeds, assistance, comparable tasks, places distinguished, route choices, confusion, obstruction and performance. Existing shared observations may count only where they actually cover the criterion on an identified build. A seed walkthrough is not automatically a complete campaign or a new phase pass. Record gaps and the owner's explicit verdict separately.

## Scope and retained obligations

The approved campaign uses fixed home/station neighbourhoods, specialised sources, earned local services and optional discovery. P9 tests and presents that city. It does not choose campaign length, Relight kit, final defence, new bosses, loot, additional mandatory projects or the internal Rot explanation. Those remain EX-09/Q07 or later phases. The city portion of RI-11 is extracted; RI-11 itself is not completed.

The 10,000-seed automated obligation and human ten-seed review remain intact under [PHASES](PHASES.md). Legacy frontage/enclosure ratios and old hour targets are not new acceptance metrics. C.7 visual differentiation credits existing urban roles and current source districts; finished silhouettes/art/audio stay Phase 12. Reference-machine performance certification remains Phase 11, and measured stalls remain open. No claim of human fairness follows from successful path queries.

## Fresh review verification

**8/8 targeted baseline tests passed:** six selected campaign cases (64.52 seconds) cover cul-de-sac access, specialised sources, first-recruit shelter access over 32 seeds, base threat origins over 32 seeds, station access and a paid freight expedition; two retained urban cases (6.66 seconds) cover saved geometry/resource/placement invariants and invalid-generation/retry handling. [Campaign log](evidence/phase9-review-2026-09-08/focused.log), [urban log](evidence/phase9-review-2026-09-08/urban.log). Tests use their existing declared fixtures; the threat-origin fixtures isolate base state and are not ordinary travel or survival observations. No new 10,000-seed campaign run or human session occurred.

The integrity check finds 192/193 source/test/build inputs byte-identical to EX-08D/P8-05. The remaining `packages/sim/fixtures/lattice/power4.json` has an unrelated formatting-only working-tree change; parsed JSON is exactly equal to the frozen fixture. This task did not write or restore it. [Exact hashes and comparison](evidence/phase9-review-2026-09-08/working-tree-difference.json). Gameplay source and compiled files are unchanged. [Integrity and reference check](evidence/phase9-review-2026-09-08/check.py), [manifest](evidence/phase9-review-2026-09-08/manifest.json), [consistency log](evidence/phase9-review-2026-09-08/consistency.log), [documentation sync](evidence/phase9-review-2026-09-08/docsync.log), [freshness](evidence/phase9-review-2026-09-08/freshness.log). The EX-08D report, guide, blank record and archived evidence remain unchanged. This review edits documentation only; no new full suite, build, browser measurement, commit or push is claimed.
