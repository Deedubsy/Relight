# Relight — decisions

**Owner implementation direction (2026-09-09):** implement EX-09C-01–09 plus RI-10/RI-12 combat/encounters now. The active campaign uses defended factory/plant sites, never a territorial frontage/ring or adjacent-block-claim economy. Site lifecycle and explicit truck recovery replace the former EX-09C-04 territory scope. Normal new-game entry is to use exploration-v2; explicit legacy saves/mode remain isolated. Real inventory, connected finite power and 1×/pause remain authoritative. Q07 final requirements and human/release gates stay open, not prerequisites of this authorised correction set.

**Current gameplay authority (2026-09-09):** [RELIGHT_CONFIRMED_GAMEPLAY.md](RELIGHT_CONFIRMED_GAMEPLAY.md) is the owner-designated reference for this audit and subsequent implementation. Where older text disagrees, the confirmed document takes precedence; completed work and historical evidence remain credited under their original scope. See [the pre-Phase-10 audit](Implementation/PRE_PHASE10_GAMEPLAY_AUDIT.md) and PROGRESS for corrective ownership. Unspecified tuning and Q07 endgame choices remain undecided.

Audit provenance: the owner’s attached request explicitly says to use RELIGHT_CONFIRMED_GAMEPLAY.md and correct missing task coverage. This is a precedence clarification, not an invented approval of new numbers. The supplied opening and missing plant lifecycle are superseded by corrected §§3/7/10; no-frontage exploration is explicitly retained. D-UI-12 direct conveyor access and D-UI-13 powered tram service must not be relocked by Gunsmith/Rail crew tasks. Specialist improvements beyond those baseline capabilities need a concrete proposal; Q07 still owns endgame requirements. No prior decision row or human verdict is rewritten as newly approved.

## Current direction — 2026-09-06

The rows below govern Version 2. The owner is the current user in the voice conversation; no additional identity is inferred. Direction approval does not validate numbers or mean implementation exists. Earlier counts and the former outstanding-question list below are historical. D-EX-Q01–06 are adopted through D-EX-11; D-EX-Q07 remains open for later progression. Earlier questions apply only where retained by the new contract.

| id | decision | status | decided by | on | via | effect / current document |
|---|---|---|---|---|---|---|
| D-CITY-03 | Extend the approved CITY-E density approach to the rest of the city before new artwork. | decided | owner (current user) | 2026-09-10 | "Yes I like that. Lets expand it to the rest of the city" | CITY-F district-specific infill/replacement of generic scenery; preserve the approved starting neighbourhood, campaign sites, factories and transport. Placement choices are implementation details; no new progression or reward rules. |
| D-CITY-02 | Focused Founders Court and adjoining street density pass before new artwork; extend Phaser authoring to add background buildings and edit plots. | decided | owner (current user) | 2026-09-10 | Acceptance of the layout review recommendation: "Ok lets go with your recommendation. I'll add the new artwork package after that". | CITY-E; preserve factory yards, transport and campaign bindings. Specific placements are implementation choices; new scenery adds no gameplay rewards. Artwork integration and human visual acceptance remain separate. |
| D-CITY-01 | Fixed authored Riverfront Arc replaces procedural new-game geography; Home → Riverside → Ironworks → Civic tram, physical neighbourhoods/interiors, existing rewards and encounters in intentional parcels, shared obstacle-aware navigation, no frontage. | decided | owner (current user) | 2026-09-09 | Attached “Relight — Authored Riverfront City Rebuild” brief and A — Riverfront Arc reference. | Confirmed Gameplay §3 and Implementation/RIVERFRONT_CITY.md; preserve old save geometry. Focused visual/functional evidence required; no push/deploy and no human or release verdict inferred. |
| D-EX-01 | Free roaming beyond powered territory, organic useful/rare discoveries and distinctive site encounters. | decided | owner (current user) | 2026-09-06 | Voice: "it's more free roaming" and "explore and find things organically"; request for "rare things that the player can find" and "unique ... enemies". | GDD §3/7/8/10/17. Supersedes treating expeditions solely as extending a supply line. Exact reward catalogue remains Q05/Q06. |
| D-EX-02 | House in a defensible cul-de-sac with one early hostile approach; second area opens tram access and the opportunity for radio restoration. | decided | owner (current user) | 2026-09-06 | Voice: "You start in a little cul-de-sac that's easily defendable"; "the tram station becomes unlocked straight away" in discussion of the second area. | GDD §5/6/8/11. Supersedes old opening/enclosure sequence and any inherited requirement that the Heart delay tram access. Exact unlock meaning/costs remain Q04. |
| D-EX-03 | Scheduled major assaults plus major restoration responses; only one major target base at a time. Smaller raids and creatures at ruins provide background danger. | decided | owner (current user) | 2026-09-06 | Voice: "a bit of both"; "Only one base would get attacked at a time"; "little raids"; later "Yes, that is exactly what I am thinking." | GDD §7. Supersedes the timed-horde rejection and continuous per-edge attack demand as the governing threat loop. Shared minor-raid frequency is accepted direction, exact rate/strength Q02. |
| D-EX-04 | Initially two days without major assaults, potentially one later; day/night is a planning rhythm. Minor raids/site encounters remain during those days. | decided | owner (current user) | 2026-09-06 | Voice: "two peaceful days to start off with" and "as you progress, we can go down to one"; agreement to the distinction between minor and major threats. | GDD §4/7/15. Supersedes prohibition on a day/night planning cycle. Exact day duration, first assault, transition and visual/mechanical lighting effects remain Q02. |
| D-EX-05 | Restorable radio infrastructure supplies organic attack warning; coarse-to-precise information is the discussed progression. | decided | owner (current user) | 2026-09-06 | Voice: "a tower or radio tower that will warn you about which base" and "Yes, that is exactly what I am thinking" after the warning progression proposal. | GDD §4/6/7/15. Warning lead, coverage, upgrade costs and outages remain Q03; not an approved free omniscient HUD. |
| D-EX-06 | A central tram supply route with multiple stations, local factory branches and movement of resources between areas. | decided | owner (current user) | 2026-09-06 | Voice: "core of your expansion being up this tram line and then branching out like a tree"; stations with factories taking resources off the tram. | GDD §9/14. Supersedes the two-stop limit as final transport scope. Routing/reservations/vehicles remain Q04; no automatic signals or global inventory. |
| D-EX-07 | Concentrated regional resources and continuing usefulness of earlier factories and bases. | decided | owner (current user) | 2026-09-06 | Voice: "iron doesn't have to be in every district" and "old bases and old factories don't become obsolete". | GDD §12/17. Distribution/longevity Q05. Iron is an example, not a new recipe approval; renewable salvage not decided. |
| D-EX-08 | Restoration provides practical capabilities; automated defence and the player's equipment both contribute. | decided | owner (current user) | 2026-09-06 | Voice: "test what you've restored, like a workshop"; approval of supplies moving up the tram line and the proposed defence rhythm. | GDD §6/8/13/15. Supersedes territory-only rewards and the rifle's rescue-only design constraint. Workshop, damage, equipment and encounter details remain Q06. |
| D-EX-09 | Apply the coherent documentation migration and successor plan before further implementation of the superseded loop. | decided | owner (current user) | 2026-09-06 | Voice: "can you make the changes to the documentation?" | Active GDD, constitution, Version 2 plan, tracker, phases and entry points. Supersedes Version 1 executable direction (D-RI-1); preserves history and reusable code. This request is documentation scope. |
| D-EX-10 | Phase 4 has just finished; forward-built systems do not mean Phase 5 or later phases are complete. | decided | owner (current user) | 2026-09-06 | Voice: "we have just finished phase four". | PROGRAMME_STATE and PHASES. Approximately 40% remains the owner's rough estimate, not a verified revised-scope percentage. |
| D-EX-11 | Adopt all recommended opening contracts and stated initial settings in EXPLORATION_DEFENCE_RECOMMENDATIONS.md; alternatives and unspecified tuning values are not adopted by implication. | decided | owner (current user) | 2026-09-06 | Voice: "Yes, I like all of those recommendations. Let's go with those" after the recommendation package and six-area summary. | GDD §5/7/12/13/14; Q01–06 decided; EX-02 owner decision complete. No code, integration or playtest success claimed. |
| D-EX-12 | Integrate the tested newer baseline and begin tram-based expansion, preserving the adopted design. | decided | owner (current user) | 2026-09-06 | Voice approval to bring in the newer version and start tram-based expansion, followed by "Okay, let's start it". | Local baseline merge and independently reviewable first freight increment EX-06A; no push, full campaign conversion or human gate approval implied. |
| D-EX-13 | Separate the new campaign rules from the legacy simulation, then build the cul-de-sac opening. | decided | owner (current user) | 2026-09-06 | Voice instruction to continue campaign separation, followed by "Then building the cul-de-sac". | EX-03 and the home-opening subset EX-04A; no automatic conversion of old saves, push or completed human play gate implied. |
| D-EX-14 | Continue with second-area tram restoration and the radio opportunity after the completed home opening. | decided | owner (current user) | 2026-09-06 | "Okay, let's move on to that" in response to the second-area station/radio being next, followed by defence work. | EX-04 continuation; implementation defaults remain provisional, and no human phase gate or push is implied. |
| D-EX-15 | Phase 5 has not started. The current opening/defence revision follows completed Phase 4 and precedes Phase 5. | decided | owner (current user) | 2026-09-07 | "Phase 5 hasn’t started yet" after the phase-mapping discussion. | Current phase status supersedes ambiguous "next incomplete" wording and any inference from historical T10. Preserve historical rows and evidence; credit useful work when Phase 5 opens after the revised-loop assessment. |
| D-EX-16 | Continue the revised campaign with the next runnable defence/threat work. | decided | owner (current user) | 2026-09-07 | "Ok let’s continue" after the phase clarification. | EX-05 implementation and verification; costs/HP/roster tuning remain provisional. Does not start Phase 5, waive a human gate or authorise a push. |
| D-EX-17 | Implement the next district supply and workshop increment. | decided | owner (current user) | 2026-09-07 | “OK lets move onto EX-06” after the status report identified its remaining extraction, workshop and resupply scope. | EX-06 implementation and verification. Costs, radii, throughput and the concrete resupply evidence threshold are provisional implementation defaults; no Phase 5 start, human gate, commit, merge or push implied. |
| D-EX-18 | Implement the optional exploration discovery and distinctive workshop encounter. | decided | owner (current user) | 2026-09-07 | “OK lets implement EX-07”. | EX-07 implementation and verification under Q05/06. Guardian tuning, automatic tool fitting and repair speed are provisional task defaults. No Phase 5 start, human gate, commit, merge or push implied. |
| D-EX-19 | Prepare the representative-loop automated evidence and human session. | decided | owner (current user) | 2026-09-07 | “Ok onto the next one” after EX-08A was identified as next. | EX-08A evidence, frozen local build and session preparation. Does not supply human observations, approve the EX-08 gate, start Phase 5 or authorise a commit, merge or push. |

| D-EX-20 | Bring Phase 5 factory implementation ahead of the representative gameplay test. | decided | owner (current user) | 2026-09-07 | “Ok I think we need phase 5 before I can do a proper gameplay test”. | Supersedes the EX-08-before-Phase-5 sequencing in D-EX-15 and the successor plan. Reconcile retained factory obligations first (EX-09A), implement and verify them (EX-09B), refresh the test build (EX-08B), then human EX-08H and assessment EX-08. Human observations and phase approval remain required; this is a sequencing decision, not a completed phase or playtest. Q07 continues to block dependent broader progression in EX-09, not independent factory work. |

| D-EX-21 | Begin Phase 5 with the current-code factory audit and retained-scope reconciliation. | decided | owner (current user) | 2026-09-07 | “OK lets get started” following the explanation that Phase 5 begins by checking existing features and defining remaining factory work. | EX-09A audit and concrete P5 increments in PROGRESS; existing approvals and provisional technical defaults retain their status. Human play/phase exit, commit, merge and push are not implied. |

| D-EX-22 | Continue the reconciled Phase 5 plan with construction commands and controls. | decided | owner (current user) | 2026-09-07 | “Looks good lets continue with that plan” after the EX-09A scope audit and P5-01 handoff. | P5-01 implementation/verification and retained T13 technical review. The 128-tile path, x-then-y skipped-pointer interpolation and 50-group saved history are provisional task defaults. Undo compensates current construction without rewinding gameplay. D-SA-2 human sign-off and later Phase 5/human work remain separate; no commit, merge or push implied. |
| D-EX-23 | Implement the next reconciled factory increment, P5-02 routing primitives. | decided | owner (current user) | 2026-09-07 | “OK onto P5-02”. | Underground pairs, rectangular priority splitters, optional inserter filters and current-recipe chains; P5_02_ROUTING_REPORT.md records implementation and checks. Endpoint/splitter prices, span, buffer and priority defaults are provisional implementation choices, not owner-approved balance. T14 technical evidence does not complete human Phase 5 exit. No commit, merge or push implied. |
| D-EX-24 | Continue with P5-03 factory inspection and item statistics. | decided | owner (current user) | 2026-09-07 | “Ok lets continue with P5-03”. | Read-only machine inspection, circuit feedback, ordinary settings commands and nominal/measured per-item rates. P5_03_INSPECTION_REPORT.md records saved observation semantics and verification. The 60-second/one-second observation window is a provisional technical default; no power-driven brightness change, balance approval, human gate, commit, merge or push is implied. |
| D-EX-25 | Implement P5-04 physical truck and selected-stop map routes. | decided | owner (current user) | 2026-09-07 | “Ok move onto P5-04”; Q08 already selects the restored second-area station. | Local boarding/exit, a street footprint, separate conserved vehicle cargo and saved driving; read-only route/service presentation reuses existing multi-stop freight. P5_04_TRANSPORT_REPORT.md records provisional technical defaults and verification. No fuel/Line truck system, balance approval, human gate, commit, merge or push is implied. |

| D-EX-26 | Implement P5-05 factory blueprint harness, campaign logistics experiments and integration verification. | decided | owner (current user) | 2026-09-07 | “Ok onto P5-05”. | Ordinary paid in-reach construction, separate campaign evidence and declared five-hour engineering workloads; provisional scenario assistance is labelled. P5_05_INTEGRATION_REPORT.md records checks and the reference-machine gap. No balance approval, human phase verdict, commit, merge or push is implied. |

| D-EX-27 | Continue with EX-09B Phase 5 implementation-readiness assessment. | decided | owner (current user) | 2026-09-07 | “Ok continue” after the P5-05 handoff named EX-09B next. | Review P5-01–05/T13–17 and retained RI-09 evidence; PHASE_5_READINESS_REPORT.md records the engineering assessment and limitations. This authorises the review, not owner approval of performance, balance or Phase 5 exit. Human observations, reference-machine validation, commit, merge and push are not implied. |

| D-EX-28 | Review Phase 6 against implemented threats, retained standards and confirmed gameplay; define the remaining increments. | decided | owner (current user) | 2026-09-07 | “OK start the phase 6 review”, after confirming current-build factory, tram/truck and defence/recovery testing was successful. | P6-00 review and P6-01–04/P6-H scope in PHASE_6_SCOPE_REPORT.md; PROGRESS owns order, with shared EX-08B follow-up preparation after threat changes. This prioritises the requested review without retroactively completing EX-08B/H or T18. Existing Q01/02/03/06 apply; D-B4-3 remains legacy-only where current Q06 requires real turret damage. No new enemy, Q07 resolution, tuning approval, phase-pass verdict or implementation of all proposed increments is inferred. |

| D-EX-29 | Implement P6-01 attack approach and lifecycle reliability. | decided | owner (current user) | 2026-09-07 | “Ok lets move to P6-01”. | Same-base/same-direction staging fallback, explicit recoverable deferral, physical withdrawal and saved schedule/roster validation per PHASE_6_SCOPE_REPORT.md. Local fallback distance and retry interval are provisional technical defaults; adopted clock, target locks and rest policy remain unchanged. P6_01_RELIABILITY_REPORT.md records implementation and actual verification. |

| D-EX-31 | Implement and run P6-03 supplied-network defence evidence. | decided | owner (current user) | 2026-09-07 | “Nice! Move onto P6-03”. | Ordinary starting stock, live threats, paid commands and reach; seeds 3/4/5 and holdouts 8/11/13; explicit turret-only/personal-support comparison, costs, failures, supply interruption/recovery, conservation and saved/full replay. PHASE_6_SCOPE_REPORT.md defines acceptance. No new combat tuning, phase verdict or push is implied. |


| D-EX-32 | Proceed with P6-04 engineering readiness review and defer the unreproduced ammunition report to owner retesting. | decided | owner (current user) | 2026-09-07 | “Continue onto P6-04. It might be fixed. I'll test after this”. | P6-AMMO remains unresolved awaiting the next playtest and no longer blocks P6-04. This authorises the review, not a claim that loading was fixed, a human Phase 6 verdict, combat tuning, shared session preparation, commit or push. |

| D-EX-33 | Prepare EX-08B shared follow-up playtest build, starts, guide and blank record. | decided | owner (current user) | 2026-09-07 | “Onto the next one then” after EX-08B was named next. | Freeze reviewed source/build and identify reproducible starting conditions; verify normal light/power interaction and retain known findings. Credit earlier owner play, include ammo retesting and Phase 6 gaps, and keep all human observations and verdicts blank. No gameplay tuning, commit or push implied. |

| D-EX-34 | Move shared playtesting after Phase 7 and begin its discovery scope review. | decided | owner (current user) | 2026-09-07 | “OK lets do the playtest after P7. Lets move onto P7”. | Supersedes EX-08-before-P7 sequencing: P7-00 reconciles the catalogue, adopted P7 implementation follows, EX-08C refreshes preparation, then shared human observations. EX-08B stays completed historical preparation; no human task is passed or waived. Q09 records the proposed catalogue separately; Q07 remains open for full progression/endgame. No commit or push implied. |

| D-EX-35 | Adopt the reviewed P7 catalogue and continue with P7-01. | decided | owner (current user) | 2026-09-07 | “All looks good! Continue on with P7”. | Q09 adopted; P7-01 implements saved recruits and explicit local Electricians recruitment with earned-unlock/save compatibility. Later increments follow PROGRESS. Post-P7 play remains required; no phase verdict, commit or push implied. |
| D-EX-36 | Implement P7-02 concrete production and Barricades. | decided | owner (current user) | 2026-09-08 | “Ok onto P7-02”. | Approved catalogue increment; initial Mixer 20 steel + 10 copper, 3×3, 60 kW, 2 stone → 1 concrete / 2s; Barricade 2 steel + 4 concrete, 240 HP. These are implementation tuning defaults, not a balance verdict. Shared play follows P7. |
| D-EX-39 | Start Phase 8 and move shared playtesting after it. | decided | owner (current user) | 2026-09-08 | “Lets do the next phase then playtest”. | Supersedes D-EX-34 sequencing only: review and implement independent Phase 8 construction tools, prepare EX-08D, then shared human observations. Preserve EX-08C/port 5179; no human criteria are passed or waived. Q10 records the Line truck adaptation separately; Q07 still governs full progression/endgame. No commit or push implied. |

### Supersession boundaries

D-RI-1's Version 1 executable plan is superseded by D-EX-09; its adoption remains historical fact. D-RI-3's old scope boundaries are revised where D-EX permits discoveries, personal defence and scheduled assaults. D-RI-5's preservation of legacy evidence is retained; its old benchmark does not define Version 2 acceptance. D-RI-6's opening, adjacent-only establishment and encounter defaults require replacement contracts; unaffected implementation details can be reused.

D-HOUR-1/3, D-P4-2/12 and related resource/starting-stock rows remain facts about legacy constants, not mandatory new-opening values. D-P5-6's selected-route display is a legacy UI decision; multi-stop information must meet Q04's contract. D6's street-first geometry remains reusable; compact expansion and mandatory enclosure acceptance are superseded. Old no-loot boundaries do not prohibit approved rare site discoveries, and those discoveries do not imply enemy loot farming.

### Opening contracts and remaining campaign decision

The [recommendation package](EXPLORATION_DEFENCE_RECOMMENDATIONS.md) was drafted and then explicitly approved by the owner on 2026-09-06 (D-EX-11). Q01–06 are **decided** for the opening. Alternatives remain unadopted; initial numerical targets still need validation and unspecified tuning details remain visible task work.

The six decided rows below share D-EX-11's owner/date/quote provenance. EX-02 records the approval even though EX-01's technical baseline audit is unfinished; that audit does not invalidate the owner's design decision. Unspecified values must not be copied from legacy balance merely to close a tuning question.

| id | contract | status | adopted contract or remaining proposal | affects |
|---|---|---|---|---|
| D-EX-30 | Implement P6-02 defence information and navigation | Reviewed P6-02 scope only; G views a known threat, K returns to the engineer; shared sim action query and turret range | decided (scope); provisional (controls) | owner (scope); coding agent (controls) | 2026-09-07 | “continue” after the completed P6-01 handoff naming P6-02 next | No new threat precision, clock, damage or progression rule. PHASE_6_SCOPE_REPORT.md defines acceptance; automated and human results remain separate. |

| D-EX-Q01 | Base identity, territory, station/corridor vulnerability, remote construction, retained rot/wells and loss/retake. | decided | D-EX-11; GDD §5. Home/station cores anchor fixed neighbourhoods; local powered establishment without a Held chain; no per-edge corridor pressure or ordinary frontier blooms; core defeat disables service and leaves a repairable site, layout and stock. | EX-03/04/05/06 |
| D-EX-Q02 | Day duration, first assault, light effects, restoration scheduling, rest progression, minor raid rate/strength and overlap. | decided | D-EX-11; GDD §7. Initial 20-minute cycle (15 daylight/5 night), Nights 3/6/9 subject to two full quiet cycles after actual completion; one quiet cycle after third station base with automated resupply. Dawn target lock; restoration nominations coalesce; finite major roster, 3–5-minute duration target; two shared minor-raid opportunities/day, 8–12-enemy initial target; no visual-sun change to mechanical light. | EX-03/04/05 |
| D-EX-Q03 | Radio unlock, lead time, precision, coverage/upgrades, power failures and targeting visibility. | decided | D-EX-11; GDD §7. Home-only major targeting before first radio restoration; then network eligibility permanently unlocked. Powered first tower names locked base at dawn; upgrade adds approach/composition. Slice-wide coverage; outages retain received warnings and never reroll targets. | EX-04/05 |
| D-EX-Q04 | Meaning of immediate tram access; unlock delivery, stations, selective cargo/reserves, routes/branches, vehicle count and interruption. | decided | D-EX-11; GDD §14. Hand-supplied second-area restoration grants unlock plus one tram, two stops and route-derived track through real inventories. Multi-stop out-and-back trunk first, local belt branches, item requests/export reserves and destination-reserved cargo; failed deliveries retain stock and bounded dwell; later tram junctions separate. | EX-03/04/06 |
| D-EX-Q05 | Resource regions/longevity, essential seed guarantees and rare site discoveries. | decided | D-EX-11; GDD §12. Current item catalogue, finite starting salvage, concentrated persistent extraction with finite throughput/operating costs. Once-only seeded optional rewards; essential progression guaranteed. Rates/yields/placement bounds remain tuning details. | EX-04/06/07 |
| D-EX-Q06 | Equipment, distinctive enemies, barriers/turrets, workshop service, damage, repair and defeat consequences. | decided | D-EX-11; GDD §13. Early wall/turret/rifle; recoverable disabled defences/core and protected production/cargo; supplied/powered workshop repair between attacks, paid manual repairs; existing knockdown/inventory retention. Field-repair-tool schematic at a Stalker-guarded workshop; exact equipment/rates/costs remain to specify. | EX-04/05/06/07 |
| D-EX-Q07 | Full project progression, endgame kit/defence and campaign length. | open | Retain the Relight goal but derive requirements from the tested station economy after EX-08. | EX-09/10 |
| D-EX-Q08 | Campaign location/unlock for the driven truck. | decided | Owner (current user), 2026-09-07, selected “At the restored second-area tram station (recommended)” in response to the Phase 5 truck question. Make a persistent truck available there after restoration. This adopts location/unlock, not a truck added to the carried tram kit, a new fuel chain or automatic Line truck behaviour; mechanical/capacity defaults are specified before implementation. | P5-04; active GDD §14 |
| D-EX-Q09 | Revised Phase 7 facility/recruit catalogue and Phase 8 boundary. | decided | Owner (current user), 2026-09-07: “All looks good! Continue on with P7”, accepting PHASE_7_SCOPE_REPORT.md and the catalogue question. Local Electricians recruitment; Concrete crew with Mixer/Concrete/Barricade; riverside Turbine hall; optional Lamplighters/Arc lamp and Surveyors/district information; truthful discovery/recipe UI. Credit existing tram/radio/workshop/repair and direct turret feed. Foundry/Arsenal/Refinery/Chemist/endgame stay with wider reconciliation; Foreman/blueprint recruitment ships with working Phase 8 tools. Costs/rates/placement remain provisional; Q07 remains open. | P7-01–05 |
| D-EX-Q10 | Campaign role of the Foreman’s automated Line truck. | decided | Owner (current user), 2026-09-08, chose “Yes—use the truck for queued construction (recommended)” in response to whether it should deliver and build queued blueprints from a chosen local supply chest using the existing truck. Adopt physical chest-to-truck-to-build delivery for player-selected plans, replacing legacy automatic border rebuilding. Keep the existing manual vehicle and its unlock; exact queue limits, service reach and timing are provisional implementation choices. | P8-02/03/04; active GDD §14; PHASE_8_SCOPE_REPORT.md |

## Earlier decision register — historical and retained-rule provenance

Earlier row contents and counts below are preserved. The supersession boundaries above are authoritative for Version 2; a historical decided cell does not override a later D-EX row.

One row per decision or open question, with provenance (`CONSTITUTION.md` rule 11). A row is
**decided** only when `decided by` (a person), `on` (a date) and `via` (a message quote, commit
or report section) are all filled in. Statuses: `open` (no recommendation yet),
`recommended` (Claude suggested; no human has chosen), `provisional` (Claude acted on its own
recommendation under rule 14; a human confirms or reverses), `decided`, `superseded` (the
later row is named in the last column), and `open (gate condition)` for the three rows Gate B
attached as conditions. A decided cell may carry the option taken ("decided (a), 75 minutes").
The `doc sentence` column says where the decision lands in the design doc and keeps the row's
earlier status text, so provenance is never rewritten. The programme never picks a value from
a recommendation on its own; rule 7 says who decides what.

Counts on 2026-09-05 (after `PROGRESS.md` RI-00 adopted the revised development plan): 37 decided, 37
provisional, 32 recommended, 2 open (gate condition), 2 superseded — 110 rows. The
D-RI-* rows record the adoption; D-GB-2, D-CU-1 and D-CU-3 became provisional on the plan's
defaults (D-RI-6) and D-GB-1-rider was superseded (D-RI-4). The questions that still want a human
are listed under **Outstanding questions** below the table.

Full history of each row before the 2026-09-04 reformat: `git show 2286b7d:docs/DECISIONS.md`.
The pre-cleanup header (T11's narrative): `git show 1078b7d:docs/DECISIONS.md`.

| id | question | recommendation | status | decided by | on | via | doc sentence |
|---|---|---|---|---|---|---|---|
| C1 | **Edges fed per assembler.** Doc ~15 (§12: one assembler = 20 mag/min; §5 in-play 1.37 mag/edge-min); proto Mk1 10 mag/min, Mk2 20 → 7 / 15 edges. Evidence: E5 (`FRONT_FIX_REPORT.md`) demand 8–48 mag/min by 3 h; calibration 2: one Mk1 feeds a 4-block blob to 1:59. Is the tempo one assembler per ~15 edges, or should the proto's Mk1/Mk2 split stay? | one 20 mag/min assembler ≈ 7 edges of the mixed mid-game front (E9-hourly), 15 civic / 11 residential at steady state (E3-block), the early front cheaper (calibration); locked without session telemetry, Phase 4's human hour can move it | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), via D-P2-2 on the Gate A `go` | §12 `[play: Gate A]`. Was: made 2026-09-03 (Gate A `go`, by recommendation D-P2-2). **Gate B, 2026-09-05 — not moved.** This row names "Phase 4's human hour" as the first place it can move; that hour was played, but no telemetry was read and no edge or magazine count was taken from it, so C1 stays `provisional` on the sim's numbers. The next played session that reads the telemetry panel is the occasion |
| C2 | **Claim cadence the game is drawn for.** Doc ~5 min after hour one (§18, `[sim: E9-claim-cadence]`); 8 min before `CALIBRATION_REPORT.md`; 15 min in hour one. E9-claim-cadence: 5 min matches §18's 25 h count, 8 does not. Is ~5 min the game, or should the drawings follow the cadence humans play at? | 15 min in hour one, then 5; §18 generated from the sim at it (D-P3-6) | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), D-P3-6 | §18 caption. Was: made 2026-09-03 (D-P3-6). **Gate B, 2026-09-05 — not moved.** The played hour took no claim minutes (it did not reach minute 30), so the cadence humans actually play at is still unmeasured and §18 stays drawn from the sim |
| C3 | **Machine slots per block.** Doc unstated (§5 "machine slots on interior blocks"; §4 lot 24×24 tiles); proto 1 (PROTO-ASSUMPTION). Calibration 2: 1 slot makes enclosure the only way to a second assembler. One slot, or a footprint budget per lot? | one ammo line per interior block is what the sim and the slice encode (GAME-ASSUMPTION); the footprint budget is Phase 4 M1 / Phase 5 (`PHASE_3_REPORT.md` decision 2); measured at prompt B M2 — see D-P3-10, D-B2-2 | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` decision 2 | Was: routed 2026-09-03 (Phase 3) |
| C4 | **Magazine buffer cap.** None stated in the doc; 400 magazines / 4,000 rounds in the proto. Calibration 1/2: pips leave green only after the buffer is empty, so amber → red is ~2 min. A game object (a chest holds 400, §13) or a proto shortcut to delete? | the 400-magazine line buffer is a chest; the Relight bank is a separate buildable object of ~20,000 magazines, Phase 10 E20 sizes it (D-P3-4). Phase 1 evidence E9-hold: a 4,000-round cap loses the hold (34–38 blocks), an uncapped bank of ~15,000 wins it | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), D-P3-4 | §16 sentence. Was: made 2026-09-03 (D-P3-4) |
| C5 | **HQ start patch sizes.** Steel "small" (§11); coal ~3,000 (§11/§12) vs ~700 (`TEST_RESULTS.md` §8); proto steel 7,680 at 64/min, empties at 2:00. Calibration 2: 3,000 coal mines out at ~106 min, 700 at ~30 min. Which hour does the start coal run out in, and should steel run out first? | coal ~700 (D1: west wanted by minute 30); steel routed to Phase 5's rubble model as a named placeholder (proto 7,680 at 64/min, empties at 2:00; the §19 steel wall is intended, D-P2-3) | **decided** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05 on this row's recommendation:** coal ~700 — already in the doc body (§11 / §25, D1's changelog line of 2026-09-03), so no doc edit — and **steel stays the named placeholder** that M1's rubble model sizes with D-P4-2. The §19 steel wall is intended (D-P2-3). §11/§12 coal ~700. Was: coal decided by D1, steel routed 2026-09-03 (Phase 3). D1 names no person **RI-01 2026-09-05:** the rail yard's coal heap takes the HQ coal patch's shape and yield (3×3, 702 dug in `E-hour-m6checks`); steel's placeholder is sized by D-P4-2's 300 a tile. |
| C6 | **Fall time after the substation stops.** 90 s (D2) vs 5 min (pre-D2). E8-fall16: at 5 min nothing ever fell; at 90 s falls happen but humans have not seen one. A rescue window a human can act in, or an ambush? | 90 s (D2); the actable window is the minutes of red pip before the 40th arrival, the 30 s after the stop is the second chance (D-P3-2) | recommended |  |  |  | §5 sentence. Was: made (D2 ratified 2026-09-03); Phase 3 `[play: Gate A]` check made by D-P3-2 without a session |
| C7 | **Substation draw.** 100 kW front / 20 kW interior (D1) vs 200 / 40 (pre-D1). E4h-*: at 200/40 the start patch could not carry hour one; at 100/20 + four Generators it can. Ratify D1 or keep 200/40 and change the Generator? | 100 / 20 (D1) | recommended |  |  |  | §14. Was: made (D1 ratified 2026-09-03) |
| C8 | **Bloom timer base and per-bloom drop.** `120/(0.5+d)`, 10 % vs `240/(0.5+d)` (halves fights, −30 % ammo) and a 20 % drop (−24 % ammo, E10). Which rhythm feels right? Gate A `[play]` | locked at the doc value (D-P3-5) with no feel evidence; `PHASE_3_REPORT.md` decision 3 asks whether to port E10 before the slice encodes it | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), D-P3-5 | §5 sentence. Was: locked 2026-09-03 at the doc value (D-P3-5) |
| C9 | **Assembler rate.** 20 mag/min (§12, from the 3 s recipe) vs proto Mk1 10, Mk2 20. Is there a Mk1/Mk2 ladder in the game or only in the proto? (with C1) | only in the proto; the game's Assembler is 20 mag/min from the 3 s recipe (D-P3-7). Note: the tile code's Mk1 Shot assembler and the calibration disagree — see `GUARDRAILS_REPORT.md` and D-P4-4 | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), D-P3-7 | §12. Was: made 2026-09-03 (D-P3-7) |
| C10 | **Start ammo.** 20 magazines (§11) vs sim `startRounds` 300 = 30 magazines. E1-start-min: the HQ never falls on 200 rounds with the ammo line from minute 10 under the 40-arrival rule; a 10- or 20-arrival rule loses it at minute 8. Which is it? | 20 magazines (D-P1-3 rider); sim `startRounds` 200; §11 states the margin; config `01dc5d02`. Phase 2: the third hopper shows amber then red for 30 s at t = 0 — D-P2-1 keeps 20 and scores C1/C2 from minute 1 | recommended |  |  |  | §11 sentence. Was: made 2026-09-03 (D-P1-3 rider, human: "whatever you recommend"); D-P2-1 by recommendation |
| D1 | **Substation draw and the start coal.** 100 kW front / 20 kW interior; HQ starts with four Generators; start coal patch ~700 (was 3,000, which mined out at 106 min; the intent is "west wanted by minute 30"). At the halved draw E4 showed machines, not substations, set the Generator count, so ammo is the hour-one lesson. Evidence `CALIBRATION_REPORT_2.md`, E4h-* | ratify as stated | recommended |  |  |  | doc §11/§12 coal ~3,000 → ~700 in the Phase 1 doc pass; closes C5 (coal) and C7. Was: ratified 2026-09-03, decided by "human" |
| D2 | **Fall time 90 s after the substation stops.** The 30 s rescue window against the 60 s refeed reset is what makes a red pip a call to action rather than an obituary. Evidence `CALIBRATION_REPORT_2.md`, E8-fall16 | ratify as stated | recommended |  |  |  | closes C6; Phase 3 still checks a human can act in the window `[play]`. Was: ratified 2026-09-03, "human" |
| D3 | **Unshot-arrival counter is per block; a refeed within 60 s resets it.** Corners fail faster, which is right. Evidence `CALIBRATION_REPORT_2.md` | ratify as stated | recommended |  |  |  | §5 must say it so the prototype's corner falls are not read as a bug. Was: ratified 2026-09-03, "human" |
| D4 | **The Relight hold is survivable by banking stock.** A design statement, not a number, pending E9-full (Phase 10 E20). Evidence `CALIBRATION_REPORT_2.md`, E11-relight-hold; Phase 1 E9-hold: 8 assemblers from hour 4 + ~15,000 banked → 0 lost | ratify as direction | recommended |  |  |  | §16. Was: made (direction), ratified 2026-09-03, "human" |
| D-25-13 | **§25 item 13 "Spike on the scattered map — closed, loses nothing" no longer matches the sim.** Phase 1 `[sim: E7, E1-ring-spike]`: at the §18 cadence spike costs 2.7× compact, holds 40 vs 52, loses 12 in five hours. Retag (spike is punished by losses) or retune? | see D-P1-2 | superseded |  |  |  | Superseded by D-P1-2 (a priced choice, not a trap; §25 item 13 carries the numbers). Was: reopened by the human 2026-09-03, closed by D-P1-2 the same day |
| D-CI | **Repo, CI and workspaces.** GitHub private repo, GitHub Actions, npm workspaces; `main` protected behind CI; push CI = lint, `tsc --strict`, `npm test`, E1–E9 at three seeds; nightly 10,000-seed and 25 h runs; Python sim as fixture exporter for one phase then retired; Linear for tasks. Evidence: Phase 0 inventory | as stated; done in Phase 1 (`Deedubsy/Relight`, `ci.yml`, `nightly.yml`); `main` protection refused on a free-plan private repo (needs Pro or public — human choice); ruleset to apply once possible: PR required, status check `ci`, bypass actor GitHub Actions | recommended |  |  |  | Was: decided 2026-09-03 by "human"; done in Phase 1 (commit f94a7a1) |
| Gate A | **Gate A verdict.** `TEST_RESULTS.md` `verdict: go` on `PROTOTYPE_TEST_PLAN.md` (Phase 2: two scenarios, `?seed=3` and `?state=b-compact-seed3`, hashes `ee23bb1c` / `825d2d09`); bot calibration and the smoke test only; the plan's thresholds were not measured | passed; every constant locked on it carries `[play: Gate A]`, a lock not a measurement; the sessions can still run (D-P3-9) | recommended |  |  |  | Was: made — passed without tester sessions, by "human (owner, `go`)" |
| D-P1-1 | **Map size.** Ship 24×22 (every run, the §18 drawings) or 24×24 (§4 as written, the constitution)? `[sim: E8-cadence]` at 24×22, then the full suite at 24×24 (`01dc5d02`) | 24×24: row 23 the river, 23 claimable rows; HQ, target and the southern wells moved (GAME-ASSUMPTION until Phase 9); every doc number re-tagged; §18 redrawn in Phase 2 | recommended |  |  |  | §4. Was: made 2026-09-03: 24×24 (commit aab784b) — no decider named |
| D-P1-2 | **§25 item 13 (D-25-13).** Spike loses 10 blocks (9–12) and pays 2.6× — a choice, a trap, or a rule change? `[sim: E7, E1-ring-spike]` | a priced choice; §19 stands, §25 item 13 closed with the numbers; the losses are the ring's far end under creation-order feeding, so a feed-order rule is a Phase 4 logistics question (DEFERRED.md) | recommended |  |  |  | §25 item 13. Was: made 2026-09-03 (human: "whatever you recommend"), commit aab784b |
| D-P1-3 | **Gate verdict on the untagged DoD.** 44 remain against ≤ 23; 29 are tile-scale numbers a block sim cannot reach. Pass on the 15 reachable, or hold Phase 2? `PROGRAMME_STATE.md` 1.3 | pass on the 15 reachable, carry the 29 as Phase 0 routed them, count only instrument-reachable numbers in later DoDs. Riders: C10 = 20 magazines, edge hopper 100 = two turrets per edge until Phase 4 M3, `main` protection waits on GitHub Pro or a public repo | recommended |  |  |  | Was: made 2026-09-03 (human: "whatever you recommend"), commit aab784b |
| D-CI-nightly | **Nightly seed count.** D-CI says 10,000 seeds nightly; a 5 h run costs ~0.3 s so that is ~3.5 h of runner time, ~6,000 hosted minutes a month. Evidence: nightly smoke, 20 seeds, 74 s (`docs/experiments/nightly.md`) | cron runs 1,000 seeds; 10,000 by `workflow_dispatch` or the self-hosted runner D-CI names | provisional | Claude Code | 2026-09-03 | commit f94a7a1 (Phase 1), `nightly.yml` | Was: made (letter of D-CI changed, intent kept); human may override |
| D-P2-1 | **Start rounds and the opening flicker.** 200 rounds (C10 = 20 magazines) leave the HQ's third hopper empty for the first 30 s, so calibration C1/C2 read "missed at 0 m"; 300 rounds meet all seven targets. Keep 20 and score from minute 1, move C10 to 30, or pre-fill three hoppers? Evidence `docs/experiments/calibration.md` (base vs lever 2a), seed-3 trace | keep 20 magazines; score C1/C2 from minute 1 (the `after 1 min` columns); the flicker teaches the pip ladder | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), on the Gate A `go` | §11 sentence `[play: Gate A]`; `calibrate.ts` scores from minute 1, C1–C7 met. Was: made 2026-09-03 by recommendation (Gate A `go`, no human word on it): (a) |
| D-P2-2 | **Edges per assembler (C1).** Compact never sees amber after the transient in three hours; the second assembler is affordable but never needed in Scenario A. Accept, feed fewer edges, or cut the buffer cap? Evidence calibration base, levers 1a/1b, Scenario A traces | accept; lock C1 in Phase 3 from Scenario B telemetry (`firstAmber`, reorders, builds) | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), on the Gate A `go` | §12 `[play: Gate A]`: ~7 edges of the mid-game front per assembler, locked from bot evidence (no B telemetry exists). Was: made 2026-09-03 by recommendation: (a) |
| D-P2-3 | **The steel wall as Scenario B's pressure.** From the 3 h snapshot steel is 0 at ≈ 3:40 whatever the tester does; the expanding bot is down to 2 blocks by 4:30, an idle tester loses 6. Intended pressure, a tuning bug, or a different snapshot? Evidence Scenario B forward runs, lever sweep 3/4a | intended; §19 gets a `[play]` sentence after the gate; option (b) stays in DEFERRED.md for Phase 5 | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), on the Gate A `go` | §19 sentence `[play: Gate A]`. Was: made 2026-09-03 by recommendation: (a) |
| D-P3-1 | **Substation draw and the hour-one lesson.** Draw stays 100/20 kW (D1); is the hour-one lesson ammo or power? Evidence E4h-gen@0,6,15,25, E5-compact-1-shot-asm-h1, E3-first, B-M4-gate (0 brownout seconds, first red pip at 5:34–6:21 before the line runs) | ammo, not power: no hour-one run browns out at four Generators, the Shot line's 8 mag/min against one assembler's 20 is what the player watches, first pip leaves green inside the first minute, first shade at minute 32–47. §11 sentence | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §11 `[play: Gate A]`. Was: made — "programme, by the evidence; human may override" |
| D-P3-2 | **Fall time / rescue window vs refeed reset.** 90 s (D2) and the 60 s reset (D3) — is the rescue window real? Evidence E1-starve-substation-N40, `PHASE_2_REPORT.md` Scenario B | kept; the rescue the player actually has is the 4–20 min of red pip before the fortieth arrival (E1-starve N40: 7.2 min median, 4.3–19.6); the 30 s after the substation stops is the second chance. §5 sentence | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §5 `[play: Gate A]`. Was: made — programme; human may override |
| D-P3-3 | **The unfed rule as it ships.** Per block; the unshot-arrival counter stops the substation at 40; it clears when every edge has had a non-empty hopper for 60 s; creep 1 tile per 3 s off, decays 1 per 20 s on, fall at 30 tiles = 90 s. Evidence E1-ring-substation-N10/N20, E1-starve-creep, E1-starve-substation-N40 | as stated: a 10- or 20-arrival threshold loses the HQ at minute 8; creep from an empty hopper falls in 2.8 min. §5 sentence | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §5 `[play: Gate A]`. Was: made — programme; human may override |
| D-P3-4 | **Is the Relight survivable with banking, or the intended ending?** Evidence E9-hold: the 400-magazine line buffer and a 4,000-magazine bank both lose the hold (0/39/42 lost), uncapped (~13,700 peak) loses none | survivable; banking is the intended play; the bank is a buildable object of ~two hours of eight-assembler production (~20,000 magazines); §16 sentence; §12 late column assumes eight assemblers; Phase 10 builds the object, E20 measures what a player fills | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §16 `[play: Gate A]`. Was: made — programme, direction D4 (human) |
| D-P3-5 | **Bloom timer and drop (C8).** Lock at `120/(0.5+d)`, 10 % with no feel verdict, or port E10 first? Evidence E10-bloom-cadence (Python, retired), `PHASE_3_REPORT.md` decision 3 | locked at the doc value; E10 not ported; §5 sentence | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §5. Was: made (lock) — programme; human may override or ask for the port |
| D-P3-6 | **Claim cadence (C2).** Evidence E8-cadence (gap 5 within 1 % of the caption; 4/6/8 miss by 19–41 %) | one claim per 15 min in hour one, then one per 5 min; §18 generated from the sim at it | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §18. Was: made — programme; human may override |
| D-P3-7 | **Assembler rate (C9).** A Mk1/Mk2 ladder in the game or only in the proto? Evidence §12, calibration | no ladder; 20 mag/min from the 3 s recipe; the proto's Mk1 is a calibration stand-in until Phase 4 places the real machine | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3), `PHASE_3_REPORT.md` | §12. Was: made — programme; human may override. The tile code's Mk1 disagrees (see `GUARDRAILS_REPORT.md` constants) |
| D-P3-8 | **Python sim retired** in Phase 3's first commit: `frontsim.py`, `phase5*.py`, `export_*.py`, result JSONs deleted; `packages/sim/fixtures/*.json` frozen at `52c4ca3`. Evidence DEFERRED.md Phase 2 re-read; every doc tag names a TS run | retire, as D-CI planned | provisional | Claude Code | 2026-09-03 | commit fb3543f (Phase 3) | fixtures README. Was: made — programme, as D-CI planned |
| D-P3-9 | **Ratify Gate A passed without sessions, or run the five sessions in parallel with Phase 4 M1** and let their telemetry move the `[play: Gate A]` locks? | run them in parallel; nothing in Phase 4 M1 depends on the locks changing by less than 2× | provisional | Claude Code | 2026-09-03 | commit a28b5ff (Phase 4 M1), on the `Go` on `PHASE_3_REPORT.md` | Was: made 2026-09-03 by recommendation (the `Go`): sessions run in parallel, by the human, any time; a contradicted lock moves and its tag becomes the session name |
| D-P3-10 | **C3/C4 for the slice:** one ammo line per interior block and a 400-magazine chest as locked, or set the lot footprint budget now? | as locked; the footprint budget is what M1 measures (measured at prompt B M2: every non-Inert HQ-neighbour face gets 1 slot from area and holds 4–9 §11-scale lines — the slot is an economic cap, not a footprint limit; see D-B2-2) | provisional | Claude Code | 2026-09-03 | commit a28b5ff (Phase 4 M1), on the `Go` | Was: made 2026-09-03 by recommendation |
| D-P3-11 | **C8 with no feel evidence:** port E10 to the harness (a day) before the slice encodes the timer, or accept the lock and let Phase 4's playtests move it? | accept; the port answers a number question, and C8 is a feel question | provisional | Claude Code | 2026-09-03 | commit a28b5ff (Phase 4 M1), on the `Go` | Was: made 2026-09-03 by recommendation: accept the lock; no E10 port |
| D-P4-1 | **World-view zoom range.** The constitution's M1 says 0.5–3×; §4 said 1.0× down to 0.2× with the map view below 0.2×. At 0.5× the 648 px canvas shows one lot (≈ 40 tiles); at 0.2× three blocks across. Keep 0.5–3× with the map key as the only route to the map view, or widen the floor? Evidence `SLICE_REPORT.md` M1 measured (1,849 tiles at 0.5×, 81 at 3×, 60 fps) | keep 0.5–3× for the slice: M2's placement wants a lot to fill the canvas, and a 0.2× floor would draw 11,000 tiles a frame for a picture the map view already gives; revisit at Gate B | provisional | Claude Code | 2026-09-03 | commit c146cf4 (Phase 4 M2), on the `go` that opened M2 | §4. Was: made 2026-09-03 by recommendation on the `go` that opened M2 |
| D-P4-2 | **Units per rubble tile.** §12 says a rubble tile holds 300 units and a block 75–105k; the sim's pool is 3,840 per block (`PROTO_CALIBRATED`). M1 draws the doc's 250–350 tiles with standing rubble following the pool proportionally (≈ 11–15 units a tile). Keep the stand-in, or set units per tile now? Evidence §12 table 12.1; `tiles.ts` `rubbleLeft`; `CALIBRATION_REPORT_2.md` | keep the stand-in through M2: the excavator digs tiles at 0.5/s and the block's pool stays the economy's truth; Phase 5's rubble model (E11/E13) sets units per tile with the deposit sizes | **decided** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05 on this row's recommendation:** the stand-in stays through M2 — the excavator digs tiles at 0.5/s and the block's pool of 3,840 is the economy's truth — and **M1's rubble model sets units per tile** alongside the deposit sizes, against §12's 300 a tile and 75–105k a block, with `E-coal` (was E11) measuring depletion. §12. Was: made 2026-09-03 by recommendation on the `go` that opened M2 **RI-01 2026-09-05 (built):** `tiles.ts` RUBBLE_UNITS_PER_TILE 300; the pool drops a tile's share when a tile is dug out and the tile thins as it goes; coal is the fourth kind (the rail yard only). East's line digs 1,750 stone (seeds 3, 5) or 1,750 copper (seed 4) in the 75-minute hour (`E-hour-ledger`). No sink for stone exists — a stone-consuming use is a new mechanic and a separate decision (`RI_PASS_1_REPORT.md` RI-01). |
| D-P4-3 | **Deposits on the outskirts.** §12 puts iron and coal deposits on the outskirts as large facilities (~2M / ~1.5M); the sim's outskirts blocks carry no pool. M1 draws a 160-tile placeholder patch on a quarter of outskirts blocks by hash, inert to the sim. Keep the placeholder until Phase 9, or place them as facilities now? Evidence `tiles.ts` GAME-ASSUMPTION deposits; §8 | keep the placeholder: nothing in minutes 0–60 mines a deposit, and a facility-placed deposit belongs with the generator's validator (Phase 9) | **decided** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05 on this row's recommendation:** the 160-tile placeholder stays inert; a facility-placed deposit belongs with the generator's validator (Phase 9). **Tension recorded at T11:** `E-coal` and `E-tram` may need the outskirts deposits to be real to measure depletion and haul, so M1's report must say which half it built — if a Phase 5 run needs them live, this row is revisited rather than quietly widened. §12. Was: made 2026-09-03 by recommendation on the `go` that opened M2 **RI-01 2026-09-05 (the placeholder half built):** the 160-tile placeholder stays inert; the tripwire is the hour bot's note and end-state adjustment when a claimed face has no rubble (`hour.ts`); no seed in three hits it in the 75-minute hour, so nothing yet asks for a live deposit. |
| D-P4-4 | **How fast is the Mk1 Shot assembler?** §11 opens with 200 steel, 100 copper, 50 stone; the calibrated sim opens with 80 / 40 / 0 (`PROTO_CALIBRATED`). §13 prices no machine, so M2 priced them (assembler 40 steel + 20 Cu, Excavator 10, belt 1, inserter 1 + 1 Cu; GA-M2-6); a §11 line is 81 steel + 23 Cu, with its second Generator 156 steel (D-P4-7). Verification pass 2026-09-04: on the calibrated chest the bot's steel was 2 at 17:34 and 0 at 26:34, the minute-45 machines refused on every seed (end counts 3 Generators / 6 Excavators / 1 Assembler, not 4 / 5 / 3); on §11's 200-steel chest seed 3 was at 2 steel by 16:34 and west and north were refused for 0 steel — held 2 at the hour. Neither start affords §11's hour. Is the lesson hand-mining, a second steel Excavator, or cheaper machines? | Mk1 makes 10 magazines per minute (one every 6 s). The faster rate (one every 3 s, 20/min) is a separate machine, the Mk2. §12 gets one row for Mk1 and one for Mk2. The §11 first hour uses Mk1. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | §11 stock line. Was: made 2026-09-03 by recommendation on the `Go` that opened M3, then reopened at M6 for the human. **Next actions line 3: the human writes this row.** — Built 2026-09-04 (`constants.ts` SHOT_MAGAZINE 6 s / SHOT_MAGAZINE_MK2_SECONDS 3, ASSEMBLER_TIERS Mk1 10 / Mk2 20; §12's table by docsync; `hour.ts` HOUR_STEEL2_AT). The steel curve's minimum after the change: **30 at 12:01** on seeds 3 / 4 / 5, above 1,100 at the hour (`B-M6-hour`, unverified). Taken provisionally (rule 14, GA-EF-1): the Excavator is placed at 12:00 rather than ~15:00 — the minimum falls at 12:01 and a 15:00 placement crosses east's kit; reversing it moves no rule or fixture |
| D-P4-5 | **Two assembler stand-ins.** With the flow layer on, hour one's magazines come from the physical line on the HQ lot, but a claimed interior block still gets the block-level "Build assembler" button (20 mag/min from stock, no footprint). Keep both through the slice, or make every assembler physical now? Evidence `session.ts` GAME-ASSUMPTION (M2); §11 10–30 min; §12 | both stand-ins through M5, decide at M6; M6 recommendation: make every assembler physical after Gate B — the hour bot already stands E4's stand-ins on the HQ lot (GA-B6-3); the block-level button stays through Gate B so the played hour is one model | provisional | Claude Code | 2026-09-03 | commit 6ce1786 (Phase 4 M3), on the `Go` that opened M3 | Was: made 2026-09-03 by recommendation on the `Go` that opened M3; M6 recommendation for after Gate B. **Gate B is past (2026-09-05, `GATE_B.md`)** and the condition attached to that recommendation — "the block-level button stays through Gate B so the played hour is one model" — is met: the hour was played on the one model and nothing about assemblers came back from it. The recommendation is therefore **unconditioned now: make every assembler physical**. Still `provisional` and the human's under rule 7; it blocks nothing. **RI-00 2026-09-05:** RI-01 makes every assembler physical and removes the block-level stand-in **RI-01 2026-09-05 (built, still provisional until signed):** `addAssembler` refused under the tile layer and the panel's button hidden; the HQ lot's stand-in Excavators removed; east's and the rail yard's lines are real Excavators belted into the Depot (`E-hour` east-line 16:46–16:51, west-line 26:39–26:44). |
| D-P4-6 | **Belt rate 7.5 vs 8.** The constitution's M2 says 7.5/s; §13/§14 said 8/s (fast 16). 7.5 is four items a tile at 1.875 tiles/s and divides 20 ticks/s cleanly; 8 does not. Evidence `flow.ts` `BELT_PER_S`; flow test 3 | accept 7.5/15: the constitution is the later word, the number is invisible to a player, and the clean tick keeps belt tests exact; doc edited, changelog `M2-rates` | provisional | Claude Code | 2026-09-03 | commit 6ce1786 (Phase 4 M3), on the `Go` that opened M3 | §13/§14 belt rows 7.5 / 15. Was: made 2026-09-03 by recommendation on the `Go` that opened M3 |
| D-P4-7 | **When do Generators and the coal Excavator get built in the first hour?** §11 opens with one 300 kW Generator on 40 coal and says one Generator alone browns out at minute 8. Settled before M4 by aligning the M3 script to E4 (Generators at 0 / 6 / 15 / 45, the coal Excavator on the HQ patch): the tile-level hour holds on seeds 3 / 4 / 5 with 0 brownout seconds (`B-M4-gate`); the Generator-feed question is moot under D-B3-4. Verification pass 2026-09-04: 166 / 166 / 170 s of brownout from 8:01 to the 10:13 rounds run — Generator 2 placed at 6:03 with 0 coal in the chest; the browser's hour 567 s. Does the start need two Generators, or should the coal Excavator's belt reach Generator 2 before the line is placed? | Generators at minutes 0, 6, 15, 45. The coal Excavator and its belt are built before Generator 2. A second steel Excavator at about minute 15. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | §11, §14, §25 item 14. Was: made 2026-09-04 (the human): settled before M4 — no name, no message cited; M6 reopened it. **Next actions line 3: the human writes this row.** — Both leave 0 brownout seconds on seeds 3 / 4 / 5; **(b) taken**: Generator 2 at 6:01 against 7:24–7:32 under (a), the rest of the hour the same (`constants.ts` START_CHEST.coal 40; the bot's `coalPlan` keeps (a) runnable, `E-hour-coal`) — `B-M6-hour`, unverified |
| D-P4-8 | **How many turrets at the start, and what is in their hoppers?** §11 says two Gun turrets on the north edge; M3 started six (GA-M3-1). Verification pass 2026-09-04: with six start turrets fed from the chest the chest holds 5–10 magazines at the bot's first take and 0 at the second; the turrets took none by hand in the first ten minutes, so §11's "walk the magazines to the turrets" carries nothing, and the first red pip comes from the first bloom at ≈ 6 min | Six turrets, all hoppers start full; the chest also holds 20 magazines (D-B1-1). §11 minutes 0–10 rewritten so the first hand-feeding happens when the first hopper empties at about minute 6. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | Superseded 2026-09-04 by D-B1-4. §11's sentence still to settle — **Next actions line 3 names this row for the human.** — Built (`constants.ts` START_TURRETS 6 spread by D-B1-4's segment rule, every hopper at 50, the chest's 20; defence test 25): every pip green from the first tick, the first red 5:33–6:18 and the first hand-feed 5:36–6:21 on every seed; §11 rewritten — `B-M6-hour`, unverified. Was: superseded by D-B1-4 (the count now comes from the human, the spread from D-B1-4) |
| D-P4-9 | **Turrets on claimed blocks.** On the HQ an edge fires only through its physical turrets (GA-M3-4); every other Held block keeps the block sim's 100-round edge hopper with no turret placed. Verification pass 2026-09-04: north fell at 45:34 / 45:35 / 58:27 on every seed, rifle on and off; the two carried turrets did not hold it; on seed 5 one north edge never got its kit. **Economy-fix task Step 6, 2026-09-05 (`ECONOMY_FIX_REPORT.md` §8.3a): the carried turrets are what loses north.** With north's ring kitted on every seed (the kit defect fixed), north still falls on 3 runs of 6, always `shade`; with §11's two carried turrets removed and nothing else changed, **no block falls on any run, four blocks Held on all six, zero shades, E-hour 8 / 10**. A turret standing on a claim takes its edge off the ring feed (`hookSyncEdges`), swapping a ring-fed 100-round hopper filled from a buffer with 100–300 rounds spare for the turret's hand-fed 50-round one; one engineer cannot keep three of them fed. Keep the split, or make every front edge physical (a claim lays its segment's turrets from stock)? | make every front edge physical after Gate B — a claim lays its segment's turrets from stock (D-B1-4's count) so §11's "two turrets carried" has turrets to carry (GA-B6-4); the block-level hopper stays through Gate B; the steel cost is D-P4-4's | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Do the blockers"), taking this row's recommendation | §14 front kits. **Built 2026-09-05** (the block-level hopper stays through Gate B, so §11's two carried turrets go): `hour.ts` lost the carry step and with it `carryTurrets`, `turretSpots`, `idleTurret` and `hqTurrets`; `HourBot.carried` is gone; `flow.ts`'s `pickUpItems` comment no longer cites them. Through Gate B a claim's edges keep the block sim's ring-fed 100-round hopper and nothing physical stands on them. The recommendation's other half — every front edge physical, a claim laying its segment's turrets from stock — is **deferred to after Gate B** (`DEFERRED.md`); restore the four functions from commit 6694b71. Measured: E-hour **10 / 10**, no block falls on any of six runs, and in `E-hour-north` north now **stands to 75:00 on all three seeds** (it fell at 72:45–74:38 before). Was: open — M6 recommendation. **RI-00 2026-09-05:** the deferred half (every front edge physical) is RI-03's **RI-03 2026-09-05:** the field half is built — a turret is placeable and fires on a Dark / Contested block next to Held ground (`flow.ts` `running`, `threat.ts`), so every front edge can be physical before the claim; the hour bot laying a claim's turrets from stock (the 6694b71 restoration) is **not built** — it changes the benchmark's balance, a Later candidate for RI-07's rerun, the human's call |
| D-P4-10 | **Coal after the HQ patch, and north's minute.** The HQ's ~700-unit patch is dug out at ~36:00; the chest's 40 and the patch carry four Generators to the hour with 2 coal left in the chest; west's coal is the block-level stand-in and makes nothing (GA-B6-3). Claimed at 65:00, north is Held in 30 s, the Generators run dry at 67:51–68:01 and north falls at 72:45–74:38 inside a 510–557 s brownout on every seed (`E-hour-north`, `B-M6-hour-north`). Re-measured on the decided minutes (D-HOUR-1, north at 40; `B-M6-hour-2`, `ECONOMY_FIX_REPORT.md` §4): north is Held at 40:29–40:34, the four Generators brown out from 40:01 to Generator 4 at 45:00 (299–300 s) and north falls at 48:54–52:43 on every seed; re-measured after the three hour-bot defect fixes (Step 6, 2026-09-05, `ECONOMY_FIX_REPORT.md` §8.3c) the brownout is 262–315 s, and in the run where nothing falls to end the deficit early it is **exactly 300 s on all six runs** — the five minutes between two decided minutes (north's claim, D-HOUR-1; Generator 4, D-P4-7), to the second; the HQ is still drawing 100 kW front, not 20 kW interior, for most of the hour; in the two-claim variant (north at 65, `E-hour-north`) it falls at 72:45–74:38 as before. §11 now reads "north at 60–75 once west's coal is real" on this row's recommendation. (a) north at 60–75 with west's coal made real first — a physical Excavator and belt on west's rubble at the claim (D-P4-9's kit brought forward for coal); (b) a bigger HQ patch (1,000+) so hour one never leaves the lot for coal; (c) the hour script lays a coal Excavator on west's rubble as a second stand-in that delivers | **(a)**: the doc's own line ("west's coal is wanted by minute 36") and the claim's reward; (b) hides the lesson, (c) is a stand-in feeding a stand-in | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Do the blockers"), taking this row's recommendation, option (a) | §11 30–60. **Half of (a) is built; half is deferred.** Built 2026-09-05: `constants.ts` `HOUR` step `claim-north` minute 40 → **65**, past the hour on purpose, so hour one is two claims (east 15, west 25) and `HOUR_END.held` is **3**, not 4; `firsthour.ts` and `hour.ts` say so in their comments and the report expects north's claim, the HQ's white border and the Electricians only once north has been claimed. **Not built: "west's coal made real".** The tile layer has no coal rubble — `rubbleOf` gives stone / copper / steel / null, and west is a rail yard, so a physical Excavator on its lot yields steel — and there is no coal quantity for the lot. Both are undecided constants (rule 7), so the work is deferred (`DEFERRED.md`) and the constants are asked as **D-P4-12**. Measured: the 300 s brownout is gone by construction — **0 brownout seconds, 0 refusals on all six runs**; `E-hour-north` carries the hour to 75:00 with north claimed at 65:00 and Held at 65:29–65:34, four Held at 75:00, no falls. Was: recommended — M6 recommendation **RI-01 2026-09-05:** (a)'s other half built — north's claim at 65:00 is inside the 75-minute hour (D-HOUR-3) and scored: Held 65:29–65:34 to 75:00 on every run, the Generators never dry (`E-hour-north`). |
| D-P4-11 | **The idle steel.** With the Mk1 at 10 mag/min the second steel Excavator's surplus is above 1,100 steel at the hour and the hands carry 790–860 magazines an hour to the hoppers against the line's 519 (`B-M6-hour`). (a) a second Shot line from the idle steel at ~30:00 in §11's script (two Mk1s = the old 20 mag/min), the hand-feed beat ending in the 30–60 window; (b) the Mk2 purchase priced from it (D-B1-1's unlock); (c) leave it — the hand-feed is the hour's rhythm and the steel is the player's | **(c)** until the played hour says whether the hand-feed reads as the beat or as tedium (Gate B's tedium row); then (a) | recommended |  |  |  | the human decides at Gate B. **Cleanup 2026-09-05:** Gate B recorded no tedium row (minutes 0–10 only, `GATE_B.md`), so the condition never arrived; §19's hands test now labels the bot's 790–860 hand-fed magazines an hour as the measured limitation, and `PROGRESS.md` T12b measures the share per variant before this row is asked again; **RI-00 2026-09-05:** RI-01's acceptance requires the hand-fed total to be explained by the resource accounting or removed **RI-01 2026-09-05:** the hand-fed total is explained, not removed — every hand-feed is a transfer the ledger balances (`E-hour-ledger`, conserved on 6/6 runs), and the accounting splits the 60-minute hour's "790–860 magazines" into 69–150 magazines and 928–932 coal in the 75-minute hour; the idle steel is 1,601–1,643 at 75:00. (c) stands; T12b measures the share per variant. |
| D-P4-12 | **West's coal, made real.** D-P4-10 (a) wants a physical coal Excavator and belt on west's rubble at the claim, so the hour's coal outlives the HQ patch. The tile layer cannot carry it: `rubbleOf` returns stone (civic), copper (residential), steel (industrial) or null (outskirts) and has no coal rubble at all; coal exists only as an outskirts deposit (`DEPOSIT_CELL_FRACTION` 0.25, `DEPOSIT_TILES` 160, `DEPOSIT_IRON_FRACTION` 0.6). West is a rail yard, so it reads `ind` and a physical Excavator on its lot digs **steel, not coal**. Two constants are missing from the **sim**: what a rail-yard lot's coal is, and how much of it there is. **Premise corrected at the Phase 5 opening, 2026-09-05:** the design doc does carry both. §12's raws table reads *"Coal \| **rail-yard rubble (direct, ~30k per block)** · coal seam (outskirts)"* — which is option (a), a rubble kind on rail-yard lots, with the doc's own quantity of **~30k a block**, 43× this row's recommended ~700. That does not decide the row (rule 7): §12's ~30k carries no `[sim: …]` tag, and taking it makes west's claim the answer to coal for the rest of the game rather than a top-up for hour two, which is a different game from the one the recommendation describes. The human still writes it — but against §12's number, not against a blank. (a) a fourth rubble kind — `coal` — on rail-yard / industrial-yard lots only, with its own per-lot quantity; (b) an outskirts-style coal deposit seeded under west's lot at generation, reusing `DEPOSIT_TILES`; (c) west stays coal-free and hour two's coal comes from walking to the outskirts deposit. | **(a)** with the quantity at the HQ patch's ~700 (the patch is what it replaces, and a claim that pays back the walk is D-P4-10's whole argument): it is one rubble kind, it keeps coal a city resource rather than a trip out of town, and (b) buries a deposit under a lot the player cannot see into. But every option adds a constant, so the human writes the row. | **decided (a), at ~700** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05 on this row's recommendation:** a fourth rubble kind, `coal`, on rail-yard / industrial-yard lots only, **~700 a lot** — the HQ patch's size, because west's claim replaces the patch rather than ending coal for the game. **This overrides a design-doc number:** §12's raws table said "rail-yard rubble (direct, ~30k per block)", untagged; the corrected premise put both numbers in front of the human and the recommendation is what was taken, so §12's line is rewritten at T11 with a changelog line. `E-coal` measures whether the Turbine hall is ever needed. Builds at M1 with D-P4-10's other half. §11 30–60 and §17 rubble kinds, when it is decided. Blocks nothing before Gate B — the hour is green without it (0 brownout seconds), and `E-hour-north`'s Generators still run dry at 67:51–68:01, which is what this row buys back. Added 2026-09-05 by the deferred half of D-P4-10 (a) **RI-01 2026-09-05 (built at 702):** `tiles.ts` RAIL_YARD_HEAP 3×3 × 78 a tile (implementation default: the HQ patch's density); the rail yard is the HQ's most westward claimable neighbour (`ground.ts` railYardOf, GAME-ASSUMPTION). `E-hour-m6checks`: first unit at the Depot 27:02–27:04, 47.9–48.0 min before the hour ends, the chest's coal never out.. **RI-05 2026-09-05:** the yard's coal reaches the Depot by belt in the benchmark (`E-hour`, unchanged) and by tram in `E-project` (27:05–27:15, the route laid from the restoration's reward, beside the benchmark and never in it) |
| D5 | **The player is an engineer on foot, with a weak weapon.** A sprite on the tile grid; the camera follows; the map view is a key (M); reach radius; a personal inventory with a slot cap; the Depot is a chest on the HQ lot; the Line truck draws from the Depot; walking is a real cost with a tedium-audit row; one rifle from the inventory (a crawler ≈ 2 s, a turret 0.6 s), the turret rules apply, the game is completable without firing; retaliation only; player HP, knockdown and respawn at HQ with the inventory intact; rot and dark blocks do not hurt the player. Fixes §22's "nothing walks" and §23's rejected avatar. Constants set: reach 8, inventory 40 stacks, rifle ≈ 2 s a crawler, unprovoked targeting never | as stated; applied in the rework (commit 27cc9b0, `REWORK_REPORT.md`) | recommended |  |  |  | §22, §23, §5. Was: made 2026-09-03 by the human; applied in the rework. **The human should fill `decided by` / `on` / `via` — the rework brief is the message.** |
| D6 | **The city is generated streets-first; blocks are the faces.** Curved arterials, a bending river, one diagonal avenue, branching secondary streets, plazas and parks inert; street widths 6–12; a block is a face, 3–7 neighbours, ≈ 20–50 tiles across; the front rule unchanged on the graph (a well dies when all its neighbours are held); slots and rubble scale with area; districts by band from HQ. Fixes §3/§17's lattice. Constants set: one slot per ≈ 600 buildable tiles, street widths 6–12, blocks 20–50 tiles with 3–7 neighbours | as stated; applied in the rework (commit 27cc9b0) | recommended |  |  |  | §3, §17. Was: made 2026-09-03 by the human; applied in the rework. **The human should fill the three columns from the rework brief.** |
| D-R1 | **The rifle and the shade.** E-rifle: a whole belt lost for 90 s falls to a shade at an unlit edge with or without the rifle; a single dry edge holds either way. (a) keep D5, the rifle is for crawlers; (b) a hand lamp on the engineer; (c) the rifle hits shades | (a); (b) only as a slice M5 experiment if Gate B testers lose a block to a shade while standing on it; (c) makes the rifle the defence (§24 risk 10) | provisional | Claude Code | 2026-09-04 | commit 042cfe0 (prompt B M1), on the human's `go` for prompt B M1 | §5 "light the edge". Was: made 2026-09-04 by recommendation (a) on the human's `go`. **Tile evidence, 2026-09-05 (`PROGRESS.md` T4, `EXPERIMENTS.md` `E-rifle-tile-rescue`):** the shade is not the fight at tile scale — **every one of the 48 rescue runs that loses a block loses it `unfed`, never to a shade**, and `E-hour` spawns **0 shades in six hours**. A whole ring 90 s dry never falls at all; at 600 s the rifle saves 3 of 7 and delays 3 more. That is the case for taking (a) off `provisional` — the caveat was that both are bot runs and that Gate B, which (b) was waiting on, had been waived. **That caveat is spent (Gate B, 2026-09-05).** The hour was played: the rifle was fired and decided nothing (*"I was just shooting at things"*), which is the play half of this row and agrees with the bots; **no shade was seen**, but the session did not reach minute 30 and §11 / E3 put the first shade at 32–47, so play neither confirms nor contradicts that half. What the gate does settle is **(b)**: STANDARDS row A.7 was answered *"Yea the dark is pretty good so far"* and **D-B5-1 (no hand lamp) stands, play-supported**, so a hand lamp on the engineer is off the table and **(a) is the only live option left**. Recommendation now: **take (a) as decided**. The row stays `provisional` until a human says so (rule 7); it blocks nothing |
| D-R2 | **The kit walk and C1/C2.** With the bots walking, first amber and first red land at the claim minute (16–31 min) because the claim kit walks from the chest. (a) keep the walk and rewrite C1/C2 to kitted edges; (b) a claim ships its kit by truck; (c) the kit is pre-stocked | (a); (b) is the truck's later life and (c) hides the tedium row the truck exists to fix | provisional | Claude Code | 2026-09-04 | commit 042cfe0 (prompt B M1), on the human's `go` for prompt B M1 | §25 changelog; `calibrate.ts` scores kitted edges only. Was: made 2026-09-04 by recommendation (a) |
| D-R3 | **The HQ's copper.** The city HQ fell at minute 27 on every calibration run until the block-only start patch gained 3,840 copper at 32/min; the tile layer's HQ copper patch is 1,200. (a) 3,840 block / 1,200 tile, reconciled at M6 with D-P4-4; (b) 1,200 in both; (c) the HQ face is residential | (a); M6 counts the hour's real copper bill | provisional | Claude Code | 2026-09-04 | commit 042cfe0 (prompt B M1), on the human's `go` for prompt B M1 | Was: made 2026-09-04 by recommendation (a); reconciled with D-P4-4 (still open) |
| D-R4 | **The generator's attempt budget.** E-variance found seed 3767 with no valid city after eight jitter streams; the validator rejects 26 % of attempts, so a budget of 8 leaves ~1 seed in 48,000 with no city. (a) raise `CITY_ATTEMPTS` 8 → 16; (b) keep 8 and restate the check; (c) relax the well-site search | (a): it changes no seed that already validates, because the loop breaks at the first valid attempt | provisional | Claude Code | 2026-09-04 | commit 27cc9b0 (rework) | Was: made 2026-09-04 by recommendation — a red check is a red build; the human can reopen it at Gate B |
| D-B1-1 | **What is in the start chest?** The game's Depot chest holds §11's 200 steel, 100 copper, 50 stone and 20 magazines; the harness keeps the calibrated 80 / 40 / 0 (config `5f3417b9`). Verification pass 2026-09-04: the two chests play different hours — the harness's held 3 with the minute-45 machines refused; the browser's held 2 with west and north refused for 0 steel. (a) keep both until M6; (b) harness on §11's numbers; (c) game on 80/40/0 | 200 steel, 100 copper, 50 stone, 20 magazines, 40 coal. The 40 coal are extra; Generator 1 also starts with its own hopper coal as now. `PROTO_CALIBRATED` keeps its old numbers (80 steel, 40 copper, 0 stone, 20 magazines), frozen; docsync must not compare them to the doc. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | §11 stock line. Was: Open — settles with D-P4-4. **Next actions line 3: the human writes this row.** — The tile-level chest is one START_CHEST (200 / 100 / 50, 40 coal, 20 magazines) for the browser and E-hour, so both play the same hour; the block-level calibration keeps its 80 / 40 / 0 (config hash unchanged; docsync still reports the pair, `GUARDRAILS_REPORT.md` §5 item 3) — `B-M6-hour` |
| D-B1-2 | **What the engineer walks through.** Belts and poles are passable, every other machine and the river are solid (`walk.ts`). (a) as built; (b) belts solid too; (c) belts carry the engineer | (a) until M2 puts a line across the HQ lot and a tester has to cross it; (c) is a system | recommended |  |  |  | Was: Open — settles at M6 |
| D-B1-3 | **Kits by hand.** The harness bot restocks kits on standing at the HQ; a human takes them from the chest (I), one a claim. (a) as built; (b) auto-restock the human at the HQ too; (c) the claim puts a kit in the pockets when made within reach of the Depot | (a): the walk to the chest is the tedium the truck removes (§19); (b) hides it from the audit | recommended |  |  |  | Was: Open — settles at the M6 tedium audit |
| D-B1-4 | **Placement by face geometry.** Substation: the buildable 3×3 nearest the lot's centroid. Streetlights: on the kerb along each segment every 4 tiles (GAME-ASSUMPTION), count from length — replaces "8 per edge". Turret kits: per segment, one per 16 tiles of segment length, minimum one (GAME-ASSUMPTION), on the segment's front ring — replaces the fixed six and is the rule-level fix for the HQ falling at minute 20. (a) as stated; (b) the substation on the street side; (c) the fixed six with an HQ branch | (a) | recommended |  |  |  | Built as `ground.ts` `faceSubstation`/`faceLights`, `flow.ts` `startTurrets`/`faceSegOf`/`edgeTurrets` (commit 906044c); supersedes D-P4-8. Was: made 2026-09-04 by the human ("Four small things before Prompt B M2", item 2). **The message is quoted; the human should add their name to close the row.** |
| D-B1-4-rider | **What is the block sim's 'edge hopper'?** | Not a separate constant; computed as (number of turrets on the segment) × 50 rounds. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | §13 Gun turret row and the block sim's hopper (`PROTO_CALIBRATED` / `DEFAULT_CONFIG.hopper`): docsync computes turrets-on-the-segment × 50 and compares that to the block sim (economy-fix task, Step 2). Question written 2026-09-04 by the human in the message that decided it |
| D-B1-5 | **Direct control.** WASD 8-way; Shift sprint 1.6× on a stamina bar that never drains below one dodge; Space dodge 3 tiles in 0.25 s with crawler invulnerability and a 1 s cooldown; left-click does what the hand holds; right-click removes; E interacts only; Tab/I pockets, B build menu, 1–9 hotbar (9 = rifle), R rotate, Q pipette, M map, Esc closes; no click-to-walk in the world view; the rifle hitscan to 9 tiles, 1.5-tile hit radius, no auto-target; §19 gains a 5 % time-in-danger guard. (a) as stated; (b) Factorio's layout; (c) without the dodge floor | (a): the layout is deliberately not Factorio's where it doesn't need to be, and the dodge floor is the whole point of having both sprint and dodge | recommended |  |  |  | Built as `body.test.ts` and the world view's hand/hotbar (commit 906044c). Was: made 2026-09-04 by the human ("Four small things before Prompt B M2", item 1). **Quoted and dated; the human adds their name.** |
| D-B2-1 | **Machine crafting.** §13 prices no machine and never says whether one is made before it is placed; M2 pays the lattice's rubble prices from the pockets at placement (GA-B2-1). (a) rubble at placement, as built; (b) a workbench recipe with a craft time; (c) machines stocked in the Depot and brought by the truck | (a) for the slice; (b) with the Phase 5 recipes, which also decide the prices | **decided** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05 on this row's recommendation:** (a) stands for the slice as built, and **(b) is what Phase 5 builds** — a workbench recipe with a craft time, priced by M1's recipe table, which also sets the prices §13 never gave. Lands at M1 (recipes) and M2 (the workbench). Was: recommended; the human decides **RI-01 2026-09-05 ((b)'s recipe half built):** the placed Assembler runs Shot, Wire, Frame or Board chosen per machine (`setRecipe`, T), each priced by `recipes.ts`; the recipes with no machine stay data (the generated §13 machines table). Machines are still paid in rubble at placement; the workbench craft time is RI-09 (T13). |
| D-B2-2 | **Slot count vs footprint (D-P3-10 measured).** `slotsOf` = floor(area / 600) gives one slot on every HQ-neighbour face (391–1,197 tiles) while the free footprint holds 4–9 §11-scale lines. (a) keep floor(area / 600) as C3's economic cap; (b) derive slots from free footprint; (c) drop slots when tile lines replace the block stand-in at M6 | (a) now, (c) at M6 — the slice measures the doc's economy as locked, and the stand-in goes with the lattice | provisional | Claude Code | 2026-09-05 | `PROGRESS.md` T11 (rule 14): this row was **not** in the human's list, so Claude took the recommendation provisionally to unblock the milestone that needs it. It stays provisional until a person signs it | **Provisional 2026-09-05, on this row's recommendation:** (a) now — `floor(area / 600)` stays C3's economic cap while the slice measures the doc's economy as locked — and **(c) at M6**, when tile lines replace the block stand-in and the slot count goes with the lattice. Was: recommended; the human decides |
| D-B2-3 | **What a pick-up carries.** §14 says "into a stack"; M2 returns the machine and each item type it held as their own stacks, a turret's rounds as whole magazines (GA-B2-2); a full pocket refuses. (a) as built; (b) contents spill as ground items; (c) contents are lost | (a); §11's "four stacks for two turrets" holds | recommended |  |  |  | Was: recommended; the human decides |
| D-B3-1 | **The craftable Substation's price.** §13.14 prices it at 20 frames + 20 wire + 10 boards, none of which exist before Phase 5; prompt B M3 pays 50 steel + 25 Cu in rubble (GA-B3-2). (a) as built; (b) free for the Electricians; (c) wait for Phase 5 | (a): it keeps the outskirts holdable in the slice at a price a §11 chest can pay once; Phase 5 swaps the number for the recipe | recommended |  |  |  | Was: made 2026-09-04 (the human): (a). No name, no message cited **RI-03 2026-09-05:** the stand-in price is now paid from the pockets when the Substation is placed on the Dark outskirts block (D-CU-3 (b)); the number is unchanged |
| D-B3-2 | **The outskirts at tile level.** §7 says an outskirts block has no substation and no streetlights; the tiles show that while the block sim still powers a Held outskirts block through its abstract substation. (a) keep the split through M5, settle with D-P4-9; (b) require the craftable Substation now (moves fixtures); (c) give the outskirts a substation, against §7 | (a) | recommended |  |  |  | Was: made 2026-09-04 (the human): (a) **RI-03 2026-09-05:** the split ends on the tile layer's claim path — an outskirts block is activated through its built Substation (D-CU-3 (b)), which powers it from then on; the legacy map claim keeps the abstract supply for the block-level bots and the compact snapshot (fixtures unchanged) |
| D-B3-3 | **When a group's unlocks land, and whether they stay.** §11 has the Electricians walk into the Depot at 30–60 min; the sim has no walk-in event. M3 unlocks on the held event and keeps the unlocks after the block falls (GA-B3-1). (a) as built; (b) lost with the block; (c) on a walk-in event | (a): a fall already costs the block; §11's walk-in is a story beat M6 can add over the same event | recommended |  |  |  | Was: made 2026-09-04 (the human): (a) |
| D-B3-4 | **Proportional brownout replaces the shed order.** When demand exceeds supply every machine on the grid runs at supply ÷ demand; nothing switches off, no substation is ever stopped by power; lights lit at any throttle above zero; belts and turrets never slowed. The §14 shed order, `SimConfig.shed`, `Block.shed` and the red cross are gone. (a) proportional; (b) the shed order with the Generator feed exempt; (c) proportional for machines, shed order for substations. Evidence `E2-sustained6h-h500-*`, `E2-matrix`; snapshot config `5f3417b9` → `68d07000` | (a): legible, cannot cascade, and the sim confirms the ring starves slowly behind the red pip | recommended |  |  |  | §14 rewritten (commit d332464). Was: made 2026-09-04 (the human); built. **The human's specification is the message; the human adds name and quote to close the row.** |
| D-B4-1 | **The rifle's button.** Prompt B M4 says "fired with the right mouse button held"; D-B1-5 put the rifle on the left click with the cursor aiming, and that is what M4 built. (a) keep D-B1-5; (b) the prompt's right button; (c) left fires only while R is held | (a) — D-B1-5 stands; placement and the shot never collide because the shot needs the rifle raised | provisional | Claude Code | 2026-09-04 | commit 7ba2b9b (prompt B M5), on the human's `go` for M5 | Was: taken by recommendation (a) on the human's `go` (M5) |
| D-B4-2 | **The threat constants** (`threat.ts` header, GA-B4-1): 4 HP a round, arm's reach 1.2 tiles, danger 3, "stands in its path" 0.9, a lamp eaten from 1.6, a stuck body gone after 30 s. Measured: fed turrets 3.0 rounds a kill, the 40th arrival 68 s after a 10-crawler bloom on a dry edge. (a) as built; (b) path 1.5; (c) stuck 10 s | (a) until M6's playtest gives the feel a number; (b) first if retaliation never happens in play | provisional | Claude Code | 2026-09-04 | commit 7ba2b9b (prompt B M5), on the human's `go` for M5 | Was: taken by recommendation (a) on the human's `go` (M5) |
| D-B4-3 | **The turret in the chain.** §7 reads "nearest lamp, then turret, then the substation"; built, the turret is an unharmed waypoint (GA-B4-6). (a) a waypoint; (b) contact damage on the turret (a machine HP number); (c) drop the turret from the chain | (a); edit §7 to "then past the turret" if chosen; (b) is Phase 5's barricade chain | provisional | Claude Code | 2026-09-04 | commit 7ba2b9b (prompt B M5), on the human's `go` for M5 | §7. Was: taken by recommendation (a) on the human's `go` (M5). **Cleanup 2026-09-05:** §22 still says turrets can be chewed; it now labels the slice's unharmed waypoint as the implementation limitation and this row as provisional. **RI-00 2026-09-05:** `REVISED_DEVELOPMENT_PLAN.md` §7.2 — "Use the existing agreed turret-damage behaviour until RI-10 reconciles it explicitly"; RI-10 signs or reverses this row |
| D-B5-1 | **The hand lamp.** Prompt B M5: the engineer carries no light — a 2-tile radius if taken. Built: none; `?handlamp=1` previews a disc, drawing only (GA-B5-7). (a) none; (b) a 2-tile lamp, drawing only; (c) a lamp that counts for the shade rule. STANDARDS row A.7: Gate B's sentence after playing dark | (a): the dark is what the claim pays off; a lamp on the sprite makes every unlit street safe to cross and takes the Lamp's job | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): none for the gate; the preview on the second play | Was: taken by recommendation (a) on the human's `go` for M6 |
| D-B5-2 | **The sequence's pace.** §6: streetlights come on at three per second; prompt B M5: the burn-off sweeps over 20 + 60·d s. Built to §6 with the rot's specks sweeping over the burn-off. (a) §6's three per second; (b) evenly over the burn-off; (c) three per second for streetlights, the lot's Lamps at the end | (a): the lights are the claim's instant reward and the fading mottling is §6's own picture | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): (a) | §6. Was: taken by recommendation (a) on the `go` for M6 |
| D-B5-3 | **The repair cost and the unlit level.** `REPAIR_COPPER` 1 Cu a light (GA-B5-5); the unlit texel `[62, 66, 98]` ≈ 25 % with a cool cast (GA-B5-1). (a) as built; (b) 2 Cu and 20 %; (c) free and 35 % | (a) until M6's played hour says whether the copper runs short and whether the dark reads | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): (a) | §4, §13. Was: taken by recommendation (a) on the `go` for M6 |
| D-B5-4 | **How far does a streetlight reach?** Built at radius 4 (`light.ts`), a powered face's kerb row was 50–68 % lit and the half-street 26–41 % (B.15); the shared street's midline is ~7 tiles from the kerb. (a) radius 7, to the street midline, so a held face lights its half of the shared street; (b) radius 4 as built; (c) radius 8, the whole street from one face | 7 tiles (reaches the middle of the street; a Held block lights its own half). Broken streetlights are still repaired by hand (D-B5-3). | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | Built (`constants.ts` STREETLIGHT_RADIUS 7; the Lamp keeps 4): kerb 65–93 %, half-street 44–65 %, lot 44–50 % lit on seeds 3 / 4 / 5; `light.test.ts` thresholds moved to kerb > 0.5, half-street > 0.4, lot < 0.6 — `B-M6-light`, unverified |
| D-B6-1 | **The hands in the hour.** The hour bot feeds turrets and Generators from the pockets with real trips to the chest (GA-B6-1); M3's `botHands` stays with the policy bots. (a) as built; (b) `botHands` for the hour bot too; (c) no bot hands anywhere | (a): Gate B's walking row needs the trips, and the player has no other hands | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): (a) | Was: open |
| D-B6-2 | **The claim minutes.** §11 gives windows (east 10–30, north 30–60); the bot claims on the calibration's clock 15 / 25 / 40 (GA-B6-2). Verification pass 2026-09-04: the bot was never idle for the clock; (c) would only move the claims earlier, into D-P4-4's steel shortfall. (a) 15 / 25 / 40; (b) 10 / 20 / 30; (c) when 60 magazines are banked | (a) | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): (a) | Was: open — the bot's clock is now east 15 / west 25 / north 65 (D-P4-10); §11 carries the minutes |
| D-B6-3 | **The replay's ground.** Gate B's "did it matter?" replays the command log with the aim commands dropped and judges every hand-fired fight by its block's end state in both runs (`replayVerdict`, GA-B6-5; fixed on the pass to compare block end states: seed 5 reads "fell anyway", seeds 3 / 4 "held anyway"). (a) per fight and the HQ; (b) the HQ's fate alone; (c) the fight's edge only | (a) with the fix | decided | Daniel | 2026-09-04 | the chat message of 2026-09-04 ("Gate B is not scored on this hour…", quoted in `SLICE_REPORT.md` "Economy fix"): (a) | Was: open |
| D-SA-1 | **Controller and Steam Deck.** Only Factorio of the seven references has native controller support; reviews forgive its absence unless the store page promises it (`STANDARDS.md` 1.19). (a) none at launch, Steam Input layout and a Deck pass in Phase 13; (b) native from Phase 5; (c) never. Cost of deciding late: panels rebuilt for focus navigation after Phase 12 | (a) | recommended |  |  |  | Was: open (with a recommendation) |
| D-SA-2 | **Localisation.** Every shipped reference has ≥ 9 languages; absence is a store filter (`STANDARDS.md` 8.5). (a) a string table in Phase 5, translation bought in Phase 13; (b) the table, English only; (c) decide at Phase 13. Cost of deciding late: a week's sweep of every literal in Phase 13 | (a); (b) is the floor | provisional | Claude Code | 2026-09-05 | `PROGRESS.md` T11 (rule 14): this row was **not** in the human's list, so Claude took the recommendation provisionally to unblock the milestone that needs it. It stays provisional until a person signs it | **Provisional 2026-09-05, on this row's recommendation:** (a) — a string table in Phase 5 (`STANDARDS.md` 8.5 is a Phase 5 build-with row), translation bought in Phase 13; (b), English only, is the floor if Phase 13 is cut. Was: open |
| D-SA-3 | **Mod support.** All seven references have it at maturity; none was punished at launch (`STANDARDS.md` 8.7). (a) a data-only surface, no scripting; (b) scripting with `step` exposed; (c) none. Cost of deciding late: none if no scripting is decided now | (a): (b) is a fourth system by proxy and a determinism risk for D-SA-4 | recommended |  |  |  | Was: open |
| D-SA-4 | **Multiplayer.** The most-requested and least-punished absence (`STANDARDS.md` 8.8); `step` is deterministic and the M6 command log replays. (a) no at launch, a determinism check in Phase 11's DoD; (b) co-op from Phase 5; (c) never | (a): lockstep stays a door for the price of one test | recommended |  |  |  | Was: open |
| D-SA-5 | **Screen-feedback intensity and its toggle.** Mindustry ships a 0–4 shake slider; DSP was marked down for shake that cannot be turned off (`STANDARDS.md` 5.6). (a) low by default with a 0–4 slider; (b) full with the slider; (c) none. Cost of deciding late: Phase 12's art brief written twice | (a): the light map is the identity and shake fights it | recommended |  |  |  | Was: open |
| D-ENGINE-1 | **If the Phase 11 performance gate fails, which engine do we port to?** The constitution said Unity in one place and Godot in another; no decided row existed. (a) C# sim, Godot renderer; (b) C# sim, Unity renderer; (c) stay in TypeScript and cut the targets | Godot. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | `CONSTITUTION.md` Engine and layout. Added by the guardrails task, Step 1 (commit 15eca71) |
| D-INSERTERS-1 | **How many inserter tiers?** The constitution's Phase 5 said "three inserters"; §13 lists one Inserter row; STANDARDS row 2.5 asks only for a filtering primitive. (a) one, the filter on it; (b) one plus a filter inserter; (c) three | One. §13 already says one. A faster inserter may be added later as found tech only if E10 shows a chain that cannot reach its rate. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | `PHASES.md` Phase 5 `[open: D-INSERTERS-1]`; §13 when Phase 5 opens. Added by the guardrails task, Step 1 (commit 15eca71) |
| D-HOUR-1 | **Where does the §11 minute list live?** | In `packages/sim/src/constants.ts`, constant `HOUR`; the §11 minute table in the design doc is generated from it by docsync. Minute list: Shot assembler line at minute 8; Generator 2 at minute 6 (after the coal Excavator); Generator 3 at 15; east claim at 15; second steel Excavator at 15; west claim at 25; north claim at 40; Generator 4 at 45. §11 prose '10 to 30 minutes' / '30 to 60 minutes' stays as prose; the generated table is the rule. | decided | Daniel | 2026-09-04 | message "fix the eight constant disagreements" of 2026-09-04 | §11 generated minute table (docsync `hour` block) and `constants.ts` `HOUR` (economy-fix task, Steps 2 and 3). Question written 2026-09-04 by the human in the message that decided it |
| D-HOUR-2 | **The minute-15 steel cluster.** On the decided minutes (D-HOUR-1) the second steel Excavator's kit, Generator 3 and east's claim kits all draw on the chest at 15:00–15:05, and chest steel reaches zero at 15:02–15:04 on every seed (`B-M6-hour-2`, `ECONOMY_FIX_REPORT.md` §4), so the no-fall hour's second condition (chest steel never zero) fails there even in the two-claim variant. The arithmetic, from Step 6 (2026-09-05, `ECONOMY_FIX_REPORT.md` §8.3b): 200 (D-B1-1) − 110 at 6:00 − 48 at 8:00 − 12 (the second steel Excavator) − 30 (Generator 3) = **0 exactly**, so the decided start stock is the decided minute list's bill to minute 15 with no margin, and no bot behaviour can change it (pocket steel at 15:00 is 2). In the browser (the 900 s headless soak, seed 3, rifle on) the cluster is harder still: the east claim is refused at 15:06 with 2 steel in the chest and never retried, so east is never Held (`ECONOMY_FIX_REPORT.md` §6). (a) the second steel Excavator at 12 (GA-EF-1's provisional value; steel min 30 at 12:01 under `B-M6-hour`); (b) Generator 3 or east's claim a few minutes after 15; (c) more start steel in the chest (`START_CHEST.steel`) | **(a)**: the only option already measured green; every option changes a decided minute or a start stock, so the human writes the row | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Do the blockers"), taking this row's recommendation | §11 10–30. **Built 2026-09-05**, option (a): `constants.ts` `HOUR` step `steel-2` minute 15 → **12**, §11's generated minute table regenerated by docsync, and GA-EF-1 (the provisional 12:00) is now this row rather than an assumption. Measured: chest steel bottoms at **30 at 12:01–12:02** on every one of the six runs, where the decided minutes gave 0 at 15:02–15:04; the browser's 15:06 claim refusal has no cluster left to fail in. Was: recommended — M6 arithmetic |
| D-GB-1 | **Light earned, or light granted?** (Gate B, Daniel, 2026-09-05.) The doc's loop is claim on the map → the block burns off over `20 + 60·d` s → the streetlights come on as the payoff (§5, §10, §11, M5). What was asked for is the opposite direction of causation: *"the lighting system should not be tied to the map. The player should have to go out and power them themselves"* — defend, mine, automate, build generators, and when there is enough power **walk poles and lamps out to the next area yourself**; the area is attacked *because* it is lighting up, "sometimes no attack". *"At the moment the player character doesn't do much of this. I want the player to feel like they're exploring more."* This is not a constant and not a tune: it moves the payoff from a consequence of a map click to a thing the player physically builds, and it makes light the **cause** of threat rather than its cure (today rot cannot exist on a lit tile). | **Decide the game before Phase 5 opens, and expect it to reorder the programme.** The two loops are both coherent and cannot both be built. Options: **(a)** keep the doc's loop, treat this as Phase 6+ flavour (cheapest, and makes the last six weeks' slice the product); **(b)** adopt it as the loop — §5, §10, §11, §19 and M5 are rewritten, the vertical slice is re-scoped, and Phase 5's factory work waits behind it (most expensive, most aligned with what the human wants to play); **(c)** a hybrid — the claim still burns off the block, but **powering and lighting the next area is a physical expedition the player walks**, which keeps §5's rule and adds the exploration; the attack-on-lighting-up becomes a threat trigger alongside the wake bloom. **Recommend (c)**: it is the only one that keeps the measured slice standing while answering the actual complaint, which is that the engineer doesn't do enough. | **decided (c), the hybrid** | Daniel | 2026-09-05 | Gate B, the four questions of `GATE_B.md`; option (c) chosen directly, alongside the verdict "proceed with conditions". **What (c) commits to:** the claim still burns off its block, so §5's rule, §11's hour and every measured number in `SLICE_REPORT.md` stand; **powering and lighting the next area becomes a physical expedition the engineer walks** — generators earned, poles and lamps carried and placed out to the next area's power box — and **an area lighting up becomes a threat trigger** beside the wake bloom, sometimes drawing an attack and sometimes not. **Not yet built, and not built by this row**: it is Phase 5 / 6 work and a condition of the gate, not of it. **Doc sentences it will move when built**: §5's light rule (light as cause of threat as well as its cure), §10's loop, §11's hour (the expedition beat), §19's hands test (the engineer's share of input rises). Each moves with a changelog line under rule 1, on the milestone that builds it, not before | §5 "light the edge", §10 the loop, §11 the hour, §19 first-hour test; `GATE_B.md` "the extra rows" |
| D-GB-2 | **"Knowing what to do next."** (Gate B, Daniel, 2026-09-05.) The one problem named in minutes 0–10: *"Knowing what to do next. Knowing what is needed next. Maybe we need a small quest system?"* §19 predicts that minutes 0–10 teach two rules (blocks bloom; ammo is made from rubble); neither was named back. **Conflicts with constitution rule 8** — "No tutorial screens. §11 is the tutorial; every rule surfaces as a toast, a tooltip, a pip, or a thing happening on screen" — and with `STANDARDS.md` 4.1, which put tutorial screens in `## Considered` for that reason. | **(a) Diagnose before prescribing.** The complaint is real and the prescription conflicts with a standing rule; those are separable. The rule-8-compatible forms are: a **next-objective line in the HUD** driven by the sim's own state (no screens, one line, always the truthful next constraint), the **claim tooltip's `front +N · closes N`** made louder, and the §11 beats surfaced as toasts that currently fire but were not noticed. **(b) A real quest system** — a list, steps, completion — which is a rule-8 change the human must take explicitly. **Recommend (a)** first and re-ask at the next session: if a HUD objective line does not fix it, (b) is justified and rule 8 changes with a changelog line rather than being quietly ignored. **A Gate B condition, to be resolved inside Phase 5.** | provisional (a) | Claude Code | 2026-09-05 | `REVISED_DEVELOPMENT_PLAN.md` §11.2 ("Prominent current goal with the reason it matters"), an implementation default adopted on "Whole plan" (D-RI-6); built by RI-02 | Constitution rule 8; §19 first-hour test; `STANDARDS.md` 4.1; `GATE_B.md` §19 table Was: open (gate condition). **RI-00 2026-09-05:** the plan's §11.2 goal line is (a), and its §2 / §3 rule out a dialogue-tree system and generic quest tokens; provisional under rule 14 — sign or reverse; `PROGRESS.md` RI-02 builds it (T12a waived into it) and T19 records whether a person followed it. **RI-02 2026-09-05:** (a) is built — `goal.ts` `currentGoal`, one HUD line with its reason and an amber support line for immediate shortages, read off the state; never wrong on 13,503 one-second samples and changed at 42/42 §11 beats in `E-hour` on seeds 3 / 4 / 5 (`RI_PASS_1_REPORT.md` "RI-02"); rule 8 unchanged; the claim tooltip and the beat toasts now name blocks by their stable names. Still provisional: T19 records whether a person followed it; (b) stays the human's |
| D-GB-3 | **Rifle range.** (Gate B, Daniel, 2026-09-05.) *"I'd want the range increased or have no range."* Today the rifle is a retaliation weapon at close quarters and E-rifle's rescue is the engineer standing on the dry street midpoint. Range is not a tagged constant in the doc; it is in the sim. | **Raise it, do not remove it, and re-run E-rifle before believing any number.** An unbounded rifle makes the turret irrelevant on the segment the player stands on, which is the mechanism §19's 10 % shooting cap exists to protect. A raise is cheap to try and E-rifle measures the consequence directly: the tile rescue is the run that would move (today: 3 of 7 falls saved, mean 38.7 HP, the busiest rescue 96 of 200 rounds). **Unblocked 2026-09-05**: D-GB-1 was decided as the hybrid, so the rifle keeps its job (a rescue) and gains an expedition to be carried on — range is now a tune, not a loop question. **A Gate B condition, to be resolved inside Phase 5.** | open (gate condition) |  |  |  | §16 the rifle; §19's shooting cap; `EXPERIMENTS.md` `E-rifle-tile-rescue`. **RI-00 2026-09-05:** `REVISED_DEVELOPMENT_PLAN.md` §8 — reuse the current rifle; range variants belong in a separate candidate configuration once the expedition exists (RI-08 tests them, D-RI-5). Still a gate condition; holds up nothing |
| D-GB-4 | **Enemy density, and whether the player's weapon should matter.** (Gate B, Daniel, 2026-09-05.) *"We need more enemies when the base is being attacked. More roaming enemies as well when the player is moving so the player's weapons mean something."* **This is the sharpest conflict the session produced.** §19 caps the player's shooting at 10 % of an hour and states the resolution explicitly: if a playtest shows more, **"the turret rules are wrong, not the rifle"**. D-B1-5 caps time in danger at 5 %. The measured hour is 0.50 / 0.69 / 0.06 % shooting and 0.00 % danger — a fifteenth to a two-hundredth of what is allowed — so **there is enormous headroom inside the existing caps**; the ask can be granted without touching them. | **(a) Spend the headroom before changing the rule.** Roaming crawlers between blocks, and a bigger bloom on a base attack, can multiply the player's trigger time twenty-fold and still sit inside §19. That is a §7 density change measured by E-rifle and E3, not a rule change, and it is the cheapest way to find out whether "weapons meaning something" is really a density problem. **(b)** If the human wants the player to be the primary defence, that is §19's own trigger — the turret rules change, not the caps — and it is D-GB-1's question again. **Recommend (a)**, measured, then re-ask. **A Gate B condition, to be resolved inside Phase 5**; it needs no permission, only a density number and a run. | open (gate condition) |  |  |  | §7 densities; §19's two caps; D-B1-5; `EXPERIMENTS.md` `E-rifle-tile-steady`. **RI-00 2026-09-05:** owned by RI-04 (emergence points, the Stalker) and RI-10 (the roster); `REVISED_DEVELOPMENT_PLAN.md` §8: "A low shooting percentage does not automatically authorise increased enemy density, new roaming rules or unavoidable combat." Still a gate condition; holds up nothing. **RI-04 2026-09-05:** emergence points and the Stalker candidate are built (§7); the benchmark's density is unchanged — the candidate sits behind `enableStalkers` / `?stalker=1` (D-RI-5) and `E-rifle-cand-stalker` measured no contact in the rifle-on hour on seeds 3 / 4 / 5 (the hour bot never nears a well block); the tile rescue's damage numbers moved with the emergence-point geometry (`E-rifle` 16/16). Still open: a density number is the human's |
| D-LP-1 | **The opening zoom.** `ROADMAP.md` §2's layout row (now `archive/pre-phase-5-cleanup/ROADMAP.md`) asks for "default zoom with the HQ lot ≈ one third of screen height"; neither the design doc nor any decided row carries it. Built as `HQ_SCREEN_FRAC = 1/3` clamped to the constitution's 0.5–3× (D-P4-1), drawing only. Measured from the code on the D6 seeds (`LAYOUT_PASS_REPORT.md` §4): on a 1040 px canvas the fit **clamps to 0.5×** and the lot fills 46 % (seed 3), 71 % (seed 4), 45 % (seed 5); the third is reached only at ~2000 px, and **seed 4's 46-tile HQ lot never reaches it at any size**, because §4's lot is 24 × 24 and a D6 lot is 29–46 tiles. (a) as built — a third, clamped, best-effort; (b) fit the whole lot plus one street on every side, whatever fraction that gives; (c) drop the rule, back to a fixed 1× | **(a)**: the clamp is the constitution's and the third is the ROADMAP's, and on a 1080p screen with a D6 lot they cannot both hold; (a) is the closest the zoom range allows and costs nothing to change. If the human wants the rule to be true rather than best-effort, (b) is the one that can be | recommended |  |  |  | `ROADMAP.md` §2 layout row ("one third of screen height"), if the human keeps the number |
| D-LP-2 | **The key strip's corner.** `ROADMAP.md` §2 (archived, same path) names four HUD corners (territory top-right, power and pockets bottom-left, speed and clock bottom-right, toasts bottom-centre) and the key strip is not one of them; STANDARDS row 4.3 requires only that it be on the HUD rather than in a tooltip. Built into the corner left over — top-left, with the in-hand line. (a) as built; (b) it hides after the first ten minutes or on a key; (c) it moves into a `?` overlay and top-left carries the in-hand line alone | **(a)** now, **(b)** at Phase 12's onboarding: 4.3 is met either way, and a strip that never leaves is the thing Phase 12 will want to fade. The check itself (4.3) is still open — T5 was waived, not run | recommended |  |  |  | `ROADMAP.md` §2 layout row, if the corner list is made five |
| D-LP-3 | **The soft light falloff.** §2 asks for "soft light falloff"; §4 says the unlit tile is "desaturated and darkened to ~25 %" and §6 / §7 treat lit as binary, so the doc carries no falloff. Built as a render-side two-pass [1,2,1]/4 blur over the lit mask with `LIGHT_SOFT = 0.7` — **the sim's lit set is untouched** (`lightPix` binary, `lightMask` / `litAt` unchanged), so what a shade may stand on and what burns off are the same. It touches the identity: the dark is what the claim pays off (D-B5-1's own reason), and a soft edge reads as less dark. It is also the pass's only frame-cost risk (7–12 ms of arithmetic per repaint, eight repaints a second, unverified in a browser). (a) as built at 0.7; (b) hard edge — revert the blur, the binary map is the look; (c) softer still — a wider blur or `LIGHT_SOFT` towards 1 | **(a)**, and this is the row that most wants a human eye: it is a look judgement, one constant and one loop to reverse, and (b) also deletes the pass's frame-cost item from the next verification pass | recommended |  |  |  | §4's "~25 %" sentence, only if (c); (a) and (b) leave the doc alone |
| D-P5-1 | **What a brownout looks like on the map view.** `PHASES.md` Phase 5 asks for "the dimming on the map view" as part of power complete. **No sentence in the design doc says it, and §14 says the opposite twice**: *"Lights do not dim: a Lamp, Floodlight or streetlight is lit at any throttle above zero and dark only on a dead grid"*, and the brownout's whole legibility argument is that *"every machine visibly slows, and the HUD shows one bar, demand over supply and the percentage everything runs at"*. §4's map-view vocabulary has ten entries and no throttle state; §24 risk 4's brownout icon sits on the substation and marks a **fall**, not a throttle. (a) no map-view dim — the HUD bar and the machine panel carry it (2.6 / 2.7, this phase); (b) a discrete mark per block in the pip idiom, the way the supply pip already works; (c) actual dimming, and §14's "lights do not dim" is rewritten with a changelog line | **(a)**, with **(b)** if the human wants a map-view read: brightness on that view already means rot density and Held / Interior state, so a third meaning on the same channel is the one change that makes §4's "fixed vocabulary, learned in the first 30 minutes" untrue. (c) is a doc rewrite that costs §14 its most-quoted sentence for a read the HUD already gives | **decided (a)** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05: no map-view dim.** The HUD bar (demand over supply and the percentage everything runs at) and the machine panel carry the brownout read; §14's "lights do not dim" and §4's ten-entry map-view vocabulary are untouched, so this row costs no doc edit. Builds at M4 with `STANDARDS.md` 2.6 / 2.7. §14's dimming rule and §4's map-view table, only if (b) or (c). Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-2 | **Which §13 machines are Phase 5's.** `PHASES.md` asks for "the §13 machine list with footprints and nothing outside it". §13 is the **whole game's** machine list, and six of its rows unlock in later phases: Cannon (Arsenal — Phase 6), Barricade and Mixer (Concrete crew — Phase 6 / 7), Pumpjack (Refinery — Phase 7), the six facilities (Phase 7), Line truck garage (Foreman — Phase 8). Building all of them in Phase 5 pulls three phases forward; the sharpest case is Concrete, which §12 lists among the eight intermediates Phase 5 owes but whose only machine is found tech. (a) read it as the second sentence — Phase 5 builds every §13 row whose unlock is `start` or belongs to a Phase 5 system, and invents no machine outside §13; each remaining row ships with the phase that unlocks it; (b) build every §13 row now and let the unlock gate hide them; (c) move the later unlocks' machines into Phase 5 and shorten Phases 6–8 | **(a)**: it is the reading that leaves the phase list intact, and it costs nothing — the recipes already exist in `recipes.ts`, so Concrete is generated data that becomes makeable when the Mixer arrives. (b) builds six machines with no way to test them in the phase that builds them | **decided (a)** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05:** Phase 5 builds every §13 row whose unlock is `start` or belongs to a Phase 5 system, invents no machine outside §13, and each remaining row — Cannon, Barricade, Mixer, Pumpjack, the six facilities, Line truck garage — ships with the phase that unlocks it. Concrete is generated recipe data that becomes makeable when the Mixer arrives. `PHASES.md` reworded at T11; §13's unlock column unchanged. `PHASES.md` Phase 5, and §13's unlock column if the phase boundaries move. Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-3 | **One belt lane, or §14's two.** §14 opens "Standard two-lane belts"; `flow.ts` has had one lane since M2. `STANDARDS.md` row 2.1 records the disagreement and routes it here in as many words: *"the human settles §14 vs `flow.ts` when Phase 5 opens"*. It is also the one standards row where the baseline **diverges** — Factorio alone of the seven references has two lanes. (a) one lane, and §14's sentence changes with a changelog line; (b) two lanes, and `flow.ts`, every splitter rule and every side-load rule are rewritten to carry them; (c) one lane now, two as a later belt tier | **(a)**. Two lanes exist to let one belt carry two inputs to one machine; §13's recipes take at most two inputs (Board: 3 wire + 1 steel) and the doc already solves that with a second belt and a second inserter, on 8-wide streets with room for both. Two lanes double the belt state and every rule that touches it for one recipe's convenience. But it is a doc sentence, so the human writes it — and it decides how much of M3 exists | **decided (a), one lane** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05: one lane.** §14's opening sentence rewritten at T11 with a changelog line — `flow.ts` was right since M2 — and `STANDARDS.md` 2.1 closes as a deliberate divergence from Factorio (the only reference of the seven with two lanes). Two inputs to one machine are served by a second belt and a second inserter on 8-wide streets. M3 builds one lane. §14's opening sentence, if (a). `STANDARDS.md` row 2.1. Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-4 | **What "blueprints from minute one" means.** Three documents disagree. §14 and §19 say *"Blueprints and copy-paste from minute one"*; `PHASES.md` puts blueprints in **Phase 8** ("blueprints with ghosted delivery"); Phase 7's dealbreaker-1 row makes them a **found unlock** ("the survivor who brings them", GA-EF-3 — the survivor panel already shows the "not yet found" row). `STANDARDS.md` 1.11 records the same and says "reported, not resolved". Phase 5's own DoD needs "a blueprint file" for the bot. (a) split the word — the **bot's blueprint file** is a harness input (a JSON the experiment runner reads), built in Phase 5 and needing no in-game item; the **in-game blueprint** stays a found unlock (Phase 7) built in Phase 8, and §14 / §19's "from minute one" is the sentence that changes; (b) blueprints really are available from minute one, and Phase 7's dealbreaker row and Phase 8's line both change; (c) the DoD's bot builds from a hand-written script instead and the word "blueprint" leaves Phase 5 | **(a)**: it meets the DoD on its own, invents no game rule, and leaves the survivor-gate that three later phases are built on. (b) is the most expensive answer in this list — it removes a found unlock that Phase 7 uses as its example of one | **decided (a), the word splits** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05:** the **bot's blueprint file** is a harness JSON the experiment runner reads, built in Phase 5 for the DoD, with no in-game item behind it; the **in-game blueprint** stays Phase 7's found unlock ("the survivor who brings them", GA-EF-3) built in Phase 8. §14, §19's tedium audit and §24 risk 1's mitigation rewritten at T11 with a changelog line; `STANDARDS.md` 1.11 closes with the gate named. §14 and §19's "from minute one", if (a) or (c); `PHASES.md` Phases 7 and 8, if (b). `STANDARDS.md` row 1.11. Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-5 | **Where the filter lives.** `STANDARDS.md` 2.5 is a Phase 5 build-with row and asks for "a filtering primitive somewhere on the line" (the check: an item can be pulled off a mixed belt in a minute). D-INSERTERS-1 decided one inserter and no tier ladder, so the row's own note ("which of the three inserters filters is the human's") is stale — and **§13 carries no filter sentence at all**. (a) a per-inserter filter setting, no cost and no new machine — §13's Inserter row gains a clause; (b) a filter on the splitter instead, so the belt does the sorting; (c) no filter in Phase 5, and 2.5 stays a named gap with a row | **(a)**: it is the smallest sentence that satisfies the standard, it keeps the one-inserter decision intact, and every reference in the baseline puts the filter on the arm rather than the belt. (b) is defensible but makes the splitter carry two rules (priority and filtering) in the phase that first builds it | **decided (a)** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05:** a per-inserter filter setting — no new machine, no tier ladder, D-INSERTERS-1 intact. §13's Inserter row gains the clause at T11 with a changelog line; `STANDARDS.md` 2.5 closes. Builds at M3. §13's Inserter row, when it is decided. `STANDARDS.md` row 2.5. Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-6 | **The tram line on the map view.** `PHASES.md` asks for tram "lines drawn on the map view" and `STANDARDS.md` 2.11 (converged 6/7) asks for long-haul transport "with lines drawn on the map". §4's map-view table is a **fixed vocabulary** of ten entries: it draws **Trams** as "moving rectangles on the street lines" and **Belts** as "thin lines with moving dashes", and there is no tram *route* — the line between a tram's two stops — anywhere in it. (a) draw the route as a thin line in the belt's idiom and add the row to §4's table with a changelog line; (b) no route line — the moving rectangle is the read, and 2.11 is met by the tram itself; (c) the route appears only while a tram stop is selected | **(a)** or **(c)**, and (c) is the one that keeps §4's vocabulary at ten for the reader who is not looking at trams. It invents no model — one tram, two stops is already the doc's — but §4's table is called "learned in the first 30 minutes", so a new permanent row in it is a doc change and not a rendering detail | **decided (c)** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05:** the route line appears **only while a tram stop is selected**, so §4's fixed vocabulary stays at ten entries and no doc sentence changes now. The sentence lands at M5 with the trams; `STANDARDS.md` 2.11 is met by the moving rectangle plus the selected-stop route. §4's map-view table, when it is decided. `STANDARDS.md` row 2.11. Added 2026-09-05 by the Phase 5 opening (rule 12) |
| D-P5-7 | **Phase 5's experiment names.** `PHASES.md` names the phase's three runs **E10 chain throughput**, **E11 coal depletion**, **E12 tram sufficiency**. `E10` is already taken: the retired Python `E10-bloom-cadence` is cited in §25 item 1, C8, D-P3-5 and D-P3-11, so a `[sim: E10-…]` tag would be ambiguous in the one section that already carries one. (a) rename to the tile-era convention the harness has used since M6 — `E-chain`, `E-coal`, `E-tram`, and `PHASES.md` corrected; (b) keep E10–E12 and rename the retired run to `E10-old` everywhere it is cited; (c) keep both and rely on the suffix to disambiguate | **(a)**: every experiment added since the tile rework is named `E-<thing>`, the numbered series stopped at E9, and (b) edits four citations of a run that no longer exists to protect a number nothing needs. Nothing is tagged `E10` until this is settled | **decided (a)** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | **Taken 2026-09-05:** `E-chain`, `E-coal`, `E-tram`, the tile-era convention the harness has used since M6. `PHASES.md` corrected at T11; `EXPERIMENTS.md` when the runs exist; the retired `E10-bloom-cadence` keeps its name in §25 item 1, C8, D-P3-5 and D-P3-11, and nothing new is tagged `E10`. `PHASES.md` Phase 5's experiment line and `EXPERIMENTS.md`, when it is decided. Added 2026-09-05 by the Phase 5 opening |
| D-GB-1-rider | **Which phase builds D-GB-1's hybrid.** D-GB-1 is decided (c) and not built: the claim still burns off the block, but powering and lighting the next area becomes a physical expedition the player walks, and lighting up becomes a threat trigger beside the wake bloom. It has no phase. It is the only inherited item that **changes** the loop rather than completing it, and it moves §5, §10, §11, §18 and §19. (a) Phase 6, with the threat trigger it contains; (b) inside Phase 5 as its own milestone and its own doc-edit pass; (c) later than Phase 6 | **(a) Phase 6.** Phase 5 is already six weeks of completing §12–§14; putting a loop change inside it means the phase that finishes the factory is also the phase that moves five sections of the doc. If it ever goes in Phase 5 it wants its own milestone, never a corner of M4 | superseded | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | Was: **decided (a), Phase 6** (Daniel, 2026-09-05). **Superseded 2026-09-05 by D-RI-4**: the hybrid is RI-03 / RI-05 / RI-06's content, defined by `REVISED_DEVELOPMENT_PLAN.md` §4–§9; Phase 6 no longer carries it. `PHASES.md` Phase 6, when Phase 6 opens; §5, §10, §11, §18, §19 when it is built. Added 2026-09-05 by `PROGRESS.md` T11 (`PHASE_5_REPORT.md` decision 1) |
| D-HOUR-3 | **The hour's length: 60 minutes or 75.** §11's "30–60 min" paragraph describes minute 65 — north is claimed at 65:00 by D-P4-10, past the hour on purpose — and both of §11's closing beats (the HQ's white border, the Electricians walking into the Depot) are gated on north, so a 3,600 s run can never reach them. That is what Gate B missed. `E-hour-north` already runs north at 65:00: Held at 65:29–65:34 and standing to 75:00 on every seed **[sim: B-M6-hour-north]**. `DEFERRED.md` parked it here: *"if the hour is ever re-scoped to 75 minutes, north's claim becomes a scored check and wants its own DoD line."* (a) re-scope hour one to 75 minutes, with north's claim a scored check and its own DoD line; (b) keep 60 and split the 65-minute beats under their own §11 heading; (c) keep 60 and drop the beats from §11 | **(a)**: the variant is already measured green on every seed, it turns the enclosure, the Electricians and the burn-off payoff from promises into scored checks, and it answers three of `PHASE_5_REPORT.md` §3d's observations at once. (b) is honest but leaves the next played hour missing the same beats Gate B missed | **decided (a), 75 minutes** | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Go with your recommendation for all"), taking this row's recommendation | §11's window headings and §19's first-hour test, **at M1** — §11's minute table is generated by docsync from `constants.ts` `HOUR`, so the re-scope is a code change first and a doc change by regeneration. North's claim gains a DoD line. Added 2026-09-05 by `PROGRESS.md` T11 (`PHASE_5_REPORT.md` decision 2). **RI-00 2026-09-05:** RI-01 builds the 75-minute hour (T12 waived into it) **RI-01 2026-09-05 (built):** `constants.ts` HOUR_MINUTES 75, `HOUR_S`; `E-hour` runs to 75:00 on seeds 3 / 4 / 5, rifle off and on, and scores north (`E-hour-north`, 6/6); §11's table and windows follow it. |
| D-CU-1 | **Where production may stand.** The old constitution rule 4 read "only interior blocks (plus the HQ) host production" and C3 (provisional) encodes one ammo line per interior block; §5 after the D6 rework gives every **Held** block's lot machine slots by area, §11 places an Excavator on east's lot the minute it is claimed, and the sim refuses placement only on a block that is not Held (`flow.ts` `placeError`). Doc and code agree with each other and not with the old rule. (a) any Held lot, slots by area, as §5 and the code say; (b) interior lots and the HQ only — §11's east Excavator and C3's line would move; (c) Held for extraction (Excavators), Interior for assembly | **(a)** — it is what the doc and the sim already say and what the hour was played on; (b) makes hour one's second claim produce nothing until north; (c) adds a rule the doc does not have. The human confirms or picks | provisional (a) | Claude Code | 2026-09-05 | `REVISED_DEVELOPMENT_PLAN.md` §4.2 ("extraction and assembly are both allowed on Held lots … do not add a new interior-only assembly restriction during this pass"), adopted on "Whole plan" (D-RI-6) | §5 "Machine slots", §11 10–30, `CONSTITUTION.md` rule 4 (now empty of game rules). Opened 2026-09-05 by the cleanup (T11a). Not a blocker: T13 builds (a) unless the human picks otherwise. Was: recommended. **RI-00 2026-09-05:** (a) as the plan's default; RI-03 keeps it, RI-09 (T13) builds the rest of placement on it **RI-03 2026-09-05:** kept (a), no interior-only restriction added; the field kit (`flow.ts` `FIELD_KIT`) is the only placement allowed off Held ground, on a Dark / Contested block next to Held. Still provisional |
| D-CU-2 | **The Chemist and Polymer.** §8's Refinery row yields "Crude → Fuel + Polymer"; §8's survivors table makes the Chemist *optional* and gives them "Fuel and Polymer recipes at the Refinery"; §16's restore kit needs 500 polymer. Either the Chemist is not optional or the Refinery does not need them. (a) the Refinery makes Fuel and Polymer on its own and the Chemist unlocks an improvement (a cheaper or faster recipe); (b) the Chemist is required and moves out of the optional list; (c) polymer leaves the restore kit | **(a)** — it keeps the optional groups optional (§8's placement rule puts them in dead-end pockets) and the Refinery row as written; the improvement is a number for Phase 7 | recommended | | | | §8 survivors table (labelled open at the cleanup); Phase 7 builds it. Opened 2026-09-05 by T11a. Not a blocker for Phase 5 |
| D-CU-3 | **The outskirts Substation's order of operations.** §5 "Outskirt blocks" says to hold one you place a craftable Substation; §13 says it goes on a *Held* face that has none; the sim places only on a Held block, so the block must be claimed from the map first and is held on the block sim's abstract power until the Substation stands (`ground.ts` GAME-ASSUMPTION, D-P4-9's split). At tile level nothing powers it in between. (a) claim first, then a grace window in which the Substation must be placed or the block falls unpowered; (b) the Substation is placeable on a Dark outskirts face from a neighbouring Held street, and standing it is the claim; (c) leave it to D-GB-1's expedition, which makes powering the next area physical anyway | **(c)** — the hybrid (Phase 6) rewrites how the next area is powered, and deciding this first would be decided twice. Until then the GAME-ASSUMPTION stays labelled in §13 | provisional (b), by the plan's activation | Claude Code | 2026-09-05 | `REVISED_DEVELOPMENT_PLAN.md` §4.1 ("An unheld outskirts block can receive the required craftable substation before activation; remove the circular requirement that it must already be Held"), adopted on "Whole plan" (D-RI-6); built by RI-03 | §5 "Outskirt blocks", §13 Substation (craftable) row (labelled at the cleanup), `PHASES.md` Phase 6. Opened 2026-09-05 by T11a. Not a blocker for Phase 5. Was: recommended (c). **RI-00 2026-09-05:** the hybrid is now RI-03, so this is decided once there: the Substation stands on the Dark outskirts block from a neighbouring Held street and the explicit Activate is the claim (b); the `ground.ts` GAME-ASSUMPTION goes with it **RI-03 2026-09-05:** built (b) — the craftable Substation is field kit: placeable on a Dark outskirts block next to Held ground, paid from the pockets (D-B3-1's stand-in), the installation the outskirts claim is delivered to and activated at; the abstract-power GAME-ASSUMPTION left `ground.ts` (what remains there is narrower: a Held outskirts block's kerb has no streetlights to light). Still provisional (D-RI-6) until signed |
| D-RI-1 | **Adopt the revised development plan.** The user pasted "Revised development plan, Version 1.0 · 2026-09-05" (now `REVISED_DEVELOPMENT_PLAN.md`, verbatim): restoration projects, physical expeditions and commissioning, infrastructure rewards, an enemy ecosystem with emergence points, bosses, presentation, and tasks RI-00 … RI-13 with a T → RI mapping. It says it replaces `RELIGHT_FUN_IMPROVEMENT_PLAN.md`, which was never in this repository (checked 2026-09-05: no file, no reference, no history). Its §1 says to record the authorising message and which defaults it adopts, and not to attribute its defaults to an earlier gate | — (the human's plan; Claude recommended nothing) | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05" | What was adopted: the plan's **Direction** as the programme's (this row); its **Implementation defaults** as provisional rows under rule 14 (D-RI-6); its **Tuning candidates** kept in candidate configurations, not the benchmark (D-RI-5); its **Later candidates** not built ahead of their task. Lands in `PROGRESS.md` (RI-00 … RI-13, the mapping recorded once), `RELIGHT-design.md` §28 (approved-not-built), `PHASES.md` (the phases the RI tasks pull forward), `CONSTITUTION.md` (D-RI-3), `CLAUDE.md`. Added 2026-09-05 by RI-00 |
| D-RI-2 | **One claim path: physical commissioning.** §5 steps 1–4 claim a block from the map, charge the Depot chest and lay the poles from the claim event (`sim.ts` claim, `flow.ts`); D-GB-1 (c) decided the hybrid but left the map claim standing. The plan §4.1: "Selecting a Dark block does not charge materials or grant ownership"; power connection, materials delivered to the installation and an explicit Activate within reach; consumed once → Contested; one commissioning event per activation (§4.3) | — | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05", adopting `REVISED_DEVELOPMENT_PLAN.md` §4.1 and §4.3 as written | Supersedes §5 steps 1–4 as the intended rule; the built map claim is the implementation limitation until RI-03 (labelled in §5 and §28). Approved-not-built: RI-03 builds it, the hour bot moves to it, and the legacy `E-hour` run stays beside it as the comparison. The Depot chest still pays; what changes is where the payment happens and that nothing is charged on the map **RI-03 2026-09-05:** built — `flow.ts` `deliverTo` / `activationCheck` / `activate` (one commissioning id an attempt, stamped on the claim event and its wake bloom); the game's map previews and sends nothing, E on a Dark block's substation delivers from the pockets and activates; the hour bot claims by the physical path and `E-hour-legacy` keeps the map claim beside it (the claim moments 8–18 s later, 4 Held and no fall on every run). The payment moved from the Depot chest at the click to the pockets at the substation, spent once at Activate; the benchmark runs economy off, so its delivery is zero there and 10 steel + 5 Cu in the game |
| D-RI-3 | **The three-systems stop becomes a scope test.** Constitution rule 2 and rule 10 stopped the programme when a §26 recount reached four systems, and `CLAUDE.md` repeated the stop. The plan §2: "Replace the old automatic 'fourth system means stop' rule with a practical scope test: a feature must strengthen the loop above, have a clear implementation boundary, and justify its interaction and testing cost. This is not permission to add unrelated systems." | — | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05", adopting `REVISED_DEVELOPMENT_PLAN.md` §2 | `CONSTITUTION.md` Scope paragraph, rule 2, rule 10 (revised by RI-00); §22, §23 and §26 of the design doc keep the count as a reported number and drop the stop; `CLAUDE.md` "Stop and ask". §2's boundaries (no loot, XP, random drops, survival, happiness, dialogue trees; no world-wide escalation; no compulsory reflex boss fights) are the fence the test runs inside |
| D-RI-4 | **Which task builds D-GB-1's hybrid.** D-GB-1-rider put it in Phase 6. The plan defines the hybrid in full (§4 ownership and field construction, §5 projects, §9 the Junction Heart) and schedules it as RI-03, RI-05 and RI-06 before the rest of the Phase 5 factory (RI-09) | — | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05", adopting `REVISED_DEVELOPMENT_PLAN.md` §13's order and mapping | Supersedes D-GB-1-rider. `PHASES.md` Phase 6's entry criterion now points at the plan; `DEFERRED.md`'s hybrid line links RI-03 / RI-05 / RI-06. **RI-05 2026-09-05:** the hybrid's second task built — the project record, the rail yard as the first project with the transport kit as its reward and the local supply depot (`project.ts`, `E-project`); RI-06's Junction Heart is the third. **RI-06 2026-09-05:** the third built as the candidate layer (`heart.ts`, `enableHeart`) — code only, unverified; the hybrid's three tasks are now all written |
| D-RI-5 | **Candidate configurations stay out of the benchmark.** The plan's Tuning candidates (Stalker and Conductor numbers, the Junction Heart's 90 s and 60 s, the opening windows of §11.1, the rifle-range variants of §8) and its §1: "Changing the established release/benchmark configuration or adding a new mechanic is a separate, visible decision" | **Keep one benchmark**: the 75-minute `E-hour` on seeds 3 / 4 / 5 at the current config hash (re-scoped by D-HOUR-3, built by RI-01) is the regression run; every candidate lives in a named candidate configuration and its runs are reported beside the benchmark, never in place of it; promotion is a `decided` row | decided | Daniel | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05", adopting `REVISED_DEVELOPMENT_PLAN.md` §1 (labels) and §11.1 | `CONSTITUTION.md` verification policy (the legacy reproduction tests kept with their original configuration, plan §14.4); `EXPERIMENTS.md` rows named `*-cand-*` when they exist; §28. **RI-04 2026-09-05:** the first candidate configuration exists — `packages/sim/src/candidates.ts` (`CANDIDATES.stalker`), outside `SimConfig` (hash b95922d2 unchanged), switched on by `enableStalkers` (the game's `?stalker=1`); reported as `E-rifle-cand-stalker` beside the benchmark rows, its checks bookkeeping only; promotion stays a decided row **RI-06 2026-09-05:** the Junction Heart is the second candidate configuration — `CANDIDATES.heart` in `candidates.ts` (outside `SimConfig`, hash unchanged), switched on per state by `enableHeart` / `?heart=1`, reported as `E-heart` (run name RI-06-cand-heart) beside `E-hour`; its 90 s / 60 s and its packet numbers are Tuning candidates, moved into the benchmark only by a human |
| D-RI-6 | **The plan's implementation defaults, adopted as provisional.** Each is labelled in the plan as "reversible and not a historical approval or validated design fact": ordinary activation needs an adjacent Held block; the outskirts Substation may stand on an unheld block before activation (D-CU-3); extraction and assembly on any Held lot (D-CU-1); the current-goal line as the guidance form (D-GB-2 (a)); non-overlapping connected neighbourhoods; the field kit list of §4.2; the project record of §5.1; the reward catalogue of §5.2; the Stalker and Conductor state machines of §7.1 / §7.2 (their numbers are candidates, D-RI-5); the Junction Heart's two feeders, Start commissioning and packets at 25 / 50 / 75 % (§9); the §11.1 opening windows (candidates); the §11.2 presentation list | Take them as written; a human reverses any one by name | provisional | Claude Code | 2026-09-05 | the chat message of 2026-09-05 ("Whole plan"), replying to the pasted "Revised development plan, Version 1.0 · 2026-09-05" — the plan's labelled defaults, none attributed to an earlier gate | Rule 14. Each default lands in the design doc's §28 with its label and is built by the RI task named there; the rows it resolves (D-GB-2, D-CU-1, D-CU-3) carry their own provisional status and cite this row. **RI-04 2026-09-05:** the Stalker state machine of §7.1 is built as the candidate (`stalker.ts`); RI-04's own defaults (the Dark frontage's kerb as the emergence mouth with M4's seeded spread, the Dark well block as the occupied site and its substation's nearest passable tile as the home, the cue list, pursuit hysteresis and timeouts, respawn only while Dark and unwatched) are listed in `RI_PASS_1_REPORT.md` "RI-04", reversible. **RI-05 2026-09-05:** the project defaults adopted — the reward grants §13's Tram depot / Rail crew kit (those rows stay the design); the Supply chest 2×2 / 200 items / 10 steel; the Tram stop 2×2 with two 200-item pools at 20 kW and an instant transfer at a powered stop; the one-tile Tram of 200 items at 8 t/s, a 4 s dwell, 20 steel + 5 Cu; the depot's stock 20 coal + 10 magazines, not consumed; `preparing` on a delivery, a pole run or a crafted Substation; a lost block keeps the reward — listed in `RI_PASS_1_REPORT.md` "RI-05", reversible **RI-06 2026-09-05:** the Heart's defaults adopted, unverified — the cabinets one tile each on the yard's lot, one inward of the emergence point toward each of the yard's first two Dark neighbours (block-index order), 4 steel + 2 Cu each; "powered" = a pole run reaching the cabinet with supply above zero; the Activate as the Start, no wake bloom, no burn-off timer (`contestUntil` = the far future); Crawler-only packets 2 / 3 / 4 alternating the approaches, born 5 s after the request on the approach's emergence point, at most 8 live; a body at a cabinet knocks it out and dies, `REPAIR_COPPER` repairs it; the charge taken once and kept through interruptions; completion spends the cabinets' materials; the reward stays the project record's (RI-05) |

## Legacy outstanding questions (2026-09-05, after RI-00)

The rows that still want a human, and what each one holds up. Everything else in the table
is decided, provisional (built on a recommendation or on the plan's labelled defaults; sign
or reverse at any time) or a recommended row whose rule is already in the doc and the code
and only lacks a name.

- **D-GB-3** rifle range, **D-GB-4** enemy density — Gate B conditions. The plan owns the
  enemy side (RI-04, RI-10) and tests the range candidate in RI-08 in a candidate
  configuration (D-RI-5). Hold up nothing.
- **D-CU-2** the Chemist and Polymer — RI-11 / Phase 7. Holds up nothing.
- **D-P4-11** the idle steel and the hand-feed — **RI-01** explains the hand-fed total by
  the resource accounting or removes it; **T12b** measures the share per variant after
  RI-01; then the human's row.
- **The plan's provisional defaults (D-RI-6)**: D-GB-2 (a) the goal line (built at RI-02, T19 tests it),
  D-CU-1 (a) production on any Held lot (RI-03), D-CU-3 (b) the outskirts Substation
  before activation (RI-03); RI-05's transport-kit sizes, caps and prices and the depot's
  stock (`RI_PASS_1_REPORT.md` "RI-05"). Sign or reverse by name; none blocks a task.
- **The tram route's cost on the hour (RI-05, no row yet)**: the two Tram stops' 20 kW
  each (§13's own figure) put the hour into a proportional brownout from 26:43 until
  Generator 4 at 45:00 where the belt route never browns out, and the Tram's 5 Cu leaves
  copper at 1 on seed 3 (`E-project-beside`). The route sits beside the benchmark (D-RI-5);
  an earlier Generator 4 when the route is chosen, a stop that draws only while
  transferring, or another price is the human's tuning decision. Holds up nothing.
- **Provisional rows the tasks embody**: D-P4-5 (every assembler physical, RI-01),
  D-B4-3 (the turret in the chain, RI-10 reconciles), D-SA-2 (string table, T13 inside
  RI-09), C3 / D-B2-2 (slots, T13 / T17), D-R1 (rifle vs shade). Sign or reverse; none
  blocks a task.
- **The human work the plan contains**: **T19**, RI-08's played session of the
  representative loop, after RI-07. Claude prepares it and cannot mark it.
- **Recommended rows whose rule is built and in the doc but unsigned** (a provenance gap,
  not a question): D5, D6, D-B1-2 … D-B1-5, D-B2-3, D-B3-1 … D-B3-4, D-SA-1/3/4/5,
  D-LP-1 … D-LP-3, D-P1-1 … D-P1-3, C6, C7, C10, D1 … D4, D-CI, Gate A (30 rows; D-P4-11 above is the 31st).

## Changelog

- 2026-09-06 — Added D-EX-01–10 and Q01–07 from the voice discussion; specified partial supersession and retained provenance. No historical measurement or approval was rewritten.

- 2026-09-06 — D-EX-11: owner accepted all recommendations; Q01–06 changed from open to decided with shared explicit provenance. Q07 stays open. Approved initial settings are not simulated or played evidence.


### D-EX-37 — P7-03 implementation (2026-09-08)

The owner’s “Ok onto P7-0” follows the completed P7-02 handoff naming P7-03 next; interpreted and announced as authorisation for that increment. Implements the already adopted Q09 catalogue: paid Turbine restoration, Lamplighters/Arc lamp and district-only Surveyors. Numerical tuning remains provisional; no human play result, later increment, commit or push is authorised by this request.


### D-EX-38 — P7-04 implementation (2026-09-08)

The owner requests “OK move onto P7-04”, authorising the adopted Q09 discovery and recipe information increment. Keep the opening station clue; remember other installation discoveries through exploration, preserve locations already exposed by old saves, and show actual service state and implemented item production/uses. Foreman/clipboard tools remain Phase 8. No gameplay tuning, human verdict or commit/push is implied.


### D-EX-40 — P8-01 Foreman and clipboard implementation (2026-09-08)

The owner requests “Lets go ahead with this” after the Phase 8 review handoff explicitly naming P8-01. Implements the adopted Foreman/clipboard boundary: permanent local recruitment, copy/paste of complete nearby machines and settings, rotation/reflection and paid atomic stamps. One saved clipboard, 128 entities/128-tile selection bounds, current reach and 50 construction-history groups are implementation defaults, not a human balance verdict. Campaign metadata 10/recruits 4 retain earlier sites and earned rewards; historical saves keep their logs with incomplete fresh-campaign replay provenance where the new shelter reservation can change old building commands. Library/ghost orders and automated truck execution remain the later P8 increments under Q10. Shared play stays after P8; no commit or push is implied.


### D-EX-41 — P8-02 saved library and ghost orders (2026-09-08)

**Authority: decided task scope.** The owner requested “continue with P8-02” after the completed P8-01 handoff. Implement the approved Phase 8 scope's saved library, validated import/export and inert queue. Q10 continues to govern physical truck delivery in P8-03; this task does not authorise its implementation or a commit/push.

**Provisional implementation choices (agent, within authorised scope):** optional plan schema 1 under metadata 10; 32 library entries and 32 order records; 120-character names, 64-character single folder labels, catalogue-kind icon tags and 262144-character portable JSON; existing 128-entity and 128×128-tile layout bounds. Monotonic IDs are not recycled. Orders own independent definitions; overlapping waiting plans are rejected. Manual matching construction counts as progress and Build remaining is an ordinary atomic paid construction group. Cancellation/completion is terminal, including after later undo/damage; removing a terminal record frees space. These are reversible engineering defaults, not attributed owner balance choices. Human play remains after P8 review/EX-08D under D-EX-39. [Implementation evidence](P8_02_LIBRARY_REPORT.md).


### D-EX-42 — P8-03 physical truck construction (2026-09-08)

**Authority: decided task scope.** The owner requested “Continue” after the P8-02 completion/handoff naming P8-03. Implement the existing truck's physical chest-supplied blueprint executor under Q10. No commit or push was requested.

**Provisional implementation choices (agent):** optional truck-work schema 1 under unchanged metadata 10; explicit source chest and up to 32 selected waiting order IDs; 3-tile loading reach, up to 20 items/s, 8-tile build service reach and one part (or tunnel pair) per second. Reuse the manual truck's street footprint/speed, with swept 0.2-tile checks, deterministic forward/reverse/quarter-turn A*, 12000 search-node and 2048-step limits, and 2-second retries. Physical chests accept locally transferred packed machines so both packed and material-cost construction are usable. Automatic builds use real cargo and do not add to the engineer's manual undo history. Pause, stop, order cancellation and manual takeover retain cargo and built parts; resumption is explicit after takeover. These are implementation defaults, not owner-approved balance values. P8-04/05 and EX-08D still precede shared human play. [Evidence](P8_03_TRUCK_REPORT.md).


### D-EX-43 — P8-04 conserved removal and order controls (2026-09-08)

**Authority: decided task scope.** The owner requested “OK onto P8-04”. Implement the scope report's area packing, interruption/order controls and measured command comparison. No commit or push was requested.

**Provisional implementation choices (agent):** explicit drag preview followed by Pack selected machines; 1–128 complete machine footprints in a 128×128 selection; captured identities/settings with current-content, reach and capacity revalidation; one atomic transaction and one undo group, with no partial success. Undo rebuilds settings from current pockets and leaves recovered contents in pockets. Loose turret rounds require buffer room; installed facilities remain. An optional order `paused` flag preserves schema 1, reserves its ghost footprint and stops automatic building of that order. Matching manual construction may still complete it. Immediate waiting-job retry and explicit source reassignment retain cargo; repeated controls do not restart identical work. Changed destinations use cancellation and a new order, preserving existing paid machines. These are implementation defaults within authorised scope, not an owner usability verdict. [Evidence](P8_04_REMOVAL_REPORT.md). P8-05 and EX-08D precede shared play; human rows remain open.


### D-EX-44 — P8-05 construction engineering review (2026-09-08)

**Authority: decided task scope.** The owner requested “Go ahead with P8-05”. Complete the retained integration acceptance and repair findings within existing rules; no commit or push requested.

**Engineering result, not a human verdict:** six fresh campaign runs use ordinary opening stock, steel mining, actual station power/restoration, recruitment and paid repeated ammunition construction. Odd seeds use Foreman-before-station/input-first builds; even seeds use Electricians-before-station, then Foreman/turret-first builds. A 16-seed query sweep covers fixed destinations and current valid/blocked turns. Natural raid coexistence is measured separately from survival: the unattended home is disabled during the browser continuation. Only status wording changes: active automatic trucks no longer say Parked; order missing materials are labelled as pocket shortages. No gameplay constants were changed. [Review and exact checks](P8_05_REVIEW_REPORT.md). P8 implementation/engineering review can hand off to EX-08D; human rows, P6-AMMO retest and prior performance findings remain open.

### D-EX-45 — EX-08D post-P8 playtest preparation (2026-09-08)

**Authority:** owner (current user), “OK do EX-08D”. Prepare the post-P8 shared playtest under D-EX-39; preserve earlier evidence.

**Preparation choice (agent):** freeze the reviewed local P8-05 source and compiled files on separate loopback port 5180; provide fresh, early chest-supplied construction, defended pre-warning network and optional Turbine starts with complete ordinary command histories and disclosed scripted/design assistance. Use an unscored guide and a new blank record covering P5/P6/P7/P8, the ammo retest and unresolved performance/exit positions. [Evidence](EX08D_PREPARATION_REPORT.md). Preparation and automated checks do not constitute human observations, approval of tuning, a phase verdict or a diagnosis of the original ammo report. No commit or push is authorised by this request.

### D-EX-46 — begin independent Phase 9 city work (2026-09-08)

**Authority:** owner (current user), “Ok lets continue with P9”. Begin Phase 9 with current-source scope reconciliation and independent city implementation. This supersedes requiring shared play to finish before this extracted city work; it does not state that play occurred or waive any human criterion.

**Reconciled scope (agent):** [Phase 9 review](PHASE_9_SCOPE_REPORT.md) maps composed campaign validation, evidence-led generation/reservation fixes, saved naming and wayfinding, the retained 10,000-seed campaign sweep, engineering preparation and human ten-seed review. Reuse the integrated urban baseline, preserve EX-08D/5180 and its unfilled record, and leave existing human tasks available. Wider project progression, mystery and endgame remain EX-09/Q07. Technical bounds and concrete fixes are recorded with their implementation; this is not a balance or phase-pass decision and authorises no commit or push.

### D-EX-47 — P9-01 completed-map validator and evidence runner (2026-09-08)

**Authority:** owner (current user), “Lets do P9-01”. Implement the reviewed composed campaign validator and evidence runner.

**Implementation choices (agent):** version-1 reports query a private clone of initialized metadata-10 campaign state, reuse physical placement rules separately from unlock/stock/reach checks, and explicitly distinguish connectivity, truck queries and gameplay/human evidence. Seeds 1–32 form the small diagnostic sweep, including 3/4/5 and holdouts 8/11/13; source hashes, all attempts and failures are retained with repeat/load/unchanged-state checks. Existing output is refused. Resumable 10,000-seed execution remains P9-04. [Report](P9_01_CITY_VALIDATION_REPORT.md) records 30 passing seeds and paid-command confirmation of survey-placement failures on 6/29; P9-02 owns those fixes. No reroll, geometry repair, balance change, human pass, commit or push is implied.


### D-UI-01 — Intuitive UI and Visual Identity phase (2026-09-08)

**Sequencing superseded by D-UI-02 below:** RI-02B now follows P9-05. The original placement and reuse direction in this record are historical; the UI design remains adopted.

**Authority: decided design direction and documentation scope.** The owner supplied “Create the Intuitive UI and Visual Identity phase” and explicitly requested an implementation-ready plan, tracker integration and CLAUDE update, without production implementation in this run. [RI-02B specification](RI-02B_UI_SPEC.md) records the selected municipal electrical-equipment direction and seven bounded tasks. Its numerical visual tokens/layout sizes are agent-chosen tunable starting values within that direction; they are not measured usability results.

RI-02B expands completed RI-02, complements integrated RI-02A and reuses P5–P8. It precedes remaining P9 engineering and broad EX-08 assessment/major expansion, without reopening completed P9-01 or waiting for city fixes. Existing human sessions remain available as historical-build observations. Phase 12/RI-13 consume the brought-forward UI evidence; P9-03 retains saved names/pins and reuses the shell/minimap. Gameplay, progression, physical reach, costs, discovery, warning schedule and existing command validation stay authoritative. New Pause explicitly uses existing time controls; ordinary panels preserve running state. The local optional UI preference record is presentation-only. No human gate is waived; no commit, push, merge, deploy or external tracker change is authorised.


### D-UI-02 — UI redesign after Phase 9 (2026-09-08)

**Authority: decided sequencing correction.** The owner asks “Can it be after phase 9?” Schedule RI-02B after Phase 9 engineering and refreshed preparation (P9-05), before broad EX-08 assessment/major expansion. This supersedes D-UI-01's placement before remaining P9 work, not its UI design. P9-02 resumes as the next implementation task; UI-01 waits on P9-05. P9-03 first supplies saved names/pins/navigation using existing UI, then RI-02B integrates and preserves it. P9-H and other human verdicts remain separately required, not inferred or waived; they do not block the subsequent UI engineering. Completed work and historical records remain unchanged.


### D-EX-48 — P9-02 buildable tram surveys and saved-layout compatibility (2026-09-08)

**Authority:** owner (current user), “Ok lets do P9-02”. Address the demonstrated seed-6/29 failures under the Phase 9 scope. D-UI-02 places the UI redesign after P9-05; that sequencing remains intact.

**Implementation choices (agent):** the existing bounded first-route search now rejects ground outside DARK/HELD, including every stop-pad tile, matching ordinary construction and the later district search. New campaigns record survey revision 2 and campaign fingerprint `9fc92bfc`; missing revision denotes the preserved older survey. Loading never reroutes an existing survey or changes its stock; older command histories cannot claim replay from the revised fresh factory. [P9-02 report](P9_02_SURVEY_RELIABILITY_REPORT.md) records geometry, ordinary paid routes, compatibility and refreshed evidence. The city algorithm, prices, human gates and broader 10,000-seed obligation are unchanged. No commit or push is authorised.


### D-EX-49 — P9-03 saved naming and wayfinding (2026-09-08)

**Authority:** owner (current user), “Continue with P9-03”. Implement the reviewed saved naming, pins and knowledge-aware navigation subset.

**Implementation choices (agent):** reuse generated city names and discovery knowledge, with optional version-1 annotations/physical visit records. Bounds are 64 characters, 80 name overrides and 32 pins; IDs are independent of names. Pins target known districts; camera/selection never grant knowledge or issue travel. Existing saves keep geometry/stock and begin recording visits on resume, with honest incomplete old-log replay provenance. New fingerprint `bd3b8939` includes navigation rules. [Report](P9_03_WAYFINDING_REPORT.md) records actual checks and source differences. RI-02B remains after P9-05; human gates, broader seed evidence and Q07 remain separate. No commit or push is authorised.


### D-EX-50 — P9-04 declared population evidence and measured repair follow-up (2026-09-08)

**Authority:** owner (current user), “Onto P9-04”. Complete the reviewed declared population, resumable evidence runner and ordinary representative/holdout probes.

**Implementation and measurement choices (agent):** declare seeds 1–10,000 inclusive; use 12 workers, immutable checksummed seed records, source/runtime/seed identity checks and explicit process-first versus warm timing. All 10,000 attempted seeds are retained: 9742 pass, 258 fail in 6 classes. No map, gameplay rule, config fingerprint or acceptance check was changed to improve this result. [Report](P9_04_POPULATION_REPORT.md) distinguishes passing code/short paid probes from the failed population and longer unattended seed-102 scenario.

**Follow-up (agent):** P9-04R records the demonstrated generation/survey repairs before P9-05. Preserve this baseline and old saves/logs, version future generation changes, exercise the failure classes and retain a fresh complete population comparison. This records required engineering work, not approval of new progression or a waived validator check. Human ten-seed/shared play, RI-02B after P9-05, Q07 and performance obligations remain separate. No human verdict, commit or push is authorised by this measurement.


### D-EX-51 — P9-04R composed survey repair and retained comparison (2026-09-09)

**Authority:** owner (current user), “Ok continue with p9-04r”. Implement the measured repair follow-up defined by D-EX-50 and PROGRESS; preserve old evidence, saved layouts and logs.

**Implementation choices (agent):** fresh survey revision 3 accepts initial/later route and stop arrangements only after complete district source/workshop/cache/recruit/Turbine composition succeeds. Reject rubble throughout stop footprints; use actual district-owned facility tiles and search beyond a preferred pad before rejecting the district. Stable candidate ordering uses the original seed and unchanged base city. Candidate metadata/events/revision are discarded on placement failure; existing safety distances, rewards, costs and validator rules remain unchanged. Older saved surveys are not rerouted, and incomplete historical session logs survive resaving without a fresh-factory replay promise.

**Evidence:** the separate complete 1–10,000 comparison passes every seed: 258 failed → passed, 9742 passed → passed, no base-generator retry-index changes. The failed P9-04 baseline remains unchanged. 351 full + 24 focused tests, 12 paid freight/truck probes, 36 powered extraction probes, 78 campaign experiment checks and eight browser save/reload cases pass. [Report](P9_04R_REPAIR_REPORT.md) retains preliminary failures, source provenance and test scope.

**Handoff:** P9-05 is runnable for engineering review and human preparation. RI-02B remains after P9-05. Human ten-seed/shared play, P6-AMMO, reference performance and Q07 remain separate; no phase verdict, commit or push is inferred.

### D-EX-52 — P9-05 engineering readiness and separate human checkpoint (2026-09-09)

**Authority:** owner (current user), “Ok onto P9-05”. Complete the approved city engineering review and refreshed human preparation. No new gameplay rule, human result, commit or push is implied.

**Measured implementation (agent):** final P9-04R's 205 source inputs and six production files match the freshly rebuilt handoff. New 351 full + 29 focused tests, typechecks/build/lint/legacy snapshot, 20 current city and two historical-save browser cases, eight prepared start/save cases, both-width truck/direct-ammo and paid Turbine controls, natural warning and 80 live edits pass. The exact-source 10,000/10,000 population and 78 campaign checks are retained and integrity checked, not rerun or relabelled. Existing session-log provenance exception in those experiments remains explicit. [Readiness report](P9_05_READINESS_REPORT.md) records boundaries and fresh evidence.

**Preparation choices (agent):** separate frozen port 5181, ten declared fresh seeds 3/4/5/8/11/13/80/88/102/842, ordinary-command assisted construction/network/Turbine starts, a comparable ten-seed protocol and blank human record. No stock/HP/clock/position injection; supplied prefixes/designs disclose assistance. Preserve EX-08D/5180, older checkpoints and prior owner Phase 5 success. Current sample frame intervals reach 116.8 ms; no performance fix or ammo diagnosis is claimed.

**Handoff:** P9-05 is done; P9-H is available and RI-02B-UI-01 is the next implementation under D-UI-02. Human city fairness/recognition/route choices and P6/P7/P8/EX-08/T18 verdicts remain separate and open; P6-AMMO awaits actual retest, reference performance remains unresolved, and wider Q07 progression/endgame is unchanged.

### D-UI-03 — UI-01 shared shell and input foundations (2026-09-09)

**Authority:** owner (current user), “Let’s move into RI-02B-UI-01”. Implement the adopted D-UI-01 specification after completed P9-05 under D-UI-02. Delivered-interface human approval/play is not yet recorded.

**Implementation choices (agent):** extract uiShell.ts with shared DOM controls, capability adapters, one drawer, modal Pause/start/save actions and explicit input/focus ownership. Existing simulation commands and domain panels remain authoritative. UI capture cancels unfinished belt/blueprint drags before pointer-up can commit them. The first city click while a drawer is open dismisses it without acting underneath. Camera framing reserves top/bottom/drawer space without moving the engineer. Readability refinement changes the candidate border from #69837D to #8DA79F after a failed raised-control contrast check; final shared normal-text/border minimums are 5.60:1 / 4.00:1. This is measured presentation tuning, not a core gameplay rule or comprehensive accessibility acceptance.

**Evidence/handoff:** [UI-01 evidence](evidence/ui-redesign/README.md) records 351 full tests, types/build/lint, both-width actual UI input/save/replay, adapter/legacy/missing-save checks and opened screenshots. Simulation/harness and save schema are unchanged, campaign fingerprint remains ba11e896, and frozen P9/EX-08D checkpoints remain intact. UI-01 is complete; UI-02 is next for core HUD/contextual interaction/truthful objectives. Later UI settings/content, existing performance findings, human gates and wider Q07 remain separate. No commit or push.

### D-UI-04 — UI-02 core HUD and truthful next actions (2026-09-09)

**Authority:** owner (current user), “Ok lets move to UI-02”. Implements the adopted D-UI-01 specification after UI-01 under D-UI-02. This is implementation authority, not a human gameplay verdict.

**Implementation choices (agent):** a single DOM campaign HUD replaces the four canvas corners. Existing clock/speed, warnings, names, navigation and discovery filters remain authoritative. A read-only sim query selects the same action for the prompt and E: pointed object first; otherwise reachable Home supplies first, then the nearest available interaction. Down prevents interaction; repairs retain priority over the damaged object. Existing commands and transfers enforce payment/reach. Construction affordability and hand-craft availability are extracted from their existing validators, with unchanged costs/queue behaviour. Opening guidance follows carried stock, real Home stock, existing machine operation, saved production and restoration; no quest/save schema or persistent UI achievement is added. The opening ID is distinct from the later repair workshop. Projects exposes one optional known objective; selection and Show location remain presentation only. Warnings use amber ahead of dusk and danger colour for active raids/assaults.

**Evidence/handoff:** [UI-02 evidence](evidence/ui-redesign/README.md) records 359 full and 22 focused tests, types/build/lint, actual two-width opening/shell/save/replay checks, prepared states and natural warning visible in Pause. UI-03 is next. Final catalogue, deeper inspection/restoration, settings/scaling, formal human review, existing performance findings and Q07 remain separate. Frozen P9/EX-08D evidence, save schema and campaign fingerprint ba11e896 are preserved. No commit or push.

### D-UI-05 — UI-03 building controls and stable shortcuts (2026-09-09)

**Authority:** owner (current user), “Ok onto UI-03”. Implements D-UI-01 after UI-01/UI-02. This is implementation authority, not a human gameplay verdict.

**Implementation choices (agent):** Equipment groups Production/Logistics/Power/Defence; Infrastructure and Library retain existing blueprint/history controls. A read-only sim knowledge projection hides unknown plans and exposes discovered locked requirements. Ten fixed slots use the specified belt/inserter/excavator/assembler/turret/lamp/pole/generator/rifle/floodlight defaults on 1–9/0. Explicit assignment swaps duplicates; clear and adjacent moves preserve other positions. Unknown saved assignments remain anonymous reserved slots. Optional `relight.ui.v1` stores version-one preferences outside gameplay saves/replay, repairs malformed records on explicit edits and falls back to session-only changes when storage is unavailable. Rifle and existing letter bindings remain. UI-06 owns the rest of preferences/rebinding/scaling.

Stock, prices, shortages and packed-first payment use sim queries and ordinary construct commands. Selection closes the drawer and restores canvas focus; rejected placement retains the tool with one cursor reason. R/Escape/repeated building and right-click packing retain their commands. Less frequent navigation actions sit in More; its Escape/focus and existing shell input capture apply. Existing blueprint copy/queue/removal/library adapters are integrated without new construction mechanics.

**Evidence/handoff:** [UI-03 evidence](evidence/ui-redesign/README.md): 365 full and 43 focused tests, types/build/lint, both-width paid placement/packing/shortcut/save/replay and input checks, preference failure checks and actual-game images. UI-04 is next. Existing save schema, campaign fingerprint ba11e896, city generation, harness and frozen UI-02/P9/EX-08D evidence remain unchanged. Formal human gates, performance findings, P6-AMMO and Q07 remain open. No commit or push.

### D-UI-06 — UI-04 inspection, scoped inventory and diagnostics (2026-09-09)

**Authority:** owner (current user), “Ok onto UI-04”. Implements D-UI-01 after UI-03. Engineering implementation is separate from human usability/gameplay acceptance.

**Implementation choices (agent):** one identity-based inspector orders name/location, operating statement, measured useful output, inputs, current constraints, existing actions and collapsible detailed rates/circuit/contents. Pin retains identity through building and Escape cancellation; a removed ID shows a dismissible removed card and never retargets replacement coordinates. A read-only sim constraint query combines the existing power/disabled and operating predicates; current missing inputs and output space are independent, while shortages for an already-running next batch are labelled separately. No-load remains neutral, and unmeasured throughput remains unavailable. Missing-input help links the existing item guide and only real known Home supplies/finite patches. Camera location is presentation only.

One transfer drawer keeps pockets, Home/chest stock, stop platform, arrivals and reserved inbound distinct. Target identity is preserved when removed. Existing transfer quantities/actions remain; read-only previews use pool/reach/stack/capacity predicates, including partial amounts and arrival-first station withdrawal. Truck preview is extracted from its command validator; cargo remains physically separate. Recipe/filter/priority, hand craft, station requests and construction delivery retain normal validated commands. Precise production/power figures remain in Management and inspection Details, outside Developer. UI-05 owns project circuits/alerts and UI-06 owns full settings/scaling/integration.

**Evidence/handoff:** [UI-04 evidence](evidence/ui-redesign/README.md): 370 full and 35 focused tests, types/build/lint, both-width paid identity/settings/transfers/save/replay and input checks plus actual working/stalled images. UI-05 is next. Save/session schema, campaign fingerprint ba11e896, city generation, harness and frozen UI-03/P9/EX-08D evidence are preserved. Human gates, P6-AMMO, Q07 and performance findings remain open. No commit or push.

### D-UI-07 — UI-05 Projects and actionable alerts (2026-09-09)

**Authority:** owner (current user), “Continue”, following UI-04 completion and the established next-task order. Implements the existing UI-05 specification; engineering checks do not substitute for human play.

**Implementation choices (agent):** one known-project selector and the existing single tracked objective. Sim queries expose actual material requirements/delivered stock, carried amounts, separately identified Home stock, local power and capacity-limited kit collection. The existing site delivery prerequisite/reach check is shared with its command. Partial delivery can run without power; ready delivery submits deliver/restore; empty pockets explain refusal or allow Restore when ready. Completion uses restoredAt despite consumed/reset buffers. Radio restoration and precision purchase remain different costs and commands. Current-state local actions dispatch ordinary logged commands immediately; a brief per-gesture lock prevents a double-click spilling into collection or another purchase. No gameplay costs, rewards, timers, power rules or schema change.

**Alerts:** read-only knownCampaignThreat/campaignWarning, saved schedule/received warning, existing close-range visible hostile facts, commissioned core HP and known machine blockers supply keyed conditions. No hidden roster or target query. Engineer/base danger outranks schedule, then production. One highest-priority HUD condition stays visible beside a narrow drawer and during Pause; at most three short transient notices coalesce repeats. The session inbox preserves dismissed messages and resolved conditions; dismissal does not alter gameplay or the live HUD. Saved received intelligence is shown after a radio outage. Inbox dismissal/history and tracked selection are presentation state; new persistent notification settings are outside this task.

**Evidence:** docs/evidence/ui-redesign/README.md UI-05; 373 full and 16 focused tests, types/build/lint/docs and actual-game checks at 1366×768 and 900×768. Paid walked checkpoints, labelled outage/threat fixtures and exact replay are distinguished. UI-06 is next; existing human gates and performance findings remain open. No commit or push.

### D-UI-08 — UI-06 scaling, preferences and integration (2026-09-09)

**Authority:** owner (current user), “Continue”, following the UI-01–05 sequence. Implements the existing UI-06 specification; developer browser QA is not human acceptance.

**Implementation choices (agent):** 100/125/150% UI scale changes shared text/control geometry independently of camera zoom. Layout uses logical viewport size to reflow compact and short screens; all ten quickbar slots retain their order. Settings offers system motion by default and explicit reduced/full overrides. The versioned browser preference record extends the existing quickbar format with scale, motion, validated binding overrides and opening-hint dismissal. Corrupt fields recover independently; denied storage keeps session settings and explains retry. Saves and simulation state contain none of these settings.

Bindings are live in handlers, held movement, buttons and Help. Same-context collisions are rejected; Ctrl/Cmd and blueprint contexts retain their established distinction. Escape, Tab navigation and numbered slot keys stay fixed; Build edits slot contents. Applying preferences releases held world input. UI target handlers receive key events before bubbling is blocked from the world. Drawer, More menu and Pause layers preserve accessible controls and focus. Existing profile-separated save/load/export and unsaved-change confirmation are retained.

**Evidence:** docs/evidence/ui-redesign/README.md UI-06; 376 full and 17 focused tests, types/build/lint/docs, eight PC scale cases, current campaign/legacy exact replay, older campaign migration, library/order/truck/freight and prior UI flows. The old P7 save loads through existing upgrades and truthfully loses complete replay eligibility; no migration or replay policy changed. Simulation, harness, city generation, costs and save schema are unchanged. UI-07 is next. Human gates and existing performance findings remain open; no commit or push.

### D-UI-09 — UI-07 actual review and bounded refinement (2026-09-09)

**Authority:** owner (current user), “Continue”, after verified UI-06. Executes the existing UI-07 review scope, including one targeted refinement pass; this is not human usability approval.

**Observed and corrected:** the startup notice and core HP label obscured the fresh engineer; a brief “You” label now identifies the character until two tiles have been walked, the core label sits above its building and the fresh paused opening uses its existing HUD instead of a transient load notice. Ordinary loaded campaigns retain a concise load acknowledgement. At high scale on short screens, reduced secondary spacing leaves more city visible; while a drawer and urgent warning are open, the compact objective card yields to danger (Projects retains its full information). HP, equipment, stock, speed and warning controls remain visible. Ultrawide drawer/camera reservations now include its actual safe margin, preventing the warning strip from covering the drawer header. All changes are presentation-only in five game files.

**Evidence and limits:** docs/evidence/ui-redesign/README.md UI-07, exact UI-06 comparison and preserved P9-05 prepared starts. Opened baseline is 1280×720, seed 3, time 0:00, camera 0.65. Paid commands verify supplies/build/inspection/project delivery; explicit prepared urgent fixtures cover ten states at two sizes. 376 full + 17 focused tests, types/build/lint/docs, eight scales and save/replay/input checks pass. No new simulation batches or content. The immutable UI-07 build is served on port 5182; mutable 5178 and frozen 5177/5179/5180/5181 remain separate.

Fresh-player observation is recommended and pending; its blank current-build observation area stays in the shared evidence note. No intuition score or unaided-play success is inferred. UI-07 engineering is complete; EX-08 remains blocked on EX-08H and broader progression retains Q07 and its other prerequisites. Existing formal human gates and performance findings remain open. No commit or push.

### D-UI-10 — Approved illustrated UI implementation and normal player time (2026-09-09)

Authority: owner’s pasted implementation request and accompanying approved concept image in this conversation. Extends RI-02B through UI-08 and supersedes prior prefilled text shortcuts, resource cards and time-acceleration retention. Implement now: ten icon shortcuts with catalogue replacement/bar swaps and accessible alternatives; compact discovered catalogue; temporary real-stack Backpack and paired legal storage transfers; quiet local minimap/HUD; player speed 0/1 only. Keep existing capacities, costs, discovery, raid timing, city artwork and headless runners. Original SVG icons are permitted. Existing optional UI preferences preserve arrangements; optional authoritative stack allocation supports split/move without a second UI inventory. Verification is focused checks plus inspected actual-game screenshots, not a new broad simulation campaign. Completed UI-01–07 evidence and human gates remain separate. See docs/RI-02B_UI_SPEC.md and docs/evidence/ui-redesign/README.md.

### D-UI-11 — Mouse flashlight and fully lit Home (2026-09-09)

Owner request: “I want the player to have a flashlight that points in the direction of the mouse. Also the main starting base isn't fully light up, it should be”. This supersedes D-B5-1's no-player-light choice for the exploration campaign. Campaign players now start with a mouse-aimed visibility beam; Home's complete lot receives built-in lighting in both the render mask and simulation light queries, including old saves. As a bounded implementation choice, the personal beam only improves visibility and does not supply power, clear rot or make shades vulnerable. The beam's 12-tile reach, 60-degree width and soft falloff are provisional visual tuning, not owner-specified values. Legacy entry keeps its prior default with the existing opt-in preview hook. Broader human gates remain open.

### D-UI-12 — Direct conveyor loading and unloading (2026-09-09)

Owner: “I don't want to have to put inserters. Conveyors should be able to deliver to and take from anything”, after describing Chest → conveyor → machine → conveyor → chest → conveyor → Turret. This explicitly supersedes mandatory inserter extraction in the exploration campaign. Conveyors touching a machine/storage side and pointing away withdraw transferable contents; inward conveyors deliver accepted items. Assemblers/Mixers export finished products, Excavators export mined items, Home and supply chests export stored materials/magazines, generators export unused coal, turrets export whole magazines, and stations export arrivals before platform stock. Conveyor entrances include underground inputs and splitter rear ports. Existing item types, recipe acceptance, belt rates and production power requirements remain; inserters remain optional filtered transport. Legacy simulation retains its earlier extraction behavior.

Implementation choices: fair saved item selection, downstream ingredient/capacity checks accounting for material already on conveyors, conserved transfers only when the receiving conveyor has room, and cyan IN / amber OUT markers matching real footprint edges. Partial turret rounds and packed machines are not converted into unsupported belt items. Existing campaign saves acquire direct connections on load; no user layout migration is needed. This changes current replay behavior from older code, so historical recorded evidence remains tied to its original build.

### D-UI-13 — Permanent tram stops, roaming enemies and map inspection (2026-09-09)

Authority: the owner's three gameplay requests in this conversation. The exploration campaign has four fixed, non-buildable tram stops. Power at least two for the existing tram to automatically serve the powered stops; an outage below two parks it without discarding freight. The surveyed trunk is permanent infrastructure. Home, the first outpost and the later outpost retain their locations where clear; the fourth platform occupies another district along the line, extending along existing streets if needed. Empty new infrastructure has no item cost or pickup reward. Existing saves adopt compatible stations/vehicle, keep all existing inventories and machinery, and retain any already-earned kit. Additional old trams park; an old save whose reserved platform is occupied uses a clear nearby platform in that district. No new kit or player-built track/stops/trams are required. Restoration still commissions bases and unlocks the truck; powering a platform itself is sufficient for tram service. Existing freight requests, exports, capacity, return cargo and local power rules remain authoritative.

Ruin crawlers roam locally, detect the player and retain pursuit beyond the starting area until the player escapes. Provisional tuning: 6-tile patrol radius, 8-tile detection and 20-tile player-to-enemy escape distance. The optional Stalker also roams its yard and chases beyond it, with 5-tile detection and the same 20-tile escape distance; legacy archetype behaviour is unchanged. Base raids retain their base objective but retaliation uses the larger escape distance. These values are implementation choices, not owner-specified balance numbers.

The map is for inspection and pins, with a prominent outlined player marker and YOU label. Clicking it never submits a move command. The local minimap gets a stronger marker too. This supersedes campaign build-your-own tram, site-home leash and city-map click-to-walk directions; historic evidence remains tied to its source build. Migrated old command logs are marked incomplete for the revised factory. Human gameplay acceptance remains pending.

### D-EX-CORRECTIONS — implemented initial defaults (2026-09-09)

The owner requested the full audit corrections and rejected the old frontage economy. EX-09C-04 covers defended site lifecycle and explicit equipment recovery. One removable +10% speed slot and paid +25% whole-round hopper/freight improvements are initial defaults, with no baseline service relocking. Three 600 kW plants, three physical cores and optional artifact caches; 15/18-minute first-day minor slots; production costs, relay equipment, Cannon/role tuning and the Heart/Furnace/Crown transition designs are new reversible implementation choices recorded in docs/Implementation/GAMEPLAY_CORRECTIONS.md and central code. Efficient empty-start measurements motivate the preparation window; they are not fresh-player balance approval. Q07 ending and human/release judgments remain open.

### D-PE-01 — Player-experience correction planning; preserve component recipes (2026-09-10)

Authority: the owner requested, “Lets plan everything except 6, We will add in more turrets that use these resources,” referring to findings in docs/Implementation/PLAYER_CLARITY_AND_FUN_AUDIT_2026-09-10.md. Plan findings 1–5 and 7–9. Exclude finding 6's recipe removal/simplification recommendation: keep Wire, Frame and Board production and existing stocks for future turret consumers. This is a planning request; it does not specify turret types, costs, ammunition, unlocks or balance, and does not implement the correction set.

The proposed implementation and acceptance scope is docs/Implementation/PLAYER_EXPERIENCE_CORRECTION_PLAN.md. Proposed stack values, initial retention of powered-light Shade vulnerability, and later evidence-led encounter revisions are agent recommendations, not separately owner-approved balance choices. Preserve the original audit as evidence; PROGRESS remains the sole execution list.

## D-PE-02 — Implement player-experience corrections (2026-09-10)

Decided: the owner requested “Lets implement these changes” following PE-PLAN. Implement A–H, excluding audit finding 6. Retain powered-light Shade vulnerability and cosmetic flashlight; use the proposed bulk stacks of 50 and Shell stacks of 20 as initial correction defaults. Preserve component recipes for future turrets; no new turret designs or reward/balance changes are specified. Human usability/fun verdicts remain separate.


## D-PE-03 — Owner playtest inventory and opening corrections (2026-09-10)

The owner requested Generator → Excavator → Storage → Belts with amounts, a turret before ammunition, visible workshop crafting, persistent named power outages with map icons, quantity-controlled machine inventories, and mouse-following stack icons. PE-PLAYTEST-02 implements those corrections using existing prices, recipes, physical stock, reach and capacity. Whole magazines remain the transferable turret unit; loose rounds stay loaded. Future turret specifications and the earlier finding-6 exclusion are unchanged.
