# Relight — decisions

Every constant, direction and verdict a human makes, and every one that is still open. Format per line: **ID · date · status · what · evidence · who decided**. The programme never picks a value from this list; it writes the question and the evidence. `open` means nobody has decided; `made (pre-programme)` means a prior session applied it to the doc before this log existed and a human has not put their name to it; `made` means a human did.

## Open constants (Phase 0 list; Phases 1–4 aim to evidence each)

| ID | Constant | Doc value today | Other values in play | Evidence to date | Question for the human | Status |
|---|---|---|---|---|---|---|
| C1 | Edges fed per assembler | ~15 (§12: one assembler = 20 mag/min; §5 in-play 1.37 mag/edge-min) | proto Mk1 10 mag/min, Mk2 20 → 7 / 15 edges | E5 (`FRONT_FIX_REPORT.md`): demand 8–48 mag/min by 3 h; calibration 2: one Mk1 feeds a 4-block blob to 1:59 | Is the tempo "one assembler per ~15 edges" (an assembler every ~5 claims) or should the proto's Mk1/Mk2 split stay? | **made 2026-09-03 (Gate A `go`, by recommendation D-P2-2; §12 `[play: Gate A]`): one 20 mag/min assembler ≈ 7 edges of the mixed mid-game front (E9-hourly), 15 civic / 11 residential at steady state (E3-block), the early front cheaper (calibration).** Locked without session telemetry; Phase 4's human hour can move it |
| C2 | Claim cadence the game is drawn for | ~5 min after hour one (§18, `[sim: E9-claim-cadence]`) | 8 min (sim before `CALIBRATION_REPORT.md`); 15 min in hour one | E9-claim-cadence: 5 min matches §18's 25 h count; 8 min does not | The §18 drawings assume ~5 min; is that the game, or should the drawings follow a slower cadence humans actually play at (Gate A telemetry)? | **made 2026-09-03 (D-P3-6): 15 min in hour one, then 5; §18 generated from the sim at it** |
| C3 | Machine slots per block | unstated (§5 "machine slots on interior blocks"; §4 lot 24×24 tiles) | 1 (proto, PROTO-ASSUMPTION) | calibration 2: 1 slot makes enclosure the only way to a second assembler; T1 (compact amber inside 60–120 min) missed and called structural | One slot, or a footprint budget per lot (§13 gives 3×3 machines on a 24×24 lot ≈ dozens)? | **routed 2026-09-03 (Phase 3): one ammo line per interior block is what the sim and the slice encode (GAME-ASSUMPTION); the footprint budget is Phase 4 M1 / Phase 5 — PHASE_3_REPORT.md decision 2** |
| C4 | Magazine buffer cap | none stated | 400 magazines / 4,000 rounds (proto) | calibration 1/2: pips leave green only after the buffer is empty, so amber → red is ~2 min | Is a shared buffer a game object (a chest holds 400, §13) or a proto shortcut to delete? | **made 2026-09-03 (D-P3-4): the 400-magazine line buffer is a chest; the Relight bank is a separate buildable object of ~20,000 magazines, Phase 10 E20 sizes it** — Phase 1 evidence: E9-hold, a 4,000-round cap loses the hold (34–38 blocks), an uncapped bank of ~15,000 magazines wins it |
| C5 | HQ start patch sizes | steel "small" (§11); coal ~3,000 (§11/§12) | coal ~700 (`TEST_RESULTS.md` §8); proto steel 7,680 at 64/min, empties at 2:00 | calibration 2: 3,000 coal mines out at ~106 min, 700 at ~30 min; hour-one power lesson depends on it | Which hour does the start coal run out in, and should steel run out first (2:00 in the proto)? | coal **decided by D1: ~700 (west wanted by minute 30)**; steel **routed 2026-09-03 (Phase 3) to Phase 5's rubble model** as a named placeholder (proto 7,680 at 64/min, empties at 2:00; the §19 steel wall is intended, D-P2-3) |
| C6 | Fall time after the substation stops | 90 s (D2) | 5 min (pre-D2) | E8-fall16: at 5 min nothing ever fell; at 90 s falls happen but humans have not seen one | Is 90 s a rescue window a human can act in (Phase 3 smoke test), or an ambush? | **made** (D2 ratified 2026-09-03); Phase 3 `[play: Gate A]` check made by D-P3-2 without a session: the actable window is the minutes of red pip before the 40th arrival, the 30 s after the stop is the second chance |
| C7 | Substation draw | 100 kW front / 20 kW interior (D1) | 200 / 40 (pre-D1) | E4h-*: at 200/40 the start patch could not carry hour one; at 100/20 + four Generators it can | Ratify D1, or keep 200/40 and change the Generator instead? | **made** (D1 ratified 2026-09-03) |
| C8 | Bloom timer base and per-bloom drop | `120/(0.5+d)`, 10 % | `240/(0.5+d)` halves fights, −30 % ammo; 20 % drop −24 % ammo (E10) | E10-bloom-cadence | Which rhythm feels right? Gate A, `[play]` | **locked 2026-09-03 at the doc value (D-P3-5) with no feel evidence**; PHASE_3_REPORT.md decision 3 asks whether to port E10 before the slice encodes it |
| C9 | Assembler rate | 20 mag/min (§12, follows from the 3 s recipe) | proto Mk1 10, Mk2 20 | with C1 | Is there a Mk1/Mk2 ladder in the game or only in the proto? | **made 2026-09-03 (D-P3-7): only in the proto; the game's Assembler is 20 mag/min from the 3 s recipe** |
| C10 | Start ammo | 20 magazines (§11) | sim `startRounds` 300 = 30 magazines | Phase 1: E1-start-min — the HQ never falls on 200 rounds (20 magazines) with the ammo line from minute 10 under the 40-arrival rule (32 accrue); a 10- or 20-arrival rule loses it at minute 8 (E1-ring-substation-N10/N20) | Reconcile in the Phase 1 doc pass; which is it? | **made 2026-09-03 (D-P1-3 rider): 20 magazines.** Sim `startRounds` 200; §11 states the margin; part of config hash `01dc5d02`. Phase 2: 200 rounds fill two of the HQ's three hoppers, so the third shows amber then red for 30 s at t = 0 (calibration C1/C2 "missed" at 0 m only); 300 rounds meet all seven targets — **D-P2-1 made 2026-09-03 by recommendation: 20 kept, C1/C2 scored from minute 1 (all seven met)** |

## Decisions applied to the doc before this log existed — ratified 2026-09-03

| ID | Date | What | Evidence | Decided by | Status |
|---|---|---|---|---|---|
| D1 | ratified 2026-09-03 | Substation draw 100 kW front / 20 kW interior; HQ starts with four Generators; **start coal patch ~700 units** (was 3,000 — that number mined out at 106 min; the intent is "west wanted by minute 30"). Reason given: at the halved draw E4 showed machines, not substations, set the Generator count (660 of 980 kW), so ammo is the hour-one lesson. | `CALIBRATION_REPORT_2.md`, E4h-* | human | **made** — doc §11/§12 coal ~3,000 → ~700 in the Phase 1 doc pass; closes C5 (coal) and C7 |
| D2 | ratified 2026-09-03 | Fall time 90 s after the substation stops. Reason: the 30 s rescue window against the 60 s refeed reset is what makes a red pip a call to action rather than an obituary. | `CALIBRATION_REPORT_2.md`, E8-fall16 | human | **made** — closes C6; Phase 3 still checks a human can act in the window `[play]` |
| D3 | ratified 2026-09-03 | Unshot-arrival counter is per block; a refeed within 60 s resets it. Corners fail faster, which is right. | `CALIBRATION_REPORT_2.md` | human | **made** — §5 must say it, so the prototype's corner falls are not read as a bug (Phase 1 doc pass) |
| D4 | ratified 2026-09-03 | The Relight hold is survivable by banking stock. Ratified as a **design statement, not a number**, explicitly pending E9-full (Phase 10 E20). | `CALIBRATION_REPORT_2.md`, E11-relight-hold | human | **made (direction)** — Phase 1 E9-hold: 8 assemblers from hour 4 + ~15,000 banked → 0 lost; 4,000-round cap → 34–38 lost; the bank a player can build is the open number (Phase 10) |

## Reopened

| ID | Date | What | Evidence | Question | Status |
|---|---|---|---|---|---|
| D-25-13 | 2026-09-03 | §25 item 13 "Spike on the scattered map — closed, loses nothing" no longer matches the sim | Phase 1 `[sim: E7, E1-ring-spike]`: at the §18 cadence spike costs 2.7× compact, holds 40 vs 52, loses 12 (12–13) in five hours with the ring fed in creation order | Does the doc say "spike is punished by losses" (retag, §7/§17/§25 rewrite) or is a spike that bleeds blocks the wrong tuning (change dmax/g/cadence)? | **reopened by the human 2026-09-03**, then **closed by D-P1-2 the same day**: a priced choice, not a trap. At 24×24 spike costs 2.6×, holds 42, loses 10 (9–12); §19 "clearly worse but valid" stands, §25 item 13 carries the numbers |

## Direction / verdicts

| ID | Date | What | Evidence | Decided by | Status |
|---|---|---|---|---|---|
| D-CI | 2026-09-03 | **GitHub private repo, GitHub Actions, npm workspaces.** One monorepo, `packages/*` as workspaces, `main` protected behind CI. On every push: lint, `tsc --strict` on all packages, `npm test` (fixtures), and the experiment suite at three seeds (E1–E9 at 5 h × 3 seeds × 4 policies; at ~60 k ticks/s well under a minute, cheap enough to gate on). Nightly: the 10,000-seed and 25-hour runs, results uploaded as artifacts and a summary committed to `docs/experiments/`; anything that needs hours runs on a self-hosted runner on the WSL2 box if Actions minutes get tight. The Python sim stays only as the fixture exporter until the TS sim has been the reference for a phase; then it is retired and the fixtures frozen. Linear's GitHub integration for task links. | Phase 0 inventory | human | **done in Phase 1** — repo `Deedubsy/Relight` (private; was created public and switched), `ci.yml` + `nightly.yml`; **main protection blocked** — rulesets and branch protection return 403 on a free-plan private repo (needs GitHub Pro or a public repo; human choice). Ruleset to apply once possible: PR required, status check `ci`, bypass actor GitHub Actions (integration 15368) so the nightly can commit. Python fixtures kept for one phase; Linear link not yet wired |
| Gate A | 2026-09-03 | `TEST_RESULTS.md` `verdict: go` | `PROTOTYPE_TEST_PLAN.md` (Phase 2: two scenarios, `?seed=3` and `?state=b-compact-seed3`, hashes `ee23bb1c` / `825d2d09`); bot calibration and the smoke test only | human (owner, "go") | **made — passed without tester sessions.** The plan's thresholds were not measured; every constant locked on it carries `[play: Gate A]`, defined in the doc changelog as a lock, not a measurement. The sessions can still run (PHASE_3_REPORT.md decision 1) |

## Phase 0 report decisions (the three, restated from `PHASE_0_REPORT.md`)

1. **Ratify or reverse D1–D4** — ratified with corrections 2026-09-03 (see table): D1 adds coal patch ~700; D4 is direction pending E9-full.
2. **D-25-13** — reopened 2026-09-03; Phase 1 E7 supplies the evidence.
3. **D-CI** — decided 2026-09-03: GitHub private repo + Actions + npm workspaces (see table).

## Phase 1 report decisions (the three, restated from `PHASE_1_REPORT.md`)

| ID | Date | Question | Evidence | Status |
|---|---|---|---|---|
| D-P1-1 | 2026-09-03 | **Map size.** Ship 24×22 (every run, the §18 drawings) or 24×24 (§4 as written, the constitution)? | `[sim: E8-cadence]` at 24×22, then the full suite at 24×24 (`01dc5d02`) | **made 2026-09-03: 24×24.** Row 23 the river, 23 claimable rows; HQ, target and the southern wells moved with the extra rows (GAME-ASSUMPTION until Phase 9's generator). Every doc number re-tagged; §18 drawings redrawn in Phase 2 |
| D-P1-2 | 2026-09-03 | **§25 item 13 (D-25-13).** Spike loses 10 blocks (9–12) and pays 2.6× — a choice, a trap, or a rule change (the doc's "clearly worse but valid")? | `[sim: E7, E1-ring-spike]` | **made 2026-09-03 (human: "whatever you recommend"): a priced choice.** §19 stands, §25 item 13 closed with the numbers; the losses are the ring's far end under creation-order feeding, so a feed-order rule is a Phase 4 logistics question (DEFERRED.md), not a tuning change |
| D-P1-3 | 2026-09-03 | **Gate verdict on the untagged DoD.** 44 remain against ≤ 23; 29 are tile-scale numbers a block sim cannot reach. Pass the gate on the 15 reachable, or hold Phase 2 until the count is met by other means? | `PROGRAMME_STATE.md` 1.3 | **made 2026-09-03 (human: "whatever you recommend"): pass on the 15 reachable**, carry the 29 as Phase 0 routed them, and count only instrument-reachable numbers in later DoDs. Riders: C10 = 20 magazines (made), edge hopper 100 = two turrets per edge until Phase 4 M3, `main` protection waits on GitHub Pro or a public repo (human). Gate now waits only on the five-minute smoke test |
| D-CI-nightly | 2026-09-03 | D-CI says 10,000 seeds nightly; a 5 h run costs ~0.3 s so that is ~3.5 h of runner time, ~6,000 hosted minutes a month. Cron runs 1,000 seeds; 10,000 by `workflow_dispatch` or the self-hosted runner D-CI already names. | nightly smoke, 20 seeds, 74 s (`docs/experiments/nightly.md`) | made (letter of D-CI changed, intent kept); human may override |

## Phase 2 report decisions (the three, restated from `PHASE_2_REPORT.md`; open until the human answers)

| ID | Date | Question | Evidence | Recommendation | Status |
|---|---|---|---|---|---|
| D-P2-1 | 2026-09-03 | **Start rounds and the opening flicker.** 200 rounds (C10 = 20 magazines) leave the HQ's third hopper empty for the first 30 s, so calibration C1/C2 read "missed at 0 m"; 300 rounds meet all seven targets. Keep 20 and score from minute 1, move C10 to 30, or pre-fill three hoppers from 20? | `docs/experiments/calibration.md` (base vs lever 2a), seed-3 trace: last non-green pip 0:30, all green 0:31 | keep 20 magazines; score C1/C2 from minute 1 (the `after 10 min` columns); the flicker teaches the pip ladder | **made 2026-09-03 by recommendation (Gate A `go`, no human word on it): (a).** `calibrate.ts` scores C1/C2 from minute 1 (columns `amber after 1 min` / `red after 1 min`); C1–C7 all met; §11 sentence `[play: Gate A]` |
| D-P2-2 | 2026-09-03 | **Edges per assembler (C1).** Compact never sees amber after the transient in three hours; the second assembler is affordable but never needed in Scenario A. Accept (A tests shape, B tests ammo), make an assembler feed fewer edges, or cut the buffer cap? | calibration base, levers 1a/1b (Mk1 5 loses blocks, Mk1 20 breaks C7), Scenario A traces | accept; lock C1 in Phase 3 from Scenario B telemetry (`firstAmber`, reorders, builds) | **made 2026-09-03 by recommendation: (a)**, with C1 locked from the bot evidence instead of B telemetry (none exists): ~7 edges of the mid-game front per assembler, §12 `[play: Gate A]` |
| D-P2-3 | 2026-09-03 | **The steel wall as Scenario B's pressure.** From the 3 h snapshot steel is 0 at ≈ 3:40 whatever the tester does; the bot that keeps expanding is down to 2 blocks by 4:30, an idle tester loses 6. Intended pressure (claim industrial, reorder, retreat), a tuning bug (new steel lever), or a different snapshot? | Scenario B forward runs (`PHASE_2_REPORT.md` Measured); lever sweep 3/4a (pool and yield cannot move without stalling cheapest) | intended; §19 gets a `[play]` sentence after the gate | **made 2026-09-03 by recommendation: (a)**; §19 sentence `[play: Gate A]`; option (b) stays in DEFERRED.md for Phase 5 |

## Phase 3 decisions (the constitution's four pre-slice decisions, the locks, and the three for the human from `PHASE_3_REPORT.md`)

Made 2026-09-03 on the Gate A `go` with no tester sessions; each is a `[play: Gate A]` sentence in the doc and the human may override any of them here.

| ID | Date | What | Evidence | Decided by | Status |
|---|---|---|---|---|---|
| D-P3-1 | 2026-09-03 | **Substation draw and the hour-one lesson.** Draw stays 100/20 kW (D1); the hour-one lesson is ammo, not power: no hour-one run browns out at four Generators, the Shot line's 8 mag/min against one assembler's 20 is what the player watches, first pip leaves green inside the first minute, first shade at minute 32–47. §11 sentence | E4h-gen@0,6,15,25, E5-compact-1-shot-asm-h1, E3-first, B-M4-gate (the tile-level hour on the E4 schedule: 0 brownout seconds, first red pip at 5:34–6:21 before the line runs) | programme, by the evidence; human may override | **made** |
| D-P3-2 | 2026-09-03 | **Fall time / rescue window vs refeed reset.** 90 s (D2) and the 60 s reset (D3) kept. The rescue the player actually has is the 4–20 min of red pip before the fortieth arrival (E1-starve N40: 7.2 min median, 4.3–19.6); the 30 s after the substation stops is the second chance, not the first. In the proto an idle Scenario B sees red ≈ 5 min before its first loss, 75 real seconds at 4×. §5 sentence | E1-starve-substation-N40, PHASE_2_REPORT.md Scenario B | programme; human may override | **made** |
| D-P3-3 | 2026-09-03 | **The unfed rule as it ships.** Per block; the unshot-arrival counter stops the substation at 40; it clears when every edge of the block has had a non-empty hopper for 60 s, which also restarts the substation if it is not shed; creep 1 tile per 3 s off, decays 1 per 20 s on, fall at 30 tiles = 90 s. A 10- or 20-arrival threshold loses the HQ at minute 8; creep from an empty hopper falls in 2.8 min. §5 sentence | E1-ring-substation-N10/N20, E1-starve-creep, E1-starve-substation-N40 | programme; human may override | **made** |
| D-P3-4 | 2026-09-03 | **Is the Relight survivable with banking, or the intended ending?** Survivable; banking is the intended play. The bank is a buildable object of ~two hours of eight-assembler production (~20,000 magazines): the 400-magazine line buffer and a 4,000-magazine bank both lose the hold (0/39/42 lost across seeds), uncapped (~13,700 peak) loses none. §16 sentence; §12 late column assumes eight assemblers; Phase 10 builds the object, E20 measures what a player fills | E9-hold | programme, direction D4 (human) | **made** |
| D-P3-5 | 2026-09-03 | **Bloom timer and drop (C8) locked** at `120/(0.5+d)`, 10 %, with no feel verdict; E10 not ported. §5 sentence | E10-bloom-cadence (Python, retired), `PHASE_3_REPORT.md` decision 3 | programme; human may override or ask for the port | **made (lock)** |
| D-P3-6 | 2026-09-03 | **Claim cadence (C2) locked** at one claim per 15 min in hour one, then one per 5 min; §18 generated from the sim at it | E8-cadence (gap 5 within 1 % of the caption; 4/6/8 miss by 19–41 %) | programme; human may override | **made** |
| D-P3-7 | 2026-09-03 | **Assembler rate (C9):** no Mk1/Mk2 ladder in the game; 20 mag/min from the 3 s recipe; the proto's Mk1 is a calibration stand-in until Phase 4 places the real machine | §12, calibration | programme; human may override | **made** |
| D-P3-8 | 2026-09-03 | **Python sim retired** in Phase 3's first commit: `frontsim.py`, `phase5*.py`, `export_*.py`, result JSONs deleted; `packages/sim/fixtures/*.json` frozen at `52c4ca3` (README there) | DEFERRED.md Phase 2 re-read; every doc tag names a TS run | programme, as D-CI planned | **made** |

Three decisions for the human (`PHASE_3_REPORT.md`), open until answered:

| ID | Date | Question | Recommendation | Status |
|---|---|---|---|---|
| D-P3-9 | 2026-09-03 | **Ratify Gate A passed without sessions, or run the five sessions in parallel with Phase 4 M1** and let their telemetry move the `[play: Gate A]` locks? | run them in parallel; nothing in Phase 4 M1 depends on the locks changing by less than 2× | **made 2026-09-03 by recommendation (the `Go` on `PHASE_3_REPORT.md`):** sessions run in parallel with Phase 4, by the human, any time; a contradicted lock moves and its tag becomes the session name |
| D-P3-10 | 2026-09-03 | **C3/C4 for the slice:** one ammo line per interior block and a 400-magazine chest as locked, or set the lot footprint budget now so Phase 4 M1 places against it? | as locked; the footprint budget is what M1 measures | **made 2026-09-03 by recommendation:** as locked; the footprint is measured when machines land on a lot (M2, since M1 is ground only). **Footprint measured at prompt B M2** (`SLICE_REPORT.md` "Prompt B M2" Measured): every non-Inert HQ-neighbour face gets 1 slot from area and holds 4–9 §11-scale lines — the slot is an economic cap, not a footprint limit; see D-B2-2 |
| D-P3-11 | 2026-09-03 | **C8 with no feel evidence:** port E10 to the harness (a day) before the slice encodes the timer, or accept the lock and let Phase 4's playtests move it? | accept; the port answers a number question, and C8 is a feel question | **made 2026-09-03 by recommendation:** accept the lock; no E10 port; Phase 4 playtests are the first place C8 moves |

## Phase 4 M1 decisions (the three for the human from `SLICE_REPORT.md` M1; made on the `go` that opened M2)

M1 is ground only: the tile grid, rubble, the river, the camera and the E toggle. Nothing below moves a sim number; each is a world-view constant the slice would otherwise encode by accident.

| ID | Date | Question | Evidence | Recommendation | Status |
|---|---|---|---|---|---|
| D-P4-1 | 2026-09-03 | **World-view zoom range.** The constitution's M1 says 0.5–3×; §4 said 1.0× down to 0.2× with the map view below 0.2×. At 0.5× the 648 px canvas shows one lot and its streets (≈ 40 tiles); at 0.2× it would show three blocks across. Keep 0.5–3× with E as the only route to the map view, or widen the floor to 0.2× so the two views meet? | `SLICE_REPORT.md` M1 measured (tiles drawn 1,849 at 0.5×, 81 at 3×, 60 fps at both) | keep 0.5–3× for the slice: M2's placement wants a lot to fill the canvas, and a 0.2× floor would draw 11,000 tiles a frame for a picture the map view already gives; revisit at Gate B if testers reach for the wheel when they want the map | **made 2026-09-03** by recommendation on the `go` that opened M2: 0.5–3× stands for the slice; revisit at Gate B |
| D-P4-2 | 2026-09-03 | **Units per rubble tile.** §12 says a rubble tile holds 300 units and a block 75–105k; the sim's pool is 3,840 per block (`PROTO_CALIBRATED`, calibrated in Phase 2). M1 draws the doc's 250–350 tiles and has each block's standing rubble follow its pool proportionally, so a tile is worth pool ÷ tiles ≈ 11–15 units. Keep the proportional stand-in through the slice, or set units per tile now so M2's excavator digs real units? | §12 table 12.1; `tiles.ts` `rubbleLeft`; `CALIBRATION_REPORT_2.md` (pool 3,840 is what keeps the steel wall at ≈ 3:40) | keep the stand-in through M2: the excavator digs tiles at 0.5/s and the block's pool stays the economy's truth, so no calibrated number moves; Phase 5's rubble model (E11/E13) sets units per tile with the deposit sizes | **made 2026-09-03** by recommendation on the `go` that opened M2: stand-in through the slice, Phase 5 sets the number |
| D-P4-3 | 2026-09-03 | **Deposits on the outskirts.** §12 puts iron and coal deposits on the outskirts as large facilities (~2M / ~1.5M); the sim's outskirts blocks carry no pool. M1 draws a 160-tile deposit patch on a quarter of outskirts blocks by hash (60 % iron, 40 % coal), inert to the sim. Keep the per-block placeholder until Phase 9's generator places deposits as named facilities, or place them now as facilities so M2's Foundry route has a real target? | `tiles.ts` GAME-ASSUMPTION deposits; §8 facility list (the Foundry is placed, deposits are not) | keep the placeholder: nothing in minutes 0–60 mines a deposit, and a facility-placed deposit belongs with the generator's validator (Phase 9) | **made 2026-09-03** by recommendation on the `go` that opened M2: placeholder until Phase 9 |

## Phase 4 M2 decisions (the three for the human from `SLICE_REPORT.md` M2)

M2 is the flow: the tile tick, Excavator, belts, inserters, the Depot, the Mk1 Shot assembler, hands. Every measured rate equals its doc rate; the three questions are about what the doc did not price and what the constitution changed.

| ID | Date | Question | Evidence | Recommendation | Status |
|---|---|---|---|---|---|
| D-P4-4 | 2026-09-03 | **Start stock and machine prices.** §11 opens with 200 steel, 100 copper, 50 stone; the calibrated sim opens with 80 steel / 40 Cu / 0 stone (`PROTO_CALIBRATED`, the Gate A numbers). §13 prices no machine, so M2 priced them (assembler 40 steel + 20 Cu as the block-level one, Excavator 10 steel, belt 1 steel, inserter 1 steel + 1 Cu; GA-M2-6). A §11 line (two Excavators, 18 belts, three inserters, one assembler) is 81 steel + 23 Cu: one steel short at the calibrated start, which a second of hand-mining covers (or the first Excavator does, once its steel is belted to the Depot). Keep the calibrated 80/40 and M2's prices, edit §11 to them, and let hand-mining be the opening's first lesson; or raise the start stock to §11's 200/100/50 so the whole line goes down without mining? | `SLICE_REPORT.md` M2 measured (placement in the browser); `flow.ts` `MACHINE_COST`; `CALIBRATION_REPORT_2.md` (80/40 is what keeps the steel wall at ≈ 3:40 for the bot, which never builds a line) | keep 80/40 and the M2 prices, edit §11's stock line to them at M6 once the played hour says whether the few seconds of hand-mining read as a lesson or a stall; a 200/100 start moves a Gate A lock for a comfort the bot never needed | **made 2026-09-03** by recommendation on the `Go` that opened M3: 80/40 and the M2 prices stand, §11 edited at M6; M3 adds that the §11 line plus its second Generator is 156 steel (D-P4-7) |
| D-P4-5 | 2026-09-03 | **Two assembler stand-ins.** With the flow layer on, hour one's magazines come from the physical line on the HQ lot (the block sim's HQ Mk1 is retired, GA-M2-11), but a claimed interior block still gets the block-level "Build assembler" button: 20 mag/min from stock with no footprint, no belts, no power. Keep the block-level assembler for claimed blocks through the slice (the slice is minutes 0–60 and §11 puts the second and third assembler on the HQ lot and the east lot), or make every assembler physical now so the slice's hour is one model? | `session.ts` GAME-ASSUMPTION (M2); §11 10–30 min (second Assembler on the east lot's copper); §12 ammo chain | keep both through M5 and decide at M6 with the played hour: §11's second assembler lands at minute 10–30 on the east lot, and M6 is where that lot gets a world-view line or a button; making every assembler physical before turrets and power exist (M3) would price a change nobody can play yet | **made 2026-09-03** by recommendation on the `Go` that opened M3: both stand-ins through M5, M6 decides; M3's turrets follow the same split (D-P4-9) |
| D-P4-6 | 2026-09-03 | **Belt rate 7.5 vs 8.** The constitution's M2 says belts carry 7.5/s; §13/§14 said 8/s (fast 16). 7.5 is four items a tile at 1.875 tiles/s and divides 20 ticks/s cleanly; 8 does not (an item every 2.5 ticks). Built 7.5, doc edited to 7.5 (fast 15), changelog line `M2-rates`. Accept 7.5/15, or restore 8/16 and let the belt carry a fractional tick? | `flow.ts` `BELT_PER_S`; flow test 3 (7.5/s measured through a corner) | accept 7.5/15: the constitution is the later word, the number is invisible to a player (a 20 mag/min assembler draws 0.67 steel/s off its belt), and the clean tick keeps belt tests exact | **made 2026-09-03** by recommendation on the `Go` that opened M3: 7.5/15 accepted |

## Phase 4 M3 decisions (the three for the human from `SLICE_REPORT.md` M3)

M3 is defence and power: turrets with hoppers, Generators, Lamps, poles, substations, streetlights, the §14 shed order through the tile machines. Every measured rate equals its doc rate; the three questions are about what the doc's opening assumes and what M3 built only on the HQ.

| ID | Date | Question | Evidence | Recommendation | Status |
|---|---|---|---|---|---|
| D-P4-7 | 2026-09-03 | **Hour-one power at the start, and the Generator feed in the shed order.** §11 opens with one 300 kW Generator on 40 coal and says one Generator alone browns out at minute 8. At tile level the §11 line itself (three Excavators, one assembler, four inserters) draws 320 kW, plus the HQ substation's 100: 420 kW from the first tick, so the assembler and two inserters are shed at 0:00 until the second Generator at minute 6 (the §18 drawing: brownout 120 s, then 500 / 600 kW and everything running). The 40 coal are 533 s of full load. And at the sim's 80 steel / 40 Cu start (D-P4-4) the line plus that Generator is 156 steel + 36 Cu, out of reach. Keep one 300 kW Generator and 40 coal and let "build the second Generator first" be the opening's lesson; start with two Generators (or a 600 kW one) so §11's line runs from placement; or raise the Generator's kW? And the 1 h soak found §14's "Excavators feeding Generators last" self-defeating at tile level: when the bot's third claim put a fourth substation on two Generators (590–600 kW against 600) the shed order reached the coal feed before any substation, the Generators burned dry and the grid was dead by the hour. Should the Generator feed (coal Excavator and its inserters) be exempt from shedding, like belts and turrets, so a brownout can only ever cost machines and substations? | `SLICE_REPORT.md` M3 measured (§18 at 10 minutes; the 1 h soak's dead grid); defence tests 4 (533 s) and 5 (the order); `docsync.ts` `section18lot`; §11, §14, §25 item 14, risk 7 | keep one 300 kW Generator and 40 coal through M6, and make the Generator feed exempt from shedding at M4 (a one-line rank change with a §14 changelog line: shedding a 20 kW feed to close a 10 kW gap costs 600 kW, and no player would read a dark city as the power rule working): a second Generator is 30 steel + 10 Cu, the coal patch is beside it, and a shed assembler with a red cross at minute 0 is the power rule's first lesson the way the red pip is the ammo rule's; decide with D-P4-4 at M6 from the played hour, since the 80/40 start is what makes it bite | **made 2026-09-04 (the human): settled before M4, not at M6.** The M3 script was stale — one Generator and the whole line at 0:00, the pre-E4 hour; aligned to E4 (Generators at 0 / 6 / 15 / 45, the machines at E4's minutes, the coal Excavator on the HQ patch) the tile-level hour holds on seeds 3 / 4 / 5 with 0 brownout seconds (`B-M4-gate`). The Generator-feed question is moot: D-B3-4 removed the shed order. One Generator and 40 coal stay (D-P4-4 at M6). |
| D-P4-8 | 2026-09-03 | **Six start turrets or two.** §11 says two Gun turrets on the north edge, then hand-feeds the west and east ones; the block sim's ring has three HQ edges with a 100-round hopper each and C10 (a Gate A lock) fills two and leaves the third empty as the pip ladder's first lesson. M3 starts six (two a side, GA-M3-1), the north pair where §18 drew them. Keep six and edit §11 at M6, or start two on the north edge and let the west and east edges begin as the block-level stand-in until the player places turrets there? | `flow.ts` `ensureFlow`; defence test 1 (the C10 fill through six turrets); §5 (two turrets an edge, "1 per 16 tiles"); §11 | keep six: C10's ladder needs three edges with hoppers, two turrets an edge is §5's own price, and a start with empty street sides would make the east pip's red mean "no turret" instead of "no ammo"; edit §11's sentence at M6 with the rest of its opening | superseded 2026-09-04 by D-B1-4: the start turrets are derived from the HQ's segments (one per 16 tiles of length, at least one a segment) — neither six nor two; §11's "two on the north edge" is still a doc line to settle at M6 |
| D-P4-9 | 2026-09-03 | **Turrets on claimed blocks.** On the HQ an edge fires only through its physical turrets (GA-M3-4); every other Held block keeps the block sim's 100-round edge hopper, fed from the Depot buffer with no turret placed, as M2 kept the block-level assembler (D-P4-5). §11's east claim (minute 10–30) is the first block a player would want to fortify. Keep the split through M5 and decide at M6 with D-P4-5, or make every front edge physical now (the claim lays two turrets a side from stock, the kit rule of §14 brought forward)? | `flow.ts` `hookSyncEdges`; `SLICE_REPORT.md` M3; §14 front kits; D-P4-5 | keep the split through M5 and decide both at M6: M4 puts enemies on the streets and M5 the light, and only then does a claimed block's world view have anything a turret changes; a claim that lays turrets from stock is one option for M6, the block-level hopper another | open |

## Rework decisions (D5, D6) — made by the human on 2026-09-03

Two human decisions change the game's presence model and its geometry. Neither adds a system (§26 recount in `REWORK_REPORT.md`). Both invalidate the Phase 4 slice as a test of the game being made: Gate B is not scored on M1–M3, and the slice is rebuilt after the rework (`docs/relight-prompt-B-vertical-slice.md`). Logged before any other rework edit, as the brief asks.

| ID | Date | Decision | What it fixes | Constants the human set (all GAME-ASSUMPTION, listed in §25 and the report) | Status |
|---|---|---|---|---|---|
| D5 | 2026-09-03 | **The player is an engineer on foot, with a weak weapon.** A sprite on the tile grid; the camera follows; the map view is a key (M), not the primary screen. Reach radius for mining, crafting, placing and picking up; a personal inventory with a slot cap; the Depot is a chest on the HQ lot the player walks back to, not a global pool. Hand placement draws from the inventory; the Line truck (found at the Tram depot, driven first, automated later) draws from the Depot. Walking is a real cost with a tedium-audit row; its removal tool is the truck. One rifle firing magazines from the inventory; a crawler takes ≈ 2 s to kill (a turret 0.6 s); the turret rules apply (shades untargetable off light, hulks slowed only, no range advantage); one upgrade at the Arsenal; the game is completable without firing. Retaliation only: crawlers keep their targets (lamp → turret → substation) and turn on the player only when shot or blocked by them. Player HP; at zero the player is knocked down and respawns at HQ with the inventory intact; no other penalty. Rot and dark blocks do not hurt the player; walk anywhere. | §22's "nothing walks" and §23's rejected avatar: the game had no presence, so the tile layer (M1–M3) was a cursor over a map | reach 8 tiles; inventory 40 stacks; rifle time-to-kill ≈ 2 s a crawler; player HP and respawn at HQ; unprovoked targeting never | **made 2026-09-03** by the human; applied in the rework |
| D6 | 2026-09-03 | **The city is generated streets-first; blocks are the faces.** Curved arterials, a bending river, one diagonal avenue, secondary streets that branch and dead-end, plazas and parks as inert faces. Street widths 6–12 tiles. A block is a face: an irregular polygon with 3–7 neighbours, ≈ 20–50 tiles across, rasterised to tiles. The front rule is unchanged, on the graph: a node is a block, an edge a shared street segment; frontage = held–dark edges; interior = a held node with no dark or contested neighbour; a well dies when all its neighbours are held; pips, ring order, blooms and the arrival counter are per edge. The experiments transfer; every number is re-run. Machine slots scale with lot area; the rubble pool scales with area. Districts are regions of the graph by band from HQ (§17). | §3/§17's 24×24 lattice: four neighbours a block made every claim the same shape, every front the same width, and the §18 drawings unreadable as a city | one slot per ≈ 600 buildable tiles, minimum one; street widths 6–12; blocks 20–50 tiles across with 3–7 neighbours | **made 2026-09-03** by the human; applied in the rework |

## Rework decisions open before the slice is rebuilt (D-R1–D-R4) — recommended in `REWORK_REPORT.md` §8, 2026-09-04

| ID | Date | Decision | Options | Recommendation | Status |
|---|---|---|---|---|---|
| D-R1 | 2026-09-04 | **The rifle and the shade.** E-rifle: a whole belt lost for 90 s falls to a shade at an unlit edge with or without the rifle; a single dry edge holds either way; damage 10 HP a kill, 62 HP a rescue, no knockdown. | (a) keep D5: the rifle is for crawlers, the lesson is "light the edge"; (b) a hand lamp on the engineer (2-tile radius) so a shade at arm's reach is targetable; (c) the rifle hits shades | **(a)**; (b) only as a slice M5 experiment if Gate B testers lose a block to a shade while standing on it; (c) makes the rifle the defence (§24 risk 10) | **made 2026-09-04 by recommendation (a)** on the human's `go` for prompt B M1: D5 stands, the rifle is for crawlers and §5's "light the edge" is the shade's lesson; the hand lamp is an M5 experiment only if a Gate B tester loses a block to a shade while standing on it |
| D-R2 | 2026-09-04 | **The kit walk and C1/C2.** With the bots walking, first amber and first red land at the claim minute (16–31 min) because the claim kit walks from the chest; C1/C2 as written cannot hold. | (a) keep the walk and rewrite C1/C2 to "no red on a kitted edge before 120 min"; (b) a claim ships its kit by truck; (c) the kit is pre-stocked on the claimed block's substation | **(a)**; (b) is the truck's later life and (c) hides the tedium row the truck exists to fix | **made 2026-09-04 by recommendation (a)** on the human's `go` for prompt B M1: the walk stays; C1/C2 rewritten in `calibrate.ts` to score kitted edges only (an unkitted edge is red by construction until the kit walks out) and re-run (`docs/experiments/calibration.md`); doc §25 changelog line |
| D-R3 | 2026-09-04 | **The HQ's copper.** The city HQ is civic and fell at minute 27 on every calibration run until the block-only start patch gained 3,840 copper at 32/min (the lattice's accidental supply); the tile layer's HQ copper patch is 1,200. | (a) 3,840 in the block sim, 1,200 in the tile layer, reconciled at M6 with D-P4-4; (b) 1,200 in both; (c) the HQ face is residential, no patch | **(a)**; M6 counts the hour's real copper bill | **made 2026-09-04 by recommendation (a)** on the human's `go` for prompt B M1: 3,840 copper at 32/min stays in the block sim's start patch and the tile layer's HQ copper patch stays 1,200; reconciled at prompt B M6 with D-P4-4 from the played hour |
| D-R4 | 2026-09-04 | **The generator's attempt budget.** E-variance's 10,000-seed run found one seed (3767) with no valid city after eight jitter streams — "no well site: west riverside" — which turned the run red. Each attempt is an independent draw and the validator rejects 26 % of them (mean attempt index 0.35), so a budget of 8 leaves ~1 seed in 48,000 with no city and the check was flaky by construction. | (a) raise `CITY_ATTEMPTS` 8 → 16 (0.26^16 ≈ 1 in 2.3 billion); (b) keep 8 and restate the check as "at most 1 in 10,000"; (c) relax the well-site search so fewer streams are rejected | **(a)**: it changes no seed that already validates, because the loop breaks at the first valid attempt, so every measured number in this report and the doc stands; (b) leaves a known-broken map reachable in play; (c) would move every well on every seed and invalidate the whole rework's evidence | **made 2026-09-04 by recommendation** — a red check is a red build and the fix cannot wait for the gate; the human can reopen it at Gate B |


## Prompt B M1 decisions (D-B1-1–D-B1-5) — recommended in `SLICE_REPORT.md` "Prompt B M1", 2026-09-04

| ID | Date | Decision | Options | Recommendation | Status |
|---|---|---|---|---|---|
| D-B1-1 | 2026-09-04 | **The chest's start stock.** The game's Depot chest holds §11's 200 steel, 100 copper, 50 stone and 20 magazines; the harness keeps the calibrated 80 steel / 40 copper / 0 stone (config `5f3417b9`), so a bot in the browser plays a richer start than the calibration's. | (a) keep both until M6's human hour decides the start (D-P4-4); (b) put the harness on §11's numbers and recalibrate; (c) put the game on 80/40/0 | **(a)**: the calibration is hash-locked and the human hour is the test of §11's numbers, not the bot's | Open — settles with D-P4-4 at M6 |
| D-B1-2 | 2026-09-04 | **What the engineer walks through.** Belts and poles are passable, every other machine and the river are solid (`walk.ts`). | (a) as built; (b) belts solid too; (c) belts carry the engineer (the Factorio rule) | **(a)** until M2 puts a line across the HQ lot and a tester has to cross it; (c) is a system | Open — settles at M6 |
| D-B1-3 | 2026-09-04 | **Kits by hand.** The harness bot restocks kits on standing at the HQ; a human takes them from the chest (I), one a claim, and a claim made with none in the pockets toasts that its edges wait. | (a) as built; (b) auto-restock the human at the HQ too; (c) the claim puts a kit in the pockets when made within reach of the Depot | **(a)**: the walk to the chest is the tedium the truck removes (§19); (b) hides it from the audit | Open — settles at the M6 tedium audit |
| D-B1-4 | 2026-09-04 | **Placement by face geometry.** Where a face's pre-existing substation, its streetlights and the HQ's start turrets stand on a D6 face, which has no lot coordinates. Substation: the buildable 3×3 nearest the lot's centroid (1×1 stand-in on a face too thin). Streetlights: on the kerb along each segment at a fixed spacing (GAME-ASSUMPTION: every 4 tiles), count per segment from its length — replaces "8 per edge". Turret kits: per segment, count from its length at the turret's range (GAME-ASSUMPTION: one per 16 tiles of segment length, minimum one), placed on the segment's front ring; a corner sliver whose front cannot hold a 2×2 is covered by the neighbouring segment's turrets that reach it — replaces the fixed six and is the rule-level fix for the HQ falling at minute 20 (no HQ special case remains in the tile sim; the block-only harness keeps an abstract start ring at t = 0 in its place). | (a) as decided; (b) the substation on the face's street side; (c) the fixed six turrets with an HQ-at-t=0 kit branch | **(a)** | **made 2026-09-04 by the human** ("Four small things before Prompt B M2", item 2); built as `ground.ts` `faceSubstation`/`faceLights`, `flow.ts` `startTurrets`/`faceSegOf`/`edgeTurrets`, `defence.test.ts` seeds 3/4/5 (every HQ segment covered, HQ holds 3 min), the minute-20 soak; supersedes D-P4-8 |
| D-B1-5 | 2026-09-04 | **Direct control.** WASD moves the engineer (8-way, solid machines/walls/river); Shift sprints at 1.6× on a stamina bar (≈ 4 s, ≈ 6 s refill) that sprint can never drain below one dodge; Space dodges 3 tiles in 0.25 s with crawler invulnerability and a 1 s cooldown for a fixed quarter of the bar. Left-click does what the hand holds (mine / place / fire); right-click removes or clears the hand; E interacts and never mines, places or fires; Tab/I pockets, B build menu, 1–9 hotbar (9 = rifle), R rotate, Q pipette, M map, Esc closes. No click-to-walk in the world view; the map's Held/street click is the only auto-walk. The rifle is a hotbar item aimed at the cursor: hitscan to the turret's 9 tiles, 1.5-tile hit radius, no auto-target. §19 gains a 5 % time-in-danger guard. The rifle, sprint and dodge are the only skill inputs and the game is completable without them. | (a) as decided; (b) Factorio's layout (click-to-walk, spacebar auto-target, no stamina); (c) direct control without the dodge floor | **(a)**: the layout is deliberately not Factorio's where it doesn't need to be, and the dodge floor is the whole point of having both sprint and dodge | **made 2026-09-04 by the human** ("Four small things before Prompt B M2", item 1); built as `body.test.ts` and the world view's hand/hotbar; the M1 "Tool keys" row (N inserter, F assembler) is superseded by the hotbar |

## Prompt B M2 decisions (D-B2-1–D-B2-3) — recommended in `SLICE_REPORT.md` "Prompt B M2", 2026-09-04

| ID | Date | Decision | Options | Recommendation | Status |
|---|---|---|---|---|---|
| D-B2-1 | 2026-09-04 | **Machine crafting.** §13 prices no machine and never says whether one is made before it is placed; M2 pays the lattice's rubble prices from the pockets at placement (GA-B2-1) and a pick-up returns the machine itself, never a refund. | (a) rubble at placement, as built — instant, the price shown on the ghost; (b) a workbench recipe with a craft time, the machine an item in the pockets first (Factorio's way); (c) machines stocked in the Depot chest and brought by the truck (§19), never crafted by hand | **(a)** for the slice; **(b)** with the Phase 5 recipes, which also decide the prices (the lattice's are stand-ins, GA-M2-6) | recommended; the human decides |
| D-B2-2 | 2026-09-04 | **Slot count vs footprint (D-P3-10 measured).** `slotsOf` = floor(area / 600) gives one slot on every HQ-neighbour face (391–1,197 tiles) and two on an Inert plaza that holds nothing, while the free footprint holds 4–9 §11-scale lines and 33–110 3×3 machines. The tile layer does not enforce the slot and the block economy does not see the tiles. | (a) keep floor(area / 600) as C3's economic cap for the slice, the tile layer free of it; (b) derive slots from free footprint (~1 per 150 free tiles, 4–9 a face) and let §11's economy scale with it — moves C3/C4 and the calibration; (c) drop slots when tile lines replace the block stand-in at M6 (D-P4-5) and let the footprint be the only limit | **(a)** now, **(c)** at M6 — the slice measures the doc's economy as locked, and the stand-in goes with the lattice | recommended; the human decides |
| D-B2-3 | 2026-09-04 | **What a pick-up carries.** §14 says "into a stack"; a machine holds items (a turret's rounds, an assembler's inputs, a belt's items). M2 returns the machine and each item type it held as their own stacks, a turret's rounds as whole magazines with the loose remainder to the line buffer (GA-B2-2); a full pocket refuses the whole pick-up with a toast. | (a) as built; (b) contents spill onto the tile as ground items (Factorio's way — the slice has no ground items); (c) contents are lost on pick-up | **(a)**; §11's "four stacks for two turrets" holds (a turret and its magazines) | recommended; the human decides |

## Prompt B M3 decisions (D-B3-1–D-B3-4) — recommended in `SLICE_REPORT.md` "Prompt B M3", D-B3-1–3 made and D-B3-4 given by the human before M4, 2026-09-04

| ID | Date | Decision | Options | Recommendation | Status |
|---|---|---|---|---|---|
| D-B3-1 | 2026-09-04 | **The craftable Substation's price.** §13.14 prices it at 20 frames + 20 wire + 10 boards, none of which exist before Phase 5; prompt B M3 pays 50 steel + 25 Cu in rubble at placement (GA-B3-2), the same stand-in rule as every machine (D-B2-1). | (a) 50 steel + 25 Cu in rubble, as built; (b) free for the Electricians — the group brings it, one a face; (c) wait for Phase 5's items and leave the outskirts unholdable until then | **(a)**: it keeps the outskirts holdable in the slice at a price a §11 chest can pay once, and Phase 5 swaps the number for the recipe with D-B2-1 | **made 2026-09-04 (the human): (a)** — 50 steel + 25 Cu in rubble until Phase 5 |
| D-B3-2 | 2026-09-04 | **The outskirts at tile level.** §7 says an outskirts block has no substation and no streetlights; the tiles now show that (134 / 131 / 128 faces on seeds 3 / 4 / 5, GA-B3-6) while the block sim still powers a Held outskirts block through its abstract substation (D-P4-9's split), so a Held outskirts face without a craftable Substation is powered by a thing the world view does not draw. | (a) keep the split through M5 and settle it with D-P4-9 at M6; (b) make the block sim require the craftable Substation for an outskirts hold now — a rules change that moves the fixtures and the calibration; (c) give the outskirts a pre-existing substation too, against §7 | **(a)**: M4's enemies and M5's light are what make a claimed block's world view matter; changing the block sim's outskirts before that moves fixtures for a state no one can play yet | **made 2026-09-04 (the human): (a)** — keep the tile/block split through M5, settle with D-P4-9 at M6 |
| D-B3-3 | 2026-09-04 | **When a group's unlocks land, and whether they stay.** §11 has the Electricians "walk into the Depot" at 30–60 min and their machines appear; the sim has no walk-in event. Prompt B M3 unlocks on the held event of the group's block and keeps the unlocks after the block falls (GA-B3-1), on keys 0 / [ / ] (GA-B3-8). | (a) on Held, kept after a fall, as built; (b) lost with the block until it is Held again; (c) on a walk-in event the sim would have to gain (the group leaves its block for the Depot after Held, §11's picture) | **(a)**: a fall already costs the block; taking the menu rows too would punish the one thing the player learned to want, and §11's walk-in is a story beat M6 can add over the same event | **made 2026-09-04 (the human): (a)** — as built |
| D-B3-4 | 2026-09-04 | **Proportional brownout replaces the shed order.** When demand exceeds supply every machine on the grid runs at supply ÷ demand; nothing switches off, nothing has an order, no substation is ever stopped by power; substations stay binary and stop only under the unfed rule. Lamps, Floodlights and streetlights are lit at any throttle above zero and dark only on a dead grid (GA in `stepFlow`); belts and turrets are never slowed. The §14 shed order, `SimConfig.shed`, `Block.shed`, the shed/restore events and the game's red cross are gone; the HUD shows brownout seconds and the percentage, the toast says every machine runs at N % and nothing switches off. | (a) proportional, as the human specified; (b) the shed order with the Generator feed exempt (the M3 recommendation); (c) proportional for machines, shed order for substations | **(a)**, given by the human: legible (machines visibly slow, one bar on the HUD), cannot cascade, and the sim confirms the ring starves slowly behind the red pip under a sustained shortfall — first pip 67–216 min into a 50 % shortfall, 12–16 at 90 %, the first fall 1.7–7.5 min after the pip in every run (`E2-sustained6h-h500-*`); a 10-minute or one-hour shortfall of up to 50 % costs nothing (`E2-matrix`). Fixtures `city{3,4,5}.json` unchanged (power off in them); snapshot config hash `5f3417b9` → `68d07000` (SimConfig lost `shed`). | **made 2026-09-04 (the human); built** |
