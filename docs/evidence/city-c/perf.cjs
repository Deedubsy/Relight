const fs=require('fs');const{chromium}=require('C:/Users/Admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{const p=await b.newPage({viewport:{width:1600,height:1000}});await p.goto('http://127.0.0.1:5178/?view=world');await p.waitForFunction(()=>window.__relight?.world);await p.evaluate(()=>{Object.assign(__relight.session.state.engineer,{x:546.5,y:251.5});__relight.world.setZoom(.5)});await p.waitForTimeout(3500);const cdp=await p.context().newCDPSession(p);await cdp.send('Profiler.enable');await cdp.send('Profiler.start');await p.evaluate(()=>__relight.world.drawMs());const a=await p.evaluate(async()=>{const f=[];let last=performance.now();for(let i=0;i<240;i++)await new Promise(r=>requestAnimationFrame(t=>{f.push(t-last);last=t;r()}));return{frames:f.sort((a,b)=>a-b),draw:__relight.world.drawMs()}});const{profile}=await cdp.send('Profiler.stop');const top=profile.nodes.filter(n=>n.hitCount).sort((a,b)=>b.hitCount-a.hitCount).slice(0,25).map(n=>({name:n.callFrame.functionName,line:n.callFrame.lineNumber,hits:n.hitCount}));const result={p50:a.frames[120],p95:a.frames[228],max:a.frames.at(-1),draw:a.draw,top};console.log(result);fs.writeFileSync('E:/Factorio2/docs/evidence/city-c/performance.json',JSON.stringify(result,null,2));fs.writeFileSync('E:/Factorio2/docs/evidence/city-c/performance.cpuprofile',JSON.stringify(profile));}finally{await b.close()}})();
