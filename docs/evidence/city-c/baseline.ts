import * as S from '../../../packages/sim/src/index';import{writeFileSync}from'node:fs';const s=S.createCampaign(),g=S.ground(s);let land=0,accessible=0;for(let y=0;y<g.th;y++)for(let x=0;x<g.tw;x++){if(g.owner[y*g.tw+x]!==-2)land++;if(S.passable(s,x,y))accessible++;}writeFileSync('docs/evidence/city-c/baseline.json',JSON.stringify({width:g.tw,height:g.th,land,accessible,buildings:S.RIVERFRONT_BUILDINGS.length,city:S.RIVERFRONT,buildingsData:S.RIVERFRONT_BUILDINGS,props:S.RIVERFRONT_PROPS}));writeFileSync('docs/evidence/city-c/before-save.json',JSON.stringify(S.makeSave(s)));console.log({land,accessible});
