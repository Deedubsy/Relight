# P9-04 failure-path analysis

These are read-only findings on the retained population source, not implemented fixes or adopted design changes. Original seeds and failures remain in `population`; ordinary attempts and repeat failures are separate evidence.

| Finding | Existing code path | Implication and candidate correction |
|---|---|---|
| No clear district facility pad (representative 80) | `campaignDistricts.ts` chooses a reachable route endpoint/stop, then greedily places workshop and three source pads; coal uses the nearest distinct place before testing its five-tile pad. `pad` throws if that selected place has no fit. | Route access alone does not establish the later reservations. Consider deterministic candidate selection against all required footprints before committing the district composition. |
| No workshop discovery side yard (88) | `campaignDiscovery.ts` restricts candidates to the chosen workshop block, outside all essential installations and the guardian leash margin, then checks reach. | An optional reward currently prevents a campaign from initializing when the already selected block has no yard. Preserve optionality and existing safety distances while trying valid composition alternatives. |
| No Electricians shelter (842) | `campaignRecruits.ts` floods only `safe` tiles, excluding cache/ruin danger radii during traversal as well as at candidates. | The safe connected component can leave no eligible district candidate. Reconcile approach/candidate constraints without silently changing reward or threat rules. |
| No initial stop pad (2313) | `expansion.ts` chooses the route before proving both adjacent stop footprints and the terminal's future street exit. | A selected route endpoint can fail its later stop contract; consider the whole route/stop arrangement while selecting candidates. |
| No district trunk extension (3610) | `campaignDistricts.ts` uses the fixed earlier terminal and excludes branches, prior stops, inert-adjacent street and obstructed stop footprints. | Initial terminal validity does not prove that the next district can connect. Validate continuation before selecting the earlier arrangement. |
| Rubble on stop footprint (102) | `expansion.ts` footprint selection and current physical placement disagree on ordinary non-patch rubble. | Seed 102 needs 300 stone mined before stop placement. The static failure remains; ordinary mining can clear it, but adds time/inventory burden. Any later relaxation of the clean-survey contract must be explicit; do not relabel the existing red row as a pass. |

All five generation failures reproduce twice at their earliest recorded seeds, before an ordinary command driver can start. Their base generator retry counts are separately retained. The stop probe uses the real paid restoration/earned kit, construction refusal, mining, placement, truck delivery, command log and conservation. Its original full-pocket stall and driver corrections remain recorded. In the final longer run, two unattended bases are disabled and freight fails; remaining fuel and zero stop throttle are in `rubble-findings.json`. This is not a defence pass or a claim that every blocked stop can be cleared safely.

The remediation should preserve existing saved geometry, stock and log provenance; version any new generation behavior; retain this full population; and rerun the declared population on the corrected source. No historical E-variance or older preset result substitutes for that comparison. Human ten-seed review and reference-machine performance remain separate.
