# UI redesign evidence

## Planning baseline — 2026-09-08

Specification: [RI-02B](../../RI-02B_UI_SPEC.md). Task statuses remain exclusively in [PROGRESS](../../PROGRESS.md). This note contains observations/checks, not a second tracker.

- Opened the actual EX-08D frozen game after restarting its existing hash-validating `serve.py`; initial connection was refused because the server was stopped. URL: `http://127.0.0.1:5180/?rules=exploration-v2&seed=3&view=world&state=/fresh.json`.
- Captured and opened [baseline-opening.png](baseline-opening.png): 1280×720, paused 0:00, seed 3, default world camera 0.65×, empty pockets. Frozen prepared fresh start; no developer walkthrough, natural progression or independent-player validation claimed.
- Visually observed small text, dominant right sidebar, repeated assault guidance and expanded future construction controls. Only the first station appeared in the current opening discovery list; the unavailable supplied screenshot's later-project list was not confirmed. No user attachment image was opened.
- Code inspection confirms radio restoration and precision upgrade have different authoritative prices and effects. No economy mismatch was repaired.
- Windows `npm run docsync:check` and `npm run freshness:check` failed to launch: `tsx` was not resolved. Sandboxed WSL returned access denied. Running the existing WSL dependencies with approved execution access succeeded in launching both checks.
- `npm run docsync:check`: passed, campaign reference and legacy generated tables match their own profiles.
- `npm run freshness:check`: failed, 12 campaign E-chain/E-coal/E-logistics/E-tram files across seeds 3/4/5 carry config hash `9d725a52`, current checkout is `9fc92bfc`. No simulation source or generated experiments were edited by this documentation run; existing/concurrent checkout differences remain outside its scope. The stale evidence was not regenerated or relabelled.
- Local consistency check: see [planning-check.json](planning-check.json). Checks new task IDs, dependencies, code/document links and entry-point pointers; this does not validate the proposed UI implementation.
- No production build, full simulation suite, long seed batch or human play session was run for this planning task. Existing P9-01 completion/evidence was preserved when its handoff changed during planning.

## Implementation evidence convention

Append a short section for each implemented package with exact source/build identity, commands and outcomes, labelled prepared states, screenshot paths and what was seen when opening them. Keep UI-07's short follow-up protocol and any actual observations here. Missing independent-player feedback remains explicitly pending and separate from developer walkthrough results. Never overwrite older frozen builds or claim their images show the redesign.

## Sequencing revision — D-UI-02

Owner moved RI-02B after Phase 9 engineering/preparation (P9-05). Checked unique task IDs, removed reverse P9 dependencies and preserved completed P9-01; P9-02 is next for implementation, UI-01 blocked on P9-05. The earlier planning-check.json retains its original checkpoint meaning. Documentation sync passes; freshness still reports the same 12 campaign fingerprint mismatches. No production code or historical experiment evidence changed.

## Cross-document pointers — 2026-09-08

Added the explicit RI-02B phase between Phase 9 and Phase 10 in PHASES, aligned GDD/agent entry points and development-plan links, and mapped brought-forward standards with remaining Phase 12 scope preserved. Marked D-UI-01 sequencing as superseded; historical reports remain intact. Corrected the stale PROGRESS summary to P9-03 after existing P9-02 completion. Phase placement, unique task IDs and one-way P9-05 → UI-01 dependencies checked. An initial docsync marker error was caused by CRLF line endings during this edit; restoring LF resolved it and docsync passed.

Freshness also passed against the current checkout on this run. The earlier recorded fingerprint failures remain historical observations; this pointer update did not regenerate experiments.

## UI-01 — Shared tokens and shell/input foundations — 2026-09-09

**Implementation complete; automated validation passed; delivered-interface human approval and independent play feedback not recorded.** Owner authorised “Let’s move into RI-02B-UI-01”. [PROGRESS](../../PROGRESS.md) alone owns status. D-UI-03 records the implementation choices. No commit or push.

[Open the current development preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json); [ordinary-command prepared construction start](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-construction.json). Port 5178 is mutable; final bytes are retained in [build.zip](ui-01/build.zip). Frozen post-P9 port 5181 and EX-08D/5180 remain unchanged. These starts reuse exact P9-05 fixture bytes; the supplied construction opening reveals Foreman/station/blueprint information and is not unaided discovery evidence.

### Delivered boundary

The actual entry now mounts a full-viewport canvas, labelled navigation and one scrollable main drawer. Existing domain panels retain their commands, stock scopes and checks. The shell routes Build/clipboard/library, Inventory, Projects, Management/base defence/production, Construction delivery, contextual inspection/cargo/station routes, Help and Navigation. Legacy ring/facilities/summary remain accessible. Developer retains profile links, coordinates and telemetry; player-needed power, buffers, production and loss history stay accessible through management or inspection.

Shared petrol/ivory/brass tokens provide 17px body, 16px secondary text, 40px button targets, clear focus and disabled styling, reduced-motion support and layer ordering. `el` and layer/focus ownership now live in `uiShell.ts`; remaining canvas HUD text reads resolved CSS presentation values. Measured text pairs have minimum **5.60:1**, control-border pairs **4.00:1**, and the primary button **9.32:1**. The candidate border `#69837D` failed against raised controls; `#8DA79F` replaces it after rendered inspection. This measures the declared opaque shared pairs, not every remaining legacy/world label or full accessibility certification.

Pause uses ordinary `setSpeed(0)` and restores the previous nonzero speed (or 1×), with Resume, save/load/download, copy link, controls/item help, Developer and a seed-entry start menu with unsaved-progress confirmation. Its focus stays inside the modal; child Help/start screens close before Pause. Ordinary drawers preserve running speed and paid actions remain possible while paused. Ctrl+S/O stay available outside text fields, including drawer controls. Missing saves show the existing explicit refusal and recovery links without replacing a campaign or changing stored saves.

Input capture clears held movement/sprint and unfinished pointer gestures; typing/select editing, UI clicks and drawer wheel events cannot invoke world actions. A captured belt/copy drag is cancelled before the old pointer-up commit path can run. Returning from a drawer requires a fresh world press; a first click on the city while a drawer is open dismisses that drawer without acting underneath it. Escape closes one layer, then clears placement/selection, then opens Pause; Escape in Pause resumes. Projects uses the shared F2 binding; J remains splitter. Camera framing reserves room for the drawer, existing top HUD and bottom navigation without changing engineer position.

This is the shell foundation. The old build-first objective, repeated canvas guidance and domain-panel content remain for UI-02–05; minimap, final quickbar/categories, structured inspection/projects and the full scaling/remapping/settings matrix are not delivered here. No gameplay costs, reward rules, save schema, generation or simulation/harness source changed. No image generation, replacement framework or new asset system was needed.

### Actual checks and visual review

- **351/351 full tests**, zero failed/skipped, 420.06 seconds: [test log](ui-01/test.log). Simulation/harness/session sources are unchanged from P9-05; no new population or long campaign batch was required.
- Package typecheck/build and lint pass: [typecheck](ui-01/typecheck.log), [final game typecheck/build](ui-01/game-build-final.log), [lint](ui-01/lint.log). The Vite large-chunk advisory remains. Documentation/profile sync, freshness, native whitespace and reference/archive/status checks are linked below.
- [Browser input script](ui-01/browser.cjs), [final log](ui-01/browser-final.log), [results](ui-01/browser-result.json): actual production UI at **1366×768 and 900×768**; text in blueprint name, scroll without zoom, Close while belt selected without placement, Escape hierarchy, modal focus, held W capture/fresh press, one drawer, running/paused semantics, cancellation of an actual affordable belt drag, local save/reload, exact replay, and Pause restoring 4×.
- [Adapter script](ui-01/adapters.cjs), [log](ui-01/adapters.log), [results](ui-01/adapters-result.json): real truck source/order selection and pause, order pause/resume, library export, cargo access, contextual machine inspection, save/load from a drawer control, cancelled fresh-city confirmation, blur/fresh input, legacy management and a missing-slot refusal preserving browser storage. The missing-slot exception is expected and separately recorded; ordinary entries have no page errors. No stock/position/HP injection was used.
- Opened actual [1280×720 seed-3 opening](ui-01/opening-1280.png), [900px Build drawer](ui-01/build-900.png), [Pause](ui-01/pause-1366.png) and [missing-save surface](ui-01/missing-save.png). Opening uses paused tick-zero P9-05 data and camera 0.65×. Canvas area/framing differs from the old 420px sidebar baseline: the engineer is centred in the remaining safe rectangle. Narrow navigation wraps into two rows; the drawer scrolls internally, readable fields stay in bounds, and the engineer remains visible between HUD and drawer. Pause leaves the existing warning readable above it. Existing HUD prose remains prominent and is explicitly UI-02 work.
- Development diagnostics: initial TypeScript build found non-iterable DOM collections (corrected with `Array.from`); initial navigation mount assumed its reference node was still a direct sidebar child (fixed with a dedicated adapter mount). Test-harness diagnostics included a route glob matching the page query, a queued-speed assertion before the next frame, an assumed library ID and inspection before world focus/camera settled. Corrected tests use actual options and normal input/frame boundaries. [Initial build log](ui-01/typecheck-initial.log), [earlier browser log](ui-01/browser.log), [final scripts/results](ui-01/browser-result.json). Final required checks pass; no simulation rule was relaxed.

No fresh performance benchmark or independent-player usability session was run for this bounded foundation step. Existing 116.8/266–300ms findings and reference-performance obligations remain open; these UI checks do not establish a performance fix. Formal P9-H/P6-H/P7-H/P8-H/EX-08H/T18 and P6-AMMO remain separate.

### File changes and reproducibility

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`, unchanged campaign fingerprint `ba11e896`. [Manifest](ui-01/manifest.json), [source archive](ui-01/source.zip), [exact delta against P9-05](ui-01/source-delta.patch): **206 source inputs, six production files and three unchanged prepared fixtures; nine existing UI files edited plus one new file**. Existing unrelated working-tree changes remain intact. This list is from the P9-05 source comparison, not the accumulated branch diff.

| File (packages/game/) | UI-01 change |
|---|---|
| src/uiShell.ts (new) | Shared DOM primitive, adapters, one drawer, Pause/start children, focus/input ownership and resolved palette |
| src/style.css | Shared tokens, full-viewport shell, drawer/modal/nav layout, contrast, reduced motion and failure surface |
| src/panel.ts | Route existing domain controls through shell; preserve management data, expose repair reasons and Pause actions |
| src/main.ts | Wire shell input/selection hooks, isolated navigation mount, global shortcut boundary and failure presentation |
| src/worldScene.ts | Guard world input; cancel captured drags; use shared HUD palette and camera safe area |
| src/cityMapScene.ts | Guard map world actions while UI owns input |
| src/mapScene.ts | Same guard for legacy map |
| src/controls.ts | Shared F2 Projects binding |
| src/view.ts | Right/bottom HUD inset presentation state |
| index.html | World landmark and accessible feedback region |

Documentation changes: this evidence note, PROGRESS/PROGRAMME_STATE/CLAUDE/README/current phase pointers, D-UI-03 and the specification's implementation note. [Docs/profile check](ui-01/docsync.log), [freshness](ui-01/freshness.log), [native diff check](ui-01/git-diff-check.log), [integrity/reference checker](ui-01/check.py), [verification](ui-01/verification.json).

**Next implementation: RI-02B-UI-02 — core HUD, contextual interaction and truthful objectives.** Other UI packages retain their dependencies; no human gate is closed by this implementation.


## UI-02 — core HUD, interaction and objective guidance (2026-09-09)

Implemented under the owner's “Ok lets move to UI-02” request and D-UI-04. The [mutable preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) opens the same paid-rule fresh seed 3 fixture paused. Formal human play and existing performance findings remain open.

The campaign now has one compact HUD: neighbourhood and one next action, actual campaign clock/speed/warning, collapsible minimap, engineer HP/tool/ammunition/pocket stacks, and one contextual interaction prompt. Four duplicate world-canvas corners and campaign seed/debug help are removed from normal play; exact management and developer data remain accessible. The minimap samples existing terrain once and uses P9 navigation's known destinations, saved names and pins. It creates no exploration state or second simulation. Drawers reflow the HUD at 900px and camera offsets keep the engineer visible. Dawn warnings are amber; active attacks use danger colour. Received intelligence and G/K keep their existing rules and remain visible in Pause.

Opening guidance derives from carried stock, Home stock, real machine status and saved production/restoration. Empty pockets point to Home; partial pockets report only the missing material; packed/affordable equipment skips collection; exhausted Home points to hand mining; an existing stopped machine points to inspection; removed equipment prompts rebuilding without undoing restored sites. Down/recovery takes priority. The opening has a distinct stable ID from the later repair workshop. Projects now allows one known objective, automatic guidance or untracking; camera location and selection do not alter gameplay/save state. Deeper Projects/restoration presentation remains UI-05.

The prompt and E execute the same read-only sim target result. Pointed objects win; with no object pointed at, reachable Home wins, then the nearest available interaction. Existing repair priority, discovery knowledge and local checks apply. Existing site commands, crafting, boarding and domain panels are reused. `buildAffordability` and `handCraftCheck` extract the existing validators; costs, inventory ownership, craft queue semantics, save schema and progression gates are unchanged. Hints are dismissible inside objective help and controls remain recoverable in Help.

### Verification and reproducibility

- [Full suite](ui-02/test.log): **359/359**, no failures or skips. [Focused suite](ui-02/focused.log): **22/22**, covering opening/partial/packed/exhausted stock, old/current loads, existing/removal/restoration/production facts, distinct tracking identity, down, unknown targets, real E command selection and whole-footprint reach. Existing full-suite radio outage/received-warning and legacy goal tests also pass.
- [Typechecks and production build](ui-02/typecheck.log), [lint](ui-02/lint.log), [docs/profile check](ui-02/docsync.log), [freshness](ui-02/freshness.log) and [diff check](ui-02/git-diff-check.log). The existing large Vite bundle advisory remains.
- [Actual-game browser checks](ui-02/browser.cjs), [final log](ui-02/browser-final.log), [results](ui-02/browser-result.json): both 1366×768 and 900×768 complete Home → E supplies → real steel transfer → Build selection, camera-only Locate, known tracking/untracking, map, HUD wheel isolation, engineer visibility, Pause restoring 4×, Ctrl+S/O and exact replay. Current construction/network starts load paused without repeating starting-supplies guidance. A natural Day 3 warning remains visible in Pause; G/K preserve state. No gameplay stock/position injection was used in these browser checks.
- [Shell regression](ui-02/regression.cjs), [final log](ui-02/regression-final.log), [results and measured contrast](ui-02/regression-browser-result.json): both widths retain typing/wheel isolation, one drawer, Escape hierarchy, modal focus, held movement release, drag cancellation, save/reload and exact replay. Shared normal text minimum remains 5.60:1; control borders 4.00:1. This is developer QA, not a human usability verdict.
- Opened actual images: [opening at baseline size](ui-02/opening-1280.png), [900px drawer](ui-02/build-900.png), [1366px drawer](ui-02/build-1366.png), [warning in Pause](ui-02/warning-paused.png); map and prepared-state captures are alongside them. Compared with the UI-01 opening. Early diagnostics retained: initial craft command type error, old test locator assuming one canvas, and a station-name assertion corrected to the existing “First tram station”; final checks pass.

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; this task remains uncommitted. [Manifest](ui-02/manifest.json), [source](ui-02/source.zip), [build](ui-02/build.zip), [delta against UI-01](ui-02/source-delta.patch): **209 source inputs; six production files plus three unchanged P9-05 prepared fixtures; 14 changed source files**. Campaign fingerprint remains `ba11e896`; harness, city generation, session/save source and frozen UI-01/P9/EX-08D evidence are preserved. [Integrity checker](ui-02/check.py) and [verification](ui-02/verification.json) bind these inputs, HTTP files, logs and documents.

Changed source files:

- `packages/game/src/cityMapScene.ts`
- `packages/game/src/hud.ts`
- `packages/game/src/main.ts`
- `packages/game/src/panel.ts`
- `packages/game/src/style.css`
- `packages/game/src/uiShell.ts`
- `packages/game/src/view.ts`
- `packages/game/src/worldScene.ts`
- `packages/sim/src/flow.ts`
- `packages/sim/src/goal.ts`
- `packages/sim/src/index.ts`
- `packages/sim/src/interaction.ts`
- `packages/sim/test/campaign.test.ts`
- `packages/sim/test/uiGuidance.test.ts`

**Next: RI-02B-UI-03 — build catalogue, stable quickbar and placement feedback.** Detailed inspection/restoration, full scaling/settings, formal P9-H/P6-H/P7-H/P8-H/EX-08H/T18 and the P6-AMMO retest remain separate. No new performance acceptance, human verdict, commit or push is claimed.


## UI-03 — catalogue, stable quickbar and placement (2026-09-09)

Implemented under the owner's “Ok onto UI-03” request and D-UI-05. The [mutable preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) opens paused. This is engineering/developer QA; human usability, gameplay verdicts and existing performance findings remain open.

Equipment now has searchable Production, Logistics, Power and Defence groups, purpose copy, real material prices, carried machine counts, missing pocket materials and exact known lock reasons. Unknown plans are omitted using a sim knowledge query. Infrastructure and Library retain the existing paid history, clipboard, queue/removal and saved blueprint adapters. Rifle remains equipment. Ten stable slots use 1–9/0 and the specified defaults; undiscovered assignments reserve their place without leaking names. Explicit assignment swaps an existing item, clearing leaves an empty slot, and adjacent moves reorder only those positions. `relight.ui.v1` stores optional version-one browser preferences separately from gameplay saves/replay. Missing, corrupt and unavailable storage fall back safely; explicit edits repair corrupt preferences. Full settings/scaling/rebinding remains UI-06.

Selecting equipment closes the drawer and restores world focus. Material affordability, shortages and packed-first placement come from existing sim costs/validators. Repeated placement and R rotation remain active until cancelled; failed placement retains the tool and shows one reason beside the cursor. Right-click packing retains its ordinary command and carried machines are consumed before materials. The More menu contains Management, Construction, Help and Navigation, keeping all ten slots visible at 900px. Escape closes More before changing world tools. UI edits do not move stock, unlock content or alter simulation hashes.

Verification:

- [Full suite](ui-03/test.log): **365/365**, zero failures/skips. [Focused suite](ui-03/focused.log): **43/43**, including preferences/knowledge/stock/read-only queries and existing paid construction, blueprint/library/order and area removal contracts. Six new focused contracts are in `packages/sim/test/uiBuild.test.ts`.
- [Final typechecks and production build](ui-03/typecheck.log), [lint](ui-03/lint.log), [docs/profile checks](ui-03/docsync.log), [freshness](ui-03/freshness.log) and [diff check](ui-03/git-diff-check.log). Existing large Vite bundle advisory remains.
- [Actual-game browser runner](ui-03/browser.cjs), [final log](ui-03/browser-final.log), [results](ui-03/browser-result.json): **1366×768 and 900×768**. Empty stock → Home/E real steel transfer → two paid excavators with repeated selection and rotation; collision refusal conserves stock/hash; packing/replacing uses the carried excavator before steel. Assignment, clearing, digit dispatch, save/reload persistence, exact replay, More/Escape and engineer visibility pass. Existing copy/queue/removal modes and saved-library export pass at both widths.
- [Input regression](ui-03/regression.cjs), [log](ui-03/regression.log), [results](ui-03/regression-browser-result.json): both widths preserve text/wheel isolation, one drawer, Escape order, modal focus, held-movement release, captured-drag cancellation, save/load/replay and Pause restoring 4×. Shared normal text minimum contrast remains 5.60:1 and control borders 4.00:1.
- [Preference browser checks](ui-03/preferences.cjs), [log](ui-03/preferences.log), [results](ui-03/preferences-result.json): corrupt fallback and repair, undiscovered reserved assignment without unlock, unavailable storage and unchanged gameplay hash. Only presentation preferences are injected in these cases; gameplay stock/position is not injected in the browser checks.
- Opened final actual-game captures: [900px stocked catalogue](ui-03/catalogue-stocked-900.png), [1366px catalogue](ui-03/catalogue-stocked-1366.png), [placement preview](ui-03/placement-900.png), [single collision reason](ui-03/collision-900.png), [stable bar](ui-03/quickbar-900.png), [existing library](ui-03/library-900.png). Final screenshots verified the duplicate click-refusal toast was removed. Initial opening captures preceded the More overflow improvement and are retained as diagnostics.

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; no commit/push. [Manifest](ui-03/manifest.json), [source archive](ui-03/source.zip), [build archive](ui-03/build.zip), [delta against UI-02](ui-03/source-delta.patch): **212 source inputs, 12 changed files, six production files and three unchanged P9-05 fixtures**. Changes are the game catalogue/panel/preferences, HUD/shell/scene wiring and CSS, sim read-only knowledge/stock queries and their focused tests. Save/session, city generation, harness, costs and campaign fingerprint `ba11e896` are preserved. [Integrity checker](ui-03/check.py) and [verification](ui-03/verification.json) bind sources, archives, served HTTP files, checks and current documents; frozen UI-02/P9/EX-08D evidence is verified unchanged.

**Next: RI-02B-UI-04 — machine inspection, inventory and diagnostics.** UI-05/06 keep their dependencies. Formal P9-H/P6-H/P7-H/P8-H/EX-08H/T18, P6-AMMO, Q07 and existing performance findings remain open.


## UI-04 — machine inspection, inventory and diagnostics (2026-09-09)

Implemented under the owner's “Ok onto UI-04” request and D-UI-06. The [mutable preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) opens paused. Human usability/gameplay verdicts and prior performance findings remain open.

Inspection now orders machine identity/location, operating statement, measured useful output, inputs, current constraints, existing local controls and collapsible Details. Nominal rates, power-limited capacity, measured observation windows, circuit supply/load/demand and separate buffers retain distinct labels. With no observation time, throughput is unavailable. The read-only multi-constraint query combines the original disabled/power/operating predicates, showing simultaneous missing input and power/output constraints; an already-running batch's future input shortage is labelled separately. No-load remains neutral. Known-source location links only genuine Home stock or finite home patches; item help uses the existing guide. No new production transitions, resources, recipes or pause mechanics are introduced.

Pin preserves machine ID while placing/cancelling tools and collapses to an unobscured identity chip. Removed IDs display a dismissible removed card, hide stale actions and never follow a replacement at the same coordinates. Transfers similarly retain store ID. One inventory drawer names pockets, Home/chest stock, station platform, arrivals and reserved inbound separately. Sim previews show capacity-limited Take/Put amounts and exact refusal reasons, with arrival-first station withdrawal. Targets refresh immediately and previous-store action messages clear. Truck pockets/cargo have separate rows and shared command/preflight capacity checks. Existing recipe/filter/priority, hand craft, freight requests, cargo and construction delivery remain ordinary commands. Management retains precise production/power figures outside Developer; construction delivery is also reachable from truck inspection and Management.

Verification:

- [Full suite](ui-04/test.log): **370/370**, zero failures/skips. [Focused suite](ui-04/focused-final.log): **35/35**, covering inspection/observations, routing, logistics and truck work. Five added contracts in `packages/sim/test/inspection.test.ts` verify simultaneous and clearing constraints, future-batch inputs, neutral/disabled states, read-only capacity-limited hand/truck previews, station pool order, remote refusal and known-source filtering. Explicit synthetic fuel/output/pool/truck fixtures are labelled in those tests; they are not human gameplay evidence.
- [Full typechecks/build](ui-04/typecheck.log), [final game typecheck/build](ui-04/game-build.log), [lint](ui-04/lint.log), [docs/profile checks](ui-04/docsync.log), [freshness](ui-04/freshness.log), [diff check](ui-04/git-diff-check.log). The existing Vite large-bundle advisory remains.
- [Actual-game browser runner](ui-04/browser.cjs), [final log](ui-04/browser-final-verified.log), [results](ui-04/browser-result.json): **six cases across 1366×768 and 900×768**. Real Home transfers and paid assembler/inserter placement; stalled/working inspection; recipe/filter commands; pin while building/cancelling; packing and replacement with a different ID; known-source camera movement without changing state; exact save/replay. Remote Home transfer refuses without stock loss. Actual stop requests, chest take/put, truck load/unload and construction-delivery access pass at both widths. No gameplay stock or position injection is used in these browser cases; starts are the frozen P9-05 ordinary-command fixtures.
- [Shell regression](ui-04/regression.cjs), [final log](ui-04/regression-final.log), [results](ui-04/regression-browser-result.json): typing/wheel capture, one drawer, Escape hierarchy, modal focus, held-key release, captured-drag cancellation, save/load/exact replay and restoring 4× through Pause. Shared text minimum contrast 5.60:1 and control borders 4.00:1.
- Opened actual-game images: [stalled inspector](ui-04/stalled-1366.png), [working inspector](ui-04/working-1366.png), [900px stalled card](ui-04/stalled-900.png), [900px working card](ui-04/working-900.png), [separate Home/pockets](ui-04/home-900.png), [remote refusal](ui-04/remote-transfer-900.png), [truck cargo](ui-04/truck-900.png). Further captures include station requests and removed identity. These are developer QA, not human usability acceptance.

Initial diagnostics are retained: type errors against the existing craft-query return shape/canvas type; a no-load test fixture that still had core demand; old test labels; Escape clearing pinned selection; the chip overlapping the expanded minimap; a transient previous-store read; and assertions treating absent zero inventory entries as numbers. Final checks use corrected code/fixtures/assertions. Earlier screenshots/logs describe their captured build, not the final verdict.

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; no commit/push. [Manifest](ui-04/manifest.json), [source](ui-04/source.zip), [build](ui-04/build.zip), [delta against UI-03](ui-04/source-delta.patch): **214 source inputs, 13 changed source files, six production files and three unchanged P9-05 fixtures**. Changes are the new inspection/inventory DOM modules; panel/main/shell/view/world/CSS integration; sim inspection/goal/pool/truck queries and focused inspection tests. Save/session, city generation, harness, costs and campaign fingerprint `ba11e896` remain unchanged. [Integrity checker](ui-04/check.py) and [verification](ui-04/verification.json) bind the source/build archives, served files, logs and current documents; frozen UI-03/P9/EX-08D evidence is preserved.

**Next: RI-02B-UI-05 — Projects, restoration service circuits and actionable alerts.** UI-06/07 retain dependencies. Formal P9-H/P6-H/P7-H/P8-H/EX-08H/T18, P6-AMMO, Q07 and existing performance findings remain open.

## UI-05 — Projects, restoration circuits and actionable alerts (2026-09-09)

Implemented under the owner's “Continue” after UI-04 and D-UI-07. The [mutable preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) opens paused. Human usability/gameplay verdicts remain separate.

Projects shows one selected known destination with Track and camera-only location controls. Material nodes use actual delivered stock, with an equivalent checklist separating carried and Home supplies and exact transfer previews. Local power is a separate node. Partial carried delivery remains legal without power; ready delivery uses ordinary deliver/restore commands. Empty-pocket refusal and ready Restore use current sim gates. Completed restoration reads “Restored · materials consumed” even after buffers reset; an outage changes service state without undoing completion. Kit collection previews the real capacity-limited reward order. Radio restoration and precision purchase show their separate dynamic costs/benefits; Turbine concrete/switching, recruits and schematic commands retain distinct actions. Actions reread current state, dispatch ordinary logged commands and guard double-clicks from activating a subsequent action.

Alerts derive from the saved schedule, received warning, known threat query, visible hostiles within the existing danger radius, commissioned core HP and known machine constraints. No hidden attack target or roster is exposed. One priority strip keeps engineer/base danger above schedule and production, including during Pause. It stays beside an open narrow drawer. Three short transient notices at most coalesce repetitions; the Projects inbox records session messages and active/dismissed/resolved conditions. Dismissal never clears real danger. Known location actions reread current conditions; resolved/unavailable locations explain refusal. Saved received radio intelligence remains visible during outage. Inbox history/dismissal and objective selection are presentation state; notification preferences remain outside this task.

Verification:

- [Full suite](ui-05/test.log): **373/373**, zero failures/skips. [Focused suite](ui-05/focused-final.log): **16/16**, covering Projects, existing discovery, opening guidance and world interaction. The three added contracts in `packages/sim/test/projects.test.ts` exercise ordinary paid walking/delivery/power/restoration/kit/radio actions, conservation, reload and read-only visibility/priority queries. Synthetic capacity/completion/nearby-threat states are explicitly labelled. The UI-02 empty-inventory assertion now checks refusal and a seven-steel partial-delivery action rather than expecting a no-op command pair.
- [Typechecks/build](ui-05/typecheck.log), [lint](ui-05/lint.log), [docs/profile checks](ui-05/docsync.log), [freshness](ui-05/freshness.log), [diff check](ui-05/git-diff-check.log). The existing large Vite bundle advisory remains.
- [Actual-game browser runner](ui-05/browser.cjs), [log](ui-05/browser-final.log), [results](ui-05/browser-result.json): **1366×768 and 900×768**. Remote tracking/location/refusal preserves state; partial delivery transfers once per double-click; powered restoration consumes buffers without accidentally collecting the kit; collection is partial when pockets fill. Radio restoration spends 20 steel/10 copper and its separate precision upgrade spends 15/10 once. Save/reload and exact paid replay pass. Dismissed threats retain the HUD indicator, actual condition removal resolves the inbox entry, and received radio history persists during outage. Repeated save notices coalesce and remain bounded. Drawer Close remains unobscured during danger.
- [Shell regression](ui-05/regression.cjs), [log](ui-05/regression.log), [results](ui-05/regression-browser-result.json): both widths preserve typing/wheel capture, one drawer, Escape hierarchy, modal focus, held-key release, drag cancellation, save/load/exact replay and restoring 4× through Pause. Shared text minimum contrast remains 5.60:1 and control borders 4.00:1. [Actual circuit colour measurements](ui-05/circuit-contrast.json) also pass 4.5:1 text and 3:1 boundaries; [runner](ui-05/contrast.cjs), [log](ui-05/contrast.log).
- Opened actual-game images: [service circuit, desktop](ui-05/service-1366.png), [service circuit, 900px](ui-05/service-900.png), [completed restoration](ui-05/restored-900.png), [radio precision purchase](ui-05/radio-900.png), [live dismissed alert](ui-05/alerts-900.png), [service outage](ui-05/outage-900.png). These are developer QA, not human acceptance.

The partial/powered/kit/radio starts are captured from paid ordinary commands and walking in the focused test using `UI05_FIXTURES=docs/evidence/ui-redesign/ui-05`. [Fixture preparation](ui-05/prepare-fixtures.py) labels the fuel outage and synthetic known raid/received warning with incomplete replay logs. Browser condition removal is a labelled resolution fixture, not a won combat encounter. Frozen P9-05 starts remain unchanged. Initial diagnostics include a test fixture lacking initialized threat state, an invalid attack next-ID counter, Playwright treating aria-disabled as native disabled, the superseded empty-pockets assertion in the [initial full run](ui-05/test-initial.log), and the narrow urgent HUD overlap corrected after screenshot review.

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; no commit/push. [Manifest](ui-05/manifest.json), [source](ui-05/source.zip), [build](ui-05/build.zip), [delta against UI-04](ui-05/source-delta.patch): **217 source inputs, 11 changed files, six production files and three unchanged P9-05 fixtures**. Save/session, city generation, harness, costs and campaign fingerprint `ba11e896` are unchanged. [Integrity checker](ui-05/check.py) and [verification](ui-05/verification.json) bind sources, archived/served builds, logs and current documents, and verify frozen UI-04/P9/EX-08D evidence.

**Next: RI-02B-UI-06 — scaling, accessibility, settings and full integration.** UI-07 retains its dependency. P9-H/P6-H/P7-H/P8-H/EX-08H/T18, P6-AMMO, Q07 and existing performance findings remain open.

## UI-06 — Scaling, accessibility, settings and integration (2026-09-09)

Implemented under the owner's “Continue” after UI-05 and D-UI-08. The [mutable preview](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) opens paused. UI-07 is next for actual visual review, usability feedback and targeted refinement; this engineering QA is not human acceptance.

Pause → Settings now provides 100/125/150% interface scale independent of camera zoom, system motion preferences with explicit reduced/full overrides, live keyboard rebinding and resets. Same-context collisions are refused; established Ctrl/Cmd and blueprint contexts remain separate. Escape, Tab navigation and numbered slot keys remain fixed, with slot contents editable in Build. Shared controls and Help show live bindings, and held movement is released when settings or other UI capture input. Compact and short layouts preserve all ten quickbar positions, reflow HUD cards and scroll long panels. The More menu stays above drawers; Pause stays above ordinary HUD controls.

The existing `relight.ui.v1` browser record now composes quickbar, scale, motion, bindings and dismissed opening hints. Older quickbar-only records remain valid. Corrupt fields fall back safely; denied storage keeps session settings with an explicit retry message. Existing profile-separated game saves, Download save recovery and unsaved-change confirmation remain available. Preferences do not enter SimState, save envelopes or replay commands.

Verification:

- [Full suite](ui-06/test.log): **376/376**, zero failures/skips. [Focused suite](ui-06/focused-final.log): **17/17**, including three new preference/collision/storage contracts and existing building/guidance tests. [Final typechecks/build](ui-06/typecheck-final.log), [lint](ui-06/lint.log), [docs/profile checks](ui-06/docsync.log), [freshness](ui-06/freshness.log) and [diff check](ui-06/git-diff-check.log). The existing Vite large-bundle advisory remains.
- [Scale matrix](ui-06/matrix.cjs), [final log](ui-06/matrix-final.log), [results](ui-06/matrix-result.json): 1366×768 at 100/150%, 1920×1080 at 100/125/150%, 2560×1440 at 125%, 3440×1440 at 100/150%. Actual drawer body text is 17/21.25/25.5px. Settings control hit tests, viewport bounds, engineer visibility, objective/engineer separation, scroll, text input and wheel isolation pass. Settings preserve state hashes and camera zoom. At 1366×768/150%, open drawers leave a small but unobscured world area; UI-07 should assess comfort and visual balance.
- [Preferences browser checks](ui-06/preferences.cjs), [results](ui-06/preferences-result.json): conflict refusal, live Projects/inspection/movement remapping, dynamic labels, focus trapping, held-key release, persistence, default reset, corruption recovery and denied-storage save/export recovery. [Accessibility checks](ui-06/accessibility.cjs), [results](ui-06/accessibility-result.json) verify both OS motion modes, explicit overrides and dismissed-hint persistence/reset. The shared [contrast measurements](ui-06/regression-browser-result.json) retain minimum text contrast 5.60:1 and control borders 4.00:1. A paused fresh 1920×1080 diagnostic sampled 211 frames at 60.2fps, 16.61ms mean, 17ms worst, none over 50ms; this is not a light-map benchmark or a resolution of existing performance findings.
- [Construction integration](ui-06/integration.cjs), [results](ui-06/integration-result.json): library export/import, long metadata, clipboard save/load/delete, order pause/resume/remove, packed chest transfers, truck start/source/retry/pause/resume/stop and manual completion through ordinary paid commands. The engineer takes real chest supplies and walks to the queued ammunition cell; no injected stock or position. Exact current-campaign replay passes. Telemetry export, copy link, cancelling a new city, legacy Management and legacy file/slot save/replay pass.
- Reused current-build browser regressions cover [building and clipboard](ui-06/building-result.json), [inspection/inventory/freight/truck](ui-06/inspection-browser-result.json), [Projects/restoration/radio/alerts](ui-06/projects-result.json) and [shell input/Pause/save/replay](ui-06/regression-browser-result.json), each at 1366px and 900px widths. These exercise the relocation inventory through its existing adapters. Runners are [building](ui-06/building.cjs), [inspection](ui-06/inspection.cjs), [Projects](ui-06/projects.cjs) and [shell](ui-06/regression.cjs). Ordinary paid checkpoints and the prior UI-05 labelled outage/known-threat/resolution fixtures retain their documented provenance; clearing a prepared raid in that runner is not won combat.
- The older [P7 campaign file](../p7-03-2026-09-08/browser-save-1366.json) loads through existing migrations. Its original hash `c39c2d7c` becomes `d14fa9bc`; existing upgrades invalidate complete replay eligibility, and the UI truthfully refuses whole-session replay. Current campaign and legacy replay remain exact. No loader or migration policy was changed.

Opened actual-game images include [150% settings and focus](ui-06/settings-1366-150.png), [150% Projects reflow](ui-06/projects-1366-150.png), [paid manual completion](ui-06/manual-order.png), [wide Projects](ui-06/projects-3440-150.png) and [legacy Management](ui-06/legacy-management.png). Initial diagnostics found the modal/HUD and More/drawer layer overlaps and key capture being stopped before reaching its field; all were corrected. Runner corrections account for queued speed timing, the `completed` order status, exact URL routing, duplicate Map buttons and the old-save replay guard; none change gameplay expectations.

Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; no commit/push. [Manifest](ui-06/manifest.json), [source](ui-06/source.zip), [build](ui-06/build.zip), [delta against UI-05](ui-06/source-delta.patch): **219 source inputs, 12 changed files, six production files and three unchanged P9-05 fixtures**. All simulation source, session/save code, city generation, harness and campaign fingerprint `ba11e896` are unchanged. [Integrity checker](ui-06/check.py) and [verification](ui-06/verification.json) bind the source/build/served files, logs, documents and local references, and preserve frozen UI-05/P9/EX-08D evidence.

**Next: RI-02B-UI-07.** P9-H/P6-H/P7-H/P8-H/EX-08H/T18, P6-AMMO, Q07 and existing performance findings remain open.

## UI-07 — Actual visual review and targeted refinement (2026-09-09)

Completed the owner's “Continue” under D-UI-09. **Engineering and developer QA are complete; independent fresh-player feedback is pending.** The [frozen UI-07 opening](http://127.0.0.1:5182/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json) is the current review build. Restart it with `python docs/evidence/ui-redesign/ui-07/serve.py`. Mutable port 5178 and all previous frozen builds remain separate.

Reviewed the actual UI-06 entry, then made one focused refinement pass. [Before opening](ui-07/before-opening-1280.png) showed the startup notice and core HP label crowding the engineer. The [final opening](ui-07/frozen-opening-1280.png) identifies the engineer with a brief “You” tag, removed after two tiles of ordinary walking. HP now sits above the core building. The fresh paused opening uses the existing clock instead of a load toast; later saved campaigns retain a short acknowledgement. Baseline is unchanged seed 3, time 0:00, 1280×720, camera 0.65, state hash `e13e92a4`, engineer world position `(421.5, 638.5)`. No stock or position was injected.

[Before compact danger](ui-07/before-warning-1366.png) hid the engineer between the alert/objective and lower HUD. The [refined compact warning](ui-07/warning-raid-1366.png) preserves danger, HP, inventory, speed and the engineer together. While a drawer and urgent warning are open on a short logical viewport, the objective card yields its space; full project information remains in Projects. Secondary spacing is reduced without reducing text size. [Before ultrawide danger](ui-07/before-warning-3440.png) covered the Projects heading; [after](ui-07/warning-raid-3440.png), the warning stops before the actual drawer boundary. Camera/HUD reservations include the ultrawide safe margin. This moves only the screen composition, not the engineer. The default ultrawide camera remains 0.85; compact opening remains 0.65. At 1366×768/150%, the closed-panel engineer shifts vertically from about 421px to 379px as the HUD reflows; the baseline 1280×720 composition stays at approximately `(640,361)`.

The staged developer walkthrough uses actual paid commands: collect Home steel/copper, preview/place/repeat/pack equipment, inspect a stalled assembler, inspect a working ammunition line, track a known project, deliver and restore, and inspect warnings. It is deliberately **not** a claim of one continuous unaided playthrough: fresh and network/construction starts reuse the unchanged ordinary-command P9-05 fixtures; delivery uses the prior UI-05 paid/walked checkpoints. Explicit outage/known-threat/resolution and urgent-state injections are presentation fixtures with no combat or replay verdict. Opened actual-game images include [paid resource-patch placement](ui-07/building-placement-1280.png), [stalled assembler](ui-07/inspection-stalled-1280.png), [working ammunition line](ui-07/inspection-working-1280.png), [project requirements/delivery](ui-07/projects-service-1280.png), [150% Projects](ui-07/after-project-1366.png), [urgent compact](ui-07/warning-raid-1366.png) and [urgent ultrawide](ui-07/warning-raid-3440.png). Hierarchy, character identification, contrast, collisions and truthful action/result states were reviewed; no further blocking issue was observed in this bounded pass.

Verification:

- [Full tests](ui-07/test.log): **376/376**, zero failures/skips; [focused guidance/build/preferences](ui-07/focused.log): **17/17**. [Typechecks](ui-07/typecheck.log), [final game build](ui-07/game-build-final.log), [lint](ui-07/lint.log), [docs/profile checks](ui-07/docsync.log), [freshness](ui-07/freshness.log), [diff check](ui-07/git-diff-check.log). Existing bundle-size and light-map performance findings remain open; no broad seed/population batch was run.
- Current-build paid browser results: [building](ui-07/building-result.json), [inspection/inventory/freight/truck](ui-07/inspection-browser-result.json), [Projects/paid restoration/radio/alerts](ui-07/projects-result.json), all at 1280px; [shell/Pause/input/save/replay](ui-07/regression-browser-result.json) at 1366px and 900px. Scripts and corresponding logs are in `ui-07/`. The final world-label adjustment was rebuilt and its affected baseline, machine, project and warning captures refreshed; no unrelated mechanics check was repeated for that label.
- [Eight scaling cases](ui-07/matrix-result.json): 1366×768 at 100/150%, 1920×1080 at 100/125/150%, 2560×1440 at 125%, 3440×1440 at 100/150%. Geometry, visible engineer, focus, scroll, text/wheel isolation and unchanged state/zoom pass. Shared contrast remains at least 5.60:1 for text and 4.00:1 for borders.
- [Twenty urgent cases](ui-07/warnings-result.json): 1366×768 and 3440×1440 at 150%, each covering minor raid, engineer hit, engineer down, nearby hostile, damaged core, disabled core, major countdown, active assault, withdrawal and unavailable target. Warning text, drawer heading/Close, player visibility and HUD separation pass; urgency and “Paused” persist in Pause. [Runner](ui-07/warnings.cjs) explicitly labels injected states, which do not represent earned gameplay outcomes.
- [Frozen-port smoke](ui-07/frozen-result.json) loads all three unchanged P9-05 starts paused with exact replay; fresh opening movement, identity-label dismissal and slot save/reload use ordinary keys and replay exactly. Initial smoke timing assumptions were corrected to await two actually walked tiles and the HUD refresh, rather than a fixed wall-clock delay.

[Manifest](ui-07/manifest.json), [source archive](ui-07/source.zip), [build archive](ui-07/build.zip) and [delta against UI-06](ui-07/source-delta.patch) bind **219 source inputs, five changed game files, six production files and three unchanged prepared starts**. Parent commit `8e54ed602da159905b0428f316f6e75cb25ae351`, branch `codex/tram-expansion`; no commit or push. All simulation source, session/save schema, city generation, costs, harness and campaign fingerprint `ba11e896` remain unchanged. [Integrity checker](ui-07/check.py) and [verification](ui-07/verification.json) bind archives, HTTP files, checks, references and documents, and preserve UI-06/P9/EX-08D evidence.

### Current-build fresh-player follow-up — pending

Recommend one independent 10–15 minute session on the [frozen fresh city](http://127.0.0.1:5182/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json). Record screen size and chosen scale; it starts paused. Ask the player to identify their engineer, obtain supplies, place a machine, explain a stall and find a known project. Let them explore before assisting. Record actual hesitation, mistaken actions, the exact help given and whether the player recovered. Do not convert these observations into an invented intuition score.

For a separately labelled later-state check, use the [prepared construction start](http://127.0.0.1:5182/?rules=exploration-v2&seed=3&view=world&state=/ui-construction.json) or [prepared factory/network](http://127.0.0.1:5182/?rules=exploration-v2&seed=3&view=world&state=/ui-network.json). These include scripted ordinary-command assistance. Note any warning seen and what the player thought it meant; no warning during a short session is not a comprehension pass. Saves and settings on port 5182 are separate from the older preview origins.

| Observation | Actual record |
|---|---|
| Participant, date, viewport, scale, start URL | Pending — no independent participant observed |
| Engineer identification and first hesitation | Pending |
| Supplies, placement and mistaken actions | Pending |
| Stall explanation and assistance provided | Pending |
| Project discovery/tracking and any warning interpretation | Pending |
| Player's own feedback and requested refinement | Pending |

This area supplies new-build context to EX-08 without editing old session records. **UI-07 engineering is done; EX-08 remains blocked on EX-08H.** Formal P9-H/P6-H/P7-H/P8-H/EX-08H/T18, P6-AMMO, Q07 and performance findings remain open. Older play observations retain their original build identity. Stop this refinement pass here; use actual new feedback to justify further changes.

## UI-08 — approved action bar, catalogue and Backpack implementation (2026-09-09)

D-UI-10 records the owner’s implementation request and accompanying concept image. The supplied image was opened and inspected. Its petrol/charcoal surfaces, warm ivory text, amber selection, illustrated slots and restrained olive bag edging guided the implementation; its decorative board text, example counts and city artwork were not copied. UI-08 extends the existing RI-02B phase. Earlier frozen evidence is retained.

Implemented in the normal campaign and legacy entry paths:

- Original shared SVG item illustrations, a 60-logical-pixel ten-slot dock, three starting assignments and preserved existing browser arrangements. Catalogue drops replace, bar drops swap, empty-UI removal previews explicitly, and invalid/world/cancelled drops preserve shortcuts. Keyboard/click assignment and removal are available. Counts distinguish packed stock, material-funded availability and rifle rounds through actual sim selectors and tooltips.
- Four discovered-equipment catalogue categories, one purpose/cost/blocker detail section, ordinary paid placement, and retained blueprint/library/construction adapters.
- Temporary Backpack with 40 real slots (legacy truck 200); authoritative optional stack allocation supports move/merge/swap/split/sort while retaining existing quantities and kit reservations. Save/load retains the allocation. Direct consumption reconciles counts; full compatible partial stacks can still accept their remaining capacity.
- Side-by-side Home/chest/station storage and Backpack, real per-stack quantities, shift-click, drag and quantity actions using the same legal transfer command. Source totals/layout and proximity/capacity are revalidated inside the command. Incoming transfers fill compatible/free slots automatically; storage retains its existing quantity pools rather than acquiring a new storage-layout system. Freight controls remain available for targeted stations.
- Quiet objective, local minimap with engineer direction and known destination, separate small day/night/assault label, collision-aware condition panel and Build/Backpack/Projects dock entries. Menu names the secondary command group. Ordinary HUD yields to menus; immediate threats expose an alert entry in the drawer header.
- Interactive `session.ts` limits every command path to 0/1. Old running speeds normalize to 1; paused saves remain paused. Accumulation is reset on load/speed transition; direct background gaps over 250 ms are discarded and main retains its existing 100 ms frame cap. Fixed timestep, day/raid rules and fast headless runners remain unchanged. Player acceleration buttons/bindings and exposed runTicks hook are removed.

Verification:

- `npm run typecheck` passed (all package TypeScript checks and production build): [log](ui08-build-final.log); final game rebuild after the legacy unlimited-supply label also passed [log](ui08-build-game-final.log). The existing Vite large-chunk advisory remains; bundle optimization was outside this UI task.
- 14 focused tests passed: [log](ui08-tests.log). Covers conservation, actual split capacity, full-stack slack, kit reservations, stale-command rejection, selected-stack transfers, save compatibility, preference preservation and elapsed-time speed policy. Existing UI build/settings tests were reused and updated only for intentionally changed defaults.
- `npm run lint`, `npm run docsync:check`, `npm run freshness:check` passed: [lint](ui08-lint.log), [docsync](ui08-docsync.log), [freshness](ui08-freshness.log). No full simulation suite or long campaign batch was run, following the owner’s bounded verification instruction.
- Actual Chrome walkthrough passed: [results](ui08-browser.json), [script](ui08-walkthrough.cjs). Real Home transfers funded a world-click Assembler placement (40 steel + 20 copper); catalogue assignment changed neither inventory nor gameplay hash. Swapping/removal/Escape cancellation, split/merge/move/sort, quantity/drag transfers and save/reload passed. An explicitly prepared 40-slot capacity case moved exactly one steel, then rejected full/out-of-reach transfers without changing totals. Prepared capacity/reach mutation is not campaign progression evidence.
- Additional input/legacy checks passed: [results](ui08-input.json), [script](ui08-input.cjs). Accessible assignment/removal, invalid world drops, pointer cancellation, quantity typing, scroll isolation and the legacy normal-speed route; no browser page errors in either final run.

Screenshots captured **and opened for visual inspection**:

- [Frozen UI-07 baseline, 1920×1080](ui08-baseline.png).
- [Final normal gameplay, 1920×1080](ui08-gameplay.png).
- [Catalogue and active drag, 1920×1080](ui08-catalogue-drag.png).
- [Storage & Backpack after ordinary transfers/paid construction, 1920×1080](ui08-storage-backpack.png).
- [1366×768 Backpack](ui08-small.png), [2560×1440 at 150% UI](ui08-enlarged.png), [3440×1440 ultrawide](ui08-ultrawide.png).

Observed and fixed during the targeted refinement: minimap covering the time label; unnecessarily early 1080p Backpack scrolling; condition HUD crossing the first shortcut at reduced effective widths; duplicate Backpack subheading. Final checks hit-test all ten dock slots and panel Close controls. Small/enlarged menus intentionally scroll rather than shrinking functional text. The original SVG silhouettes are simpler than the painted concept; world art is unchanged. Intermediate `ui08-first-*` screenshots record the earlier layout, not the delivered state. An initial placement probe used the wrong 3×3 anchor and was rejected correctly; the walkthrough was corrected to the existing centered footprint. A split equal to the entire stack was also correctly rejected before the test used a partial amount.

Current preview: [UI-08 fresh opening](http://127.0.0.1:5178/?rules=exploration-v2&seed=3&view=world&state=/ui-fresh.json). It uses the unchanged prepared P9 fresh fixture with the new UI. The walkthrough starts from that fixture and performs legal UI actions, except the explicitly labelled capacity/reach case. Source/build identities are recorded in [ui08-source.json](ui08-source.json). Port 5182 remains the frozen UI-07 baseline; no commit/push was performed. Independent human feedback and existing gameplay/performance gates remain pending.

Launch from the repository: `npm run dev -w packages/game -- --host 127.0.0.1`, then open the printed URL with `?rules=exploration-v2&seed=3&view=world`. The already-running 5178 preview serves the production build and its prepared fixture.

### UI-08 follow-up — keep the world still when opening panels

Owner feedback: opening inventory or interacting should not move the screen. Removed dynamic HUD insets from the WorldScene follow target; panels now overlay the full viewport without recentering it. HUD layout still uses those insets. Engineer following, world zoom and explicit Locate / Return to engineer remain available.

`npm run build -w packages/game` passed TypeScript and production build ([log](ui08-camera-build.log)); scoped whitespace check passed. [Browser results](ui08-camera.json) compare the engineer and a second world landmark’s exact screen coordinates plus zoom before opening, while open and after closing Build, Backpack, Projects, Home supplies and machine Inspection, at 1920×1080 and 1366×768. All ten cases passed with no page errors. [Storage screenshot](ui08-camera-storage.png) was captured and opened for visual inspection. No simulation code or gameplay rule changed, so the long simulation suite was not repeated. [Current camera-fix source/build hashes](ui08-camera-source.json) supersede the earlier UI-08 build identity for the mutable 5178 preview. Frozen 5182 is unchanged. Reload the current 5178 tab to use the fix.

### UI-08 follow-up — Tab toggles inventory closed

Owner feedback: pressing Tab again should close inventory instead of cycling through its slots. The shell captures plain Tab for open inventory/storage, cancels any UI drag, closes the drawer and restores world focus. This also works from a quantity field or a mouse-opened Backpack. Held-key repeats are consumed; Shift+Tab, other drawers and Pause retain focus navigation.

Game TypeScript/production build passed ([log](ui08-tab-build.log)). Five grouped actual Chrome checks passed with no page errors: [results](ui08-tab.json), [script](ui08-tab.cjs). They cover repeated presses, held Tab, slot focus, mouse opening, quantity focus with unchanged gameplay hash, Shift+Tab, Build and Pause. This input-only follow-up did not repeat the simulation suite or visual screenshots. [Source/build hashes](ui08-tab-source.json) identify the updated mutable 5178 preview; frozen 5182 remains unchanged. Reload 5178 to use this fix.

### UI-08 follow-up — manual mining feedback

Owner reported no indication of mining activity, progress or collected quantity. Added a passive HUD panel over existing hand-mining state: hover controls/rate, next-item progress, per-hold pickup counts from `handMinedOf`, current Backpack quantities and a three-second release receipt. Pause, full Backpack, distance, no remaining rubble and persistent machine-only sources explain why mining cannot proceed. Opening UI cancels the existing mining command and hides this feedback. Gameplay quantities, timings and save schema are unchanged.

Game TypeScript/build, all six grouped browser checks (no page errors), documentation consistency, freshness and scoped whitespace checks passed. Verification artifacts: [game TypeScript/build](ui08-mining-build.log), [actual Chrome checks](ui08-mining.json), [browser script](ui08-mining.cjs). The fresh Home scenario uses ordinary mining, pause and inventory input. Capacity, distance and persistent-source visits are explicitly prepared scenarios, not campaign progression. Browser checks compare displayed pickup totals to real inventory and verify release/menu cancellation. Screenshots at [1366×768](ui08-mining-progress.png) and [1920×1080](ui08-mining-wide.png) were captured and opened for visual inspection. The first distance probe changed the unused engineer reach field; the corrected scenario moves the engineer beyond the actual reach rule.

[Documentation consistency](ui08-mining-docsync.log) and [freshness](ui08-mining-freshness.log) retain their command results. No full simulation suite was repeated for this renderer-only follow-up, following the owner's bounded verification instruction. [Source/build hashes](ui08-mining-source.json) identify the mutable 5178 preview; frozen 5182 remains unchanged. Reload 5178 to use the mining feedback.

### UI-08 follow-up — player inventory on the left

Owner requested player inventory on the left and the interactable inventory on the right. Reordered the shared Backpack/storage sections in the DOM, so reading and focus order follow the same arrangement; narrow single-column layouts show Backpack first. Transfer behavior is unchanged. Validation: [game TypeScript/build](ui08-inventory-order-build.log), [browser layout and transfer check](ui08-inventory-order.json), [docsync](ui08-inventory-order-docsync.log), [freshness](ui08-inventory-order-freshness.log). No simulation tests were added or repeated for this ordering-only change. The mutable 5178 preview is updated; frozen 5182 is unchanged.

Ordering follow-up results: game build/typecheck, documentation checks and two browser sizes passed; transferring five steel from right-hand Home storage to left-hand Backpack conserved the totals. [1366×768 screenshot](ui08-inventory-order.png) was opened and inspected. Scoped whitespace check passed.

### UI-08 follow-up — flashlight and full Home lighting

D-UI-11 records the owner's request and supersedes the old no-hand-lamp campaign direction. Campaign flashlight defaults on and follows the mouse without clicking, including while paused; it holds direction during menu input capture. A soft 12-tile/60-degree visibility cone and small origin glow compose into a cropped visible lighting texture. Mouse aiming does not repaint the city-sized light data or mutate sim state. It is a visibility aid, not a powered defensive light. Home's entire lot now lights in both `lightMask` and `litAt`, automatically for existing saves.

[Game TypeScript/build](ui08-flashlight-build.log) and [five focused lighting tests](ui08-flashlight-tests.log) passed, including unchanged legacy-light cases, complete Home coverage, mask/query agreement, save/load and remaining darkness on other lots. [Browser results](ui08-flashlight-browser.json) / [script](ui08-flashlight.cjs) confirm mouse aiming leaves the paused gameplay hash and static texture paint count unchanged, with menu capture and resize checks. Opposing screenshot pixel samples brighten only on the aimed side. The dark-area position is an explicit prepared scene, not campaign progression. [Home at 1366×768](ui08-flashlight-home.png), [right beam](ui08-flashlight-right.png), [left beam](ui08-flashlight-left.png) and [1920×1080](ui08-flashlight-wide.png) record the visual review. No broad performance claim is inferred from these bounded checks.

[Additional focused defence checks and lint](ui08-flashlight-validation.log), [docsync/freshness](ui08-flashlight-docs-final.log) and [source/build hashes](ui08-flashlight-source.json) retain validation provenance. No full simulation campaign was repeated. Mutable 5178 contains the changes; frozen 5182 is unchanged. Reload 5178 to use the new lighting.

Lighting validation follow-up: all 10 campaign-defence tests and lint passed. All four linked screenshots were opened for visual inspection. An initial documentation check rejected CRLF line endings around generated markers; the original LF convention was restored before rerunning (initial log retained as ui08-flashlight-docs.log).

### UI-08 follow-up — direct conveyors and visible connections

D-UI-12 records the owner's change from mandatory inserter extraction to direct conveyor loading/unloading. Campaign belts, underground entrances and splitter rear ports take supported items from adjacent sources when pointing away; ordinary delivery/acceptance handles inward connections. Finished recipe products, mined items, Home/chest stock, unused generator coal, whole turret magazines and station arrivals/platforms use their actual conserved stores. Saved pickup selection is fair. Downstream room accounts for items already travelling, so a mixed-material source can feed an assembler without excess copper blocking required steel. Conveyor power/rates and recipe production rules are retained; legacy extraction is unchanged. Partial rounds and packed machines are not fabricated into belt items.

Always-visible cyan IN / amber OUT markers show usable sides, expanding to exact edge tiles on hover/placement; inserters show pickup/drop outlines. Hover legend, inspection, catalogue and storage guidance describe the new rule. No source text shipped claiming an inserter is required in campaign.

Verification: [all-package types/build](ui08-direct-belts-typecheck.log), [lint](ui08-direct-belts-lint.log), [final game rebuild](ui08-direct-belts-build-final.log), [four direct-transfer plus eleven existing routing tests](ui08-direct-belts-tests-final.log). Direct tests cover the exact user-described line, mixed stock, all four orientations, full destinations, conservation, Home/turret extraction and save continuation. The initial long-line fixture did not fit within reach; the corrected test walks to an actual legal site and builds with carried materials. [Prepared fixture](direct-belt-fixture.json) is exported from those paid actions, without a complete session command-history claim.

[Browser script](ui08-direct-belts.cjs) / [results](ui08-direct-belts-browser.json) run that layout at ordinary speed and confirm the turret receives magazines without inserters, correct inspection text and hover legend, and no page errors. Screenshots at [1366×768](ui08-direct-belts-small.png) and [1920×1080](ui08-direct-belts-wide.png) are retained for actual visual inspection. [Documentation checks](ui08-direct-belts-docs.log) and [source/build hashes](ui08-direct-belts-source.json) complete the current preview provenance. No full long-duration campaign suite or human verdict is claimed. Mutable 5178 is updated; frozen 5182 remains unchanged. Save before refreshing a prepared-start URL.

Direct-conveyor final results: 15/15 focused tests, all-package types, final build, lint, docsync/freshness and scoped whitespace checks passed. Final browser run had no page errors and put 10 rounds into the turret through the exact conveyor-only layout. Both-size screenshots were inspected during refinement; the final wide screenshot was reopened after enlarging and separating the port arrows.


## D-UI-13 — permanent tram, roaming enemies and map (2026-09-09)

Owner's three gameplay changes implemented in the simulation and renderer. Four permanent stations occupy separate districts, adopting the two initial survey stops and later stop, with a fourth on the trunk or a street extension. Home starts as the only powered stop. The existing freight vehicle serves powered stops automatically, parks below two powered stops, and retains cargo on outages. New cargo is not reserved for unpowered destinations. Public stops/track/vehicle cannot be built or packed. Existing checkpoint machinery and stock are retained, compatible stations/tram adopted, and old earned kits retained. New restorations no longer grant a transport kit. Base restoration and truck unlock remain separate from station power. Ruin crawlers patrol locally and chase to a 20-tile separation; the optional Stalker also roams/chases beyond its yard. Map clicks inspect/pin without queuing movement. Main map and minimap player markers have stronger contrast; main map labels YOU.

Actual engineering evidence:
- `ui08-tram-roaming-focused-final.txt`: 72 focused tests run, 71 initially passed. Its single rail-validator assertion exposed missing validation of the retained district survey after adding a fixed route. Corrected; `ui08-tram-roaming-recheck.txt` passes all 12 validator/fixed-tram tests, including that assertion. Existing tests expecting buildable campaign stops/kit or home-leashed guardians were updated to the owner's new contract, preserving conservation, command rejection and migration assertions.
- `ui08-roaming-defence.txt`: the focused existing ruin-patrol/save-continuation test passes.
- `ui08-fixed-tram-seeds.txt`: all 32 diagnostic seeds pass four distinct stop districts, exactly one powered start, open Home entrance and valid saved metadata. No repeat of the historical 10,000-seed campaign is claimed.
- `ui08-tram-map-browser.json` and `ui08-tram-map-*.png`: isolated Chrome checks on fresh and network checkpoint upgrades, at 1366×768 and 1920×1080. No map-click movement commands, position/target unchanged, no page errors. Screenshots inspected. `ui08-tram-world-*.png` records the standing public infrastructure. This did not refresh the owner's tab or establish human gameplay acceptance.
- `ui08-tram-roaming-build-final.txt`, `ui08-tram-roaming-lint-final.txt`, `ui08-tram-roaming-docsync.txt`: typecheck/build, lint and docsync. Standard Vite bundle-size advisory remains.
- `ui08-tram-roaming-freshness.txt`: 12 existing campaign experiment artifacts are stale under the revised profile; historical evidence has not been regenerated or relabelled as new acceptance.
- The initial unrestricted `npm test` attempt was interrupted after finding superseded-contract failures; it is not reported as a passing full suite. Owner-directed focused verification is recorded above.

Mutable preview 5178 is updated; frozen previews are unchanged. Save before refreshing and load the saved slot to migrate an ongoing game. No commit/push. Balance and human gameplay retest remain pending.

Final follow-through: `ui08-tram-guidance.txt` passes all 13 discovery/UI guidance tests. `ui08-tram-harness.txt` passes all 12 driver/fixed-tram tests. Existing factory/defence scenario setup now adopts standing public rail; the historical removable-track failure drill has been changed to a paid remote-generator outage/reinstallation. That full multi-night scenario has not been rerun; no refreshed historical balance/defence verdict is claimed. Public route inspection treats normally unpowered stops as excluded from active service, while retaining their visible power status.
