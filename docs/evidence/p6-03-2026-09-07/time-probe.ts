import {createCampaign,ground,blockOfTile} from '../../../packages/sim/src/index';import {DefenceDriver} from '../../../packages/harness/src/defenceScenario';
const s=createCampaign(3),G=ground(s),gate=G.opening!.gate[1],x=gate%G.tw,y=Math.floor(gate/G.tw),d=new DefenceDriver(s),direction=G.opening!.direction,dx=[0,1,0,-1][direction],dy=[-1,0,1,0][direction];d.walk(x+.5+dx*5,y+.5+dy*5);console.log({x,y,direction,e:s.engineer.x,ey:s.engineer.y,home:s.campaign!.homeBlock,block:blockOfTile(s,Math.floor(s.engineer.x),Math.floor(s.engineer.y)),away:d.awaySeconds,tick:s.flow!.tick});const before=d.awaySeconds;for(let i=0;i<20;i++)d.run(.1);console.log({before,after:d.awaySeconds,tick:s.flow!.tick,e:s.engineer.x,ey:s.engineer.y});
