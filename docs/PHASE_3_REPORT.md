# Phase 3 report — absorb Gate A

Phase 3 of the programme: lock the constants `TEST_RESULTS.md` §8 informs, make the four pre-slice decisions from evidence, re-run E1–E9 at the locked values, redraw §18 from the compact bot at the locked cadence. Built 2026-09-03 on branch `phase-3` (stacked on `phase-2`, PR #3). Canonical config hash unchanged: `01dc5d02`. Full check set green locally (typecheck, `npm test`, lint, E1–E9 45/45, `docsync:check`, `snapshot:check`, calibration C1–C7 met on three seeds); the PR's CI run is the proof.

## Where the phase and the constitution disagreed

| | The constitution / the doc said | What happened | Where it is recorded |
|---|---|---|---|
| Gate A | five testers, two scenarios, a human writes `verdict: go` from telemetry | the owner wrote `go` in reply to the Phase 2 report, with no tester sessions; the plan's thresholds were not measured | `TEST_RESULTS.md` §1 (verdict `go`, every other section `not measured`), `DECISIONS.md` Gate A row, doc changelog defines `[play: Gate A]` as a lock made without sessions |
| C1 from Scenario B telemetry (D-P2-2's recommendation) | lock edges per assembler from `firstAmber`, reorders, builds | no telemetry exists; locked from the bots instead: ~7 edges of the mid-game front per 20 mag/min assembler, not §12's ~15 | §12 sentence `[sim: E9-hourly, E3-block, calibration] [play: Gate A]` |
| §18 at 5 h | "a fat blob" 14×9 hand sketch | the compact bot at the locked cadence makes a **river strip, 14 wide × 4 deep**: it scores claims by front added and the river is a free wall, so it goes east and west before north; first interior at 86 min; Arsenal 3.3 h, Turbine hall 4.0 h; **Foundry at 14.1 h** because the bot has no facility pull | §18 caption, generated drawings, DEFERRED.md "Facility pull in the bots" |
| §18 at 25 h | two spurs, 15 blocks between wells | a fat blob filling centre and north, two of five wells taken head-on (5.0 h, 17.2 h), the whole west and rows 0–3 never held | §18 caption; E8-wells |
| C8 bloom rhythm | Gate A decides by feel (`[play]`) | no feel verdict; locked at the doc value on purpose, E10 not ported | §5 sentence, D-P3-5, decision 3 below |
| D-P2-1/2/3 | open until the human answers | the `go` carried no word on them; each made by its recommendation, overridable | `DECISIONS.md` Phase 2 table, `TEST_RESULTS.md` §10 |

## Built

- **`TEST_RESULTS.md`** — frontmatter `verdict: go`, dated, owner; §8 Decision column filled for all seven constants; §10 the three D-P2 decisions by recommendation.
- **Design doc** — 18 `[play: Gate A]` tags across §5 (bloom lock; unfed rule as shipped; rescue window), §11 (C10 and the opening flicker; hour-one lesson), §12 (C1 edges per assembler; C9 no ladder), §16 (survivable, bank size), §19 (steel wall intended), §25 items 1/5/10/12/14, §27 note; §4 and the changelog header describe the generated §18 and define the tag; 11 changelog lines.
- **§18** — legend rewritten; the 10-minute world-view lot sketch kept (hand-drawn, tile scale, Phase 4 M1 redraws); three map-view drawings generated by `npm run docsync` from `packages/sim` (`renderMap` in `queries.ts`; `section18` generator in `packages/tools/src/docsync.ts`: compact bot, seed 3, gap 300 s, production off, at 0:10 / 5 h / 25 h) between `<!-- docsync:section18 -->` markers, CI fails on drift; a hand caption with the three-seed means and the timeline.
- **E8** — targets are the locked cadence's three-seed means (52/19/36 at 5 h, 292/43/253 at 25 h); new check that gap 5 is the best match of 4/5/6/8; E8-wells unchanged.
- **Calibration** — C1/C2 scored from minute 1 (D-P2-1): `firstAmberAfter1` / `firstRedAfter1`, md columns `amber after 1 min` / `red after 1 min`; `docs/experiments/calibration.{json,md}` regenerated: C1–C7 all met, i1/i2 informational still missed.
- **Python retired** (D-P3-8) — `frontsim.py`, `phase5.py`, `phase5b.py`, `export_fixtures.py`, `export_power_fixtures.py`, both result JSONs, `__pycache__` deleted; `packages/sim/fixtures/README.md` freezes the fixtures at `52c4ca3`.
- **`DECISIONS.md`** — C1/C2/C4/C8/C9/C10 made, C3 and C5-steel routed, Gate A made, D-P2-1/2/3 made by recommendation, new Phase 3 table D-P3-1…D-P3-8 and the three open D-P3-9/10/11. **`DEFERRED.md`** re-read (Python and §18 bullets closed, one item added). **`PROGRAMME_STATE.md`** Phase 3 section, header, inventory rows.

## Assumed (GAME-ASSUMPTION)

- **One ammo line per interior block** (C3) is what the sim and the slice encode; the lot footprint budget is a Phase 4 M1 / Phase 5 number. The doc's §13 machine rows stay untagged.
- **Steel patch size** (C5 steel) is a Phase 5 placeholder: proto 7,680 at 64/min, empties at 2:00. The §19 steel wall it produces is intended (D-P2-3).
- **The bot stands in for the player** for every `[play: Gate A]` lock. Where a bot and a player would differ in kind, not degree, it is named: facility pull (§18, DEFERRED.md) and ring reordering (never done by a bot).
- **C8 at the doc value** without a feel verdict (D-P3-5).

## Deferred

`DEFERRED.md` "Re-read at the Phase 3 gate": Gate A tester sessions (human, any time; recommended alongside Phase 4 M1), E10 port (only on request), facility pull in the bots (Phase 4 playtests / Phase 5 harness), slot budget (Phase 4 M1 / Phase 5), steel patch (Phase 5), Relight bank object (Phase 10, now sized), the 10-minute lot sketch (Phase 4 M1), steel income outside industrial blocks (Phase 5, only if a tester calls the wall a bug). Nothing else moved.

## Measured

### The locks and their evidence

| Constant | Locked value | Run(s) | Number |
|---|---|---|---|
| C1 edges per assembler | ~7 of the mixed mid-game front; 15 civic / 11 residential steady state | E9-hourly, E3-block, calibration | 52–67 mag/min over 20–21 edges at 5–12 h (2.5–3.4 mag/edge-min); one Mk1 (10) + 400 stock carries an 8–11-edge front to 2:30 with no pip |
| C2 cadence | 15 min in hour one, then 5 | E8-cadence | gap 5 within 1 % of the caption; 4 / 6 / 8 miss by 31 / 19 / 41 % |
| C4 buffer | 400-magazine chest; bank a separate object | E9-hold | 4,000-magazine bank loses 0 / 39 / 42; uncapped (~13,700 peak) loses 0 |
| C8 bloom | `120/(0.5+d)`, 10 % | none (E10 retired with Python) | — |
| C9 rate | 20 mag/min, no ladder | §12 recipe, calibration | Mk1 5 loses blocks, Mk1 20 breaks C7 (Phase 2 sweep) |
| C10 start ammo | 20 magazines; score from minute 1 | E1-start-min, calibration | 32 arrivals accrue vs 40; C1–C7 met from minute 1 |
| D-P3-1 hour one | ammo, not power, at 100/20 kW | E4h-gen@0,6,15,25, E5-compact-1-shot-asm-h1, E3-first | no brownout at four Generators; first shade minute 32–47 |
| D-P3-2 rescue | red-pip minutes before the 40th arrival; 30 s the second chance | E1-starve-substation-N40, Scenario B | 7.2 min median [4.3–19.6]; proto idle B: red ≈ 4:15, loss ≈ 4:20 |
| D-P3-3 unfed rule | per block, 40, 60 s all-fed clears and restarts, creep 1/3 s, fall 30 = 90 s | E1-ring-substation-N10/N20, E1-starve-creep | N10/N20 lose the HQ at minute 8; creep alone falls in 2.8 min |
| D-P3-4 Relight | survivable; bank ≈ 20,000 magazines | E9-hold | as C4 |

### §18 on seed 3 (compact, gap 300, production off)

| | held | front | interior | lost | wells neutralised |
|---|---|---|---|---|---|
| 0:10 | 1 | 3 | 0 | 0 | 0 of 5 |
| 5 h | 52 | 20 | 35 | 0 | 0 of 5 (the (22,21) well falls just after) |
| 25 h | 292 | 37 | 258 | 0 | 2 of 5 |

Timeline: first interior 85.6 min; Electricians 2.2 h; Concrete crew 2.8 h; Arsenal 3.3 h; Turbine hall 4.0 h; Foreman 4.1 h; well (22,21) 5.0 h; Refinery 7.8 h; Gunsmith 8.5 h; Rail crew 9.6 h; **Foundry 14.1 h**; well (12,10) 17.2 h. Held per hour 1–25: 4, 16, 28, 40, 52, 64, 76, 88, 100, 112, 124, 136, 148, 160, 172, 184, 196, 208, 220, 232, 244, 256, 268, 280, 292 (exactly 12 per hour once every 5-minute claim lands: the cadence, not the map, sets the count). Three-seed means: 52 / 19 / 36 at 5 h, 292 / 43 / 253 at 25 h.

### Calibration from minute 1 (`docs/experiments/calibration.md`)

C1 met (compact: enclosure 60 m, amber never, three seeds); C2 met (red never); C3–C7 unchanged and met. i1 (compact amber 60–120 min) and i2 (held 20–30 at 120 min) still missed, informational: compact holds 16 at 120 min and never ambers, the same edges-per-assembler fact C1's lock now states.

### Speed

Full check set ≈ 2 min; `npm run docsync` (three 25 h runs are not needed: one run to 25 h, drawings sampled) ≈ 4 s.

## Definition of done

| DoD (constitution Phase 3) | Status |
|---|---|
| Constants `TEST_RESULTS.md` §8 informs locked as `[play]` sentences and `DECISIONS.md` lines, edges per assembler above all | done: C1, C2, C4, C8, C9, C10, survivors; 18 `[play: Gate A]` tags; C3 and C5-steel routed with a named owner phase |
| E1–E9 re-run at the locked values, retagged | done: 45/45; no canonical number moved, so no `[sim]` number changed; E8 retargeted to the locked cadence |
| Four pre-slice decisions, each a `DECISIONS.md` line and a §5/§11/§16 sentence | done: D-P3-1 (§11), D-P3-2 (§5), D-P3-3 (§5), D-P3-4 (§16) |
| §18's three example maps redrawn from the compact bot at the locked cadence | done, generated by docsync and CI-checked |
| **No open constant remains that the slice would otherwise encode by accident** | **met** — `PROGRAMME_STATE.md` 3.3 |
| Experiments green in CI | green locally; PR #3's run is the proof |
| `DEFERRED.md` re-read, every item a phase | done |
| §26 recount | three systems, 5/10, unchanged |
| Three decisions for a human | below |

## Three decisions (for the human)

1. **D-P3-9 — Gate A without sessions.** The gate is passed on your `go`; every lock carries `[play: Gate A]` and the test plan's thresholds were never measured. Options: (a) ratify as is and let Phase 4's own playtests (M1–M5, the constitution's human hour per milestone) be the first human evidence; (b) run the five sessions now, in parallel with Phase 4 M1, and move any lock they contradict (the tag becomes the session name); (c) hold Phase 4 until the sessions run. **Recommendation: (b)** — nothing in M1 depends on a lock moving by less than 2×, and the shape question §27 is scored on is only answerable by people.
2. **D-P3-10 — C3/C4 for the slice.** As locked, the slice encodes one ammo line per interior block and a 400-magazine chest as the line buffer. Options: (a) as locked; Phase 4 M1 measures the footprint a 24×24 lot actually gives and Phase 5 sets the budget; (b) set the footprint budget now from §13 (3×3 machines, dozens per lot) so M1 places against it and the "no free interior slot" pressure changes in kind before it is ever played. **Recommendation: (a).**
3. **D-P3-11 — C8 with no feel evidence.** The bloom timer and drop are locked at the doc's values with nothing behind them but the retired Python E10 (halving the base halves fights and cuts ammo 30 %). Options: (a) accept; C8 is a feel question and Phase 4's playtests are the first place to ask it; (b) port E10 to the harness first (a day) so the slice at least encodes a timer whose ammo and fight-count consequences are in `EXPERIMENTS.md`. **Recommendation: (a)**, with (b) on request; a number will not tell you whether the pulse reads as a threat.
